---
phase: 08-rtsp-camera
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - supabase/functions/notifications/index.ts
  - tests/smoke/camera_down.sh
  - tests/smoke/camera_recovered.sh
  - ai_agent/supabase_client.py
  - ai_agent/scheduler.py
autonomous: true
requirements: [RTSP-01, RTSP-03]
tags: [phase-8, rtsp, edge-function, supabase, deno, fcm, send-push-to-users, scheduler-wiring, T-8-03]

user_setup:
  - service: supabase
    why: "supabase functions deploy notifications (case camera-down + camera-recovered 활성화)"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (Phase 4·7 작성됨)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env (Phase 4·7 작성됨)"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token"
    dashboard_config: []

must_haves:
  truths:
    - "notifications/index.ts 에 case 'camera-down' + case 'camera-recovered' 2 cases 추가 (default 위, Phase 4·7 패턴 동일 위치)"
    - "manager 권한 사용자 N명 → sendPushToUsers (plural) — RESEARCH 정정 #5 (Phase 4 single-recipient 패턴 그대로 복사 거부)"
    - "manager SELECT: WHERE group_id=$1 AND user_role IN ('manager','general_manager')"
    - "Android channel — Option B (deadline 우선): data.type='camera_alert', channel_id 명시 X (fcm_default_channel 재사용). Android 코드 변경 0 (Pitfall 3 / RESEARCH 정정 #4)"
    - "Edge Function 배포: `supabase functions deploy notifications` 성공"
    - "4 curl smoke ALL PASS — camera-down 정상 200 / camera-down payload 누락 400 / camera-down no-manager 200+sent:0 / camera-recovered 정상 200"
    - "ai_agent/supabase_client.py 에 update_camera_health(camera_id) 헬퍼 추가 — datetime.now(timezone.utc).isoformat() 사용 (RESEARCH A6 — '\"now()\"' 문자열 패턴 거부)"
    - "scheduler.py 4 detector 진입점에 capture 성공 직후 bridge.update_camera_health(camera_id) wiring (capture 호출 시그니처는 zero-change — SC #4 보존)"
    - "FCM only — 다른 채널 X (Phase 4 D-11 일관)"
    - "D-09 알림 전이 회귀 가드: 동일 카메라 down 지속 중 재발사 0 — 30분 cooldown + last_alert_at 갱신 (plan 08-02 의 함수가 보장, 본 plan 의 Edge Function 은 단순 push)"
    - "PostgREST '\"now()\"' 문자열 hardcode 0건 (RESEARCH A6 — datetime ISO 사용)"
  artifacts:
    - path: "supabase/functions/notifications/index.ts"
      provides: "case 'camera-down' + case 'camera-recovered' (Edge Function action-routing)"
      contains: "case \"camera-down\""
    - path: "tests/smoke/camera_down.sh"
      provides: "Edge Function 3 curl smoke (정상 200 / payload 누락 400 / no-manager 200+sent:0)"
    - path: "tests/smoke/camera_recovered.sh"
      provides: "Edge Function 1 curl smoke (정상 200)"
    - path: "ai_agent/supabase_client.py"
      provides: "update_camera_health(camera_id) 헬퍼 — PATCH cameras SET last_frame_at=<iso utc>"
      contains: "def update_camera_health"
  key_links:
    - from: "ai_agent/scheduler.py 4 detector 진입점"
      to: "bridge.update_camera_health(camera_id)"
      via: "capture(...) 성공 직후 1줄 추가"
      pattern: "update_camera_health"
    - from: "ai_agent/supabase_client.py update_camera_health"
      to: "PostgREST cameras.last_frame_at"
      via: "self._client.table('cameras').update({'last_frame_at': now_iso}).eq('camera_id', camera_id).execute()"
      pattern: "datetime.*isoformat"
    - from: "notifications/index.ts case 'camera-down'"
      to: "_shared/fcm.ts sendPushToUsers (plural)"
      via: "userIds = managers.map(m => m.user_id), sendPushToUsers(supabase, userIds, payload)"
      pattern: "sendPushToUsers"
---

<objective>
Edge Function 의 camera-down/recovered routing + ai_agent 의 last_frame_at 갱신 wiring —
plan 08-02 의 healthcheck 인프라가 호출할 endpoint 활성화 + scheduler 가 capture 성공 시점
에 cameras.last_frame_at 을 PATCH 하여 healthcheck 의 진실 source 를 채움.

Purpose:
- (백엔드) RESEARCH 정정 #5 의 sendPushToUsers (plural) 패턴 — manager N명 동시 push.
  D-09 알림 전이 + 30분 cooldown 보존 (Phase 4·7 일관). FCM only.
- (Python) RESEARCH A6 의 PostgREST '"now()"' 함정 회피 — datetime.now(timezone.utc).
  scheduler 의 4 detector 진입점에 1줄씩 추가 (capture 호출 자체는 zero-change).

Output:
- supabase/functions/notifications/index.ts (+2 cases, ~80 lines)
- tests/smoke/camera_down.sh (3 cases) + tests/smoke/camera_recovered.sh (1 case)
- ai_agent/supabase_client.py (update_camera_health 헬퍼)
- ai_agent/scheduler.py (4 detector 진입점에 update_camera_health 호출 wiring)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rtsp-camera/08-CONTEXT.md
@.planning/phases/08-rtsp-camera/08-RESEARCH.md
@.planning/phases/08-rtsp-camera/08-02-PLAN.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@.planning/phases/07-watch-app-bridge/07-02-SUMMARY.md
@supabase/functions/notifications/index.ts
@supabase/functions/_shared/fcm.ts
@ai_agent/supabase_client.py
@ai_agent/scheduler.py

<interfaces>
<!-- Edge Function 이 받을 payload (plan 08-02 의 cameras_healthcheck() 가 net.http_post 으로 보냄) -->

```typescript
// case 'camera-down' body shape:
{ action: "camera-down", camera_id: number, group_id: number | null, last_frame_at: string }
// → 200 { ok: true, sent: <int>, failed: <int>, skipped: <int> }
//   400 if camera_id or group_id missing
//   500 on supabase error

// case 'camera-recovered' body shape:
{ action: "camera-recovered", camera_id: number, group_id: number | null }
// → 200 { ok: true, sent, failed, skipped }
```

<!-- _shared/fcm.ts 에서 (plural — Phase 4·7 의 single 과 별개) -->
```typescript
sendPushToUsers(
  supabase: SupabaseClient,
  userIds: string[],
  payload: { title: string; body: string; data?: Record<string, string> }
): Promise<{ sent: number; failed: number; skipped: number }>;
```

<!-- ai_agent/supabase_client.py SupabaseBridge 신규 메서드 -->
```python
def update_camera_health(self, camera_id: int) -> None:
    """capture 성공 직후 cameras.last_frame_at = <utc now iso>. PATCH 1회."""
```

<!-- ai_agent/scheduler.py 의 wiring (capture 호출 직후 1줄씩 추가) -->
- _process_single_camera (line ~78 capture 직후, ~10분 cycle)
- _process_fall_for_camera (line ~144 capture 직후, ~1분 cycle)
- _process_detection_for_camera (line ~235 capture 직후, ~1분 cycle)
- 4번째 진입점 — `grep -n '\bcapture(' ai_agent/scheduler.py` 로 정확한 line 확인 후 동일 패턴
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifications/index.ts 2 cases 추가 + deploy + 4 curl smoke</name>
  <read_first>
    - supabase/functions/notifications/index.ts (현재 case routing — line 12 switch 부터 default 까지)
    - supabase/functions/_shared/fcm.ts (sendPushToUser + sendPushToUsers 시그니처)
    - .planning/phases/07-watch-app-bridge/07-02-PLAN.md + 07-02-SUMMARY.md (Edge Function deploy + curl smoke 패턴)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pattern 4: Edge Function camera-down (multi-recipient FCM)" (line 437-494)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Example D: curl smoke" (line 793-820)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 3: Android camera_alerts channel" (line 610-616) — Option B 채택
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md §D-09 (알림 전이 — push only, notifications insert 0 패턴)
  </read_first>
  <action>
    1. **신규 case 'camera-down' 삽입** (notifications/index.ts default 위, RESEARCH §Pattern 4 line 442-488 그대로):
       ```typescript
       case "camera-down": {
           const { camera_id, group_id, last_frame_at } = body;
           if (!camera_id || group_id === undefined || group_id === null) {
               return err("camera_id and group_id are required");
           }

           const { data: cam, error: camErr } = await supabase
               .from("cameras")
               .select("camera_id, device_name, install_area")
               .eq("camera_id", camera_id)
               .maybeSingle();
           if (camErr) return err(camErr.message, 500);

           // RESEARCH 정정 #5: manager 권한 사용자 N명 → sendPushToUsers (plural)
           const { data: managers, error: mgrErr } = await supabase
               .from("profiles")
               .select("user_id")
               .eq("group_id", group_id)
               .in("user_role", ["manager", "general_manager"]);
           if (mgrErr) return err(mgrErr.message, 500);

           const userIds = (managers ?? []).map((m: { user_id: string }) => m.user_id);
           if (userIds.length === 0) {
               return ok({ ok: true, sent: 0, reason: "no managers in group" });
           }

           const camName = cam?.device_name ?? `camera-${camera_id}`;
           const camArea = cam?.install_area ?? "";
           const r = await sendPushToUsers(supabase, userIds, {
               title: "카메라 통신두절",
               body:  `${camName} (${camArea}) 5분 이상 frame 무수신`,
               data: {
                   // Pitfall 3 / RESEARCH 정정 #4: Option B (deadline 우선) — fcm_default_channel 재사용.
                   // type='camera_alert' 만 보내고 channel_id 미지정 → Android 측 코드 변경 0.
                   // v1.1 에서 'camera_alerts' 채널 분리 예정.
                   type:          "camera_alert",
                   camera_id:     String(camera_id),
                   severity:      "WARNING",
                   last_frame_at: String(last_frame_at ?? ""),
               },
           });
           return ok({ ok: true, sent: r.sent, failed: r.failed, skipped: r.skipped });
       }
       ```

    2. **신규 case 'camera-recovered'** (camera-down 사본, 텍스트만 변경):
       ```typescript
       case "camera-recovered": {
           const { camera_id, group_id } = body;
           if (!camera_id || group_id === undefined || group_id === null) {
               return err("camera_id and group_id are required");
           }

           const { data: cam } = await supabase
               .from("cameras")
               .select("camera_id, device_name, install_area")
               .eq("camera_id", camera_id)
               .maybeSingle();

           const { data: managers, error: mgrErr } = await supabase
               .from("profiles")
               .select("user_id")
               .eq("group_id", group_id)
               .in("user_role", ["manager", "general_manager"]);
           if (mgrErr) return err(mgrErr.message, 500);

           const userIds = (managers ?? []).map((m: { user_id: string }) => m.user_id);
           if (userIds.length === 0) {
               return ok({ ok: true, sent: 0, reason: "no managers in group" });
           }

           const camName = cam?.device_name ?? `camera-${camera_id}`;
           const camArea = cam?.install_area ?? "";
           const r = await sendPushToUsers(supabase, userIds, {
               title: "카메라 회복",
               body:  `${camName} (${camArea}) frame 수신 재개`,
               data: {
                   type:      "camera_alert",
                   camera_id: String(camera_id),
                   severity:  "NORMAL",
               },
           });
           return ok({ ok: true, sent: r.sent, failed: r.failed, skipped: r.skipped });
       }
       ```

    3. **`supabase functions deploy notifications`** 실행 — deploy 성공 확인. Phase 7 02
       SUMMARY 의 deploy gate 패턴 (Rule 3 stash 복원 위해 _shared/{supabase,response,cors,fcm}.ts
       복원 + .env 작성 사전 확인).

    4. **신규 smoke 스크립트 `tests/smoke/camera_down.sh`** (RESEARCH §Example D + Phase 7 watch_ack.sh 컨벤션). 본문 핵심:
       - 정상 200: `{"action":"camera-down","camera_id":1,"group_id":1,"last_frame_at":"2026-05-15T12:00:00Z"}` → http 200 + body 에 `"sent"` 포함
       - 누락 400: `{"action":"camera-down"}` → http 400
       - no-manager 200+sent:0: `{"action":"camera-down","camera_id":1,"group_id":99999,"last_frame_at":"..."}` → http 200 + body 에 `"sent":0` 포함
       - SCRIPT_DIR 자동 탐색 + `source .env` + `set -euo pipefail` + `curl -w '\n%{http_code}'` + grep 검증 + 마지막 `=== camera_down: 3/3 PASS ===` 출력

    5. **신규 smoke 스크립트 `tests/smoke/camera_recovered.sh`** (1 case): 정상 200 — `{"action":"camera-recovered","camera_id":1,"group_id":1}` → http 200 + body `"sent"` 포함. 마지막 `=== camera_recovered: 1/1 PASS ===` 출력.

    6. **D-09 알림 전이 회귀 가드** (notifications insert 5분 윈도우 0건):
       smoke 후 SQL `SELECT count(*) FROM notifications WHERE created_at > now() - INTERVAL '5 minutes' AND type='camera_alert';` 기대 = 0 (push-only). SUMMARY 에 기재.
  </action>
  <verify>
    <automated>chmod +x tests/smoke/camera_down.sh tests/smoke/camera_recovered.sh && supabase functions deploy notifications 2>&1 | tail -10 && bash tests/smoke/camera_down.sh 2>&1 | tee /tmp/p8t3a.log | tail -20 && bash tests/smoke/camera_recovered.sh 2>&1 | tee /tmp/p8t3b.log | tail -10 && grep -q 'camera_down: 3/3 PASS' /tmp/p8t3a.log && grep -q 'camera_recovered: 1/1 PASS' /tmp/p8t3b.log && echo SMOKE_OK</automated>
  </verify>
  <done>
    - notifications/index.ts 에 `case "camera-down"` + `case "camera-recovered"` 둘 다 정확히 1회씩 (`grep -c 'case "camera-' supabase/functions/notifications/index.ts` >= 2)
    - `sendPushToUsers` 사용 (plural — 정정 #5) — 본 plan 추가분 `>= 2`
    - manager role filter — `grep -c "'manager', 'general_manager'\|\"manager\", \"general_manager\"" supabase/functions/notifications/index.ts` 증분
    - `supabase functions deploy notifications` 성공
    - `bash tests/smoke/camera_down.sh` exit 0, "3/3 PASS"
    - `bash tests/smoke/camera_recovered.sh` exit 0, "1/1 PASS"
    - notifications insert 증분 0 (D-09 회귀 가드 — push only, DB insert 없음)
    - `channel_id` payload 부재 (Pitfall 3 회피 — Option B fcm_default_channel 재사용)
  </done>
</task>

<task type="auto">
  <name>Task 2: ai_agent/supabase_client.py update_camera_health 헬퍼 + scheduler.py 4 detector wiring</name>
  <read_first>
    - ai_agent/supabase_client.py (현재 SupabaseBridge — fetch_active_cameras 패턴 line 35-44, register_ai_event line 124-154)
    - ai_agent/scheduler.py 진입점 정확한 위치 확인: `grep -n '^def _process\|capture(' ai_agent/scheduler.py | head -30`
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Example B: ai_agent/supabase_client.py" (line 663-679)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"A6" (line 844 — PostgREST '"now()"' 함정)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 8: scheduler 4 detector 동시 PATCH" (line 645-650)
  </read_first>
  <action>
    1. **신규 메서드 `SupabaseBridge.update_camera_health(camera_id: int) -> None`** in `ai_agent/supabase_client.py` (기존 클래스 끝, `close()` 직전):
       ```python
       def update_camera_health(self, camera_id: int) -> None:
           """capture 성공 직후 cameras.last_frame_at = utc now (RTSP-03 헬스체크 source).

           PostgREST 의 '"now()"' 문자열은 server-side eval 안 됨 (RESEARCH A6) →
           Python datetime.now(timezone.utc).isoformat() 으로 ISO 문자열 전달.
           실패 시 warn 만 (capture 자체는 성공 — 알림 흐름 차단 X).
           """
           from datetime import datetime, timezone
           now_iso = datetime.now(timezone.utc).isoformat()
           try:
               self._client.table("cameras") \
                   .update({"last_frame_at": now_iso}) \
                   .eq("camera_id", camera_id) \
                   .execute()
           except Exception as e:
               log.warning("update_camera_health failed camera_id=%s: %s", camera_id, e)
       ```

    2. **scheduler.py 4 detector 진입점 wiring** — `grep -n '\bcapture(' ai_agent/scheduler.py` 로 정확한 line 확인 후 capture(...) 성공 직후 (try block 내, 후속 코드 진입 전) 정확히 1줄씩 삽입:
       - `_process_single_camera`: line ~78 capture 직후 → `bridge.update_camera_health(camera_id)`
       - `_process_fall_for_camera`: line ~144 capture 직후 (cv2.imread 전) → `bridge.update_camera_health(camera_id)`
       - `_process_detection_for_camera`: 해당 함수 capture 직후 → `bridge.update_camera_health(camera_id)`
       - 4번째 진입점 (grep 으로 확인): 동일 패턴

    3. **capture(...) 시그니처 zero-change 가드 (SC #4)** — capture 호출 인자 (rtsp_url, tmp_path, ffmpeg_bin=, seek_seconds=) 그대로. update_camera_health 호출은 별 라인. 검증:
       `git diff ai_agent/scheduler.py | grep -E '^\-.*capture\(|^\+.*capture\(' | grep -v update_camera_health` 출력 0 (capture 호출 line 변경 0).

    4. **Pitfall 8 — 4× PATCH 허용 (default)**: cycle dedup 미구현. 부하 미미 (1 cycle = camera 당 4× = 분당 20×). v1.1 에서 process-local cache 추가 가능 — SUMMARY 에 deferred 명시.

    5. **회귀 가드** — PostgREST '"now()"' 문자열 hardcode 0건:
       `grep -v '^[ ]*#' ai_agent/supabase_client.py | grep -c '"now()"'` == 0
       `grep -c 'datetime.now.*timezone.utc' ai_agent/supabase_client.py` >= 1

    6. **단위 테스트 회귀** — Phase 1·2·3 의 ai_agent/tests 전체 PASS 유지 (snapshot 변경 없음, scheduler wiring 만 추가, supabase_client 신규 메서드 — 단위 테스트 영향 없음).
  </action>
  <verify>
    <automated>grep -c 'def update_camera_health' ai_agent/supabase_client.py && (grep -v '^[ ]*#' ai_agent/supabase_client.py | grep -c '"now()"') && grep -c 'datetime.now.*timezone.utc' ai_agent/supabase_client.py && (grep -c 'update_camera_health' ai_agent/scheduler.py) && cd ai_agent && python -m pytest tests/ -x -v 2>&1 | tail -8</automated>
  </verify>
  <done>
    - `ai_agent/supabase_client.py` 에 `def update_camera_health` 정확히 1회 정의
    - PostgREST '"now()"' 문자열 hardcode 0건 (코멘트 제외)
    - `datetime.now(...timezone.utc...)` 사용 1회 이상 (RESEARCH A6)
    - `ai_agent/scheduler.py` 에 `update_camera_health` 호출 4회 (4 detector 진입점)
    - capture(...) 호출 line 변경 0 (SC #4 — `git diff` 검증)
    - 전체 ai_agent/tests/ suite PASS 유지 (regression 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 08-03)

| Boundary | Description |
|----------|-------------|
| pg_cron → notifications endpoint | service_role JWT (Vault 에서 SELECT, plan 08-02) 첨부. anon JWT 도 호출 가능 (action-routing 자체는 anon allowable — Phase 4·7 동일). |
| ai_agent → PostgREST cameras PATCH | service_role key (.env 보호, Phase 4·7 동일) — RLS 우회 update. |
| Edge Function → FCM HTTP v1 | _shared/fcm.ts 의 RS256 signing (Firebase service account, FCM_SERVICE_ACCOUNT secret). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03 | E (Elevation of Privilege) | notifications endpoint anonymous 접근 (camera-down/recovered case) | accept (Phase 4·7 일관 패턴) | Edge Function action-routing 은 anon 허용 (Phase 4 watch-alert 동일). 본 case 의 trust 는 (a) cron 에서만 호출 + (b) sendPushToUsers 가 manager 권한 사용자에게만 push. 외부 attacker 가 anon key 로 이 endpoint 를 호출해도 — group_id 가 잘못이면 sent=0, 맞으면 manager 가 단순 알림 받음 (no DB mutation). 회귀 가드: notifications insert 0건 (D-09 — push only). v1.1 에서 service_role bearer 검증 추가 가능. |
</threat_model>

<verification>
1. `supabase functions deploy notifications` 성공
2. `bash tests/smoke/camera_down.sh` → "camera_down: 3/3 PASS"
3. `bash tests/smoke/camera_recovered.sh` → "camera_recovered: 1/1 PASS"
4. notifications insert 증분 0 (D-09 회귀 가드)
5. ai_agent/supabase_client.py 에 `update_camera_health` 정의 + datetime.now(timezone.utc) 사용 + '"now()"' 문자열 0건
6. ai_agent/scheduler.py 의 4 detector 진입점에 `update_camera_health` 호출 4회
7. capture(...) 호출 line 변경 0 (SC #4 — `git diff` 검증)
8. 전체 ai_agent/tests/ suite PASS (regression 0)
</verification>

<success_criteria>
- [ ] case 'camera-down' + case 'camera-recovered' 운영 배포 + 4 curl smoke ALL PASS
- [ ] sendPushToUsers (plural) 사용 — manager N명 동시 push (정정 #5)
- [ ] Pitfall 3 회피 — channel_id 명시 X (Option B fcm_default_channel 재사용)
- [ ] D-09 알림 전이 회귀 가드 — notifications insert 0
- [ ] ai_agent.scheduler 4 detector 진입점 capture 직후 update_camera_health 호출
- [ ] PostgREST '"now()"' 함정 회피 — datetime.now(timezone.utc).isoformat()
- [ ] capture(...) 시그니처 zero-change (SC #4)
- [ ] RTSP-01·03 의 backend wiring 완전 — plan 08-04 의 mediamtx 합성 검증으로 E2E 가능
</success_criteria>

<output>
After completion, create `.planning/phases/08-rtsp-camera/08-03-SUMMARY.md`
</output>
