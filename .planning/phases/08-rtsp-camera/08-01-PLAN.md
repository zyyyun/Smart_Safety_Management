---
phase: 08-rtsp-camera
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_agent/snapshot.py
  - ai_agent/tests/test_snapshot_rtsp.py
autonomous: true
requirements: [RTSP-01]
tags: [phase-8, rtsp, snapshot, cv2, capture, drift-test, opencv-ffmpeg, T-8-05-noted]

must_haves:
  truths:
    - "snapshot.capture() URL scheme 분기 — rtsp://·rtsps:// → capture_rtsp() / 그 외 → 기존 ffmpeg subprocess (_capture_ffmpeg)"
    - "capture_rtsp 가 cv2.VideoCapture(url, cv2.CAP_FFMPEG) + 2초 sleep + cap.isOpened()+frame is not None 가드 (drift_test.py 검증 패턴) 사용"
    - "RTSP 끊김 시 2초 고정 backoff 3회 (D-02 amended). 3회 모두 실패 시 SnapshotError raise"
    - "scheduler.py 의 4 detector 진입점 (line 72/128/228/328) 의 capture(...) 호출 zero-change — SC #4 자동 충족"
    - "opencv-python prebuilt FFMPEG=YES 회귀 가드: requirements.txt 의 opencv-python>=4.7 명세 그대로"
    - "OPENCV_FFMPEG_CAPTURE_OPTIONS=rtsp_transport;tcp 환경변수 설정 (Pitfall 6 — UDP default 회피)"
    - "6 pytest cases ALL PASS — 첫시도 성공 / 2회 fail+3회 success / 3회 모두 fail / isOpened=False / dispatch rtsp / dispatch mp4"
  artifacts:
    - path: "ai_agent/snapshot.py"
      provides: "capture(URL scheme 분기 wrapper) + capture_rtsp(D-01·02) + _capture_ffmpeg(기존 subprocess)"
      contains: "def capture_rtsp"
    - path: "ai_agent/tests/test_snapshot_rtsp.py"
      provides: "RTSP-01·03 단위 테스트 6 cases"
      contains: "def test_rtsp_success_first_attempt"
  key_links:
    - from: "ai_agent/scheduler.py:36"
      to: "snapshot.capture (URL scheme 분기 wrapper)"
      via: "from snapshot import SnapshotError, capture (UNCHANGED)"
      pattern: "from snapshot import"
    - from: "ai_agent/snapshot.py capture()"
      to: "capture_rtsp() (rtsp://) 또는 _capture_ffmpeg() (mp4/file)"
      via: "URL.lower().startswith(('rtsp://','rtsps://'))"
      pattern: "rtsp://"
---

<objective>
RTSP 스트림 frame 추출을 ai_agent 에 추가 — drift_test.py 검증된 cv2.VideoCapture(CAP_FFMPEG)
패턴을 snapshot.py 에 이식. URL scheme 분기 wrapper 로 mp4 fallback 호환 (SC #4).

Purpose: ai_agent/scheduler.py 4 detector 진입점이 cameras.live_url_detail 의 rtsp:// URL
도 mp4 와 동일하게 처리하도록 — 분기점이 snapshot.py 내부 1줄에 한정. drift_test.py 의
검증된 timing (2초 sleep + 3회 retry + cap.isOpened()+frame is not None 가드) 그대로 채택.

Output:
- ai_agent/snapshot.py: capture() wrapper + capture_rtsp() (D-01·02 신규) + _capture_ffmpeg() (기존 본문 rename)
- ai_agent/tests/test_snapshot_rtsp.py: 6 pytest cases (RTSP-01·03 단위 검증)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rtsp-camera/08-CONTEXT.md
@.planning/phases/08-rtsp-camera/08-RESEARCH.md
@ai_agent/snapshot.py
@ai_agent/scheduler.py
@ai_agent/requirements.txt
@D:\2025_산업안전\산업안전\Dirft 카메라\DriftX3\drift_test.py

<interfaces>
<!-- 4 detector 진입점이 호출하는 단일 진입점. 본 plan 후에도 시그니처 zero-change. -->

From ai_agent/snapshot.py (현재):
```python
class SnapshotError(RuntimeError): ...
def capture(rtsp_url: str, output_path: Path | str, *, ffmpeg_bin: str = "ffmpeg",
            timeout_sec: int = 30, seek_seconds: float | None = None) -> Path:
    """RTSP 또는 영상 파일에서 1 프레임 추출."""
```

From ai_agent/scheduler.py (호출 패턴, 본 plan 후 그대로):
```python
from snapshot import SnapshotError, capture
# line 78:  capture(rtsp_url, tmp_path, ffmpeg_bin=settings.ffmpeg_bin)
# line 139: capture(rtsp_url, tmp_path, ffmpeg_bin=settings.ffmpeg_bin, seek_seconds=settings.fall_demo_seek_sec)
# line 235: capture(rtsp_url, tmp_path, ffmpeg_bin=settings.ffmpeg_bin)
# line 335: capture(rtsp_url, tmp_path, ffmpeg_bin=settings.ffmpeg_bin)
```

From drift_test.py (검증된 RTSP 패턴 — D-01·02 source of truth):
```python
cap = cv2.VideoCapture(rtsp_url, cv2.CAP_FFMPEG)
time.sleep(2)
ret, frame = cap.read()
if ret and cap.isOpened() and frame is not None: ...
# 3회 retry, 시도 사이 time.sleep(2)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snapshot.py 에 capture_rtsp + URL scheme 분기 wrapper 추가</name>
  <read_first>
    - ai_agent/snapshot.py (현재 capture() 함수 본문 line 24-96)
    - .planning/phases/08-rtsp-camera/08-CONTEXT.md §decisions D-01·D-02 (line 50-125)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pattern 1: cv2 capture_rtsp" + §"Pattern 2: snapshot.capture URL scheme 분기" (line 259-335)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 6: RTSP UDP default" (line 631-637)
    - D:\2025_산업안전\산업안전\Dirft 카메라\DriftX3\drift_test.py (검증된 RTSP 패턴 source of truth)
  </read_first>
  <behavior>
    - Test 1 (test_rtsp_success_first_attempt): 첫 시도 ret=True + cap.isOpened()=True + frame is not None → output_path 반환, time.sleep 1회 (handshake), cap.read 1회
    - Test 2 (test_rtsp_retry_then_success): [(False,None),(False,None),(True,frame)] → 3번째 시도 성공, time.sleep 5회 (handshake×3 + retry×2)
    - Test 3 (test_rtsp_three_failures_raises): 3회 모두 ret=False → SnapshotError("failed after 3 attempts") raise
    - Test 4 (test_rtsp_isOpened_false_treated_as_fail): ret=True+frame OK 지만 cap.isOpened()=False → SnapshotError raise (drift_test 가드)
    - Test 5 (test_capture_dispatch_rtsp): capture("rtsp://...") → capture_rtsp() 호출, _capture_ffmpeg() 미호출
    - Test 6 (test_capture_dispatch_mp4): capture("https://.../source_v2.mp4") → _capture_ffmpeg() 호출, capture_rtsp() 미호출
  </behavior>
  <action>
    1. **Refactor 기존 `capture()` 본문을 `_capture_ffmpeg()` 로 rename** (snapshot.py:24-96 의 ffmpeg subprocess 로직 전체). 시그니처 동일: `def _capture_ffmpeg(rtsp_url, output_path, *, ffmpeg_bin="ffmpeg", timeout_sec=30, seek_seconds=None) -> Path`.

    2. **신규 `capture_rtsp()` 함수 추가** (per D-01 amended + drift_test.py 패턴). 정확한 본문:
       ```python
       BACKOFF_SEC = 2  # drift_test.py try_connect 패턴

       def capture_rtsp(url: str, output_path: Path | str, *, max_attempts: int = 3) -> Path:
           output_path = Path(output_path)
           output_path.parent.mkdir(parents=True, exist_ok=True)

           last_err: str | None = None
           for attempt in range(max_attempts):
               cap = cv2.VideoCapture(url, cv2.CAP_FFMPEG)
               try:
                   cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)  # latest frame only (D-01 권장)
                   time.sleep(BACKOFF_SEC)  # RTSP handshake 대기 (drift_test.py)
                   ret, frame = cap.read()
                   if ret and cap.isOpened() and frame is not None:
                       ok = cv2.imwrite(str(output_path), frame)
                       if ok and output_path.exists() and output_path.stat().st_size > 0:
                           return output_path
                       last_err = "cv2.imwrite returned False or empty file"
                   else:
                       last_err = (
                           f"cap.read ret={ret} isOpened={cap.isOpened()} "
                           f"frame_None={frame is None}"
                       )
               finally:
                   cap.release()
               if attempt < max_attempts - 1:
                   time.sleep(BACKOFF_SEC)  # retry 사이 wait (D-02)

           raise SnapshotError(
               f"RTSP capture failed after {max_attempts} attempts (url={url}): {last_err}"
           )
       ```

    3. **신규 `capture()` wrapper** — 기존 시그니처 보존, 내부에서 URL scheme 분기:
       ```python
       def capture(
           url: str,
           output_path: Path | str,
           *,
           ffmpeg_bin: str = "ffmpeg",
           timeout_sec: int = 30,
           seek_seconds: float | None = None,
       ) -> Path:
           """RTSP 또는 mp4/file 에서 1 프레임 추출. URL scheme 으로 자동 분기 (D-01)."""
           if url.lower().startswith(("rtsp://", "rtsps://")):
               return capture_rtsp(url, output_path)  # seek/timeout 무시 (RTSP 라이브)
           return _capture_ffmpeg(
               url, output_path,
               ffmpeg_bin=ffmpeg_bin,
               timeout_sec=timeout_sec,
               seek_seconds=seek_seconds,
           )
       ```

    4. **Pitfall 6 회피 — TCP 강제** (snapshot.py 모듈 top-level, import 직후):
       ```python
       import os
       os.environ.setdefault("OPENCV_FFMPEG_CAPTURE_OPTIONS", "rtsp_transport;tcp")
       ```

    5. **import 추가**: `import cv2`, `import time`. 기존 ffmpeg 로직은 그대로 (`subprocess`, `Path`).

    6. **scheduler.py 변경 X 가드** — 작업 후 grep 으로 검증: `from snapshot import` line 그대로, `capture(rtsp_url, tmp_path` 호출 4곳 모두 시그니처 그대로.
  </action>
  <verify>
    <automated>cd ai_agent && python -m pytest tests/test_snapshot_rtsp.py -x -v 2>&1 | tee /tmp/p8t1.log; tail -30 /tmp/p8t1.log; grep -c '6 passed' /tmp/p8t1.log</automated>
  </verify>
  <done>
    - ai_agent/snapshot.py 에 `def capture_rtsp(` 1회 + `def _capture_ffmpeg(` 1회 + `def capture(` 1회 정의됨 (`grep -c '^def ' ai_agent/snapshot.py` >= 3)
    - `cv2.VideoCapture` + `cv2.CAP_FFMPEG` 둘 다 capture_rtsp 본문에 존재 (`grep -c 'cv2.CAP_FFMPEG' ai_agent/snapshot.py` >= 1)
    - drift_test.py 의 3-가드 모두 존재 (`grep -c 'isOpened()' ai_agent/snapshot.py` >= 1, `grep -c 'frame is not None' ai_agent/snapshot.py` >= 1)
    - URL 분기 1줄 존재 (`grep -c "startswith.*rtsp" ai_agent/snapshot.py` >= 1)
    - TCP 강제 환경변수 (`grep -c 'OPENCV_FFMPEG_CAPTURE_OPTIONS' ai_agent/snapshot.py` == 1)
    - scheduler.py 무변경 가드: `git diff --stat ai_agent/scheduler.py` 출력 빈 줄 (수정 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: 6 pytest cases (RTSP-01·03 단위 검증)</name>
  <read_first>
    - ai_agent/tests/test_scheduler_buffer.py (기존 mock 패턴 — sys.path insert 컨벤션)
    - ai_agent/tests/test_fusion.py (pytest fixture 패턴)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Example C: ai_agent/tests/test_snapshot_rtsp.py" (line 681-791)
    - ai_agent/snapshot.py (Task 1 산출물)
  </read_first>
  <action>
    1. **신규 파일** `ai_agent/tests/test_snapshot_rtsp.py` 작성. 정확한 6 cases (RESEARCH §Example C 그대로):
       - `test_rtsp_success_first_attempt` — 첫 시도 ret=True+frame+isOpened, time.sleep call_count == 1, cap.read call_count == 1
       - `test_rtsp_retry_then_success` — caps=[fail, fail, success], time.sleep call_count == 5 (handshake×3 + retry×2)
       - `test_rtsp_three_failures_raises` — 3회 fail → `pytest.raises(snapshot.SnapshotError, match="failed after 3 attempts")`
       - `test_rtsp_isOpened_false_treated_as_fail` — ret=True+frame, isOpened=False → SnapshotError (max_attempts=1)
       - `test_capture_dispatch_rtsp` — `snapshot.capture("rtsp://cam/live", ...)` → monkeypatched `snapshot.capture_rtsp` called=True, `snapshot._capture_ffmpeg` called=False
       - `test_capture_dispatch_mp4` — `snapshot.capture("https://storage.example/fire/source_v2.mp4", ...)` → reverse

    2. **fixture 패턴** (RESEARCH §Example C line 700-715):
       ```python
       @pytest.fixture
       def fake_frame():
           arr = MagicMock(name="bgr_ndarray")
           arr.shape = (720, 1280, 3)
           return arr

       def _make_cap(read_outcomes: list, is_opened: bool = True):
           cap = MagicMock(name="VideoCapture")
           cap.isOpened.return_value = is_opened
           cap.read.side_effect = read_outcomes
           return cap
       ```

    3. **sys.path 컨벤션** (test_scheduler_buffer.py line 1-7 미러):
       ```python
       import sys
       from pathlib import Path
       AGENT_DIR = Path(__file__).resolve().parents[1]
       if str(AGENT_DIR) not in sys.path:
           sys.path.insert(0, str(AGENT_DIR))
       import snapshot  # noqa
       ```

    4. **monkeypatch.setattr(snapshot, "time", MagicMock(sleep=MagicMock()))** 패턴으로 실제 sleep 발생 X (테스트 ~2초 내 종료).

    5. **회귀 가드**: 동시에 기존 test_scheduler_buffer.py + test_fusion.py 도 PASS 유지 (full suite green).
  </action>
  <verify>
    <automated>cd ai_agent && python -m pytest tests/test_snapshot_rtsp.py -x -v 2>&1 | tee /tmp/p8t2a.log; grep -E '6 passed|7 passed' /tmp/p8t2a.log; cd ai_agent && python -m pytest tests/ -x -v 2>&1 | tee /tmp/p8t2b.log; tail -5 /tmp/p8t2b.log</automated>
  </verify>
  <done>
    - 신규 파일 `ai_agent/tests/test_snapshot_rtsp.py` 존재 (`test -f ai_agent/tests/test_snapshot_rtsp.py`)
    - 6개 test 함수 모두 정의 (`grep -c '^def test_' ai_agent/tests/test_snapshot_rtsp.py` == 6)
    - `python -m pytest tests/test_snapshot_rtsp.py -x -v` exit code 0 + "6 passed" 출력
    - 전체 ai_agent/tests/ suite PASS 유지 — Phase 2·3 의 test_scheduler_buffer + test_fusion regression 0
    - 실행 시간 < 5초 (monkeypatched sleep)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 08-01)

| Boundary | Description |
|----------|-------------|
| Internal cv2 → Python | RTSP 스트림 → cv2.VideoCapture in-process. 외부 untrusted input 진입점 없음 — cameras.live_url_detail 은 manager 가 입력한 신뢰값 (RLS cameras_update_manager). |

## STRIDE Threat Register

본 plan 은 in-process Python 변경만 — 신규 trust boundary 없음. Phase 8 전체에 분산된
T-8-01~05 중 T-8-05 (cameras.live_url_detail credentials 노출) 만 본 plan 과 *간접* 연관
(URL 을 capture_rtsp 로 전달). 직접 mitigation 없음 — 노출 자체는 plan 08-02 의 cameras
ALTER 와 무관 (live_url_detail 은 002_tables.sql 에서 이미 존재).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-05 (noted) | I (Information Disclosure) | cameras.live_url_detail (rtsp://user:pass@... 평문) | accept (v1.0 PoC) | v1.1 Vault 마이그 권장 (별도 phase). 본 plan 은 URL 을 cv2 에 in-process 전달만 — 추가 노출 없음. |
</threat_model>

<verification>
1. `cd ai_agent && python -m pytest tests/test_snapshot_rtsp.py -x -v` → 6 passed
2. `cd ai_agent && python -m pytest tests/ -x -v` → 전체 suite green (regression 0)
3. `git diff --stat ai_agent/scheduler.py` → 변경 0줄 (SC #4 — capture 호출자 무변경 가드)
4. `grep -c 'def capture_rtsp\|def _capture_ffmpeg\|def capture(' ai_agent/snapshot.py` >= 3
5. `grep -c 'OPENCV_FFMPEG_CAPTURE_OPTIONS' ai_agent/snapshot.py` == 1 (Pitfall 6 가드)
6. `grep -c 'opencv-python>=4' ai_agent/requirements.txt` == 1 (회귀 가드 — opencv prebuilt FFMPEG=YES 의존성 명시 보존)
</verification>

<success_criteria>
- [ ] capture_rtsp() 가 drift_test.py 의 3-가드 (cap.isOpened + frame is not None + ret) 준수
- [ ] capture() URL scheme 분기 (rtsp:// → capture_rtsp / 그 외 → _capture_ffmpeg) 동작
- [ ] 6 pytest cases ALL PASS, 전체 suite regression 0
- [ ] scheduler.py 무변경 (SC #4 — 4 detector 진입점 호출 그대로)
- [ ] backoff = 2초 고정 3회 (D-02 amended), 3회 실패 시 SnapshotError
- [ ] OPENCV_FFMPEG_CAPTURE_OPTIONS=rtsp_transport;tcp 환경변수 module-level 설정 (Pitfall 6)
- [ ] RTSP-01 의 "URL scheme 분기" 부분 충족 (실 RTSP 검증은 plan 08-04 mediamtx)
</success_criteria>

<output>
After completion, create `.planning/phases/08-rtsp-camera/08-01-SUMMARY.md`
</output>
