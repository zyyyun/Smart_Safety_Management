---
phase: 08-rtsp-camera
plan: 04
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - .gitignore
  - bin/mediamtx/
  - scripts/mediamtx.yml
  - scripts/start_rtsp_mock.sh
  - scripts/stop_rtsp_mock.sh
  - scripts/restore_cameras_mp4.sql
autonomous: true
requirements: [RTSP-01, RTSP-02, RTSP-03]
tags: [phase-8, rtsp, mediamtx, ffmpeg, synthetic-validation, e2e, healthcheck-roundtrip, T-8-04, RTSP-02-deferred]

user_setup: []

must_haves:
  truths:
    - ".gitignore 에 `bin/` 추가 (advisor #3 — ~30MB 바이너리 git 노출 차단, mediamtx 다운로드 *전*에 적용)"
    - "bin/mediamtx/mediamtx.exe 다운로드 (https://github.com/bluenviron/mediamtx/releases/latest, Windows amd64)"
    - "scripts/mediamtx.yml — 5 paths (fire/helmet/forklift/person/fall) runOnDemand 패턴 + 정확한 reference_media 파일 매핑 (advisor #1)"
    - "scripts/start_rtsp_mock.sh + stop_rtsp_mock.sh launcher — Windows Git Bash 호환"
    - "scheduler --once-detect 실행 시 mediamtx 5 paths 중 ≥1개 detection_events row 적재 (RTSP-01 합성 검증 — Pitfall 5 fire conf 0.10 D-19 fallback 가능)"
    - "mediamtx kill 시 capture_rtsp 가 backoff 3회 시도 + SnapshotError raise + scheduler [SNAPSHOT_ERR] log (RTSP-03 backoff)"
    - "5분+ 대기 → cameras_healthcheck() 가 down 전이 + Edge Function camera-down 호출 + manager FCM 발사 (RTSP-03 헬스체크 round-trip)"
    - "mediamtx 재시작 → 1분 cycle 내 cameras.last_frame_at 갱신 → cameras_healthcheck() 가 ok 전이 + camera-recovered FCM (RTSP-03 회복)"
    - "mediamtx 잔존 차단 — stop_rtsp_mock.sh 가 시연 후 즉시 정리 (T-8-04)"
    - "scripts/restore_cameras_mp4.sql — 검증 후 cameras.live_url_detail 원복 (Phase 1 mp4 URL)"
    - "RTSP-02 명시적 deferred — `[deferred — Drift X3 실기기 부재] mediamtx 로컬 ≈0초 측정으로 SC #2 부분 충족, v1.1 6월 검단·포천 LP-3 본격`"
  artifacts:
    - path: ".gitignore"
      provides: "bin/ 차단 — mediamtx 30MB 바이너리 무커밋"
      contains: "bin/"
    - path: "bin/mediamtx/mediamtx.exe"
      provides: "RTSP server (5 paths runOnDemand)"
    - path: "scripts/mediamtx.yml"
      provides: "5 paths config — 정확한 reference_media 파일 매핑"
      contains: "rtspAddress: :8554"
    - path: "scripts/start_rtsp_mock.sh"
      provides: "Windows Git Bash 호환 mediamtx launcher"
    - path: "scripts/stop_rtsp_mock.sh"
      provides: "mediamtx + ffmpeg 정리 (T-8-04)"
    - path: "scripts/restore_cameras_mp4.sql"
      provides: "cameras.live_url_detail 원복 SQL"
  key_links:
    - from: "scheduler --once-detect"
      to: "mediamtx RTSP server (rtsp://localhost:8554/{fire,helmet,...})"
      via: "cameras.live_url_detail UPDATE → snapshot.capture URL 분기 → capture_rtsp"
      pattern: "rtsp://localhost:8554"
    - from: "mediamtx kill"
      to: "capture_rtsp backoff 3회 → SnapshotError"
      via: "RTSP TEARDOWN signal (SIGKILL) — Pitfall 5"
      pattern: "SnapshotError"
    - from: "5분+ no-frame"
      to: "cameras_healthcheck() down 전이 + camera-down FCM"
      via: "pg_cron 1분 trigger + Vault sr_key + net.http_post"
      pattern: "camera-down"
---

<objective>
Wave 4 — mediamtx 합성 RTSP server 로 RTSP-01·03 E2E 검증. 사용자 제공 reference_media 의
실제 파일 매핑 + .gitignore 가드 + launcher/cleanup 스크립트 + scheduler --once-detect
E2E + backoff 검증 + 5분 healthcheck round-trip 검증. RTSP-02 (Drift X3 실기기) 는
명시적 deferred — mediamtx 로컬 ≈0초 측정으로 SC #2 부분 충족, v1.1 6월 검단·포천 본격.

Purpose: Wave 1·2·3 의 산출물 (snapshot.capture_rtsp + cameras_healthcheck() + Edge
Function camera-down/recovered) 을 합성 환경에서 끝-끝 검증. Drift X3 실기기 부재로
완전 PoC 차단 → mediamtx + 5종 mp4 publish 로 RTSP-01 의 detection_events 적재 +
RTSP-03 의 backoff/healthcheck/recovery 모두 verifiable.

Output:
- .gitignore 갱신 (bin/)
- bin/mediamtx/mediamtx.exe 다운로드 (.gitignore 차단됨)
- scripts/mediamtx.yml + start_rtsp_mock.sh + stop_rtsp_mock.sh
- scripts/restore_cameras_mp4.sql (검증 후 원복)
- E2E 검증 결과 SUMMARY.md 기재 (RTSP-01·03 충족 증거 + RTSP-02 deferred 표기)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-rtsp-camera/08-CONTEXT.md
@.planning/phases/08-rtsp-camera/08-RESEARCH.md
@.planning/phases/08-rtsp-camera/08-01-PLAN.md
@.planning/phases/08-rtsp-camera/08-02-PLAN.md
@.planning/phases/08-rtsp-camera/08-03-PLAN.md
@.planning/phases/07-watch-app-bridge/07-04-SUMMARY.md
@.gitignore
@ai_agent/scheduler.py
@ai_agent/snapshot.py

<interfaces>
<!-- 검증 대상 — Wave 1·2·3 의 산출물 -->

snapshot.capture (plan 08-01):
```python
capture("rtsp://localhost:8554/fire", tmp_path) → tmp_path  # capture_rtsp 분기
capture("rtsp://localhost:8554/dead", tmp_path) → SnapshotError after 3 attempts
```

cameras_healthcheck() (plan 08-02):
```sql
-- pg_cron 매분 호출
-- DOWN: last_frame_at < now() - 5min AND health_state != 'down' AND (last_alert_at NULL OR < now()-30min)
-- RECOVERY: last_frame_at >= now() - 5min AND health_state = 'down'
```

notifications case 'camera-down'/'camera-recovered' (plan 08-03):
```typescript
// pg_net 의 net.http_post 으로 호출됨, sendPushToUsers (plural) — manager FCM
```

<!-- 사용자 환경 — 실제 reference_media 파일 (advisor #1 검증 결과) -->
- reference_media/fire/ 비어있음 (Storage 의 fire/source_v2.mp4 가 진실 source) → 로컬 매핑은 reference_media/fire_aihub_0087.mp4 사용 (DATA-02 source)
- reference_media/helmet/ 비어있음 → 로컬 매핑은 reference_media/helmet_h0_demo.mp4 사용 (DATA-01 source)
- reference_media/forklift/ 비어있음 → 재사용 reference_media/helmet_h0_L2_09-08_001.mp4 (forklift detector 의 검출 의도와 무관 — RTSP-01 은 'frame 추출 + detector 호출 가능' 만 검증)
- reference_media/person/ 비어있음 → 재사용 reference_media/helmet_h0_L2_09-08_001.mp4 (person 도 same)
- reference_media/fall/E02_001.mp4 존재
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore + mediamtx 다운로드 + scripts (mediamtx.yml + start/stop launcher)</name>
  <read_first>
    - .gitignore (현재 — bin/ 항목 부재 verified)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pattern 5: mediamtx + ffmpeg synthetic publish" (line 496-555)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 5: ffmpeg subprocess vs mediamtx graceful shutdown" (line 624-629)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Open Question 4: mediamtx 다운로드 시점" (line 867-870) + §"A1·A5" (line 839, 843)
  </read_first>
  <action>
    1. **`.gitignore` 에 `bin/` 추가** — *MUST* be 첫 step (advisor #3, mediamtx 다운로드 *전*에 적용해야 git 노출 차단). Edit `.gitignore` 끝에 신규 섹션:
       ```
       # -- Phase 8 RTSP 합성 검증 (mediamtx binary 30MB)
       bin/
       ```
       commit 또는 stage 후 진행.

    2. **mediamtx Windows binary 다운로드** — `bin/mediamtx/` 디렉터리 생성 후:
       - URL: https://github.com/bluenviron/mediamtx/releases/latest
       - 파일: `mediamtx_v1.x.x_windows_amd64.zip`
       - 압축 해제 → `bin/mediamtx/mediamtx.exe` (필수) + `bin/mediamtx/mediamtx.yml` (default config — 무시, 본 plan 의 scripts/mediamtx.yml 사용)
       - 검증: `test -x bin/mediamtx/mediamtx.exe && bin/mediamtx/mediamtx.exe --version` → 버전 출력
       - 검증: `git status` 출력에 `bin/` 부재 (.gitignore 적용 확인)

    3. **신규 `scripts/mediamtx.yml`** — 5 paths runOnDemand 패턴 (RESEARCH §Pattern 5 line 504-524 + advisor #1 의 정확한 파일 매핑):
       ```yaml
       # Source: https://mediamtx.org/docs/usage/publish + bluenviron/mediamtx
       # Phase 8 plan 04 — 5 paths, runOnDemand 패턴 (클라이언트 connect 시 ffmpeg 시작)
       logLevel: info
       rtspAddress: :8554

       paths:
         fire:
           # advisor #1: reference_media/fire/ 비어있음 → fire_aihub_0087.mp4 사용 (DATA-02 source)
           runOnDemand: ffmpeg -re -stream_loop -1 -i reference_media/fire_aihub_0087.mp4 -c copy -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
           runOnDemandRestart: yes
         helmet:
           # advisor #1: reference_media/helmet/ 비어있음 → helmet_h0_demo.mp4 (DATA-01 source)
           runOnDemand: ffmpeg -re -stream_loop -1 -i reference_media/helmet_h0_demo.mp4 -c copy -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
           runOnDemandRestart: yes
         forklift:
           # advisor #1: forklift mp4 부재 → helmet_h0_L2_09-08_001.mp4 재사용 (RTSP-01 = frame 추출 + detector 호출 가능 검증, 검출 자체는 v1.1)
           runOnDemand: ffmpeg -re -stream_loop -1 -i reference_media/helmet_h0_L2_09-08_001.mp4 -c copy -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
           runOnDemandRestart: yes
         person:
           # advisor #1: person mp4 부재 → 동일 재사용
           runOnDemand: ffmpeg -re -stream_loop -1 -i reference_media/helmet_h0_L2_09-08_001.mp4 -c copy -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
           runOnDemandRestart: yes
         fall:
           # 정상 매핑 — reference_media/fall/E02_001.mp4
           runOnDemand: ffmpeg -re -stream_loop -1 -i reference_media/fall/E02_001.mp4 -c copy -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
           runOnDemandRestart: yes
       ```

    4. **신규 `scripts/start_rtsp_mock.sh`** (Windows Git Bash, RESEARCH §Pattern 5 line 528-545):
       ```bash
       #!/usr/bin/env bash
       # scripts/start_rtsp_mock.sh — Phase 8 plan 04 mediamtx launcher
       set -euo pipefail
       ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
       MEDIAMTX_BIN="$ROOT/bin/mediamtx/mediamtx.exe"
       CONFIG="$ROOT/scripts/mediamtx.yml"

       if [[ ! -x "$MEDIAMTX_BIN" ]]; then
           echo "[FATAL] mediamtx binary not found at $MEDIAMTX_BIN"
           echo "        Download: https://github.com/bluenviron/mediamtx/releases/latest"
           echo "        Extract to: $ROOT/bin/mediamtx/"
           exit 1
       fi

       cd "$ROOT"
       echo "[INFO] mediamtx server starting on :8554 (paths: fire, helmet, forklift, person, fall)"
       exec "$MEDIAMTX_BIN" "$CONFIG"
       ```

    5. **신규 `scripts/stop_rtsp_mock.sh`** — T-8-04 mitigation (시연 후 mediamtx 잔존 차단):
       ```bash
       #!/usr/bin/env bash
       # scripts/stop_rtsp_mock.sh — Phase 8 plan 04 cleanup (T-8-04)
       set -uo pipefail
       echo "[INFO] killing mediamtx + spawned ffmpeg publishers"
       # Windows Git Bash — taskkill 우선, fallback pkill
       if command -v taskkill >/dev/null 2>&1; then
           taskkill //F //IM mediamtx.exe 2>/dev/null || true
           taskkill //F //IM ffmpeg.exe   2>/dev/null || true
       else
           pkill -9 -f mediamtx 2>/dev/null || true
           pkill -9 -f 'ffmpeg.*rtsp://localhost:8554' 2>/dev/null || true
       fi
       echo "[INFO] cleanup complete"
       ```
       Pitfall 5 fix: SIGKILL (taskkill /F) 사용 — graceful shutdown 의 RTSP TEARDOWN 회피 (capture_rtsp backoff 가 정상 시도하도록).

    6. **신규 `scripts/restore_cameras_mp4.sql`** — 검증 후 원복:
       ```sql
       -- Phase 8 plan 04 검증 후 원복 — Phase 1 의 mp4 URL 로 복귀
       -- 본 SQL 은 검증용 임시 cameras 변경을 되돌림.
       UPDATE public.cameras SET live_url_detail =
         'https://xbjqxnvemcqubjfflain.supabase.co/storage/v1/object/public/reference-videos/fire/source_v2.mp4'
         WHERE camera_id = 1;
       UPDATE public.cameras SET live_url_detail =
         'https://xbjqxnvemcqubjfflain.supabase.co/storage/v1/object/public/reference-videos/helmet/source_v2.mp4'
         WHERE camera_id = 5;
       -- 다른 카메라 (2·3·기타) 는 검증 단계에서 변경 X (Task 2 의 UPDATE 가 1·5 만 변경)
       ```

    7. **chmod +x** scripts (Windows Git Bash 에서도 호환): `chmod +x scripts/start_rtsp_mock.sh scripts/stop_rtsp_mock.sh`.
  </action>
  <verify>
    <automated>grep -c '^bin/' .gitignore && test -x bin/mediamtx/mediamtx.exe && bin/mediamtx/mediamtx.exe --version 2>&1 | head -3 && git status --porcelain bin/ 2>&1 | grep -c '^' && test -f scripts/mediamtx.yml && grep -c 'rtspAddress: :8554' scripts/mediamtx.yml && test -x scripts/start_rtsp_mock.sh && test -x scripts/stop_rtsp_mock.sh && test -f scripts/restore_cameras_mp4.sql && echo SETUP_OK</automated>
  </verify>
  <done>
    - `.gitignore` 에 `bin/` 등장 (`grep -c '^bin/' .gitignore` >= 1)
    - `bin/mediamtx/mediamtx.exe` 실행 가능 (--version 출력)
    - `git status bin/` 에 항목 0건 (.gitignore 적용 확인)
    - `scripts/mediamtx.yml` 존재 + 5 paths (fire/helmet/forklift/person/fall) + 정확한 reference_media 파일 매핑 (advisor #1)
    - `scripts/start_rtsp_mock.sh` + `scripts/stop_rtsp_mock.sh` 모두 실행 권한
    - `scripts/restore_cameras_mp4.sql` 존재 (camera_id 1·5 원복 SQL)
    - mediamtx 다운로드 30MB 가 git stage 에 0건
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E 합성 검증 — RTSP-01 detection_events + RTSP-03 backoff + 5분 healthcheck round-trip</name>
  <read_first>
    - scripts/mediamtx.yml + start_rtsp_mock.sh + stop_rtsp_mock.sh (Task 1 산출물)
    - .planning/phases/08-rtsp-camera/08-CONTEXT.md §"D-04 합성 검증 전략" (line 193-227)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Validation Architecture" (line 902-945)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 5: mediamtx kill" (line 624-629)
    - ai_agent/scheduler.py (run_detection_cycle 진입점 + --once-detect CLI flag — `grep -n 'argparse\|once-detect' ai_agent/scheduler.py`)
  </read_first>
  <action>
    **검증 절차 — 5단계, 모두 SUMMARY.md 에 evidence 기록:**

    1. **(준비) cameras 임시 갱신** — camera_id 1 (fire) + 5 (helmet) 만 mediamtx URL 로:
       ```sql
       UPDATE public.cameras SET live_url_detail='rtsp://localhost:8554/fire'   WHERE camera_id=1;
       UPDATE public.cameras SET live_url_detail='rtsp://localhost:8554/helmet' WHERE camera_id=5;
       ```
       기록: SUMMARY 에 변경 전 URL 보존 (restore_cameras_mp4.sql 와 일치 확인).

    2. **(RTSP-01 합성 검증) mediamtx 시작 + scheduler --once-detect**:
       - terminal A: `bash scripts/start_rtsp_mock.sh` (foreground or `&`)
       - mediamtx 시작 후 ~5초 wait (runOnDemand 가 ffmpeg publish 시작 대기)
       - terminal B: `cd ai_agent && python -m scheduler --once-detect` (또는 `python scheduler.py --once-detect` — 정확 entry point는 `argparse` grep 결과 따름)
       - 기대 log: `[OK] camera_id=1 ...` + `[FIRE] ... event_id=...` 또는 helmet 의 `[head]` 등 ≥1건. fire conf 0.10 (Phase 1 D-19 fallback) 적용됨.
       - 검증 SQL:
         ```sql
         SELECT count(*) FROM detection_events WHERE created_at > now() - INTERVAL '5 minutes';
         ```
         기대 ≥ 1. 0이면 fire conf 0.10 + frames_required=5 로 spike 미달 가능 — helmet 으로 확인.

    3. **(RTSP-03 backoff 검증) mediamtx kill → backoff 3회 → SnapshotError**:
       - terminal A 의 mediamtx 를 SIGKILL (Ctrl-C 가 아닌 — Pitfall 5): `bash scripts/stop_rtsp_mock.sh` (taskkill /F)
       - terminal B: `python -m scheduler --once-detect` 재실행
       - 기대 log: `[SNAPSHOT_ERR] camera_id=1: RTSP capture failed after 3 attempts (url=rtsp://localhost:8554/fire): ...` (drift_test 가드 + plan 08-01 의 SnapshotError 메시지)
       - 시간 측정: 시도 사이 2초 + 시도 내 2초 handshake = ~12초 (3회). 정확 시간 SUMMARY 에 기록.

    4. **(RTSP-03 healthcheck round-trip 검증) 5분+ 대기 → cameras_healthcheck() down 전이 + camera-down FCM**:
       - 시점 t0 = mediamtx kill 후 cameras.last_frame_at 마지막 갱신 시각 (Task 2 의 step 2 의 capture 성공 시각). SQL: `SELECT camera_id, last_frame_at, health_state FROM cameras WHERE camera_id IN (1,5);` → 기록.
       - t0 + 5분 + 1분 (cron 다음 tick) = t0 + 6분 후 SQL 재조회:
         ```sql
         SELECT camera_id, health_state, last_alert_at FROM cameras WHERE camera_id IN (1,5);
         ```
         기대: health_state='down', last_alert_at 갱신.
       - Edge Function log 확인 (Supabase Dashboard → Functions → notifications → Logs): `case 'camera-down'` invocation + sendPushToUsers result (sent ≥ 1 if testuser1 manager group_id matches).
       - 또는 net.http_response 테이블 (pg_net 가 보관) — `SELECT status_code, content::text FROM net._http_response ORDER BY created DESC LIMIT 5;` → 200.

    5. **(RTSP-03 회복 검증) mediamtx 재시작 → 1분 cycle → cameras.last_frame_at 갱신 → ok 회복 + camera-recovered FCM**:
       - terminal A: `bash scripts/start_rtsp_mock.sh` (재시작)
       - terminal B: `python -m scheduler --once-detect` (capture 성공 → update_camera_health 호출)
       - SQL 즉시 검증: `SELECT camera_id, last_frame_at FROM cameras WHERE camera_id=1;` — 갱신됨
       - 다음 cron tick (≤1분) 대기 후: `SELECT camera_id, health_state, last_alert_at FROM cameras WHERE camera_id=1;` — health_state='ok' 회복
       - net._http_response 에 camera-recovered 200 추가 1건

    6. **(정리) cameras URL 원복 + mediamtx kill**:
       - `psql -f scripts/restore_cameras_mp4.sql` (camera_id 1·5 mp4 URL 원복)
       - `bash scripts/stop_rtsp_mock.sh` (mediamtx + ffmpeg 정리)
       - 검증: `tasklist | grep -E 'mediamtx|ffmpeg'` 출력 0줄 (T-8-04)

    7. **RTSP-02 deferred 표기 — SUMMARY.md** 에 명시:
       > **RTSP-02 deferred — Drift X3 실기기 부재.** mediamtx 로컬 검증으로 RTSP frame 추출
       > + detection_events 적재 + ≤10s 지연 (mediamtx 로컬 ≈ 0초 측정) 부분 충족.
       > 실기기 검증은 v1.1 6월 검단·포천 LP-3 단계에서 본격. (Phase 7 04 deferred 패턴 동일.)

    **모든 결과 SUMMARY 에 timestamp + log tail + SQL 결과 + Edge Function log 캡처 첨부.**
  </action>
  <verify>
    <automated>bash scripts/start_rtsp_mock.sh > /tmp/p8_mediamtx.log 2>&1 & echo $! > /tmp/p8_mediamtx.pid; sleep 8; psql "$SUPABASE_DB_URL" -c "UPDATE cameras SET live_url_detail='rtsp://localhost:8554/fire' WHERE camera_id=1; UPDATE cameras SET live_url_detail='rtsp://localhost:8554/helmet' WHERE camera_id=5;"; cd ai_agent && timeout 90 python -m scheduler --once-detect 2>&1 | tee /tmp/p8_once1.log | tail -30; psql "$SUPABASE_DB_URL" -c "SELECT camera_id,last_frame_at FROM cameras WHERE camera_id IN (1,5);"; bash ../scripts/stop_rtsp_mock.sh; cd .. && timeout 30 python -m ai_agent.scheduler --once-detect 2>&1 | tee /tmp/p8_backoff.log | tail -20; grep -c 'SNAPSHOT_ERR\|failed after 3 attempts' /tmp/p8_backoff.log; psql "$SUPABASE_DB_URL" -f scripts/restore_cameras_mp4.sql; echo "MANUAL: 5min wait + healthcheck round-trip 검증은 SUMMARY 에 timestamp 로 기록"</automated>
  </verify>
  <done>
    - mediamtx 시작 후 cameras.live_url_detail UPDATE 성공 (camera 1 + 5 → rtsp://localhost:8554/...)
    - scheduler --once-detect 실행 시 capture_rtsp 가 성공 + cameras.last_frame_at 갱신 (RTSP-01 충족 — `SELECT last_frame_at FROM cameras WHERE camera_id IN (1,5)` 갱신 확인)
    - detection_events row ≥ 1건 적재 (5분 윈도우, RTSP-01 합성 충족 증거 — fire conf 0.10 D-19 fallback 또는 helmet 으로 확인)
    - mediamtx kill 후 scheduler --once-detect 실행 시 `SNAPSHOT_ERR` + `failed after 3 attempts` log (RTSP-03 backoff 충족)
    - 5분+ 대기 후 cameras.health_state='down' 전이 + last_alert_at 갱신 (RTSP-03 healthcheck round-trip — SUMMARY 에 timestamp 기록)
    - net._http_response 에 camera-down 200 + (회복 후) camera-recovered 200 (각 1건)
    - mediamtx 재시작 + scheduler 1 cycle → cameras.last_frame_at 갱신 → 다음 cron tick 에 health_state='ok' 회복
    - scripts/restore_cameras_mp4.sql 적용 후 cameras 원복 (camera 1·5 → mp4 storage URL)
    - mediamtx + ffmpeg process 잔존 0 (`tasklist` 출력 0줄, T-8-04)
    - SUMMARY 에 RTSP-02 deferred 명시 (Drift X3 실기기 부재 + mediamtx 로컬 ≈0초 측정 + v1.1 LP-3)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 08-04)

| Boundary | Description |
|----------|-------------|
| Local mediamtx + ffmpeg processes | 검증 단계만 — :8554 listen 로컬 only. 외부 노출 0 (방화벽 default). |
| 시연 종료 후 mediamtx 잔존 | T-8-04 — stop_rtsp_mock.sh 가 강제 정리 (taskkill /F 또는 pkill -9). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | T (Tampering) / D (DoS) | mediamtx 가 시연 후 운영 동작 시 잔존 — 외부 attacker 가 :8554 접근 시 RTSP server 노출 | mitigate | scripts/stop_rtsp_mock.sh 가 시연 종료 시 즉시 정리 (taskkill /F /IM mediamtx.exe + ffmpeg.exe). 검증 task 의 마지막 step 에 명시. SUMMARY 에 `tasklist` 출력 0줄 evidence 첨부. v1.x 영구 운영 시 systemd service + auth (deferred). |
</threat_model>

<verification>
1. `.gitignore` 에 `bin/` 추가 + `git status bin/` 0 항목
2. `bin/mediamtx/mediamtx.exe --version` 출력
3. `scripts/mediamtx.yml` + `start_rtsp_mock.sh` + `stop_rtsp_mock.sh` + `restore_cameras_mp4.sql` 모두 존재 + 권한
4. mediamtx 시작 후 scheduler --once-detect → cameras.last_frame_at 갱신 (RTSP-01)
5. detection_events row ≥ 1건 (RTSP-01 합성)
6. mediamtx kill → scheduler --once-detect → `SNAPSHOT_ERR ... failed after 3 attempts` (RTSP-03 backoff)
7. 5분+ 대기 → cameras.health_state='down' + last_alert_at 갱신 (RTSP-03 healthcheck)
8. net._http_response 에 camera-down 200 + camera-recovered 200
9. mediamtx 재시작 + scheduler → health_state='ok' 회복
10. 정리 후 mediamtx + ffmpeg process 0건 (T-8-04)
11. SUMMARY 에 RTSP-02 deferred 명시
</verification>

<success_criteria>
- [ ] .gitignore bin/ 차단 (advisor #3) — mediamtx 30MB 무커밋
- [ ] mediamtx + 5 paths config + launcher 동작
- [ ] RTSP-01 합성 충족: scheduler --once-detect → capture_rtsp 성공 + detection_events row + cameras.last_frame_at 갱신
- [ ] RTSP-03 backoff 검증: mediamtx kill → SnapshotError after 3 attempts (~12초)
- [ ] RTSP-03 healthcheck round-trip: 5분 무수신 → down 전이 + camera-down FCM
- [ ] RTSP-03 회복: mediamtx 재시작 → ok 회복 + camera-recovered FCM
- [ ] T-8-04 mitigation: 시연 후 mediamtx + ffmpeg process 0건 (잔존 0)
- [ ] cameras 원복 (camera 1·5 mp4 storage URL)
- [ ] RTSP-02 deferred 표기 SUMMARY 에 명시 (Drift X3 실기기 부재 + v1.1 LP-3)
- [ ] Phase 8 ROADMAP SC #1·#3·#4 충족 + SC #2 부분 충족 (실기기만 deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/08-rtsp-camera/08-04-SUMMARY.md` (RTSP-02 deferred frontmatter status)
</output>
