---
phase: 08-rtsp-camera
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - supabase/migrations/012_cameras_health.sql
  - tests/sql/test_012_cameras_health_isolation.sql
autonomous: true
requirements: [RTSP-03]
tags: [phase-8, rtsp, supabase, migration, pg-cron, pg-net, vault, healthcheck, schema-push, blocking, T-8-01, T-8-02, T-8-05]

user_setup:
  - service: supabase
    why: "012 마이그레이션 운영 DB 적용 (pg_net 활성화 + Vault 시드 + cameras ALTER + healthcheck 함수 + cron job)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token (Phase 4·7 동일)"
      - name: SUPABASE_DB_PASSWORD
        source: "Phase 4 010 에서 이미 .env 적용됨"
    dashboard_config:
      - task: "(Fallback) Vault 시드 — `service_role_key` + `edge_function_base_url` 2개 secrets, supabase db push 가 SQL 시드 실패 시 필요"
        location: "Supabase Dashboard → Project Settings → Vault → New Secret"

must_haves:
  truths:
    - "012 마이그레이션이 `CREATE EXTENSION IF NOT EXISTS pg_net WITH SCHEMA extensions;` 첫 줄에 명시 (Pitfall 1 / RESEARCH 정정 #2)"
    - "cameras 에 last_frame_at TIMESTAMPTZ + health_state TEXT('ok'|'degraded'|'down'|'unknown') + last_alert_at TIMESTAMPTZ 3 컬럼 ALTER ADD"
    - "Vault 시드 — vault.secrets 에 'service_role_key' + 'edge_function_base_url' 2 rows (마이그 SQL 시드 또는 dashboard 1회)"
    - "cameras_healthcheck() 함수 SECURITY DEFINER + Vault 에서 sr_key/base_url SELECT (RESEARCH 정정 #3 — current_setting GUC 패턴 거부)"
    - "DOWN 전이: last_frame_at IS NOT NULL AND last_frame_at < now()-5min AND health_state != 'down' AND (last_alert_at IS NULL OR last_alert_at < now()-30min) — Phase 4 D-09 알림 전이 + 30분 cooldown"
    - "RECOVERY 전이: health_state='down' AND last_frame_at >= now()-5min — ok 회복 + 종료 알림 1회"
    - "pg_cron 1분 주기 'cameras_healthcheck_minute' job 등록 (Phase 4 010 의 unschedule + schedule idempotent 패턴)"
    - "supabase db push --linked --yes 적용 성공 (운영 DB)"
    - "SQL test isolation: anon role 로 cameras.health_state UPDATE 시도 → permission denied (003 의 cameras_update_manager 정책 회귀 가드, T-8-02)"
  artifacts:
    - path: "supabase/migrations/012_cameras_health.sql"
      provides: "pg_net 활성화 + cameras 3 컬럼 ALTER + Vault 시드 + cameras_healthcheck() + pg_cron job"
      contains: "CREATE EXTENSION IF NOT EXISTS pg_net"
    - path: "tests/sql/test_012_cameras_health_isolation.sql"
      provides: "anon role 의 cameras.health_state UPDATE 차단 회귀 가드"
      contains: "SET LOCAL ROLE anon"
  key_links:
    - from: "pg_cron job 'cameras_healthcheck_minute'"
      to: "public.cameras_healthcheck() 함수"
      via: "cron.schedule('* * * * *', $$SELECT public.cameras_healthcheck();$$)"
      pattern: "cron.schedule.*cameras_healthcheck"
    - from: "cameras_healthcheck() DOWN 분기"
      to: "notifications/index.ts case 'camera-down' (plan 08-03 신규)"
      via: "net.http_post + Vault sr_key + body.action='camera-down'"
      pattern: "'camera-down'"
    - from: "cameras_healthcheck() RECOVERY 분기"
      to: "notifications/index.ts case 'camera-recovered' (plan 08-03 신규)"
      via: "net.http_post + body.action='camera-recovered'"
      pattern: "'camera-recovered'"
---

<objective>
운영 DB 에 cameras 헬스체크 인프라 lock — pg_net 활성화 + Vault 시드 + cameras 3 컬럼 ALTER
+ cameras_healthcheck() 함수 (5분 임계 / 30분 cooldown / Phase 4 D-09 알림 전이) + pg_cron
1분 주기 job. 본 plan 의 핵심 gate = `[BLOCKING] supabase db push --linked --yes`.

Purpose: Edge Function 'camera-down'/'camera-recovered' (plan 08-03) 가 호출될 인프라 +
RTSP-03 의 "헬스체크 SQL + 5분 무수신 알림" 요건. Vault + SECURITY DEFINER 로 service_role
key 코드/git 노출 0 (T-8-01 mitigation).

Output:
- supabase/migrations/012_cameras_health.sql (운영 DB 적용 완료)
- tests/sql/test_012_cameras_health_isolation.sql (RLS 회귀 가드)
- Vault 시드 검증 (vault.decrypted_secrets 에 2 rows)
- pg_cron 'cameras_healthcheck_minute' 등록 검증 (cron.job 1 row)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rtsp-camera/08-CONTEXT.md
@.planning/phases/08-rtsp-camera/08-RESEARCH.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@supabase/migrations/001_extensions.sql
@supabase/migrations/002_tables.sql
@supabase/migrations/003_rls_policies.sql

<interfaces>
<!-- Plan 08-03 의 Edge Function camera-down/recovered 가 받을 payload 계약 -->

cameras_healthcheck() 가 net.http_post 으로 보내는 body (DOWN):
```json
{
  "action": "camera-down",
  "camera_id": <int>,
  "group_id": <int|null>,
  "last_frame_at": "<iso timestamp>"
}
```

RECOVERY:
```json
{
  "action": "camera-recovered",
  "camera_id": <int>,
  "group_id": <int|null>
}
```

Headers: `Authorization: Bearer <Vault sr_key>`, `Content-Type: application/json`
URL: `<Vault base_url>` + "/notifications" (예: https://xbjqxnvemcqubjfflain.supabase.co/functions/v1/notifications)

cameras 컬럼 추가 (002 ALTER):
```sql
last_frame_at  TIMESTAMPTZ  -- snapshot.capture 성공 시점 (plan 08-03 의 update_camera_health 가 갱신)
health_state   TEXT DEFAULT 'unknown' CHECK (health_state IN ('ok','degraded','down','unknown'))
last_alert_at  TIMESTAMPTZ  -- 30분 cooldown + ok↔down 전이 시점
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 012_cameras_health.sql 작성</name>
  <read_first>
    - supabase/migrations/001_extensions.sql (현재 활성 extensions: postgis + pg_cron)
    - supabase/migrations/002_tables.sql (line 57-80, cameras 테이블 정의)
    - supabase/migrations/003_rls_policies.sql (cameras_update_manager 정책 — T-8-02 회귀 가드)
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md §D-09 (알림 전이 원칙) + §D-11 (FCM only)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pattern 3: cameras_healthcheck() pg_cron + pg_net + Vault" (line 337-435)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 1·2·4·7" (line 596-650)
    - .planning/phases/04-watch-j2208a-pipeline 의 010_watch_pipeline.sql 의 cron.unschedule + cron.schedule idempotent 패턴 참고
  </read_first>
  <action>
    1. **신규 파일** `supabase/migrations/012_cameras_health.sql`. 정확한 골격 (RESEARCH §Pattern 3 line 342-435 적용):

       **(A) pg_net 활성화 (Pitfall 1 — RESEARCH 정정 #2)** — 첫 줄:
       ```sql
       CREATE EXTENSION IF NOT EXISTS pg_net WITH SCHEMA extensions;
       ```

       **(B) cameras 3 컬럼 ALTER ADD** (Pitfall 4 — NULL 가드는 (D) 의 WHERE 절):
       ```sql
       ALTER TABLE public.cameras
           ADD COLUMN IF NOT EXISTS last_frame_at  TIMESTAMPTZ,
           ADD COLUMN IF NOT EXISTS health_state   TEXT DEFAULT 'unknown'
               CHECK (health_state IN ('ok','degraded','down','unknown')),
           ADD COLUMN IF NOT EXISTS last_alert_at  TIMESTAMPTZ;
       ```

       **(C) Vault 시드 — SQL idempotent 시도 (RESEARCH A2 ASSUMED)**:
       ```sql
       -- service_role_key + edge_function_base_url 2 secrets.
       -- ON CONFLICT DO NOTHING (재실행 안전). 실패 시 dashboard fallback 발동.
       INSERT INTO vault.secrets (name, secret) VALUES
           ('edge_function_base_url',
            'https://xbjqxnvemcqubjfflain.supabase.co/functions/v1')
       ON CONFLICT (name) DO NOTHING;
       -- service_role_key 는 dashboard 시드 권장 (마이그 git 노출 회피, T-8-01).
       -- 본 마이그는 placeholder INSERT 시도하지 않음 — Task 2 검증에서 수동 시드 확인.
       ```

       **(D) cameras_healthcheck() 함수** — `LANGUAGE plpgsql SECURITY DEFINER` (Pitfall 7), Vault SELECT, 5분 임계 + 30분 cooldown + 알림 전이 (Phase 4 D-09):
       ```sql
       CREATE OR REPLACE FUNCTION public.cameras_healthcheck() RETURNS void
       LANGUAGE plpgsql SECURITY DEFINER AS $$
       DECLARE
           r        RECORD;
           sr_key   TEXT;
           base_url TEXT;
       BEGIN
           SELECT decrypted_secret INTO sr_key
               FROM vault.decrypted_secrets WHERE name = 'service_role_key';
           SELECT decrypted_secret INTO base_url
               FROM vault.decrypted_secrets WHERE name = 'edge_function_base_url';

           IF sr_key IS NULL OR base_url IS NULL THEN
               RAISE WARNING 'cameras_healthcheck: vault secret missing (sr_key=%, base_url=%) — skip',
                   (sr_key IS NOT NULL), (base_url IS NOT NULL);
               RETURN;
           END IF;

           -- (a) DOWN 전이: 5분 무수신 + 30분 cooldown 통과 (Pitfall 4 — IS NOT NULL 가드)
           FOR r IN
               SELECT camera_id, group_id, last_frame_at
               FROM public.cameras
               WHERE last_frame_at IS NOT NULL
                 AND last_frame_at < now() - INTERVAL '5 minutes'
                 AND health_state IS DISTINCT FROM 'down'
                 AND (last_alert_at IS NULL OR last_alert_at < now() - INTERVAL '30 minutes')
           LOOP
               UPDATE public.cameras
                   SET health_state = 'down', last_alert_at = now()
                   WHERE camera_id = r.camera_id;
               PERFORM net.http_post(
                   url := base_url || '/notifications',
                   headers := jsonb_build_object(
                       'Authorization', 'Bearer ' || sr_key,
                       'Content-Type',  'application/json'
                   ),
                   body := jsonb_build_object(
                       'action',        'camera-down',
                       'camera_id',     r.camera_id,
                       'group_id',      r.group_id,
                       'last_frame_at', r.last_frame_at
                   )
               );
           END LOOP;

           -- (b) RECOVERY 전이 (D-09 종료 알림 패턴)
           FOR r IN
               SELECT camera_id, group_id
               FROM public.cameras
               WHERE health_state = 'down'
                 AND last_frame_at >= now() - INTERVAL '5 minutes'
           LOOP
               UPDATE public.cameras
                   SET health_state = 'ok', last_alert_at = now()
                   WHERE camera_id = r.camera_id;
               PERFORM net.http_post(
                   url := base_url || '/notifications',
                   headers := jsonb_build_object(
                       'Authorization', 'Bearer ' || sr_key,
                       'Content-Type',  'application/json'
                   ),
                   body := jsonb_build_object(
                       'action',    'camera-recovered',
                       'camera_id', r.camera_id,
                       'group_id',  r.group_id
                   )
               );
           END LOOP;
       END;
       $$;
       ```

       **(E) pg_cron 1분 주기 등록** (Phase 4 010 idempotent 패턴):
       ```sql
       DO $$ BEGIN
           PERFORM cron.unschedule('cameras_healthcheck_minute');
       EXCEPTION WHEN OTHERS THEN NULL; END $$;
       SELECT cron.schedule(
           'cameras_healthcheck_minute',
           '* * * * *',
           $$SELECT public.cameras_healthcheck();$$
       );
       ```

    2. **회귀 가드 — service_role hardcode 0건**: 마이그 본문에 `eyJ` (JWT prefix) 또는
       raw service_role JWT 등장 0회. grep 검증.

    3. **last_frame_at 컬럼명 일관 (cross-cutting must_have)**: 마이그 + 함수 + 함수 호출
       모두 `last_frame_at` (camelCase 또는 ack_at 같은 다른 이름 X). grep 검증.
  </action>
  <verify>
    <automated>test -f supabase/migrations/012_cameras_health.sql && grep -c 'CREATE EXTENSION IF NOT EXISTS pg_net' supabase/migrations/012_cameras_health.sql && grep -c 'SECURITY DEFINER' supabase/migrations/012_cameras_health.sql && grep -c 'cameras_healthcheck' supabase/migrations/012_cameras_health.sql && grep -v '^--' supabase/migrations/012_cameras_health.sql | grep -c 'eyJ' | grep -q '^0$' && echo OK</automated>
  </verify>
  <done>
    - 신규 파일 `supabase/migrations/012_cameras_health.sql` 존재
    - `CREATE EXTENSION IF NOT EXISTS pg_net` 1회 (Pitfall 1 / RESEARCH 정정 #2)
    - `SECURITY DEFINER` 1회 (Pitfall 7)
    - `last_frame_at IS NOT NULL` 1회 (Pitfall 4 NULL 가드)
    - `vault.decrypted_secrets` 2회 SELECT (sr_key + base_url)
    - `INTERVAL '5 minutes'` + `INTERVAL '30 minutes'` 모두 등장 (D-03 임계 + cooldown)
    - `'camera-down'` + `'camera-recovered'` 둘 다 정확히 등장 (plan 08-03 의 case 와 1:1)
    - `cron.schedule.*cameras_healthcheck_minute` 1회
    - service_role JWT hardcode 0건 (T-8-01: 코멘트 제외 후 `eyJ` count == 0)
    - `last_frame_at` 컬럼명 일관 (`grep -c 'last_frame_at' supabase/migrations/012_cameras_health.sql` >= 5)
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push 적용 + Vault 시드 검증 + RLS isolation SQL test</name>
  <read_first>
    - supabase/migrations/012_cameras_health.sql (Task 1 산출물)
    - supabase/migrations/003_rls_policies.sql (cameras_update_manager 정책)
    - .planning/phases/07-watch-app-bridge/07-01-SUMMARY.md (이전 supabase db push 적용 패턴)
    - .planning/phases/08-rtsp-camera/08-RESEARCH.md §"Pitfall 2: Vault 시드 누락" + §"A2 / A4 / A8" (line 604-608, 840-846)
    - .env (SUPABASE_DB_PASSWORD + SUPABASE_ACCESS_TOKEN — Phase 4·7 에서 작성됨)
  </read_first>
  <action>
    1. **[BLOCKING] supabase db push --linked --yes** 실행 — 012 마이그 운영 DB 적용. NOTICE
       4종 (pg_net 생성 + cameras ALTER 3컬럼 + 함수 정의 + cron job 등록) 확인. 실패 시 즉시
       중단 + Wave 3 진입 차단.

    2. **Vault 시드 검증 — 2 secrets 존재 확인**:
       ```sql
       SELECT count(*) FROM vault.decrypted_secrets
           WHERE name IN ('service_role_key','edge_function_base_url');
       ```
       기대값 `2`. **결과가 0~1 인 경우** → Supabase Dashboard → Project Settings → Vault
       → New Secret 으로 수동 시드:
         - name: `service_role_key`, secret: <Project Settings → API → service_role key>
         - name: `edge_function_base_url`, secret: `https://xbjqxnvemcqubjfflain.supabase.co/functions/v1`
       시드 후 위 SELECT 다시 → `2` 확인 후에만 통과.

    3. **pg_cron job 등록 검증**:
       ```sql
       SELECT jobname, schedule, active FROM cron.job WHERE jobname='cameras_healthcheck_minute';
       ```
       기대: 1 row, schedule='* * * * *', active=true.

    4. **A4/A8 검증 — testuser1 + cameras 1·5 group_id 정합성** (advisor #6 권고):
       ```sql
       SELECT user_id, user_role, group_id FROM profiles WHERE user_id='testuser1';
       SELECT camera_id, group_id FROM cameras WHERE camera_id IN (1,5);
       ```
       기대: testuser1 의 user_role IN ('manager','general_manager') AND group_id 가 cameras
       1 또는 5 의 group_id 와 일치. **불일치 시** SUMMARY 에 경고 기재 (plan 08-03 의
       camera-down smoke 가 sent=0 가능 — 데모 차단). 필요 시 testuser1 의 group_id 또는
       camera 의 group_id UPDATE 로 정합 (수동, SUMMARY 에 SQL 기록).

    5. **수동 healthcheck 호출** (Open Question 2 — SECURITY DEFINER owner 확인):
       ```sql
       SELECT public.cameras_healthcheck();
       ```
       기대: void return + 에러 0. RAISE WARNING 가능 (sr_key/base_url null) — 단,
       permission denied / function does not exist 같은 hard error 면 즉시 fix.

    6. **신규 SQL test** `tests/sql/test_012_cameras_health_isolation.sql` 작성 — anon role
       로 cameras.health_state UPDATE 시도 → 차단 검증 (T-8-02 회귀 가드, 003 의
       cameras_update_manager 정책):
       ```sql
       -- T-8-02 회귀 가드: anon 의 cameras.health_state UPDATE 차단
       BEGIN;
       SET LOCAL ROLE anon;
       DO $$
       BEGIN
           BEGIN
               UPDATE public.cameras SET health_state='down' WHERE camera_id=1;
               -- 도달하면 ASSERT 실패 (정책 누락 또는 anon 권한 과다)
               RAISE EXCEPTION 'T-8-02 REGRESSION: anon updated cameras.health_state';
           EXCEPTION WHEN insufficient_privilege OR others THEN
               -- 기대: insufficient_privilege 또는 RLS 차단
               RAISE NOTICE 'T-8-02 OK: anon UPDATE blocked (%)', SQLERRM;
           END;
       END $$;
       ROLLBACK;
       ```
       psql 또는 supabase CLI 로 실행 후 `T-8-02 OK` NOTICE 확인.
  </action>
  <verify>
    <automated>supabase db push --linked --yes 2>&1 | tee /tmp/p8t2push.log; tail -20 /tmp/p8t2push.log; grep -E 'NOTICE|applied|cameras_healthcheck|pg_net' /tmp/p8t2push.log; psql "$SUPABASE_DB_URL" -c "SELECT count(*) FROM vault.decrypted_secrets WHERE name IN ('service_role_key','edge_function_base_url');" 2>&1; psql "$SUPABASE_DB_URL" -c "SELECT jobname,active FROM cron.job WHERE jobname='cameras_healthcheck_minute';" 2>&1; psql "$SUPABASE_DB_URL" -f tests/sql/test_012_cameras_health_isolation.sql 2>&1 | grep -E 'T-8-02 OK|REGRESSION'</automated>
  </verify>
  <done>
    - `supabase db push --linked --yes` exit 0, 012 마이그 운영 DB 적용 완료
    - `vault.decrypted_secrets` 에 service_role_key + edge_function_base_url 2 rows 존재
    - `cron.job` 에 `cameras_healthcheck_minute` 1 row, active=true, schedule='* * * * *'
    - `SELECT public.cameras_healthcheck();` 실행 성공 (에러 0, RAISE WARNING 만 가능)
    - testuser1 group_id ↔ cameras 1·5 group_id 정합 확인 (불일치 시 SUMMARY 경고 기재)
    - `tests/sql/test_012_cameras_health_isolation.sql` 실행 시 `T-8-02 OK` NOTICE 출력 (REGRESSION 0)
    - cameras 5 rows 모두 health_state='unknown' 초기값 + last_frame_at NULL (Pitfall 4 — 즉시 down 폭발 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 08-02)

| Boundary | Description |
|----------|-------------|
| pg_cron → pg_net → Edge Function | 1분 주기 internal trigger → service_role JWT 첨부 → notifications endpoint. Vault 가 sr_key 보관 (코드/git 노출 0). |
| anon role → cameras UPDATE | RLS 정책 (003 cameras_update_manager) 으로 차단. T-8-02 회귀 가드 SQL test. |
| cameras.live_url_detail 평문 RTSP creds | 002_tables.sql 부터 존재. v1.0 PoC accept. T-8-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | I (Information Disclosure) | cameras_healthcheck() 의 service_role JWT | mitigate | Supabase Vault 사용 — vault.decrypted_secrets SELECT, 함수 본문 hardcode 0건. 회귀 가드: `grep -v '^--' 012_cameras_health.sql \| grep -c 'eyJ'` == 0 (raw JWT 부재). 회전: dashboard 에서 secret 갱신 즉시 반영. |
| T-8-02 | E (Elevation of Privilege) | anon role 의 cameras.health_state UPDATE | mitigate | 003_rls_policies.sql 의 cameras_update_manager 정책 (manager only). 회귀 가드: tests/sql/test_012_cameras_health_isolation.sql 가 anon UPDATE 차단 확인. 본 plan 은 새 ALTER 가 정책 깨지 않도록 (ENABLE ROW LEVEL SECURITY 기존 유지) 보존. |
| T-8-05 | I (Information Disclosure) | cameras.live_url_detail 평문 RTSP creds | accept (v1.0 PoC) | v1.1 Vault 마이그 권장 (별도 phase, deferred ideas 에 명시). 본 plan 은 컬럼 변경 없음 — 노출 surface 증가 0. |
</threat_model>

<verification>
1. `supabase db push --linked --yes` 적용 성공 (012 마이그)
2. `psql -c "SELECT count(*) FROM vault.decrypted_secrets WHERE name IN ('service_role_key','edge_function_base_url');"` == `2`
3. `psql -c "SELECT jobname,active FROM cron.job WHERE jobname='cameras_healthcheck_minute';"` → 1 row + active=true
4. `psql -c "SELECT public.cameras_healthcheck();"` → 에러 0
5. `psql -f tests/sql/test_012_cameras_health_isolation.sql` → `T-8-02 OK` NOTICE
6. cameras 5 rows 모두 last_frame_at IS NULL + health_state='unknown' (즉시 down 알림 폭발 0)
7. testuser1 user_role + group_id ↔ cameras 1·5 group_id 정합 확인 (SUMMARY 기재)
</verification>

<success_criteria>
- [ ] 012_cameras_health.sql 운영 DB 적용 완료 (pg_net + cameras ALTER 3 + Vault 시드 + 함수 + cron job)
- [ ] Vault 에 2 secrets (service_role_key + edge_function_base_url) — SQL 시드 또는 dashboard 시드
- [ ] cameras_healthcheck() 함수 SECURITY DEFINER + Vault SELECT + 5분 임계 + 30분 cooldown + 알림 전이 (Phase 4 D-09)
- [ ] pg_cron 'cameras_healthcheck_minute' 1분 주기 active
- [ ] T-8-01 회귀 가드 (service_role JWT hardcode 0)
- [ ] T-8-02 회귀 가드 (anon role 의 cameras UPDATE 차단)
- [ ] testuser1 ↔ cameras 1·5 group_id 정합 (plan 08-03 smoke test 의 sent>=1 보장)
- [ ] RTSP-03 의 "헬스체크 SQL + 5분 무수신 알림" backend infra 준비 완료
</success_criteria>

<output>
After completion, create `.planning/phases/08-rtsp-camera/08-02-SUMMARY.md`
</output>
