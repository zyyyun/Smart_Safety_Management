---
phase: 07-watch-app-bridge
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified: []
autonomous: false
requirements: [BRIDGE-01, BRIDGE-02, BRIDGE-03]
tags: [phase-7, watch, e2e, poc, demo, validation]

must_haves:
  truths:
    - "Phase 4 04-04 단축 PoC (2시간 + REMOVED 5분 시나리오) 가 testuser1 워치 + PC BLE 마스터로 1회 실행됨 — 실패/미실행 시 07-01 의 seed_watch_demo.py 로 fallback"
    - "Android 실기기 또는 에뮬레이터에서 HomeWorkerActivity 진입 → 워치 카드가 PoC 데이터 표시 (HR/temp/wear-state) — Realtime UPDATE 가 ≤3초 안에 카드에 반영됨 (BRIDGE-01 SC #1)"
    - "SafetyAlertsActivity 진입 → REMOVED alert 1건 표시 + acknowledge 버튼 클릭 → ack_at 컬럼 갱신이 ≤5초 안에 카드에 반영 (BRIDGE-02 SC #2)"
    - "SettingDeviceManagementActivity 진입 → J2208A 워치 섹션의 status badge 가 'connected' 또는 'paired' 표시 + (PoC 진행 중이면 'connected') (BRIDGE-03 SC #3)"
    - "수요일 2026-05-20 까지 본 phase 완료 — Phase 4 의 데이터가 앱에서 실시간으로 보이고 양방향 명령 가능 (BRIDGE-04 deadline SC)"
    - "시연 영상 또는 캡처 (HomeWorker 카드 라이브 갱신 + ack 사이클 + 페어링 status) 1세트 SUMMARY 에 첨부"
  artifacts:
    - path: ".planning/phases/07-watch-app-bridge/07-04-SUMMARY.md"
      provides: "PoC 실행 결과 + E2E 시연 evidence (영상/캡처 경로)"
      min_lines: 80
  key_links:
    - from: "단축 PoC 실행"
      to: "07-01 seed_watch_demo.py fallback"
      via: "PoC 실패 시 1회 실행"
      pattern: "seed_watch_demo.py"
    - from: "Android 실기기"
      to: "Supabase Realtime push"
      via: "supabase-kt 2.2.0 WSS"
      pattern: "≤3초"
    - from: "SafetyAlertsActivity acknowledge"
      to: "Edge Function watch-ack + DB ack_at"
      via: "Retrofit POST"
      pattern: "≤5초"
---

<objective>
Phase 7 Wave 4 — Wave 1·2·3 산출물의 통합 검증. 사용자가 J2208A 워치 + PC BLE 로 단축 PoC 를 실행 (또는 07-01 의 seed_watch_demo.py 로 fallback) → Android 실기기에서 (a) HomeWorkerActivity 의 워치 카드가 PoC 데이터로 라이브 갱신, (b) SafetyAlertsActivity 의 ack 사이클 ≤5초, (c) SettingDeviceManagementActivity 의 페어링 status 표시 — 3종 SC 를 사용자 시연으로 검증한다. 본 plan 은 사용자 워치 착용 + PC BLE 연결이 필요하므로 **autonomous: false**.

Purpose: BRIDGE-01·02·03 의 사람-시연 검증 본체. 수요일 2026-05-20 시연 자료 (영상/캡처) 산출.

Output:
- 단축 PoC 실측 데이터 (또는 fallback seed)
- 자동 SQL 검증 결과 (minute_summary/wear_state_events/safety_alerts row counts)
- E2E 시연 영상 또는 캡처 1세트
- 07-04-SUMMARY.md (Phase 7 종결)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-watch-app-bridge/07-CONTEXT.md
@.planning/phases/07-watch-app-bridge/07-RESEARCH.md
@.planning/phases/07-watch-app-bridge/07-01-SUMMARY.md
@.planning/phases/07-watch-app-bridge/07-02-SUMMARY.md
@.planning/phases/07-watch-app-bridge/07-03-SUMMARY.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@scripts/j2208a_sensor_reader.py
@scripts/seed_watch_demo.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN — 단축 PoC 실행 (testuser1 워치 2시간 + REMOVED 5분 시나리오) OR fallback seed 실행</name>
  <what-built>Wave 1·2·3 의 모든 산출물 (Realtime SDK + 011 RLS + Edge Function 2 case + Android UI 3 화면) 이 통합 검증 가능 상태로 준비됨.</what-built>
  <how-to-verify>
    **Path A (권장 — 실측 PoC):**

    1. PC 와 J2208A 워치 (`MAC: 21:02:02:06:01:69`) 준비. PC 에 BLE 어댑터 또는 PC 내장 BLE 사용.
    2. testuser1 가 J2208A 워치를 손목에 착용.
    3. PC 에서 BLE 데몬 실행:
       ```bash
       cd D:/2026_산업안전/Smart_Safety_Management
       # .env 가 SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY 보유 (Phase 4 03)
       python scripts/j2208a_sensor_reader.py
       ```
    4. **0~30분**: 정상 착용 운용 (산책, 사무실 등). minute_summary 30 행 + wear-state WORN 유지.
    5. **30~35분**: 워치 손목에서 탈착 → REMOVED 알림 발생 예상 (5분 후 OFF state machine 트리거 — D-09 알림). FCM 푸시 도착 확인.
    6. **35~120분**: 워치 재착용 → REMOVED 해소 (resolved_at 채워짐) → 정상 운용 60분 추가.
    7. PoC 종료 — Ctrl+C 로 데몬 우아 종료.

    검증 SQL (PC 어디서든):
    ```bash
    psql "$DATABASE_URL" <<'SQL'
    SELECT count(*) AS minute_count FROM minute_summary
        WHERE device_id = (SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69')
        AND minute_ts >= now() - interval '2.5 hours';
    -- expect >= 100
    SELECT count(*) AS state_event_count FROM wear_state_events
        WHERE device_id = (SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69')
        AND ts >= now() - interval '2.5 hours';
    -- expect >= 3 (WARMUP→WORN, WORN→OFF, OFF→WORN)
    SELECT count(*) AS alert_count FROM safety_alerts
        WHERE device_id = (SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69')
        AND raised_at >= now() - interval '2.5 hours';
    -- expect >= 1 (REMOVED, 미해결 ack_at IS NULL)
    SQL
    ```

    **Path B (fallback — 실측 PoC 실패 또는 시간 부족 시):**

    ```bash
    # device_id 확인
    psql "$DATABASE_URL" -c "SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69'"

    # 07-01 의 seed 스크립트 실행 (--wipe 로 깨끗하게 시드)
    python scripts/seed_watch_demo.py --device-id <id> --wipe
    # expect: minute_summary: 120 rows / wear_state_events: 3 rows / safety_alerts: 2 rows
    ```

    Path B 채택 시 시연 스토리 = "사전 PoC 데이터 재생" (실시간 라이브 X, 시연 직전 INSERT 발생).

    **공통 verify**:
    - 위 SQL 의 row counts 가 expected 충족
    - `safety_alerts WHERE ack_at IS NULL` 미해결 alert 1건 이상 존재 (Task 3 의 ack 사이클 입력)
  </how-to-verify>
  <resume-signal>
    "PoC complete (Path A — 2h 실측 / minute=N / state=N / alert=N)" 또는 "Fallback seed complete (Path B — 120/3/2)" 형식으로 보고. SQL 결과 행 수와 어떤 path 채택했는지 명시.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 자동 SQL verification — PoC/seed 데이터의 행 수 + 무결성 + Realtime publication 활성 검증</name>
  <files></files>
  <read_first>
    - 07-01 의 011_watch_app_rls.sql + tests/sql/test_011_rls_isolation.sql (publication ADD TABLE 확인 SQL)
    - 07-02 의 case 'watch-ack' 가 사용하는 컬럼 (ack_at IS NULL 가드)
  </read_first>
  <action>
    Task 1 의 PoC 또는 seed 완료 후 자동 SQL 검증 1회 — 본 task 는 executor 가 psql 만 실행 (autonomous):

    ```bash
    cd D:/2026_산업안전/Smart_Safety_Management
    DEVICE_ID=$(psql "$DATABASE_URL" -tAc "SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69'")
    echo "DEVICE_ID=$DEVICE_ID"

    psql "$DATABASE_URL" <<SQL
    -- (1) 데이터 존재 검증
    SELECT 'minute_summary' AS tbl, count(*) AS n FROM minute_summary WHERE device_id = $DEVICE_ID;
    SELECT 'wear_state_events' AS tbl, count(*) AS n FROM wear_state_events WHERE device_id = $DEVICE_ID;
    SELECT 'safety_alerts' AS tbl, count(*) AS n FROM safety_alerts WHERE device_id = $DEVICE_ID;

    -- (2) 미해결 alert 존재 (Task 3 ack 사이클 입력)
    SELECT alert_id, alert_type, severity, raised_at, resolved_at, ack_at
      FROM safety_alerts
      WHERE device_id = $DEVICE_ID AND ack_at IS NULL
      ORDER BY raised_at DESC LIMIT 5;

    -- (3) Realtime publication 4 테이블 활성 확인 (07-01 의 011 적용 후 영구)
    SELECT pubname, tablename FROM pg_publication_tables
      WHERE pubname = 'supabase_realtime'
        AND schemaname = 'public'
        AND tablename IN ('safety_alerts','wear_state_events','device_watches','devices')
      ORDER BY tablename;
    -- expect 4 rows

    -- (4) RLS 정책 4 v1_poc 존재 (07-01 회귀 가드)
    SELECT policyname FROM pg_policies
      WHERE schemaname = 'public' AND policyname LIKE '%_v1_poc'
      ORDER BY policyname;
    -- expect 4 rows
    SQL
    ```

    expected 결과:
    - minute_summary >= 100 (Path A 실측 또는 Path B seed 120)
    - wear_state_events >= 3
    - safety_alerts >= 1 미해결
    - publication 4 테이블 등록
    - RLS 정책 4종 등록

    SQL 결과는 SUMMARY.md 에 그대로 첨부 (executor 가 stdout 캡처).
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; psql "$DATABASE_URL" -tAc "SELECT count(*) FROM safety_alerts WHERE ack_at IS NULL AND device_id = (SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69')" | awk '{ if ($1 + 0 >= 1) print "PASS"; else exit 1 }' &amp;&amp; psql "$DATABASE_URL" -tAc "SELECT count(*) FROM pg_publication_tables WHERE pubname='supabase_realtime' AND schemaname='public' AND tablename IN ('safety_alerts','wear_state_events','device_watches','devices')" | awk '{ if ($1 + 0 == 4) print "PASS"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - SQL (1): minute_summary >= 100, wear_state_events >= 3, safety_alerts >= 1 (Path A) 또는 minute_summary == 120, wear_state_events == 3, safety_alerts == 2 (Path B)
    - SQL (2): 미해결 alert 최소 1행 — Task 3 의 ack 시연 입력
    - SQL (3): publication 4 행 (07-01 회귀 가드)
    - SQL (4): RLS 정책 4 행 (07-01 회귀 가드)
    - SQL stdout 텍스트가 SUMMARY.md 의 Evidence 섹션에 첨부 가능 형태로 캡처됨
  </acceptance_criteria>
  <done>자동 SQL 검증 4종 PASS — Wave 1·2·3 의 인프라 + Edge Function + Android 가 활성 데이터를 받을 준비 완료. Task 3 의 사용자 E2E 시연 가능.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-VERIFY — Android 실기기 E2E 시연 (3 SC 모두 시각 확인 + 영상/캡처)</name>
  <what-built>Wave 1·2·3 모두 적용 + Task 1 의 PoC/seed 데이터 + Task 2 의 SQL 검증 PASS. Android 앱이 실제 데이터로 시연 가능한 상태.</what-built>
  <how-to-verify>
    **시연 환경:**
    - Android 실기기 (testuser1 계정 로그인) — 또는 Android Studio 에뮬레이터 (FCM 푸시는 실기기 권장)
    - 같은 운영 Supabase URL 가리킴 (BuildConfig.SUPABASE_URL)
    - 기기 인터넷 연결 확인

    **빌드 + 설치:**
    ```bash
    cd D:/2026_산업안전/Smart_Safety_Management
    gradlew app:assembleDebug
    adb install -r app/build/outputs/apk/debug/app-debug.apk
    ```

    **시연 1 — BRIDGE-01 (≤3초 라이브 갱신):**
    1. testuser1 로그인 → HomeWorkerActivity 진입
    2. profile_bar 아래 "J2208A 워치" 카드 표시 확인 — HR/temp 숫자 + wear-state 라벨 (예: "착용 중") + "정상 운용 중" 또는 활성 alert 표시
    3. **Path A 진행 중 (워치 착용 중)이면**: 손목에서 워치 5초 떼었다가 다시 착용 → BLE daemon 이 OFF 또는 ABNORMAL 잡고 → wear_state_events INSERT → **카드의 wear-state 라벨이 ≤3초 안에 변경** (착용 중 → 미착용 → 착용 중)
    4. **Path B (seed) 시**: SQL 으로 device_watches UPDATE 1건 실행 → `psql -c "UPDATE device_watches SET heart_rate = 95 WHERE device_id=$DEVICE_ID"` → **카드의 HR 숫자가 ≤3초 안에 95 로 변경**
    5. 영상 캡처 (15초 정도) — 변경 직전 ~ 직후

    **시연 2 — BRIDGE-02 (acknowledge ≤5초):**
    1. 카드 탭 → SafetyAlertsActivity 진입
    2. 미해결 alert (REMOVED 또는 TACHY) 카드 표시 + "확인" 버튼 노출
    3. "확인" 버튼 클릭 → 1초 안에 "확인됨" 메시지 + 카드 색상 회색 처리 (Realtime UPDATE 가 ack_at 갱신을 카드에 반영)
    4. 같은 alert 두 번째 "확인" 시도 → "이미 확인됨" 메시지 (idempotency)
    5. **하단 "1차 경고용, 의료기기 아님" fine print 표시 확인** (의료기기 면책)
    6. 영상 캡처 또는 스크린샷 3장 (전 / 클릭 직후 / 두 번째 시도)

    **시연 3 — BRIDGE-03 (페어링 status):**
    1. 뒤로 → SettingDeviceManagementActivity 진입
    2. 화면 하단 "J2208A 워치" 섹션 + status badge 표시
    3. (Path A 워치 살아있음) → status = "연결됨" (초록) — last_comm_at 가 최근 5분 내
    4. (Path B 또는 워치 꺼짐) → status = "끊김" (노랑) 또는 "미등록" (회색)
    5. "해제" 버튼 클릭 → "해제됨" 메시지 → status badge "미등록" 으로 즉시 변경
    6. MAC TextField 에 "21:02:02:06:01:69" 입력 → "등록" → status "연결됨" 또는 "끊김" 으로 복귀
    7. 잘못된 MAC ("not-a-mac") 입력 → "MAC 형식이 올바르지 않습니다" 에러
    8. 스크린샷 4장 (status badge 3 종 + MAC validation 에러)

    **시연 4 (선택 — 시간 허용 시) — FCM watch_alert 도착:**
    - PoC 진행 중 REMOVED 발생 시 자동 FCM 푸시 도착 → 알림 트레이 캡처 + 알림 탭 → SafetyAlertsActivity 진입 → 해당 alert 강조 (highlightAlertId)

    **모든 영상/캡처는 `docs/phase7_demo/` 또는 SUMMARY.md 첨부 디렉토리에 저장. 파일명 = `bridge-01-live.mp4`, `bridge-02-ack.mp4`, `bridge-03-pair.png` 등.**
  </how-to-verify>
  <resume-signal>
    "approved" + 영상/캡처 경로 (예: docs/phase7_demo/bridge-01-live.mp4 + bridge-02-ack-1.png + ...) 또는 "issues found: <description>". 시연 1·2·3 각각의 시각 확인 결과 (PASS/FAIL + 측정 지연 시간) 보고.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE PC daemon → Supabase (service_role) | Trusted — Phase 4 03 에서 .env 보호 완료 |
| Android (anon) → Realtime/Edge Function | Untrusted client. RLS (07-01) + Edge Function ownership SQL (07-02) 가 보안 경계 |

## STRIDE Threat Register

본 plan 은 운영 검증/시연 — 신규 코드/엔드포인트 추가 X. 위협은 Wave 1·2·3 에서 모두 mitigate 됨:
- T-7-01 RLS bypass → 07-01 mitigate
- T-7-02 cross-worker ack → 07-02 mitigate (Test 3 ownership smoke 가 회귀 가드)
- T-7-03 MAC spoofing → 07-02 mitigate (Test 3 spoofing smoke)
- T-7-04 Realtime channel auth → 07-01 + 07-03 accept (v1.0 PoC 한정)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-09 | Information Disclosure | 시연 영상에 anon key 또는 testuser1 개인정보 노출 | mitigate | 캡처 전 (a) 화면에서 BuildConfig 노출 안 함 확인, (b) 영상 편집으로 testuser1 의 사적 정보 (예: 이름, 전화번호) blur 처리, (c) Supabase Dashboard 화면 캡처 시 anon key 가리기 |

</threat_model>

<verification>
Wave 4 phase-gate (= Phase 7 종결 게이트):
1. Task 1: PoC 또는 seed 1회 완료 + 사용자 보고
2. Task 2: 4종 SQL verification PASS (data + publication + RLS 회귀)
3. Task 3: 3 시연 시나리오 모두 PASS + 영상/캡처 산출물 1세트 확보
4. SUMMARY.md (07-04-SUMMARY) 작성 — Path A/B 채택 명시 + SQL stdout + 영상/캡처 경로 + 측정 지연 시간 (BRIDGE-01 ≤3초 / BRIDGE-02 ≤5초 — 실측 ms 또는 초 기록)
5. STATE.md 갱신 — Phase 7 ✓ COMPLETE + BRIDGE-01·02·03 [x] 체크 + phases_done 4→5 (이전 Phase 4 04-04 미실행 상태 유지)
6. ROADMAP.md Phase 7 섹션 — Plans [x] 4건, Goal/SC 변경 X
</verification>

<success_criteria>
Plan 04 = Phase 7 종결 조건 (전부 PASS 시 마일스톤 진척):
- [ ] Task 1: 단축 PoC 실행 (Path A 또는 Path B fallback) + 사용자 보고
- [ ] Task 2: 4종 SQL verification PASS
- [ ] Task 3: 3 시연 시나리오 PASS (BRIDGE-01 ≤3초 + BRIDGE-02 ≤5초 + BRIDGE-03 status 표시) + 영상/캡처 1세트
- [ ] BRIDGE-01·02·03 모두 SC 충족
- [ ] BRIDGE-04 (deadline 2026-05-20) 준수
- [ ] 의료기기 면책 fine print 시각 확인
- [ ] SUMMARY.md 작성 (Path 채택 + SQL + 영상 경로 + 측정 지연)
- [ ] STATE.md + ROADMAP.md 갱신
</success_criteria>

<output>
Phase 7 종결 산출물:
- `.planning/phases/07-watch-app-bridge/07-04-SUMMARY.md` (Phase 7 전체 retrospective + Wave 4 시연 결과)
- `docs/phase7_demo/` 또는 SUMMARY 가 가리키는 위치에 영상/캡처 1세트
- STATE.md 갱신 (phases_done +1, Phase 7 COMPLETE)
- ROADMAP.md Phase 7 의 Plans [x] 4건 체크
- (선택) PROJECT.md 의 Status 업데이트 — Phase 7 완료 + 5/20 deadline met
</output>
