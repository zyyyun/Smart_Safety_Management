---
phase: 07-watch-app-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/proguard-rules.pro
  - supabase/migrations/011_watch_app_rls.sql
  - scripts/seed_watch_demo.py
  - tests/sql/test_011_rls_isolation.sql
autonomous: true
requirements: [BRIDGE-01, BRIDGE-02, BRIDGE-03]
tags: [phase-7, watch, infra, supabase-kt, rls, migration]

user_setup:
  - service: supabase
    why: "supabase db push 실행 (011_watch_app_rls.sql 운영 DB 적용) — 인증된 supabase CLI 필요. CI 가 아니므로 사용자 로컬 supabase login 상태 가정."
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "app/build.gradle.kts 에 supabase-kt 2.2.0 + ktor-cio 2.3.9 의존성이 정확한 GAV 로 lock 되어 있다 (D-01 amended — 3.x 거부)"
    - "core library desugaring 활성화 (minSdk 24 + supabase-kt minSdk 26 충돌 회피, Pitfall 2)"
    - "BuildConfig 에 SUPABASE_URL + SUPABASE_ANON_KEY 가 노출되어 Application 싱글톤이 읽을 수 있다 (anon key 만, service_role 절대 X)"
    - "011_watch_app_rls.sql 가 운영 DB 에 적용되어 (a) safety_alerts/wear_state_events/device_watches SELECT 정책이 narrowing 됨 (Pitfall 4 해소), (b) supabase_realtime publication 에 4 테이블이 ADD 되어 broadcast 활성화됨 (Pitfall 6 해소)"
    - "scripts/seed_watch_demo.py 가 Phase 4 04-04 PoC 실패/미실행 시 fallback 으로 minute_summary≈120 + wear_state_events≥3 + safety_alerts≥2 행을 시드한다 (D-05 fallback)"
    - "tests/sql/test_011_rls_isolation.sql 가 다른 user_id 로 SELECT 시 0행 반환을 검증 (BRIDGE-03b)"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "supabase-kt 2.2.0 + ktor-cio 2.3.9 + desugaring + BuildConfig"
      contains: "io.github.jan-tennert.supabase:realtime-kt:2.2.0"
    - path: "app/proguard-rules.pro"
      provides: "supabase/ktor reflection keep rules (Pitfall 8)"
      contains: "io.github.jan.supabase"
    - path: "supabase/migrations/011_watch_app_rls.sql"
      provides: "RLS narrowing + publication ALTER (D-04b)"
      contains: "ALTER PUBLICATION supabase_realtime"
    - path: "scripts/seed_watch_demo.py"
      provides: "PoC fallback seed script (D-05)"
      contains: "minute_summary"
    - path: "tests/sql/test_011_rls_isolation.sql"
      provides: "RLS ownership assertion (BRIDGE-03b)"
      contains: "expect 0 rows"
  key_links:
    - from: "app/build.gradle.kts"
      to: "io.github.jan-tennert.supabase:realtime-kt:2.2.0"
      via: "Maven dependency declaration"
      pattern: "supabase:realtime-kt:2\\.2\\.0"
    - from: "supabase/migrations/011_watch_app_rls.sql"
      to: "supabase_realtime publication"
      via: "ALTER PUBLICATION ADD TABLE"
      pattern: "ALTER PUBLICATION supabase_realtime ADD TABLE"
    - from: "app/build.gradle.kts"
      to: "BuildConfig.SUPABASE_URL"
      via: "buildConfigField + buildFeatures.buildConfig"
      pattern: "buildConfigField.*SUPABASE_URL"
---

<objective>
Phase 7 Wave 1 — 워치-앱 양방향 연동의 인프라 토대 구축. Android 앱이 Supabase Realtime SDK 를 빌드 단계에서 통합 가능하도록 의존성 + BuildConfig + ProGuard 규칙을 lock 하고, 운영 DB 의 RLS 를 narrowing + Realtime publication 을 활성화한다. 추가로 단축 PoC fallback seed script + RLS isolation SQL 테스트를 미리 깔아둔다.

Purpose: Wave 2 (Edge Function) 와 Wave 3 (Android UI) 가 빌드/배포 가능한 환경을 보장. 이 plan 이 끝나야 supabase-kt SDK 호출 코드를 실제 컴파일·배포할 수 있다.

Output:
- supabase-kt 2.2.0 + ktor-cio 2.3.9 + desugaring 적용된 app/build.gradle.kts
- ProGuard keep 규칙 (release build 안전)
- 011_watch_app_rls.sql 운영 DB 적용 완료 (supabase db push)
- scripts/seed_watch_demo.py (D-05 fallback)
- tests/sql/test_011_rls_isolation.sql (BRIDGE-03b)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-watch-app-bridge/07-CONTEXT.md
@.planning/phases/07-watch-app-bridge/07-RESEARCH.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@.planning/phases/04-watch-j2208a-pipeline/04-01-SUMMARY.md
@app/build.gradle.kts
@app/proguard-rules.pro
@supabase/migrations/010_watch_pipeline.sql
@supabase/migrations/003_rls_policies.sql

<interfaces>
<!-- 010_watch_pipeline.sql 가 이미 만든 컬럼 / 테이블 (executor 가 011 작성 시 직접 참조) -->

devices (확장됨, 010 마이그레이션):
  device_id INTEGER PRIMARY KEY
  device_type VARCHAR (e.g. 'WATCH')
  serial_number VARCHAR UNIQUE
  user_id VARCHAR (FK → profiles.user_id)
  mac_address VARCHAR(17) UNIQUE   -- 010 ADD
  firmware_version VARCHAR(20)     -- 010 ADD
  last_comm_at TIMESTAMPTZ         -- 010 ADD

safety_alerts (010 신규):
  alert_id BIGSERIAL PRIMARY KEY
  device_id INTEGER FK
  alert_type TEXT  -- TACHY|REMOVED|COMMS_LOST
  severity TEXT    -- CAUTION|WARNING|DANGER
  raised_at TIMESTAMPTZ
  resolved_at TIMESTAMPTZ
  ack_at TIMESTAMPTZ              -- ⚠ 컬럼명 = ack_at (NOT acknowledged_at)
  reason JSONB

wear_state_events (010 신규): event_id, device_id, ts, from_state, to_state, reason JSONB
minute_summary (010 신규): device_id, minute_ts, hr_median, temp_median, temp_iqr, steps_delta, dominant_state, good_ratio
device_watches (002 기존, snapshot): device_id, heart_rate, body_temp, battery_level, updated_at
  ⚠ 003_rls_policies.sql:295 의 device_watches_select USING (true) 가 본 plan 에서 narrowing 대상

testuser1 시드:
  user_id = 'testuser1'
  J2208A device serial_number = 'J2208A-21:02:02:06:01:69'
  mac_address = '21:02:02:06:01:69'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: app/build.gradle.kts + proguard-rules.pro — supabase-kt 2.2.0 + ktor-cio 2.3.9 + desugaring + BuildConfig</name>
  <files>app/build.gradle.kts, app/proguard-rules.pro</files>
  <read_first>
    - app/build.gradle.kts (현재 dependency block — Retrofit 4.12.0, Compose Compiler 1.5.10, minSdk 24)
    - app/proguard-rules.pro (현재 규칙)
    - .planning/phases/07-watch-app-bridge/07-RESEARCH.md §Standard Stack (Installation), §Common Pitfalls 1, 2, 7, 8
    - .planning/phases/07-watch-app-bridge/07-CONTEXT.md §D-01 (버전 정정 CRITICAL)
  </read_first>
  <action>
    `app/build.gradle.kts` 수정:

    1. `android.defaultConfig` 블록 안에 BuildConfig 필드 추가 (BuildConfig.SUPABASE_URL / SUPABASE_ANON_KEY 가 코드에서 읽힐 수 있도록):
       ```kotlin
       buildConfigField("String", "SUPABASE_URL", "\"https://qjmpxyenkqcdrwnsxvcs.supabase.co\"")
       buildConfigField("String", "SUPABASE_ANON_KEY", "\"<anon-key-from-supabase-dashboard>\"")
       ```
       (executor 는 운영 DB 의 anon key 를 Supabase Dashboard → Settings → API 에서 복사해서 채워 넣음. anon key 는 git 커밋 OK — RLS 가 보안 경계.)

    2. `android.buildFeatures` 블록을 확장 (현재 `compose = true` 만 있음):
       ```kotlin
       buildFeatures {
           compose = true
           buildConfig = true   // ← 추가
       }
       ```

    3. `android.compileOptions` 블록을 확장 (현재 sourceCompatibility = 1.8 만 있음):
       ```kotlin
       compileOptions {
           isCoreLibraryDesugaringEnabled = true   // ← 추가 (Pitfall 2)
           sourceCompatibility = JavaVersion.VERSION_1_8
           targetCompatibility = JavaVersion.VERSION_1_8
       }
       ```

    4. `dependencies` 블록 끝에 supabase-kt + ktor + desugaring 추가 (Retrofit OkHttp 4.12.0 와 충돌 회피 = ktor-cio 사용, NOT okhttp engine — Pitfall 7):
       ```kotlin
       // Phase 7 / D-01 (amended) — Supabase Realtime + PostgREST (Kotlin 1.9.22 호환)
       implementation("io.github.jan-tennert.supabase:realtime-kt:2.2.0")
       implementation("io.github.jan-tennert.supabase:postgrest-kt:2.2.0")
       implementation("io.ktor:ktor-client-cio:2.3.9")

       // Phase 7 / Pitfall 2 — minSdk 24 + supabase-kt minSdk 26 → core library desugaring 필수
       coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.0.4")
       ```

       **금지 (Pitfall 1):** `io.github.jan-tennert.supabase:realtime-kt:3.x.x` (Kotlin 2.3.21 강제 — Compose Compiler 1.5.10 ABI 비호환).
       **금지 (Pitfall 7):** `io.ktor:ktor-client-okhttp:*` (Retrofit 의 OkHttp 4.12.0 와 transitive 충돌).

    `app/proguard-rules.pro` 수정:

    파일 끝에 supabase-kt + Ktor + kotlinx.serialization keep 규칙 추가 (Pitfall 8 — release build 시 R8 strip 방지):
    ```proguard
    # Phase 7 / D-01 — Supabase-kt + Ktor reflection keep rules (Pitfall 8)
    -keep class io.github.jan.supabase.** { *; }
    -keep class io.ktor.** { *; }
    -keepclassmembers class kotlinx.serialization.** { *; }
    -keepattributes *Annotation*, InnerClasses
    -dontnote kotlinx.serialization.AnnotationsKt
    ```

    현재 `app/build.gradle.kts:31` 의 `isMinifyEnabled = false` 는 그대로 둠 (release 빌드도 R8 비활성). 위 규칙은 v1.1 minify 활성화 대비 선반영.
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; grep -c 'supabase:realtime-kt:2.2.0' app/build.gradle.kts &amp;&amp; grep -c 'supabase:postgrest-kt:2.2.0' app/build.gradle.kts &amp;&amp; grep -c 'ktor-client-cio:2.3.9' app/build.gradle.kts &amp;&amp; grep -c 'desugar_jdk_libs:2.0.4' app/build.gradle.kts &amp;&amp; grep -c 'isCoreLibraryDesugaringEnabled = true' app/build.gradle.kts &amp;&amp; grep -c 'buildConfig = true' app/build.gradle.kts &amp;&amp; grep -c 'SUPABASE_URL' app/build.gradle.kts &amp;&amp; grep -c 'SUPABASE_ANON_KEY' app/build.gradle.kts &amp;&amp; grep -cE 'realtime-kt:3\.' app/build.gradle.kts | grep -q '^0$' &amp;&amp; grep -cE 'ktor-client-okhttp' app/build.gradle.kts | grep -q '^0$' &amp;&amp; grep -c 'io.github.jan.supabase' app/proguard-rules.pro</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'supabase:realtime-kt:2.2.0' app/build.gradle.kts` == 1
    - `grep -c 'supabase:postgrest-kt:2.2.0' app/build.gradle.kts` == 1
    - `grep -c 'ktor-client-cio:2.3.9' app/build.gradle.kts` == 1
    - `grep -c 'desugar_jdk_libs:2.0.4' app/build.gradle.kts` == 1
    - `grep -c 'isCoreLibraryDesugaringEnabled = true' app/build.gradle.kts` == 1
    - `grep -c 'buildConfig = true' app/build.gradle.kts` == 1
    - `grep -cE 'realtime-kt:3\.' app/build.gradle.kts` == 0 (3.x 사용 금지)
    - `grep -cE 'ktor-client-okhttp' app/build.gradle.kts` == 0 (okhttp engine 사용 금지)
    - `grep -c 'io.github.jan.supabase' app/proguard-rules.pro` >= 1
    - (선택, executor 가 Android Studio 또는 gradle 사용 가능하면) `gradlew app:assembleDebug` 빌드 성공
  </acceptance_criteria>
  <done>build.gradle.kts 가 정확한 GAV 로 supabase-kt 2.2.0 + ktor-cio 2.3.9 + desugaring + BuildConfig 활성화. proguard 규칙 추가. 빌드는 Wave 3 의 SupabaseClient 초기화 코드가 들어간 후 검증 가능.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: supabase/migrations/011_watch_app_rls.sql + tests/sql/test_011_rls_isolation.sql + [BLOCKING] supabase db push</name>
  <files>supabase/migrations/011_watch_app_rls.sql, tests/sql/test_011_rls_isolation.sql</files>
  <read_first>
    - .planning/phases/07-watch-app-bridge/07-CONTEXT.md §D-04 + D-04b (RLS USING true v1.0 + publication ALTER 명시)
    - .planning/phases/07-watch-app-bridge/07-RESEARCH.md §Pattern 5 RLS, §Pitfall 4 (device_watches_select USING(true) DROP), §Pitfall 6 (publication 미포함)
    - supabase/migrations/010_watch_pipeline.sql (4 테이블 정의 + 기존 RLS ENABLE 만 + 정책 미등록)
    - supabase/migrations/003_rls_policies.sql (기존 RLS 패턴, 특히 device_watches_select USING(true) 위치)
  </read_first>
  <behavior>
    - Test 1 (RED before push): tests/sql/test_011_rls_isolation.sql — `SELECT count(*) FROM safety_alerts` as anon role 가 011 적용 전에는 (테이블 정책 0개 + RLS ENABLE) → 0 행 (service_role 만 접근). 011 적용 후에도 `mac_address IS NOT NULL` device 의 alert 는 노출되지만 다른 user_id 로 query 시 mac 가 NULL 이면 0 행.
    - Test 2 (publication): `SELECT pubname FROM pg_publication_tables WHERE schemaname='public' AND tablename IN ('safety_alerts','wear_state_events','device_watches','devices')` → 011 적용 전 0~1 행, 적용 후 4 행 모두 'supabase_realtime' 으로 등록됨.
    - Test 3 (idempotency): 011 을 두 번 실행해도 `CREATE POLICY IF NOT EXISTS` / `DROP POLICY IF EXISTS` 패턴으로 에러 X.
  </behavior>
  <action>
    **Step A — `supabase/migrations/011_watch_app_rls.sql` 작성** (D-04b amended 명시: RLS USING true v1.0 + publication ALTER):

    ```sql
    -- ============================================
    -- 011: Watch App Bridge RLS + Realtime publication
    -- Phase 7 — per CONTEXT.md D-04 / D-04b (amended 2026-05-14)
    --
    -- Adds:
    --   (A) DROP 003 의 device_watches_select USING (true) 정책 (Pitfall 4)
    --   (B) safety_alerts / wear_state_events / device_watches SELECT 정책 narrowing
    --       — v1.0 PoC = mac_address IS NOT NULL 패턴 (auth.uid() 미사용 — Firebase Auth 환경)
    --       — v1.1 Auth 도입 시 즉시 USING (auth.uid() = ...) 으로 강화 필요 (Assumption A2)
    --   (C) ALTER PUBLICATION supabase_realtime ADD TABLE — Realtime broadcast 활성화 (Pitfall 6)
    --   (D) UPDATE 정책 = 추가 안 함 (페어링 + ack 모두 Edge Function 경유 — D-03 + D-04b)
    --
    -- 의존: 010_watch_pipeline.sql (4 테이블 + RLS ENABLE 완료)
    -- 적용: supabase db push  (또는)  psql $DATABASE_URL -f 011_watch_app_rls.sql
    -- 재실행 안전: DROP IF EXISTS + CREATE POLICY (PostgreSQL 12+ 가 IF NOT EXISTS 지원 안 함 →
    --             DROP 후 CREATE)
    -- ============================================

    -- ============================================
    -- (A) Pitfall 4 — 003 의 device_watches_select USING(true) 제거
    -- ============================================
    DROP POLICY IF EXISTS "device_watches_select" ON public.device_watches;

    -- ============================================
    -- (B) v1.0 PoC SELECT 정책 — anon + authenticated 모두 mac_address IS NOT NULL 인 device 의 행만
    -- ============================================
    DROP POLICY IF EXISTS "safety_alerts_select_v1_poc" ON public.safety_alerts;
    CREATE POLICY "safety_alerts_select_v1_poc" ON public.safety_alerts
        FOR SELECT
        TO anon, authenticated
        USING (
            device_id IN (
                SELECT device_id FROM public.devices
                WHERE mac_address IS NOT NULL
            )
        );

    DROP POLICY IF EXISTS "wear_state_events_select_v1_poc" ON public.wear_state_events;
    CREATE POLICY "wear_state_events_select_v1_poc" ON public.wear_state_events
        FOR SELECT
        TO anon, authenticated
        USING (
            device_id IN (
                SELECT device_id FROM public.devices
                WHERE mac_address IS NOT NULL
            )
        );

    DROP POLICY IF EXISTS "device_watches_select_v1_poc" ON public.device_watches;
    CREATE POLICY "device_watches_select_v1_poc" ON public.device_watches
        FOR SELECT
        TO anon, authenticated
        USING (
            device_id IN (
                SELECT device_id FROM public.devices
                WHERE mac_address IS NOT NULL
            )
        );

    -- devices 테이블도 paired 워치 status 조회용 SELECT 허용
    DROP POLICY IF EXISTS "devices_select_paired_watch_v1_poc" ON public.devices;
    CREATE POLICY "devices_select_paired_watch_v1_poc" ON public.devices
        FOR SELECT
        TO anon, authenticated
        USING (
            device_type = 'WATCH' AND mac_address IS NOT NULL
        );

    -- ============================================
    -- (C) Pitfall 6 — supabase_realtime publication 에 4 테이블 명시 추가
    --     이미 추가된 경우 NOTICE 출력 후 무시 (재실행 안전)
    -- ============================================
    DO $$
    BEGIN
        BEGIN
            ALTER PUBLICATION supabase_realtime ADD TABLE public.safety_alerts;
        EXCEPTION WHEN duplicate_object THEN
            NULL;
        END;
        BEGIN
            ALTER PUBLICATION supabase_realtime ADD TABLE public.wear_state_events;
        EXCEPTION WHEN duplicate_object THEN
            NULL;
        END;
        BEGIN
            ALTER PUBLICATION supabase_realtime ADD TABLE public.device_watches;
        EXCEPTION WHEN duplicate_object THEN
            NULL;
        END;
        BEGIN
            ALTER PUBLICATION supabase_realtime ADD TABLE public.devices;
        EXCEPTION WHEN duplicate_object THEN
            NULL;
        END;
    END $$;

    -- ============================================
    -- 적용 명령
    --   supabase db push                                    (권장)
    --   psql "$DATABASE_URL" -f 011_watch_app_rls.sql      (대안)
    -- ============================================
    ```

    **Step B — `tests/sql/test_011_rls_isolation.sql` 작성** (BRIDGE-03b — 실제 ownership isolation 검증):

    ```sql
    -- tests/sql/test_011_rls_isolation.sql
    -- Phase 7 / BRIDGE-03b — RLS isolation assertion (planner-spec)
    -- 사용: psql "$DATABASE_URL" -f tests/sql/test_011_rls_isolation.sql
    --      (service_role 으로 connect 후 SET ROLE anon 으로 정책 검증)
    --
    -- 검증 시나리오:
    --   (a) 011 publication ADD TABLE 가 4 테이블 모두 supabase_realtime 에 포함시켰는지
    --   (b) 011 의 SELECT 정책이 mac_address IS NOT NULL 패턴으로 narrowing 됐는지
    --   (c) testuser1 의 paired 워치 (mac=21:02:02:06:01:69) 가 anon role 으로도 SELECT 가능
    --
    -- v1.0 PoC 한정: cross-user isolation 은 strict 하지 않음 (auth.uid() 미사용) —
    --   본 테스트는 v1.0 정책의 *의도된 행동* 만 검증. v1.1 Auth 도입 시 cross-user
    --   strict isolation 테스트로 교체.

    \echo '=== Test 1: publication 에 4 테이블 ADD 검증 ==='
    SELECT pubname, schemaname, tablename
      FROM pg_publication_tables
     WHERE pubname = 'supabase_realtime'
       AND schemaname = 'public'
       AND tablename IN ('safety_alerts','wear_state_events','device_watches','devices')
     ORDER BY tablename;
    -- expect 4 rows

    \echo '=== Test 2: 011 정책 4종이 등록됐는지 ==='
    SELECT schemaname, tablename, policyname
      FROM pg_policies
     WHERE schemaname = 'public'
       AND policyname LIKE '%_v1_poc'
     ORDER BY tablename, policyname;
    -- expect 4 rows: safety_alerts/wear_state_events/device_watches/devices

    \echo '=== Test 3: 003 의 device_watches_select USING(true) 가 제거됐는지 ==='
    SELECT count(*) AS legacy_open_policy_count
      FROM pg_policies
     WHERE schemaname = 'public'
       AND tablename = 'device_watches'
       AND policyname = 'device_watches_select';
    -- expect 0 (DROP IF EXISTS 가 제거함)

    \echo '=== Test 4: anon role 으로 paired 워치의 safety_alerts SELECT 가능 ==='
    SET ROLE anon;
    SELECT count(*) AS visible_alert_count
      FROM public.safety_alerts
     WHERE device_id IN (
         SELECT device_id FROM public.devices
          WHERE mac_address = '21:02:02:06:01:69'
     );
    -- expect >= 0 (PoC 데이터 미적재 시 0, 적재 후 >0)
    RESET ROLE;

    \echo '=== Test 5: anon role 으로 mac_address IS NULL 인 device 의 alert 는 0행 ==='
    SET ROLE anon;
    -- 가짜 device (mac_address NULL) 시드 후 검증
    -- (이 테스트는 read-only 라 시드 INSERT 안 함 — 정책 자체만 확인)
    SELECT count(*) AS isolated_count
      FROM public.safety_alerts sa
     WHERE sa.device_id IN (
         SELECT device_id FROM public.devices
          WHERE mac_address IS NULL
     );
    -- expect 0 (mac NULL device 자체가 정책 USING 절에서 배제됨)
    RESET ROLE;

    \echo '=== ALL RLS ISOLATION TESTS COMPLETE ==='
    ```

    **Step C — [BLOCKING] 운영 DB 적용** — `supabase db push` 실행 (디렉토리 = repo root):

    ```bash
    cd D:/2026_산업안전/Smart_Safety_Management
    supabase db push
    # 또는 Non-TTY: SUPABASE_ACCESS_TOKEN=<token> supabase db push
    ```

    **Step D — verify SQL 실행**:

    ```bash
    # SUPABASE_DB_URL 또는 운영 connection string 사용
    psql "$DATABASE_URL" -f tests/sql/test_011_rls_isolation.sql
    ```

    expect: Test 1 = 4 rows, Test 2 = 4 rows, Test 3 = 0, Test 4 >= 0, Test 5 = 0.
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; grep -c 'ALTER PUBLICATION supabase_realtime ADD TABLE' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'safety_alerts_select_v1_poc' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'wear_state_events_select_v1_poc' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'device_watches_select_v1_poc' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'devices_select_paired_watch_v1_poc' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'DROP POLICY IF EXISTS "device_watches_select"' supabase/migrations/011_watch_app_rls.sql &amp;&amp; grep -c 'public.safety_alerts' tests/sql/test_011_rls_isolation.sql &amp;&amp; grep -c 'pg_publication_tables' tests/sql/test_011_rls_isolation.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ALTER PUBLICATION supabase_realtime ADD TABLE' supabase/migrations/011_watch_app_rls.sql` == 4 (4 tables)
    - `grep -c '_v1_poc' supabase/migrations/011_watch_app_rls.sql` >= 4 (4 SELECT policies)
    - `grep -c 'DROP POLICY IF EXISTS "device_watches_select"' supabase/migrations/011_watch_app_rls.sql` == 1 (Pitfall 4 fix)
    - tests/sql/test_011_rls_isolation.sql 존재 + pg_publication_tables / pg_policies 쿼리 포함
    - (BLOCKING) `supabase db push` 명령 성공 (executor 가 STDOUT 으로 "Applied migration 011" 확인)
    - (BLOCKING) `psql "$DATABASE_URL" -f tests/sql/test_011_rls_isolation.sql` 실행 결과: Test 1 = 4 rows, Test 2 = 4 rows, Test 3 = 0, Test 5 = 0
  </acceptance_criteria>
  <done>011 마이그레이션 작성 + 운영 DB push + isolation SQL 테스트 작성 + 5종 verification 통과. Wave 2 의 case 'watch-ack' 가 정확한 컬럼 (`ack_at`) UPDATE 가능, Wave 3 의 Realtime SDK 가 broadcast 받을 수 있는 publication 활성화 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: scripts/seed_watch_demo.py — D-05 fallback seed (PoC 실패/미실행 시 시연 안전망)</name>
  <files>scripts/seed_watch_demo.py</files>
  <read_first>
    - .planning/phases/07-watch-app-bridge/07-CONTEXT.md §D-05 (단축 PoC 산출물 — minute_summary≈120, wear_state_events≥3, safety_alerts≥2)
    - supabase/migrations/010_watch_pipeline.sql (4 테이블 컬럼 + check constraint)
    - .env.example (서버 인증 키 변수명 — Phase 4 03 에서 작성됨)
  </read_first>
  <action>
    `scripts/seed_watch_demo.py` 신규 작성 — Phase 4 04-04 단축 PoC 가 시연 직전까지 실행되지 않거나 실패할 경우의 fallback. testuser1 의 device_id 에 (a) 120 분 minute_summary (HR/temp/wear 합리적 분포), (b) wear_state_events 3건 (WARMUP→WORN, WORN→OFF, OFF→WORN), (c) safety_alerts 2건 (REMOVED 발생/해소 — D-09 알림 전이 원칙) 을 시드.

    구조:

    ```python
    #!/usr/bin/env python3
    """
    Phase 7 / D-05 fallback — 단축 PoC 미실행 또는 실패 시 시연 데이터 시드.

    실측 PoC (Phase 4 04-04) 가 우선 — 본 스크립트는 시연 직전 fallback.

    사용:
      python scripts/seed_watch_demo.py --device-id 1 --duration-min 120
      python scripts/seed_watch_demo.py --device-id 1 --duration-min 120 --wipe

    환경 변수 (.env 에서 읽음 — Phase 4 03 패턴):
      SUPABASE_URL                  https://<project>.supabase.co
      SUPABASE_SERVICE_ROLE_KEY     service_role key (RLS 우회용 — 본 스크립트 한정)

    산출물:
      - minute_summary: --duration-min 행 (default 120, good_ratio 0.7~0.95 분포)
      - wear_state_events: 3 행 (WARMUP→WORN, WORN→OFF, OFF→WORN — REMOVED 시나리오)
      - safety_alerts: 2 행 (REMOVED 발생 + REMOVED 해소 — resolved_at 채움 = D-09 전이)

    안전:
      - --wipe 플래그 없으면 INSERT 만 (기존 데이터 보존)
      - --wipe 시 device_id 의 (minute_summary, wear_state_events, safety_alerts) DELETE 후 시드
      - raw_events 는 시드 안 함 (5–6Hz × 7200s = 40k 행 → fallback 가치 낮음)
    """

    from __future__ import annotations
    import argparse
    import json
    import os
    import random
    import sys
    import urllib.request
    from datetime import datetime, timedelta, timezone
    from pathlib import Path

    REPO_ROOT = Path(__file__).resolve().parents[1]
    ENV_FILE = REPO_ROOT / ".env"


    def load_env() -> dict[str, str]:
        """Phase 4 03 패턴 — .env 파일 직접 파싱 (python-dotenv 무 의존)."""
        env: dict[str, str] = {}
        if not ENV_FILE.exists():
            print(f"ERROR: {ENV_FILE} 없음. .env.example 복사 + service_role key 채우세요.", file=sys.stderr)
            sys.exit(1)
        for line in ENV_FILE.read_text(encoding="utf-8").splitlines():
            line = line.strip()
            if not line or line.startswith("#") or "=" not in line:
                continue
            k, v = line.split("=", 1)
            env[k.strip()] = v.strip().strip('"').strip("'")
        for key in ("SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY"):
            if key not in env:
                print(f"ERROR: .env 에 {key} 없음", file=sys.stderr)
                sys.exit(1)
        return env


    def supabase_request(env: dict, method: str, path: str, body: list | dict | None = None) -> tuple[int, str]:
        url = f"{env['SUPABASE_URL'].rstrip('/')}/rest/v1/{path}"
        data = json.dumps(body).encode("utf-8") if body is not None else None
        req = urllib.request.Request(
            url,
            data=data,
            method=method,
            headers={
                "apikey": env["SUPABASE_SERVICE_ROLE_KEY"],
                "Authorization": f"Bearer {env['SUPABASE_SERVICE_ROLE_KEY']}",
                "Content-Type": "application/json",
                "Prefer": "return=representation",
            },
        )
        try:
            with urllib.request.urlopen(req, timeout=15) as resp:
                return resp.status, resp.read().decode("utf-8")
        except urllib.error.HTTPError as e:
            return e.code, e.read().decode("utf-8", errors="replace")


    def wipe_device(env: dict, device_id: int) -> None:
        for table in ("safety_alerts", "wear_state_events", "minute_summary"):
            status, body = supabase_request(env, "DELETE", f"{table}?device_id=eq.{device_id}")
            print(f"[WIPE] {table} device_id={device_id} → HTTP {status}")


    def seed_minute_summary(env: dict, device_id: int, duration_min: int, start: datetime) -> int:
        rows = []
        for i in range(duration_min):
            minute_ts = start + timedelta(minutes=i)
            # 시연 시나리오: 0~30 분 = WORN 정상, 30~35 분 = OFF (REMOVED), 35~120 분 = WORN 복귀
            if 30 <= i < 35:
                state = "OFF"
                hr = None
                temp = round(random.uniform(28.0, 30.0), 1)
                good = round(random.uniform(0.20, 0.35), 2)  # 결측 임계 (D-17 = 0.30) 근처
            else:
                state = "WORN"
                hr = random.randint(72, 92)
                temp = round(random.uniform(35.6, 36.4), 1)
                good = round(random.uniform(0.70, 0.95), 2)
            rows.append({
                "device_id": device_id,
                "minute_ts": minute_ts.isoformat(),
                "hr_median": hr,
                "temp_median": temp,
                "temp_iqr": round(random.uniform(0.1, 0.5), 1),
                "steps_delta": random.randint(0, 30) if state == "WORN" else 0,
                "dominant_state": state,
                "good_ratio": good,
            })
        status, body = supabase_request(env, "POST", "minute_summary", rows)
        if status >= 300:
            print(f"ERROR minute_summary insert: {status} {body}", file=sys.stderr)
            sys.exit(2)
        return len(rows)


    def seed_wear_state_events(env: dict, device_id: int, start: datetime) -> int:
        rows = [
            # (a) WARMUP → WORN at +1 min
            {"device_id": device_id, "ts": (start + timedelta(minutes=1)).isoformat(),
             "from_state": "WARMUP", "to_state": "WORN", "reason": {"trigger": "temp>=T_warm"}},
            # (b) WORN → OFF at +30 min (REMOVED 시작)
            {"device_id": device_id, "ts": (start + timedelta(minutes=30)).isoformat(),
             "from_state": "WORN", "to_state": "OFF", "reason": {"trigger": "temp<T_off"}},
            # (c) OFF → WORN at +35 min (REMOVED 해소)
            {"device_id": device_id, "ts": (start + timedelta(minutes=35)).isoformat(),
             "from_state": "OFF", "to_state": "WORN", "reason": {"trigger": "temp>=T_warm"}},
        ]
        status, body = supabase_request(env, "POST", "wear_state_events", rows)
        if status >= 300:
            print(f"ERROR wear_state_events insert: {status} {body}", file=sys.stderr)
            sys.exit(2)
        return len(rows)


    def seed_safety_alerts(env: dict, device_id: int, start: datetime) -> int:
        # D-09 알림 전이 원칙: REMOVED 발생 1 건 (resolved_at NULL) + REMOVED 해소 1 건 (resolved_at 채움)
        # 시연 시 ack 버튼으로 ack_at 채움 (Wave 3)
        raised = (start + timedelta(minutes=30)).isoformat()
        resolved = (start + timedelta(minutes=35)).isoformat()
        rows = [
            {"device_id": device_id, "alert_type": "REMOVED", "severity": "WARNING",
             "raised_at": raised, "resolved_at": None, "ack_at": None,
             "reason": {"source": "seed_watch_demo.py", "trigger": "OFF≥5min"}},
            {"device_id": device_id, "alert_type": "REMOVED", "severity": "WARNING",
             "raised_at": raised, "resolved_at": resolved, "ack_at": None,
             "reason": {"source": "seed_watch_demo.py", "trigger": "REMOVED resolved"}},
        ]
        status, body = supabase_request(env, "POST", "safety_alerts", rows)
        if status >= 300:
            print(f"ERROR safety_alerts insert: {status} {body}", file=sys.stderr)
            sys.exit(2)
        return len(rows)


    def main() -> None:
        ap = argparse.ArgumentParser()
        ap.add_argument("--device-id", type=int, required=True, help="testuser1 J2208A device_id (010 시드)")
        ap.add_argument("--duration-min", type=int, default=120, help="minute_summary 행 수 (default 120)")
        ap.add_argument("--wipe", action="store_true", help="시드 전 device 의 기존 행 삭제")
        args = ap.parse_args()

        env = load_env()
        if args.wipe:
            wipe_device(env, args.device_id)

        random.seed(42)  # 재현 가능
        start = datetime.now(timezone.utc) - timedelta(minutes=args.duration_min)

        n_summary = seed_minute_summary(env, args.device_id, args.duration_min, start)
        n_wear = seed_wear_state_events(env, args.device_id, start)
        n_alert = seed_safety_alerts(env, args.device_id, start)

        print(f"\n[SEED COMPLETE] device_id={args.device_id}")
        print(f"  minute_summary: {n_summary} rows")
        print(f"  wear_state_events: {n_wear} rows")
        print(f"  safety_alerts: {n_alert} rows")


    if __name__ == "__main__":
        main()
    ```

    실행 (executor 가 verify 단계에서 1회 dry-run 권장):

    ```bash
    # device_id 확인
    psql "$DATABASE_URL" -c "SELECT device_id FROM devices WHERE serial_number='J2208A-21:02:02:06:01:69'"

    # 시드
    python scripts/seed_watch_demo.py --device-id <id> --wipe
    ```
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; python -c "import ast; ast.parse(open('scripts/seed_watch_demo.py', encoding='utf-8').read()); print('SYNTAX OK')" &amp;&amp; grep -c 'minute_summary' scripts/seed_watch_demo.py &amp;&amp; grep -c 'wear_state_events' scripts/seed_watch_demo.py &amp;&amp; grep -c 'safety_alerts' scripts/seed_watch_demo.py &amp;&amp; grep -c 'SUPABASE_SERVICE_ROLE_KEY' scripts/seed_watch_demo.py &amp;&amp; grep -c 'D-05' scripts/seed_watch_demo.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('scripts/seed_watch_demo.py').read())"` succeeds (syntactic OK)
    - `grep -c 'minute_summary' scripts/seed_watch_demo.py` >= 3
    - `grep -c 'wear_state_events' scripts/seed_watch_demo.py` >= 3
    - `grep -c 'safety_alerts' scripts/seed_watch_demo.py` >= 3
    - `grep -c 'SUPABASE_SERVICE_ROLE_KEY' scripts/seed_watch_demo.py` >= 1 (.env 활용)
    - (선택, executor 가 .env + DB 접근 가능 시) 1회 `--device-id <id> --wipe` 실행 → "minute_summary: 120 rows / wear_state_events: 3 rows / safety_alerts: 2 rows" 출력
  </acceptance_criteria>
  <done>scripts/seed_watch_demo.py 가 D-05 fallback 으로 PoC 데이터 형태 (minute_summary 120 + wear_state_events 3 + safety_alerts 2 with REMOVED 발생/해소) 를 시드 가능. Wave 4 단축 PoC 가 실패해도 시연 흐름 보장.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android (anon key) → Supabase Postgres | Untrusted client read 가 RLS 정책으로 차단됨. anon key 는 라우팅 토큰일 뿐, 보안 경계는 RLS. |
| BLE PC daemon (service_role) → Supabase | Trusted (service_role) — 본 plan 범위 밖이지만 Phase 4 03 에서 .env 보호 완료. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Information Disclosure | anon key 노출 + RLS bypass — 003 의 device_watches_select USING(true) 가 모든 device 의 HR/temp 노출 | mitigate | 011 마이그레이션이 (a) 003 의 USING(true) DROP, (b) safety_alerts/wear_state_events/device_watches/devices SELECT 정책을 `mac_address IS NOT NULL` 패턴으로 narrowing. v1.0 PoC = testuser1 단일 사용자라 잠정 정책. v1.1 Auth 도입 시 즉시 `auth.uid()` 기반으로 강화 (Assumption A2 — RESEARCH §State of the Art) |
| T-7-04 | Information Disclosure | Realtime 채널 authorization — anon key + filter `device_id=eq.{N}` 만으로는 다른 device 데이터 접근 가능 | accept | RLS USING true v1.0 한계 — 클라이언트가 잘못된 filter 보내도 RLS 가 행 수준에서 차단 (mac_address IS NOT NULL 패턴이 testuser1 단일 사용자 환경에서는 사실상 본인 device 한정). v1.1 Auth 도입 시 즉시 강화 — 본 plan 의 011 RLS 가 narrowing 하면서 잔여 위험 documenting |
| T-7-supply | Supply Chain | supabase-kt 3.x 자동 채택 시 Kotlin 2.3.21 강제 → Compose Compiler 1.5.10 ABI 비호환 (런타임 크래시 가능) | mitigate | 본 plan 이 정확한 GAV `supabase-kt:2.2.0` + `ktor-client-cio:2.3.9` 로 lock. proguard 룰 추가로 release 빌드 안전. acceptance_criteria 의 grep -E 'realtime-kt:3\.' == 0 가 회귀 가드 |

T-7-02 (cross-worker ack), T-7-03 (MAC spoofing) 는 07-02-PLAN 의 Edge Function 책임 — 본 plan 의 SELECT 정책은 ack/pair UPDATE 경로 안 거치므로 해당 보안은 07-02 에서 mitigate.
</threat_model>

<verification>
Wave 1 phase-gate:
1. `grep` gate 4종 (위 task 1·2·3 의 acceptance_criteria 의 grep 명령 모두 통과)
2. `supabase db push` 성공 + Supabase Dashboard 의 Database → Migrations 탭에 011 등장
3. `psql "$DATABASE_URL" -f tests/sql/test_011_rls_isolation.sql` → Test 1=4행, Test 2=4행, Test 3=0, Test 5=0
4. (선택) `gradlew app:assembleDebug` 빌드 성공 — Wave 3 의 코드 추가 후 검증 가능, 본 plan 만으로는 의존성 추가만으로도 빌드 통과해야 함
5. (선택, .env + DB 접근 가능 시) `python scripts/seed_watch_demo.py --device-id <id>` 1회 dry-run 성공
</verification>

<success_criteria>
Plan 01 완료 조건 (전부 PASS 시 Wave 2 진입 가능):
- [ ] supabase-kt 2.2.0 + ktor-cio 2.3.9 + desugaring + BuildConfig 가 app/build.gradle.kts 에 정확히 들어감 (3.x 거부 회귀 가드 + okhttp engine 거부 회귀 가드)
- [ ] proguard-rules.pro 에 supabase/ktor keep 규칙 추가 (release build 미래 보장)
- [ ] supabase/migrations/011_watch_app_rls.sql 작성 + 운영 DB 적용 (supabase db push) + 003 의 USING(true) 제거 + 4 테이블 publication ADD
- [ ] tests/sql/test_011_rls_isolation.sql 작성 + 운영 DB 에서 5종 assertion 통과
- [ ] scripts/seed_watch_demo.py 작성 + ast.parse 통과 + (선택) 1회 dry-run 성공
- [ ] 모든 must_haves.truths 검증 가능
</success_criteria>

<output>
After completion, create `.planning/phases/07-watch-app-bridge/07-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Highlight:
- 정확한 GAV 채택 근거 (RESEARCH §Standard Stack alternatives — 3.x 거부 이유 1차 출처)
- 011 운영 DB 적용 결과 (Migrations 탭 캡처 또는 SQL 응답)
- isolation SQL 5종 결과
- (선택) seed dry-run 결과
- T-7-01 mitigate / T-7-04 accept (v1.0 한계) 명시
</output>
