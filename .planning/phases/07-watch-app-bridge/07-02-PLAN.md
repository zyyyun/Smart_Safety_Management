---
phase: 07-watch-app-bridge
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - supabase/functions/notifications/index.ts
  - tests/smoke/watch_ack.sh
  - tests/smoke/watch_pair.sh
autonomous: true
requirements: [BRIDGE-02, BRIDGE-03]
tags: [phase-7, watch, edge-function, supabase, ack, pair, deploy]

user_setup:
  - service: supabase
    why: "supabase functions deploy notifications (case watch-ack + watch-pair 활성화)"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (Phase 4 03 에서 이미 작성)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env (Phase 4 03)"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token"
    dashboard_config: []

must_haves:
  truths:
    - "notifications/index.ts switch 안에 case 'watch-ack' 가 등장 — SQL `UPDATE safety_alerts SET ack_at=now() WHERE alert_id=$1 AND device_id IN (SELECT FROM devices WHERE user_id=$2) AND ack_at IS NULL` (D-03)"
    - "ack_at 컬럼명 일관 — `acknowledged_at` 표기 사용 X (Pitfall 5)"
    - "case 'watch-ack' 의 WHERE 가 device_id IN (... user_id) — T-7-02 mitigation"
    - "case 'watch-pair' 가 같은 파일에 등장 — payload `{action, user_id, mac_address, op}` + WHERE `(user_id IS NULL OR user_id=$2)` (D-04b)"
    - "case 'watch-pair' WHERE 가 다른 worker 의 워치 가로채기 차단 — T-7-03 mitigation"
    - "Phase 4 D-09 알림 전이 준수 — ack 자체가 새 alert 생성 X (DB UPDATE 만)"
    - "ack idempotency — `.is('ack_at', null)` 가드로 두 번째 ack 시 0 rows + 404"
    - "supabase functions deploy 성공 + curl smoke 2종 (ack + pair) 모두 PASS"
  artifacts:
    - path: "supabase/functions/notifications/index.ts"
      provides: "case 'watch-ack' + case 'watch-pair'"
      contains: "case \"watch-ack\""
    - path: "tests/smoke/watch_ack.sh"
      provides: "ack curl smoke (BRIDGE-02b)"
      contains: "watch-ack"
    - path: "tests/smoke/watch_pair.sh"
      provides: "pair curl smoke (BRIDGE-03)"
      contains: "watch-pair"
  key_links:
    - from: "notifications/index.ts case 'watch-ack'"
      to: "safety_alerts.ack_at"
      via: "supabase.from('safety_alerts').update({ack_at: ...})"
      pattern: "ack_at"
    - from: "notifications/index.ts case 'watch-pair'"
      to: "devices.mac_address + devices.user_id"
      via: "supabase.from('devices').update / insert"
      pattern: "case \"watch-pair\""
---

<objective>
Phase 7 Wave 2 — `notifications/index.ts` 에 case 'watch-ack' (BRIDGE-02) + case 'watch-pair' (BRIDGE-03) 를 추가하고 운영 Deno Deploy 에 배포한다. Phase 4 03 의 case 'watch-alert' 와 동일 파일·동일 패턴. 각 case 는 ownership 검증 SQL 을 내부에 포함하여 anon key 환경에서도 보안 경계를 service_role 단에서 강제. 배포 후 curl smoke 2종으로 200 OK + 응답 형태 검증.

Purpose: Wave 3 의 SafetyAlertsActivity acknowledge 버튼 + PairWatchSection 등록 버튼이 호출할 backend 엔드포인트 배치. T-7-02 (cross-worker ack) + T-7-03 (MAC spoofing) 두 위협의 mitigation 본체.

Output:
- supabase/functions/notifications/index.ts 에 case 2개 추가
- 운영 Deno Deploy 재배포
- tests/smoke/watch_ack.sh + watch_pair.sh 작성 + smoke PASS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-watch-app-bridge/07-CONTEXT.md
@.planning/phases/07-watch-app-bridge/07-RESEARCH.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@.planning/phases/04-watch-j2208a-pipeline/04-03-SUMMARY.md
@supabase/functions/notifications/index.ts
@supabase/functions/_shared/supabase.ts
@supabase/functions/_shared/response.ts
@supabase/migrations/010_watch_pipeline.sql

<interfaces>
notifications/index.ts 현재 구조 (Phase 4 03 의 case 'watch-alert' 가 base):

  import { createAdminClient } from "../_shared/supabase.ts";
  import { ok, err, optionsResponse } from "../_shared/response.ts";
  import { sendPushToUser, sendPushToUsers } from "../_shared/fcm.ts";

  Deno.serve(async (req) => {
    if (req.method === "OPTIONS") return optionsResponse();
    try {
      const { action, ...body } = await req.json();
      const supabase = createAdminClient();   // service_role
      switch (action) {
        case "list": { ... }
        case "mark_read": { ... }
        case "send_group": { ... }
        case "send_individual": { ... }
        case "watch-alert": { ... }  // Phase 4 03
        // ★ 본 plan: case "watch-ack" + case "watch-pair" 를 default 위에 추가
        default: return err(`Unknown action: ${action}`);
      }
    } catch (e) { return err(e.message, 500); }
  });

createAdminClient() = service_role → RLS 우회. ownership 검증은 SQL WHERE 절에서 수동 강제.

010 safety_alerts: alert_id, device_id, alert_type, severity, raised_at, resolved_at, **ack_at**, reason
010 devices: device_id, device_type, serial_number, **mac_address**, firmware_version, last_comm_at, user_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notifications/index.ts — case 'watch-ack' + ownership SQL + idempotency + curl smoke</name>
  <files>supabase/functions/notifications/index.ts, tests/smoke/watch_ack.sh</files>
  <read_first>
    - supabase/functions/notifications/index.ts (기존 case 'watch-alert' 위치 — line 131~179, default err 분기 위)
    - .planning/phases/07-watch-app-bridge/07-CONTEXT.md §D-03 (payload 계약 + SQL + 응답)
    - .planning/phases/07-watch-app-bridge/07-RESEARCH.md §Pattern 4 (case 'watch-ack' 구현 예제, `.is('ack_at', null)` idempotency)
    - .planning/phases/04-watch-j2208a-pipeline/04-03-SUMMARY.md (Phase 4 03 watch-alert 패턴)
    - supabase/functions/_shared/response.ts (ok/err 헬퍼 시그니처)
  </read_first>
  <behavior>
    - Test 1 (배포 후 정상 ack): testuser1 의 미해결 alert 1건 ack → HTTP 200 + `{ok:true, ack_at:"...", alert_id:N}` + DB 의 ack_at 채워짐
    - Test 2 (idempotency): 같은 alert 두 번째 ack → HTTP 404 + "already acknowledged" (0 rows updated, `.is('ack_at', null)` 가드)
    - Test 3 (ownership): 다른 user_id 로 같은 alert ack 시도 → HTTP 404 (subquery 결과 device 0개)
    - Test 4 (D-09 알림 전이): ack 호출 후 notifications 테이블 insert 발생 안 함, FCM 발송 안 함
  </behavior>
  <action>
    Step A — `supabase/functions/notifications/index.ts` 수정 (case 'watch-alert' 와 default 사이에 case 'watch-ack' 추가, ack_at 컬럼명 사용):

      case "watch-ack": {
        const { user_id, alert_id } = body;
        if (!user_id || !alert_id) {
          return err("user_id, alert_id are required");
        }

        // ownership 검증 (T-7-02)
        const { data: ownDevices, error: devErr } = await supabase
          .from("devices")
          .select("device_id")
          .eq("user_id", user_id);
        if (devErr) return err(devErr.message, 500);
        const ownDeviceIds = (ownDevices ?? []).map((d) => d.device_id);
        if (ownDeviceIds.length === 0) {
          return err("user has no devices", 404);
        }

        // ack_at 갱신 — server-side now() + idempotency 가드
        const nowIso = new Date().toISOString();
        const { data, error } = await supabase
          .from("safety_alerts")
          .update({ ack_at: nowIso })
          .eq("alert_id", alert_id)
          .in("device_id", ownDeviceIds)
          .is("ack_at", null)
          .select();

        if (error) return err(error.message, 500);
        if (!data || data.length === 0) {
          return err("alert not found or already acknowledged or not owned by user", 404);
        }
        return ok({ ok: true, ack_at: data[0].ack_at, alert_id });
      }

    위치: 기존 case "watch-alert" 의 `return ok({ success: true });` 직후 (line 179 부근), `default:` 분기 (line 181) 위.

    **컬럼명 강제**: `ack_at` 만 사용, `acknowledged_at` 사용 금지 (Pitfall 5).
    **클라이언트 시계 신뢰 X**: 항상 `new Date().toISOString()` 으로 서버 측에서 ack_at 세팅.
    **알림 발생 없음**: notifications insert / sendPushToUser 호출 안 함 — D-09 알림 전이 원칙.

    Step B — `tests/smoke/watch_ack.sh` 작성 (Phase 4 03 패턴):

      #!/bin/bash
      # tests/smoke/watch_ack.sh — Phase 7 BRIDGE-02b curl smoke
      # Usage: bash tests/smoke/watch_ack.sh <alert_id>
      set -euo pipefail
      SCRIPT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
      [[ -f "$SCRIPT_DIR/.env" ]] || { echo "ERROR: .env 없음" >&2; exit 1; }
      set -a; source "$SCRIPT_DIR/.env"; set +a
      : "${SUPABASE_URL:?}"
      : "${SUPABASE_ANON_KEY:?SUPABASE_ANON_KEY not set (07-01 BuildConfig + .env)}"
      ALERT_ID="${1:-1}"
      USER_ID="${TEST_USER_ID:-testuser1}"
      URL="${SUPABASE_URL%/}/functions/v1/notifications"

      echo "=== Test 1: 정상 ack (alert_id=$ALERT_ID, user_id=$USER_ID) ==="
      HTTP=$(curl -sS -o /tmp/wa1.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-ack\",\"alert_id\":$ALERT_ID,\"user_id\":\"$USER_ID\"}")
      echo "  HTTP $HTTP"; head -c 500 /tmp/wa1.json; echo
      [[ "$HTTP" == "200" ]] || { echo "EXPECTED 200" >&2; exit 2; }
      grep -q '"ok":true' /tmp/wa1.json || { echo "EXPECTED ok:true" >&2; exit 3; }
      grep -q '"ack_at"' /tmp/wa1.json || { echo "EXPECTED ack_at field" >&2; exit 4; }

      echo "=== Test 2: idempotency (same alert 두번째 ack expect 404) ==="
      HTTP=$(curl -sS -o /tmp/wa2.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-ack\",\"alert_id\":$ALERT_ID,\"user_id\":\"$USER_ID\"}")
      echo "  HTTP $HTTP"; head -c 500 /tmp/wa2.json; echo
      [[ "$HTTP" == "404" ]] || { echo "EXPECTED 404 already-acked" >&2; exit 5; }
      grep -q 'already acknowledged\|not found' /tmp/wa2.json || { echo "EXPECTED already-acked msg" >&2; exit 6; }

      echo "=== Test 3: ownership (다른 user_id expect 404) ==="
      HTTP=$(curl -sS -o /tmp/wa3.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-ack\",\"alert_id\":$ALERT_ID,\"user_id\":\"someone_else\"}")
      echo "  HTTP $HTTP"; head -c 500 /tmp/wa3.json; echo
      [[ "$HTTP" == "404" ]] || { echo "EXPECTED 404 ownership" >&2; exit 7; }

      echo "=== ALL WATCH-ACK SMOKE TESTS PASSED ==="

    `chmod +x tests/smoke/watch_ack.sh`.

    Step C — 배포:
      cd D:/2026_산업안전/Smart_Safety_Management
      supabase functions deploy notifications
      # (또는 Non-TTY: SUPABASE_ACCESS_TOKEN=<t> supabase functions deploy notifications)

    Step D — smoke 실행 (seed_watch_demo.py 가 만든 미해결 alert_id 사용):
      psql "$DATABASE_URL" -c "SELECT alert_id FROM safety_alerts WHERE ack_at IS NULL ORDER BY raised_at DESC LIMIT 1"
      bash tests/smoke/watch_ack.sh <alert_id>
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; grep -c 'case "watch-ack"' supabase/functions/notifications/index.ts &amp;&amp; grep -c "ack_at" supabase/functions/notifications/index.ts &amp;&amp; (grep -c "acknowledged_at" supabase/functions/notifications/index.ts || echo 0) &amp;&amp; grep -c 'is("ack_at", null)' supabase/functions/notifications/index.ts &amp;&amp; grep -c 'user has no devices' supabase/functions/notifications/index.ts &amp;&amp; test -f tests/smoke/watch_ack.sh &amp;&amp; grep -c 'watch-ack' tests/smoke/watch_ack.sh &amp;&amp; grep -c 'already acknowledged' tests/smoke/watch_ack.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "watch-ack"' supabase/functions/notifications/index.ts` == 1
    - `grep -c 'ack_at' supabase/functions/notifications/index.ts` >= 3
    - `grep -c 'acknowledged_at' supabase/functions/notifications/index.ts` == 0 (Pitfall 5)
    - `grep -c 'is("ack_at", null)' supabase/functions/notifications/index.ts` == 1 (idempotency)
    - `tests/smoke/watch_ack.sh` 존재 + 실행 권한
    - (BLOCKING) `supabase functions deploy notifications` 성공
    - (BLOCKING) seed 후 `bash tests/smoke/watch_ack.sh <alert_id>` 종료 코드 0 + 출력 "ALL WATCH-ACK SMOKE TESTS PASSED"
    - DB: `psql -c "SELECT ack_at FROM safety_alerts WHERE alert_id=<id>"` → NOT NULL
  </acceptance_criteria>
  <done>case 'watch-ack' 배포 + 3종 smoke PASS (정상/idempotency/ownership) + ack_at 컬럼명 일관 + T-7-02 mitigation 가동.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: notifications/index.ts — case 'watch-pair' + MAC 재검증 + spoofing 차단 + curl smoke</name>
  <files>supabase/functions/notifications/index.ts, tests/smoke/watch_pair.sh</files>
  <read_first>
    - supabase/functions/notifications/index.ts (Task 1 직후, case 'watch-ack' 가 추가된 상태)
    - .planning/phases/07-watch-app-bridge/07-CONTEXT.md §D-04b (Edge Function 'watch-pair' + WHERE `(user_id IS NULL OR user_id=$2)` 패턴)
    - .planning/phases/07-watch-app-bridge/07-RESEARCH.md §Pattern 6 (Edge Function 경유 Path A)
    - supabase/migrations/010_watch_pipeline.sql (devices.mac_address UNIQUE, device_type='WATCH', testuser1 시드)
  </read_first>
  <behavior>
    - Test 1 (정상 pair): payload `{action, user_id:testuser1, mac_address:"21:02:02:06:01:69", op:"pair"}` → HTTP 200 + `{ok:true, device_id, mac_address}` + DB devices.user_id 갱신
    - Test 2 (MAC 형식 invalid): mac_address=`not-a-mac` → HTTP 400 + "invalid MAC format"
    - Test 3 (spoofing 차단): 다른 user_id 가 같은 mac 으로 pair 시도 → HTTP 409 + "already paired to another user" (T-7-03)
    - Test 4 (unpair): `op:"unpair"` → HTTP 200 + DB mac_address NULL 화 (본 user 의 워치만)
    - Test 5 (re-pair idempotent): 같은 user 가 같은 mac 으로 재등록 → HTTP 200
  </behavior>
  <action>
    Step A — `supabase/functions/notifications/index.ts` 에 case 'watch-pair' 추가 (case 'watch-ack' 바로 아래, `default:` 위):

      case "watch-pair": {
        const { user_id, mac_address, op } = body;
        if (!user_id || !op) {
          return err("user_id, op are required");
        }
        if (op !== "pair" && op !== "unpair") {
          return err(`invalid op: ${op} (expected "pair" or "unpair")`);
        }

        if (op === "pair") {
          // MAC 재검증 (클라이언트 검증 우회 차단 — T-7-03)
          if (!mac_address || typeof mac_address !== "string") {
            return err("mac_address is required for op=pair");
          }
          const MAC_REGEX = /^([0-9A-F]{2}:){5}[0-9A-F]{2}$/;
          const macUpper = mac_address.toUpperCase();
          if (!MAC_REGEX.test(macUpper)) {
            return err(`invalid MAC format: ${mac_address}`, 400);
          }

          // ownership 검증 (T-7-03 spoofing 차단): mac 의 device 가 (a) 미할당 or (b) 본인 보유
          const { data: existing, error: selErr } = await supabase
            .from("devices")
            .select("device_id, user_id, mac_address")
            .eq("mac_address", macUpper)
            .eq("device_type", "WATCH")
            .maybeSingle();
          if (selErr) return err(selErr.message, 500);

          if (existing && existing.user_id && existing.user_id !== user_id) {
            return err("watch already paired to another user", 409);
          }

          if (existing) {
            // idempotent UPDATE (같은 user 가 같은 mac 재등록 OK)
            const { error: updErr } = await supabase
              .from("devices")
              .update({ user_id })
              .eq("device_id", existing.device_id);
            if (updErr) return err(updErr.message, 500);
            return ok({ ok: true, device_id: existing.device_id, mac_address: macUpper, op: "pair" });
          } else {
            // 새 device INSERT (010 시드 외 MAC)
            const { data, error: insErr } = await supabase
              .from("devices")
              .insert({
                device_type: "WATCH",
                serial_number: `J2208A-${macUpper}`,
                mac_address: macUpper,
                user_id,
              })
              .select()
              .single();
            if (insErr) return err(insErr.message, 500);
            return ok({ ok: true, device_id: data.device_id, mac_address: macUpper, op: "pair" });
          }
        } else {
          // op === "unpair" — 본인 워치만 mac_address NULL
          const { data, error } = await supabase
            .from("devices")
            .update({ mac_address: null })
            .eq("user_id", user_id)
            .eq("device_type", "WATCH")
            .select();
          if (error) return err(error.message, 500);
          if (!data || data.length === 0) {
            return err("no paired watch found for user", 404);
          }
          return ok({ ok: true, count: data.length, op: "unpair" });
        }
      }

    Step B — `tests/smoke/watch_pair.sh` 작성:

      #!/bin/bash
      # tests/smoke/watch_pair.sh — Phase 7 BRIDGE-03 curl smoke
      set -euo pipefail
      SCRIPT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
      set -a; source "$SCRIPT_DIR/.env"; set +a
      : "${SUPABASE_URL:?}"
      : "${SUPABASE_ANON_KEY:?}"
      USER_ID="${TEST_USER_ID:-testuser1}"
      MAC="21:02:02:06:01:69"
      URL="${SUPABASE_URL%/}/functions/v1/notifications"

      echo "=== Test 1: pair 정상 ==="
      HTTP=$(curl -sS -o /tmp/wp1.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-pair\",\"user_id\":\"$USER_ID\",\"mac_address\":\"$MAC\",\"op\":\"pair\"}")
      echo "  HTTP $HTTP"; head -c 500 /tmp/wp1.json; echo
      [[ "$HTTP" == "200" ]] || { echo "EXPECTED 200" >&2; exit 2; }
      grep -q '"ok":true' /tmp/wp1.json || { echo "EXPECTED ok:true" >&2; exit 3; }

      echo "=== Test 2: invalid MAC format (expect 400) ==="
      HTTP=$(curl -sS -o /tmp/wp2.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-pair\",\"user_id\":\"$USER_ID\",\"mac_address\":\"not-a-mac\",\"op\":\"pair\"}")
      echo "  HTTP $HTTP"; head -c 300 /tmp/wp2.json; echo
      [[ "$HTTP" == "400" ]] || { echo "EXPECTED 400 invalid MAC" >&2; exit 4; }
      grep -q 'invalid MAC' /tmp/wp2.json || { echo "EXPECTED invalid MAC msg" >&2; exit 5; }

      echo "=== Test 3: spoofing (다른 user 같은 mac) expect 409 ==="
      HTTP=$(curl -sS -o /tmp/wp3.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-pair\",\"user_id\":\"someone_else\",\"mac_address\":\"$MAC\",\"op\":\"pair\"}")
      echo "  HTTP $HTTP"; head -c 300 /tmp/wp3.json; echo
      [[ "$HTTP" == "409" ]] || { echo "EXPECTED 409 spoofing block" >&2; exit 6; }
      grep -q 'already paired' /tmp/wp3.json || { echo "EXPECTED already-paired" >&2; exit 7; }

      echo "=== Test 4: unpair ==="
      HTTP=$(curl -sS -o /tmp/wp4.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-pair\",\"user_id\":\"$USER_ID\",\"op\":\"unpair\"}")
      echo "  HTTP $HTTP"; head -c 300 /tmp/wp4.json; echo
      [[ "$HTTP" == "200" ]] || { echo "EXPECTED 200 unpair" >&2; exit 8; }

      echo "=== Test 5: re-pair idempotent ==="
      HTTP=$(curl -sS -o /tmp/wp5.json -w "%{http_code}" -X POST "$URL" \
          -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
          -H "Content-Type: application/json" \
          -d "{\"action\":\"watch-pair\",\"user_id\":\"$USER_ID\",\"mac_address\":\"$MAC\",\"op\":\"pair\"}")
      echo "  HTTP $HTTP"; head -c 300 /tmp/wp5.json; echo
      [[ "$HTTP" == "200" ]] || { echo "EXPECTED 200 re-pair" >&2; exit 9; }

      echo "=== ALL WATCH-PAIR SMOKE TESTS PASSED ==="

    `chmod +x tests/smoke/watch_pair.sh`.

    Step C — 재배포 + smoke:
      supabase functions deploy notifications
      bash tests/smoke/watch_pair.sh
  </action>
  <verify>
    <automated>cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; grep -c 'case "watch-pair"' supabase/functions/notifications/index.ts &amp;&amp; grep -c "MAC_REGEX" supabase/functions/notifications/index.ts &amp;&amp; grep -c "already paired to another user" supabase/functions/notifications/index.ts &amp;&amp; grep -c 'op === "unpair"' supabase/functions/notifications/index.ts &amp;&amp; test -f tests/smoke/watch_pair.sh &amp;&amp; grep -c "watch-pair" tests/smoke/watch_pair.sh &amp;&amp; grep -c "invalid MAC" tests/smoke/watch_pair.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'case "watch-pair"' supabase/functions/notifications/index.ts` == 1
    - `grep -c 'MAC_REGEX' supabase/functions/notifications/index.ts` >= 1
    - `grep -c 'already paired to another user' supabase/functions/notifications/index.ts` == 1 (T-7-03 mitigation)
    - `grep -c 'op === "unpair"' supabase/functions/notifications/index.ts` == 1
    - `tests/smoke/watch_pair.sh` 존재 + 실행 권한
    - (BLOCKING) `supabase functions deploy notifications` 성공 (Task 1 + Task 2 한 번에 deploy 가능)
    - (BLOCKING) `bash tests/smoke/watch_pair.sh` 종료 코드 0 + "ALL WATCH-PAIR SMOKE TESTS PASSED" + 5개 Test 모두 expected HTTP code 통과
    - DB: `psql -c "SELECT mac_address, user_id FROM devices WHERE serial_number LIKE 'J2208A-%' AND user_id='testuser1'"` → 1행 (Test 5 후)
  </acceptance_criteria>
  <done>case 'watch-pair' 배포 + 5종 smoke PASS (정상/MAC invalid/spoofing/unpair/re-pair) + T-7-03 mitigation 가동.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android (anon key) → Edge Function (service_role) | Untrusted client payload. Edge Function 내부에서 모든 필드 재검증 + ownership SQL WHERE |
| Edge Function → Postgres | Trusted (service_role 가 RLS 우회) — ownership 은 SQL WHERE 절에서 수동 강제 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Tampering | case 'watch-ack' — worker A 가 worker B 의 alert_id 를 알아내고 ack 호출 (DoS 또는 evidence tampering) | mitigate | SQL: `device_id IN (SELECT device_id FROM devices WHERE user_id=$user_id)` — payload 의 user_id 가 보유한 device 의 alert 만 ack 가능. 다른 user 의 alert_id 보내도 IN 절 0행 매칭 → 404 응답. Task 1 의 Test 3 (ownership smoke) 가 회귀 가드. **Note**: 본 phase 는 user_id 자체의 위조를 막지 못함 (Firebase Auth user_id 가 클라이언트에서 보내는 값) — v1.1 Supabase Auth + JWT sub claim 도입 시 강화. v1.0 PoC 한정 잠정 신뢰. |
| T-7-03 | Spoofing | case 'watch-pair' — worker A 가 worker B 의 mac_address 를 알아내고 자기 user_id 로 pair 시도 (워치 가로채기) | mitigate | SQL: 기존 device row 의 user_id 가 NULL (미할당) 이거나 본 user_id 와 일치할 때만 UPDATE. 다른 user 가 이미 paired 한 워치는 409 응답. MAC 형식 재검증 (정규식, 클라이언트 우회 차단). Task 2 의 Test 3 (spoofing smoke) 가 회귀 가드. **Note**: 마찬가지로 user_id 자체 위조 (Firebase Auth 클라이언트 토큰) 는 본 phase 미대응 — v1.1 강화. |
| T-7-05 | Tampering | case 'watch-ack' 의 ack_at 클라이언트 시계 위조 | mitigate | 클라이언트 payload 의 ack_at 필드 미수신 — Edge Function 내부에서 `new Date().toISOString()` 만 사용. payload 의 ack_at 은 무시됨. |
| T-7-06 | Information Disclosure | case 'watch-ack' 의 alert_id enumeration (다른 worker 의 alert 정보 노출) | mitigate | 응답 = `{ok, ack_at, alert_id}` 만, alert 의 alert_type/severity/reason 미노출. ownership 실패 시 404 응답 (200 vs 404 timing leak 가능 but DoS 제외 시 의미 적음 — accept) |

T-7-01 (RLS bypass) 는 07-01 의 RLS 마이그레이션 책임 — 본 plan 의 Edge Function 은 service_role 이라 RLS 우회. T-7-04 (Realtime auth) 는 07-01 의 publication 책임.
</threat_model>

<verification>
Wave 2 phase-gate:
1. `grep` gate (Task 1·2 의 acceptance_criteria 명령 모두 통과)
2. `supabase functions deploy notifications` 성공 (executor STDOUT 에 "Deployed Function notifications" 또는 동등 문구)
3. `bash tests/smoke/watch_ack.sh <alert_id>` 종료 코드 0 + "ALL WATCH-ACK SMOKE TESTS PASSED"
4. `bash tests/smoke/watch_pair.sh` 종료 코드 0 + "ALL WATCH-PAIR SMOKE TESTS PASSED"
5. DB 후행 검증:
   - `psql -c "SELECT ack_at FROM safety_alerts WHERE alert_id=<id>"` → NOT NULL
   - `psql -c "SELECT mac_address FROM devices WHERE user_id='testuser1' AND device_type='WATCH'"` → '21:02:02:06:01:69'
6. (D-09 회귀): smoke 실행 후 `psql -c "SELECT count(*) FROM notifications WHERE user_id='testuser1' AND created_at >= now() - interval '5 minutes'"` 는 smoke 호출 시점 대비 증가 X (ack 가 새 알림 발생 안 함)
</verification>

<success_criteria>
Plan 02 완료 조건 (전부 PASS 시 Wave 3 진입 가능):
- [ ] case 'watch-ack' 가 notifications/index.ts 에 추가 + ack_at 컬럼명 + idempotency + ownership
- [ ] case 'watch-pair' 가 같은 파일에 추가 + MAC 재검증 + spoofing 차단 (T-7-03)
- [ ] supabase functions deploy notifications 성공
- [ ] tests/smoke/watch_ack.sh 3종 PASS
- [ ] tests/smoke/watch_pair.sh 5종 PASS
- [ ] D-09 알림 전이 회귀 가드 통과 (ack ≠ 새 알림)
- [ ] 모든 must_haves.truths 검증 가능
</success_criteria>

<output>
After completion, create `.planning/phases/07-watch-app-bridge/07-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Highlight:
- 2 case 추가 위치 (case 'watch-alert' 와 default 사이) + 1회 deploy 로 모두 활성화
- ack_at 컬럼명 일관성 (`acknowledged_at` 0건 회귀 가드)
- T-7-02·03 mitigation 의 SQL WHERE 절 정확한 인용
- 8종 smoke (ack 3 + pair 5) 결과
- D-09 알림 전이 회귀 가드 결과 (notifications 미증가)
</output>
