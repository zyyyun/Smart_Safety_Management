---
phase: 01-vision-demo-videos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai_agent/scripts/upload_reference_videos.py
  - ai_agent/detector_configs.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
requirements_addressed_extra: [MODEL-03]

must_haves:
  truths:
    - "fire/helmet 신규 영상이 Storage `reference-videos` 버킷의 `source_v2.mp4` 키로 업로드됨"
    - "cameras.live_url_detail (camera_id 1, 5) 가 신규 `source_v2.mp4` URL 을 가리킴"
    - "detector_configs.py 의 fire/helmet conf_thres ≥ 0.5, helmet target_classes = ['head'] 로 영구 적용됨"
    - "임시조치 주석 (`# 데모용 하향`, `# helmet 위주라 검출`) 이 운영급 주석으로 교체됨"
    - "`python -m ai_agent.main --once-detect` 1회 실행 시 detection_events 에 camera_id 1 (fire) 과 camera_id 5 (helmet) 행이 accuracy ≥ 0.5 로 insert 됨"
    - "`--only fire,helmet` 옵션이 fire/helmet 만 처리하고 forklift/person 은 건드리지 않음"
  artifacts:
    - path: "ai_agent/scripts/upload_reference_videos.py"
      provides: "fire/helmet SOURCES dict 가 신규 detection mp4 + source_v2.mp4 remote 키"
      contains: "source_v2.mp4"
    - path: "ai_agent/detector_configs.py"
      provides: "fire/helmet 운영급 임계 (conf 0.5+, helmet target_classes=['head'])"
      contains: "['head']"
    - path: "Supabase Storage: reference-videos/fire/source_v2.mp4"
      provides: "fire 신규 데모 영상 (4분 19초, 39MB, h264 yuv420p)"
    - path: "Supabase Storage: reference-videos/helmet/source_v2.mp4"
      provides: "helmet 신규 데모 영상 (동일 mp4, 작업자 등장 → head 검출 가능)"
  key_links:
    - from: "ai_agent/scripts/upload_reference_videos.py SOURCES['fire']"
      to: "D:\\2025_산업안전\\산업안전\\발표자료용 영상\\detection(fire, helmet).mp4"
      via: "kind=mp4 src 직접 매핑 (재인코딩 X)"
      pattern: "detection\\(fire, helmet\\)\\.mp4"
    - from: "ai_agent/scripts/upload_reference_videos.py SOURCES['helmet']"
      to: "동일 detection(fire, helmet).mp4"
      via: "kind=mp4 (image_loop 에서 전환), 동일 src 경로"
      pattern: "detection\\(fire, helmet\\)\\.mp4"
    - from: "process_one() remote_path"
      to: "Storage 키 `{event_key}/source_v2.mp4`"
      via: "f-string remote_path 변경 (line 206)"
      pattern: "source_v2\\.mp4"
    - from: "cameras.live_url_detail (camera_id 1, 5)"
      to: "Storage public URL (source_v2.mp4)"
      via: "update_camera_url() — 자동 호출"
      pattern: "live_url_detail.*source_v2\\.mp4"
    - from: "detector_configs.py DETECTOR_CONFIGS['fire']['conf_thres']"
      to: "0.5 (이상)"
      via: "직접 값 변경"
      pattern: "\"conf_thres\":\\s*0\\.5"
    - from: "detector_configs.py DETECTOR_CONFIGS['helmet']['target_classes']"
      to: "['head']"
      via: "None → 리스트 변경"
      pattern: "\"target_classes\":\\s*\\[\"head\"\\]"
---

<objective>
helmet/fire 데모 영상 2종을 운영급 임계 (helmet `target_classes=['head']` + `conf_thres ≥ 0.5`,
fire `conf_thres ≥ 0.5`) 에서 검출 가능한 새 영상 (`detection(fire, helmet).mp4`) 으로 교체.
`reference-videos` 버킷에 `source_v2.mp4` 신규 객체 업로드 + `cameras.live_url_detail` 갱신
+ `detector_configs.py` 영구 변경 (MODEL-03 흡수, CONTEXT D-04) + `--once-detect` 1회로
detection_events 에 accuracy ≥ 0.5 검증.

Purpose: Phase 1 SC #1~#4 충족 → Phase 2 (frames 연속 룰) 진입 전 운영급 임계가 신규 영상에서
실제 검출 가능함을 보장. v0.5 임시조치 (fire/helmet conf 0.10 + helmet target_classes None)
완전 종료. 5월 PPT 데모 (Phase 6) 의 시연 흐름이 운영급 conf 로 동작 가능.

Output:
- `ai_agent/scripts/upload_reference_videos.py` — fire/helmet SOURCES + remote_path 갱신
- `ai_agent/detector_configs.py` — fire/helmet 운영급 임계 + 주석 갱신
- Storage `reference-videos/fire/source_v2.mp4`, `reference-videos/helmet/source_v2.mp4`
- `cameras.live_url_detail` 갱신 (camera_id 1, 5)
- detection_events 신규 행 (fire + helmet, accuracy ≥ 0.5)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-vision-demo-videos/01-CONTEXT.md
@ai_agent/scripts/upload_reference_videos.py
@ai_agent/detector_configs.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase by orchestrator. -->
<!-- No codebase exploration needed — all targets and pre-validated facts below. -->

From `ai_agent/scripts/upload_reference_videos.py`:
- `LEGACY_BASE = Path(r"D:\2025_산업안전\산업안전\모델 7종")` (line 35) — 신규 영상은 이 base 밖 (`D:\2025_산업안전\산업안전\발표자료용 영상\`) 에 있으므로 LEGACY_BASE 와 join 하지 말고 절대경로 그대로 사용
- `SOURCES["fire"]` (lines 40-44): `kind=mp4`, `src=LEGACY_BASE / "화재 탐지" / "input.mp4"`, `camera_id=1`
- `SOURCES["helmet"]` (lines 45-51): `kind=image_loop`, `src_dir=...hard_hat_images`, `duration=60`, `camera_id=5`
- `SOURCES["forklift"]` (lines 52-56): `kind=gif`, `camera_id=4` — **건드리지 말 것**
- `SOURCES["person"]` (lines 57-63): `kind=mp4_clip`, `camera_id=3` — **건드리지 말 것**
- `process_one()` line 206: `remote_path = f"{event_key}/source.mp4"` — `source_v2.mp4` 로 변경
- `--only fire,helmet` 필터링 (lines 236-243) — 정상 동작 (수정 X)
- `process_one` 의 `kind` 분기: `mp4` 는 `src=Path(...)` + 파일존재 확인 후 그대로 업로드 (재인코딩 X) — fire/helmet 둘 다 `kind=mp4` 로 전환하면 동일 mp4 업로드 2회

From `ai_agent/detector_configs.py`:
- `DETECTOR_CONFIGS["fire"]` (lines 24-37):
  - 줄 30-31: 임시조치 주석 (`# 레거시 fire 영상의 fire 검출 conf 가 0.10~0.15 수준이라 데모용 하향.` / `# 운영 시 올려서 false positive 줄일 것.`) — 운영급 주석으로 교체
  - 줄 32: `"conf_thres": 0.10` → `0.5`
  - 줄 35: `"target_classes": None` — 변경 X (fire 는 단일 클래스)
- `DETECTOR_CONFIGS["helmet"]` (lines 38-52):
  - 줄 44-46: 임시조치 주석 (`# 의미상 'head' 가 알람 대상이지만 데모 image_loop 영상이 helmet 위주라 검출.` / `# 데모 목적 단순 알람 발생 우선 → target_classes=None + 낮은 임계.` / `# 후속에서 head 가 나오는 영상으로 교체 + target_classes=['head'] 복원.`) — 운영급 주석으로 교체
  - 줄 47: `"conf_thres": 0.10` → `0.5`
  - 줄 50: `"target_classes": None` → `["head"]`
- `DETECTOR_CONFIGS["forklift"]` / `["person"]` — **건드리지 말 것**

신규 영상 (orchestrator 가 ffprobe 로 사전 검증):
- 절대경로: `D:\2025_산업안전\산업안전\발표자료용 영상\detection(fire, helmet).mp4`
- 39 MB · 4:19 (259.33s) · 960x504 · 30fps · h264 yuv420p · AAC (재인코딩 불필요)
- 50 MB Storage 제한 충족
- fire 와 helmet 두 이벤트 동시 등장 → 동일 mp4 를 fire/helmet 양쪽 SOURCE 에 매핑 (CONTEXT D-02)

검증 환경:
- `.env` 에 이미 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 존재
- `python -m ai_agent.main --once-detect` 가 5종 detector 1 사이클 실행 → detection_events insert
- SQL: `select event_id, event_type_id, accuracy, camera_id from detection_events where camera_id in (1, 5) order by created_at desc limit 10;` (CONTEXT D-06)
- forklift / person / fall 의 detection_events 는 무관 (변경 없음)

롤백 경로:
- 기존 `fire/source.mp4`, `helmet/source.mp4` Storage 객체는 **삭제 X** (보존)
- 문제 발생 시 `cameras.live_url_detail` 만 원래 URL 로 SQL UPDATE → 즉시 롤백
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: detector_configs.py 운영급 임계 영구 적용 (MODEL-03 흡수)</name>

  <read_first>
    - ai_agent/detector_configs.py (전체)
    - .planning/phases/01-vision-demo-videos/01-CONTEXT.md (D-04, D-05)
    - .planning/REQUIREMENTS.md (DATA-01/02 절 + MODEL-03 절)
  </read_first>

  <files>ai_agent/detector_configs.py</files>

  <action>
    `DETECTOR_CONFIGS` dict 의 `fire` 와 `helmet` 항목만 다음과 같이 변경. `forklift`,
    `person` 항목은 절대 수정 금지. 변경 대상은 정확히 다음 5곳.

    1. **fire 임시조치 주석 교체 (줄 30-31)**:
       기존:
       ```python
           # 레거시 fire 영상의 fire 검출 conf 가 0.10~0.15 수준이라 데모용 하향.
           # 운영 시 올려서 false positive 줄일 것.
       ```
       교체:
       ```python
           # 운영급 임계 (Phase 1 / DATA-02 / D-04). 신규 영상
           # `발표자료용 영상/detection(fire, helmet).mp4` 에서 fire conf >= 0.5 로 검출 검증.
       ```

    2. **fire conf_thres 변경 (줄 32)**: `"conf_thres": 0.10,` → `"conf_thres": 0.5,`

    3. **helmet 임시조치 주석 교체 (줄 44-46)**:
       기존:
       ```python
           # 의미상 'head' 가 알람 대상이지만 데모 image_loop 영상이 helmet 위주라 검출.
           # 데모 목적 단순 알람 발생 우선 → target_classes=None + 낮은 임계.
           # 후속에서 head 가 나오는 영상으로 교체 + target_classes=['head'] 복원.
       ```
       교체:
       ```python
           # 운영급 임계 (Phase 1 / DATA-01 / D-04). 안전모 미착용 = head 객체 검출 시 알람.
           # 신규 영상 `발표자료용 영상/detection(fire, helmet).mp4` 에 작업자 등장 → head 검출 검증.
       ```

    4. **helmet conf_thres 변경 (줄 47)**: `"conf_thres": 0.10,` → `"conf_thres": 0.5,`

    5. **helmet target_classes 변경 (줄 50)**: `"target_classes": None,` → `"target_classes": ["head"],`

    Python 따옴표는 파일 기존 컨벤션 (큰따옴표) 유지. 들여쓰기 4 spaces 유지.

    **하지 말 것**:
    - forklift conf_thres (0.25) / person conf_thres (0.30) 변경 금지
    - fire target_classes (None) 변경 금지 (fire 는 단일 클래스)
    - 파일 다른 부분 (docstring, framework 키, weights 경로) 변경 금지
    - conf_thres 0.55 / 0.6 등 미세 조정은 본 task 범위 외 (Task 3 에서 실측 후 검토)
  </action>

  <verify>
    <automated>
      cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; \
      python -c "from ai_agent.detector_configs import DETECTOR_CONFIGS as C; \
        assert C['fire']['conf_thres'] >= 0.5, f\"fire conf {C['fire']['conf_thres']}\"; \
        assert C['helmet']['conf_thres'] >= 0.5, f\"helmet conf {C['helmet']['conf_thres']}\"; \
        assert C['helmet']['target_classes'] == ['head'], f\"helmet tc {C['helmet']['target_classes']}\"; \
        assert C['forklift']['conf_thres'] == 0.25, 'forklift unchanged'; \
        assert C['person']['conf_thres'] == 0.30, 'person unchanged'; \
        print('OK')"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -n '"conf_thres": 0.5' ai_agent/detector_configs.py | wc -l` returns ≥ 2 (fire 와 helmet 각 1회 이상)
    - `grep -n '"target_classes": \["head"\]' ai_agent/detector_configs.py | wc -l` returns 1
    - `grep -c '데모용 하향' ai_agent/detector_configs.py` returns 0 (임시조치 주석 제거됨)
    - `grep -c 'helmet 위주라 검출' ai_agent/detector_configs.py` returns 0
    - `grep -c '운영급 임계' ai_agent/detector_configs.py` returns ≥ 2
    - `grep -n '"conf_thres": 0.25' ai_agent/detector_configs.py | wc -l` returns 1 (forklift 보존)
    - `grep -n '"conf_thres": 0.30' ai_agent/detector_configs.py | wc -l` returns 1 (person 보존)
    - 위 verify 명령 exit 0, stdout = "OK"
  </acceptance_criteria>

  <done>
    fire/helmet conf_thres ≥ 0.5, helmet target_classes = ['head'], 임시조치 주석 운영급 주석으로 교체.
    forklift / person 항목 무변경. 파이썬 임포트 정상 (verify python -c 통과).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 (rev2, 2026-05-04): upload_reference_videos.py — fire/helmet 분리 mp4 (A1 hybrid)</name>

  <revision_note>
    **2026-05-04 변경 사유 (CONTEXT D-01·D-02 rev2 + D-18 + D-19 참조):**
    이전 단일 mp4 (`detection(fire, helmet).mp4`) 가 empirical 측정 결과 기존 YOLO
    가중치와 부적합 — fire max conf 0.039, helmet 'head' label 0건. 사용자 결정
    A1 (AI-Hub fire + 자체 촬영 helmet) 적용. 이전 LEGACY_DEMO_MP4 단일 상수 폐기,
    두 개 별도 상수 사용. **선결 조건**: 사용자가 두 mp4 파일 준비 + 경로 제공 후
    Task 2 의 placeholder 두 줄을 실제 경로로 채워야 실행 가능.
  </revision_note>

  <read_first>
    - ai_agent/scripts/upload_reference_videos.py (전체 — Task 2 v1 의 LEGACY_DEMO_MP4 부분 revert 필요)
    - .planning/phases/01-vision-demo-videos/01-CONTEXT.md (D-01·02 rev2, D-18, D-19)
  </read_first>

  <files>ai_agent/scripts/upload_reference_videos.py</files>

  <action>
    **선결 조건**: 두 mp4 파일이 disk 에 존재해야 Task 실행 가능.
    - AI-Hub fire mp4: 사용자가 다운로드 후 경로 제공 (예: `D:\2025_산업안전\산업안전\AI-Hub\화재\<선택>.mp4`)
    - 자체 촬영 helmet mp4: 사용자가 휴대폰 30s 촬영 후 경로 제공 (예: `D:\2025_산업안전\산업안전\자체촬영\helmet_30s.mp4`)
    두 경로가 placeholder `<...>` 로 남아있으면 Task 2 진입 금지 — BLOCKED 유지.

    **변경 1 (Task 2 v1 의 LEGACY_DEMO_MP4 단일 상수 폐기, 두 개 별도 상수로 교체)**

    파일 상단의 기존 LEGACY_DEMO_MP4 정의 (현재 v1 적용된 상태):
    ```python
    LEGACY_DEMO_MP4 = Path(r"D:\2025_산업안전\산업안전\발표자료용 영상\detection(fire, helmet).mp4")
    ```

    위 1줄을 다음 4줄로 교체 (사용자가 실제 경로 채움):
    ```python
    # Phase 1 / DATA-01 / CONTEXT D-01 rev2 / D-18 학습: 보유 가중치 (fire_best.pt) 와
    # 호환되는 데이터셋 → AI-Hub 화재 mp4 사용. 사용자가 직접 다운로드 후 경로 제공.
    AI_HUB_FIRE_MP4 = Path(r"<USER_PROVIDES: AI-Hub 화재 mp4 절대 경로>")

    # Phase 1 / DATA-02 / CONTEXT D-01 rev2: 자체 촬영 helmet 30s mp4. 작업자가
    # helmet 미착용 + 착용 구간 모두 포함하여 'head' label 검출 시도.
    SELF_SHOT_HELMET_MP4 = Path(r"<USER_PROVIDES: 자체 촬영 helmet 30s mp4 절대 경로>")
    ```

    **변경 2: SOURCES dict 의 `fire` 와 `helmet` 항목 — 두 별도 상수 사용**

    Task 2 v1 가 적용된 fire/helmet 항목 (둘 다 `LEGACY_DEMO_MP4` 가리킴) 을 다음으로 교체:
    ```python
        "fire": {
            "camera_id": 1,
            "kind": "mp4",
            "src": AI_HUB_FIRE_MP4,
        },
    ```
    ```python
        "helmet": {
            "camera_id": 5,
            "kind": "mp4",
            "src": SELF_SHOT_HELMET_MP4,
        },
    ```

    **변경 3 (Task 2 v1 에서 이미 적용 — 무변경)**: process_one() 의 remote_path 는
    `f"{event_key}/source_v2.mp4"` 그대로. 새 mp4 가 같은 키에 덮어쓰기.

    **하지 말 것**:
    - placeholder `<USER_PROVIDES: ...>` 를 그대로 두고 Task 3 진입
    - forklift / person 항목 변경
    - 기존 Storage 객체 (`fire/source.mp4`, `helmet/source.mp4` v0.5 baseline) 삭제
    - empirical 검증 없이 PLAN done 표시
  </action>

  <verify>
    <automated>
      cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; \
      python -c "import importlib.util, sys; \
        spec = importlib.util.spec_from_file_location('uploader', 'ai_agent/scripts/upload_reference_videos.py'); \
        mod = importlib.util.module_from_spec(spec); \
        spec.loader.exec_module(mod); \
        S = mod.SOURCES; \
        fire_src = str(S['fire']['src']); helmet_src = str(S['helmet']['src']); \
        assert 'USER_PROVIDES' not in fire_src, f'fire src placeholder 남음: {fire_src}'; \
        assert 'USER_PROVIDES' not in helmet_src, f'helmet src placeholder 남음: {helmet_src}'; \
        from pathlib import Path; \
        assert Path(fire_src).exists() and Path(fire_src).stat().st_size > 1_000_000, f'fire mp4 부재 또는 너무 작음: {fire_src}'; \
        assert Path(helmet_src).exists() and Path(helmet_src).stat().st_size > 1_000_000, f'helmet mp4 부재 또는 너무 작음: {helmet_src}'; \
        assert S['fire']['kind'] == 'mp4' and S['helmet']['kind'] == 'mp4'; \
        assert S['forklift']['kind'] == 'gif' and S['person']['kind'] == 'mp4_clip', '다른 detector 무변경'; \
        assert S['fire']['camera_id'] == 1 and S['helmet']['camera_id'] == 5; \
        assert fire_src != helmet_src, 'fire/helmet 분리 mp4 검증'; \
        print('OK')"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'AI_HUB_FIRE_MP4' ai_agent/scripts/upload_reference_videos.py` returns ≥ 2 (정의 1 + fire 사용 1)
    - `grep -c 'SELF_SHOT_HELMET_MP4' ai_agent/scripts/upload_reference_videos.py` returns ≥ 2 (정의 1 + helmet 사용 1)
    - `grep -c 'LEGACY_DEMO_MP4' ai_agent/scripts/upload_reference_videos.py` returns 0 (Task 2 v1 의 단일 상수 폐기)
    - `grep -c 'source_v2.mp4' ai_agent/scripts/upload_reference_videos.py` returns ≥ 1 (remote_path D-03 보존)
    - `grep -c 'USER_PROVIDES' ai_agent/scripts/upload_reference_videos.py` returns 0 (placeholder 모두 실제 경로로 교체)
    - `grep -c '"kind": "image_loop"' ai_agent/scripts/upload_reference_videos.py` returns 0 (helmet mp4 전환 유지)
    - `grep -c '"kind": "gif"' ai_agent/scripts/upload_reference_videos.py` returns 1 (forklift 보존)
    - `grep -c '"kind": "mp4_clip"' ai_agent/scripts/upload_reference_videos.py` returns 1 (person 보존)
    - 위 verify 명령 exit 0, stdout = "OK"
  </acceptance_criteria>

  <done>
    SOURCES dict 의 fire = AI_HUB_FIRE_MP4, helmet = SELF_SHOT_HELMET_MP4 (분리 mp4).
    placeholder 모두 실제 경로로 교체됨. forklift / person 무변경.
    모듈 임포트 + SOURCES 분리 검증 + 두 mp4 파일 실재 검증 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 부분 재업로드 + --once-detect 1회 + detection_events SQL 검증</name>

  <read_first>
    - ai_agent/scripts/upload_reference_videos.py (Task 2 완료 후 갱신본)
    - ai_agent/detector_configs.py (Task 1 완료 후 갱신본)
    - .planning/phases/01-vision-demo-videos/01-CONTEXT.md (D-06)
    - .planning/ROADMAP.md (Phase 1 SC #1~#4)
  </read_first>

  <files>(코드 변경 없음 — 실행/검증 task. 산출물: Storage 객체 + cameras 행 + detection_events 행)</files>

  <action>
    Task 1, 2 완료 직후, 작업 디렉터리 `D:/2026_산업안전/Smart_Safety_Management` 에서 순서대로 실행.
    중간 단계 실패 시 즉시 중단하고 stderr 출력 보고.

    **Step 3.1: fire/helmet 부분 재업로드**

    ```bash
    python ai_agent/scripts/upload_reference_videos.py --only fire,helmet
    ```

    기대 출력:
    - `=== fire (camera_id=1) ===` 와 `=== helmet (camera_id=5) ===` 두 섹션
    - 각 섹션 마지막 `-> OK camera_id=1 ← https://...source_v2.mp4` (helmet 은 camera_id=5)
    - 마지막 `=== 요약 ===` 에 `성공 2 / 실패 0`
    - exit code 0
    - **forklift / person 섹션 미출력** (--only 필터링 동작)

    실패 시 (예: ffmpeg 부재, .env 누락, Storage 권한 거부) stderr 그대로 보고하고 중단.

    **Step 3.2: ai_agent 1회 검출 사이클**

    ```bash
    python -m ai_agent.main --once-detect
    ```

    기대: 5종 detector 가 각각 신규/기존 영상에서 프레임 추출 + YOLO 추론 + detection_events insert.
    fire/helmet 만 신규 영상이고 forklift/person 은 기존 그대로 → 5종 모두 무손상 동작.
    명령 종료 코드 0. (1~3분 소요 가능 — timeout 600000 ms 사용.)

    **Step 3.3: detection_events SQL 검증**

    Python + supabase-py 로 실행 (`.env` 의 `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` 사용):

    ```python
    import os
    from dotenv import load_dotenv
    from supabase import create_client
    load_dotenv()
    c = create_client(os.environ["SUPABASE_URL"], os.environ["SUPABASE_SERVICE_ROLE_KEY"])
    rows = c.table("detection_events") \
        .select("event_id, event_type_id, accuracy, camera_id, created_at") \
        .in_("camera_id", [1, 5]) \
        .order("created_at", desc=True) \
        .limit(20) \
        .execute().data
    fire_rows = [r for r in rows if r["camera_id"] == 1]
    helmet_rows = [r for r in rows if r["camera_id"] == 5]
    print(f"fire rows (cam=1): {len(fire_rows)}")
    print(f"helmet rows (cam=5): {len(helmet_rows)}")
    for r in (fire_rows + helmet_rows)[:10]:
        print(f"  cam={r['camera_id']} event_type={r['event_type_id']} accuracy={r['accuracy']} created={r['created_at']}")
    # SC #2 / #3 검증: 최근 1시간 내 row + accuracy >= 0.5
    from datetime import datetime, timezone, timedelta
    one_hour_ago = (datetime.now(timezone.utc) - timedelta(hours=1)).isoformat()
    recent_fire_pass = any(r["accuracy"] >= 0.5 and r["created_at"] >= one_hour_ago for r in fire_rows)
    recent_helmet_pass = any(r["accuracy"] >= 0.5 and r["created_at"] >= one_hour_ago for r in helmet_rows)
    assert recent_fire_pass, f"FAIL: fire (camera_id=1) 최근 1h 내 accuracy>=0.5 행 없음. rows={fire_rows[:3]}"
    assert recent_helmet_pass, f"FAIL: helmet (camera_id=5) 최근 1h 내 accuracy>=0.5 행 없음. rows={helmet_rows[:3]}"
    # SC #1 검증: cameras.live_url_detail 가 source_v2.mp4 가리킴
    cams = c.table("cameras").select("camera_id, live_url_detail").in_("camera_id", [1, 5]).execute().data
    for cam in cams:
        assert "source_v2.mp4" in (cam["live_url_detail"] or ""), \
            f"FAIL: camera_id={cam['camera_id']} live_url_detail={cam['live_url_detail']}"
    print("PASS: Phase 1 SC #1, #2, #3 모두 통과")
    ```

    `scripts/verify_phase1.py` 등 임시 파일로 저장 후 `python scripts/verify_phase1.py` 실행해도 OK,
    또는 `python -c "..."` 로 inline 실행 OK. 실행 후 임시 파일은 git 추가하지 않음 (또는 삭제).

    **재시도 정책**:
    - Step 3.2 의 `--once-detect` 가 fire/helmet 에서 단일 사이클로 검출 못한 경우 (예: 영상의 첫 프레임이
      검출 어려운 구간), `--once-detect` 1회 추가 재실행 (총 2회) 후 재검증. 그래도 실패 시 task FAIL.
    - conf_thres 0.5 에서 accuracy 가 약간 부족 (예: 0.45) 한 경우, Task 1 의 conf_thres 를 0.45 로
      낮추는 것이 아니라 영상 진행 시점 / 추가 사이클 / 다른 sample 프레임 으로 재검증. CONTEXT D-04
      는 "0.5 이상 (또는 그 이상으로 미세조정)" 을 명시 — 0.5 미만 후퇴 금지.

    **하지 말 것**:
    - 기존 `reference-videos/fire/source.mp4` 또는 `reference-videos/helmet/source.mp4` Storage 객체 삭제 금지 (롤백 보존)
    - detection_events 의 기존 행 DELETE 금지 (Phase 6 데모 빌드에서 별도 검토)
    - cameras 의 forklift (camera_id=4) / person (camera_id=3) live_url_detail 변경 금지
    - 검증 실패 시 SC 기준을 임의로 완화 금지 (accuracy >= 0.5 는 D-04 합의)
  </action>

  <verify>
    <automated>
      cd "D:/2026_산업안전/Smart_Safety_Management" &amp;&amp; \
      python -c "import os; from dotenv import load_dotenv; from supabase import create_client; \
        from datetime import datetime, timezone, timedelta; \
        load_dotenv(); \
        c = create_client(os.environ['SUPABASE_URL'], os.environ['SUPABASE_SERVICE_ROLE_KEY']); \
        cams = c.table('cameras').select('camera_id, live_url_detail').in_('camera_id', [1, 5]).execute().data; \
        assert all('source_v2.mp4' in (cam['live_url_detail'] or '') for cam in cams), f'cameras: {cams}'; \
        rows = c.table('detection_events').select('camera_id, accuracy, created_at').in_('camera_id', [1, 5]).order('created_at', desc=True).limit(20).execute().data; \
        one_hour_ago = (datetime.now(timezone.utc) - timedelta(hours=1)).isoformat(); \
        fr = [r for r in rows if r['camera_id'] == 1 and r['accuracy'] >= 0.5 and r['created_at'] >= one_hour_ago]; \
        hr = [r for r in rows if r['camera_id'] == 5 and r['accuracy'] >= 0.5 and r['created_at'] >= one_hour_ago]; \
        assert fr, f'fire fail: {[r for r in rows if r[\"camera_id\"]==1][:3]}'; \
        assert hr, f'helmet fail: {[r for r in rows if r[\"camera_id\"]==5][:3]}'; \
        print(f'PASS fire={len(fr)} helmet={len(hr)}')"
    </automated>
  </verify>

  <acceptance_criteria>
    - `python ai_agent/scripts/upload_reference_videos.py --only fire,helmet` exit 0, stdout 에 "성공 2 / 실패 0" 포함
    - 위 stdout 에 forklift / person 섹션 헤더 (`=== forklift`, `=== person`) 미출력
    - `python -m ai_agent.main --once-detect` exit 0
    - SQL: `cameras.live_url_detail` for camera_id=1 ends with `source_v2.mp4` (verify 명령 통과)
    - SQL: `cameras.live_url_detail` for camera_id=5 ends with `source_v2.mp4`
    - SQL: detection_events 에 camera_id=1 (fire) 행이 최근 1h 내 accuracy >= 0.5 로 ≥ 1건 존재
    - SQL: detection_events 에 camera_id=5 (helmet) 행이 최근 1h 내 accuracy >= 0.5 로 ≥ 1건 존재
    - 위 verify 명령 exit 0, stdout 에 "PASS fire=" 시작
  </acceptance_criteria>

  <done>
    Storage `reference-videos/fire/source_v2.mp4` 와 `reference-videos/helmet/source_v2.mp4` 신규 객체 존재.
    cameras (camera_id 1, 5) live_url_detail 가 source_v2.mp4 가리킴. detection_events 에 fire/helmet
    accuracy ≥ 0.5 행 신규 insert. Phase 1 SC #1, #2, #3, #4 (--only 부분 처리) 모두 통과.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 파일시스템 → Supabase Storage | `upload_reference_videos.py` 가 service role key 로 Storage 업로드 (기존 v0.5 ai_agent 패턴 그대로). 새 trust boundary 추가 X. |
| 로컬 ai_agent → detection_events DB | `--once-detect` 가 Edge Function `create_ai_event` 통해 insert. 기존 검증된 경로 (`62b51fd` 5종 detector E2E + `7458a9e` 이미지 업로드 E2E 에서 검증). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information disclosure) | `reference-videos/source_v2.mp4` (public bucket) | accept | 영상은 공개 시연용 데모 자산. PII / 영업비밀 없음 (작업자 일반 모습 + fire 이벤트). 기존 source.mp4 가 이미 public bucket 에 있던 동일 신뢰 등급 자산. |
| T-01-02 | T (Tampering) | `cameras.live_url_detail` UPDATE | mitigate | service role key 는 `.env` 만 존재 + `.gitignore` 처리됨 (이전 phase 에서 검증). update_camera_url() 는 camera_id eq 필터 — 광범위 UPDATE 불가능. |
| T-01-03 | D (Denial of Service) | Storage 50 MB / file 제한 | mitigate | 신규 영상 39 MB < 50 MB (orchestrator ffprobe 사전 확인). ensure_bucket() file_size_limit=52428800 그대로. |
| T-01-04 | E (Elevation of Privilege) | service role key 노출 | accept | 본 phase 는 새 secret / 새 권한 도입 X. 기존 `SUPABASE_SERVICE_ROLE_KEY` 사용 패턴 그대로. |

**전반**: Phase 1 = 데모 자산 교체 + config 영구 적용. 새 인증 / 새 PII / 새 외부 의존성 없음.
유일한 신규 attack surface = 39 MB mp4 1개 업로드 (기존 v0.5 source.mp4 와 동일 신뢰 등급). **NEW THREAT SEVERITY: NONE.**
</threat_model>

<verification>
## Phase 1 통합 검증

ROADMAP Phase 1 Success Criteria 와 1:1 매핑:

**SC #1**: `reference-videos` 버킷에 helmet/fire 신규 영상 1개씩 업로드 + cameras.live_url_detail 갱신
- 검증: Task 3 verify SQL `cameras.live_url_detail` for camera_id 1, 5 ends with `source_v2.mp4`
- 검증: Storage 에 `fire/source_v2.mp4`, `helmet/source_v2.mp4` 객체 존재 (Task 3 의 upload 명령 stdout `성공 2 / 실패 0`)

**SC #2**: helmet 신규 영상 + `target_classes=['head']` + `conf_thres ≥ 0.5` 로 head bbox ≥ 1프레임 검출 (DATA-01)
- 검증: Task 1 — `target_classes=['head']`, `conf_thres=0.5` 적용 (Task 1 verify python -c)
- 검증: Task 3 — detection_events camera_id=5 행 accuracy ≥ 0.5 ≥ 1건 (Task 3 verify SQL)

**SC #3**: fire 신규 영상 + `conf_thres ≥ 0.5` 로 fire bbox ≥ 1프레임 검출 (DATA-02)
- 검증: Task 1 — `conf_thres=0.5` 적용
- 검증: Task 3 — detection_events camera_id=1 행 accuracy ≥ 0.5 ≥ 1건

**SC #4**: SOURCES dict 갱신 + `--only fire,helmet` 부분 재업로드 (DATA-03), 다른 3종 무손상
- 검증: Task 2 — SOURCES['fire'/'helmet'] 신규 mp4 매핑 (Task 2 verify python -c)
- 검증: Task 3 Step 3.1 — 명령 exit 0 + stdout 에 forklift/person 섹션 미출력
- 검증: 다른 3종 (fall, forklift, person) 의 cameras.live_url_detail 무변경 (--only 필터링 동작)

## MODEL-03 흡수 (CONTEXT D-04)

Phase 2 의 MODEL-03 ("fire/helmet `conf_thres` 0.5+, helmet `target_classes` ['head'] 로 복원")
은 Task 1 에서 동시 적용 → Phase 2 시작 시 MODEL-03 자동 충족 상태.
Phase 2 PLAN 작성 시 MODEL-03 은 "이미 Phase 1 에서 적용됨" 으로 표기 가능.
</verification>

<success_criteria>
Phase 1 PLAN 01 완료 = 다음 모두 TRUE:

1. `ai_agent/detector_configs.py` 의 fire/helmet conf_thres ≥ 0.5, helmet target_classes = ['head'] (Task 1)
2. 임시조치 주석 (`데모용 하향`, `helmet 위주라 검출`) 제거 + 운영급 주석으로 교체 (Task 1)
3. `ai_agent/scripts/upload_reference_videos.py` SOURCES['fire'/'helmet'] 가 신규 detection mp4 매핑 + remote_path = `{event_key}/source_v2.mp4` (Task 2)
4. forklift / person SOURCES, conf_thres 변경 없음 (Task 1, 2)
5. Storage `reference-videos/fire/source_v2.mp4` + `reference-videos/helmet/source_v2.mp4` 객체 존재 (Task 3)
6. `cameras.live_url_detail` for camera_id 1, 5 가 신규 source_v2.mp4 URL 가리킴 (Task 3)
7. detection_events 에 camera_id=1 fire 행 accuracy ≥ 0.5 ≥ 1건 신규 (Task 3)
8. detection_events 에 camera_id=5 helmet 행 accuracy ≥ 0.5 ≥ 1건 신규 (Task 3)
9. `--only fire,helmet` 옵션이 forklift/person 건드리지 않음 (Task 3)
10. 모든 Task 의 verify automated 명령 exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-vision-demo-videos/01-01-SUMMARY.md` with:

- 변경된 파일 (2): detector_configs.py, upload_reference_videos.py — 정확한 변경 라인 / 변경 전후 값
- 신규 Storage 객체 (2): fire/source_v2.mp4, helmet/source_v2.mp4 + URL
- cameras 갱신 (2 행): camera_id=1, 5 의 live_url_detail before/after
- detection_events 결과 표: cam_id, event_type, accuracy, created_at (fire + helmet 각 ≥ 1행)
- MODEL-03 상태: "Phase 1 에서 흡수 완료, Phase 2 PLAN 작성 시 별도 task 불필요" 명시
- 발견 이슈 / 추후 검토 (예: detection_events 분포가 0.5~0.6 에 몰리면 conf_thres 미세 조정 필요 여부)
- 롤백 절차: cameras.live_url_detail 만 SQL UPDATE 로 source.mp4 (구) URL 로 환원
</output>
