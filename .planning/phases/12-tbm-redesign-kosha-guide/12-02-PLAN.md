---
phase: 12-tbm-redesign-kosha-guide
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - supabase/functions/notifications/index.ts
  - tests/smoke/tbm_start_v2.sh
  - tests/smoke/tbm_end_v2.sh
  - tests/smoke/tbm_missed_v2.sh
  - tests/smoke/ops_create.sh
  - tests/smoke/ops_update.sh
  - tests/smoke/ops_toggle.sh
autonomous: true
requirements: [TBM-04, TBM-05, TBM-08]
tags: [phase-12, tbm, edge-function, supabase, deno, fcm, ops-catalog, work-scope, hazards-snapshot, T-9-02, T-9-04, T-9-09, T-12-01, T-12-02, T-12-03, T-12-06, P-NEW-1, P-NEW-4, P-NEW-5]

user_setup:
  - service: supabase
    why: "supabase functions deploy notifications — Phase 9 의 4 case (tbm-start/checkin/end/missed) amend + 신규 3 case (ops-create/update/toggle) 활성화"
    env_vars:
      - name: SUPABASE_URL
        source: ".env"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env"
      - name: SUPABASE_ACCESS_TOKEN
        source: "supabase CLI login token"
      - name: SUPABASE_ANON_KEY
        source: ".env (curl smoke 용)"
    dashboard_config: []

must_haves:
  truths:
    - "Phase 9 의 4 case 모두 014 schema 에 맞춰 amend — tbm-start (work_scope + hazards/controls snapshot + FCM payload work_scope) / tbm-checkin (변경 0 — session_id 가 work_scope 정보 보유) / tbm-end (key_hazard_id + feedback_notes 옵션 인자 추가, TBM-05) / tbm-missed (변경 0 — work_scope 무관)"
    - "tbm-start payload 에 work_scope NOT NULL 추가 — 누락 시 400 'work_scope is required'"
    - "tbm-start 의 23505 catch shape 그대로 유지 — 응답 텍스트만 갱신 `'이미 오늘 \\\${work_scope} 세션이 존재합니다'` (P-NEW-1)"
    - "tbm-start 가 tbm_templates 의 hazards + controls 도 SELECT (Phase 9 의 checklist + title 외 추가) — session INSERT 시 hazards_snapshot = body.hazards ?? tmpl.hazards, controls_snapshot = body.controls ?? tmpl.controls (D-01 사용자 수정 반영 + T-12-05 snapshot)"
    - "tbm-start 가 is_active=false 인 OPS 차단 — `if (tmpl.is_active === false) return err('OPS 비활성화됨', 400)` (P-NEW-4 race mitigation)"
    - "tbm-start FCM data payload 에 work_scope 추가 — `data.work_scope` (P-NEW-5 worker 가 N 세션 구분)"
    - "tbm-end payload 에 옵션 key_hazard_id + feedback_notes 인자 추가 — UPDATE 시 같이 PATCH (TBM-05 회의록 7번 핵심 필드)"
    - "신규 case 'ops-create' — manager 가 새 OPS 추가. 필수 검증 (work_type + title + hazards.length>=1 + controls.length>=1, D-07). is_custom=true 강제. JSON schema 화이트리스트 (T-12-03)"
    - "신규 case 'ops-update' — manager 가 기존 OPS 수정. work_type UNIQUE 검증 + hazards/controls 필수 (D-07)"
    - "신규 case 'ops-toggle' — manager 가 OPS 활성/비활성 토글. payload: {template_id, is_active}. tbm_templates UPDATE is_active only. Option B hybrid (RESEARCH §Edge Function 권장)"
    - "ops-create/update/toggle 모두 manager 권한 가드 — payload 의 user_id 로 profiles 조회 + user_role IN ('manager','general_manager') 확인 (T-12-01 mirror)"
    - "D-09 회귀 가드 유지: 7 case 모두 본문에 `notifications.insert(` 등장 0회 (push-only, Phase 9 패턴)"
    - "Edge Function 배포: `supabase functions deploy notifications` 성공"
    - "21 smoke ALL PASS — tbm-start 3 (200 work_scope=산세 / 200 다른 work_scope=도금 / 409 같은 work_scope 중복) + tbm-end 3 (200 key_hazard_id 포함 / 200 미포함 / 404 leader 불일치) + tbm-missed 3 (200 / 200 leader-only / 400) + ops-create 3 (200 / 400 hazards 빈 / 400 controls 빈) + ops-update 3 (200 / 400 / 404 unknown template) + ops-toggle 3 (200 비활성화 / 200 재활성화 / 400 missing template_id)"
    - "Pitfall P-NEW-1 검증: 같은 group·date·work_scope 중복 → 409. 같은 group·date 다른 work_scope → 200 (UNIQUE 완화 확인)"
    - "Pitfall P-NEW-4 검증: is_active=false 인 OPS 로 tbm-start 호출 → 400"
    - "기존 case 무변경 검증 — watch-ack / watch-pair / camera-down / camera-recovered / tbm-checkin / tbm-missed 의 body 컬럼명 (work_scope 제외) git diff 최소 (FCM payload 에 work_scope 추가 외 변경 0)"
  artifacts:
    - path: "supabase/functions/notifications/index.ts"
      provides: "Phase 9 의 4 case amend + 신규 3 case (ops-create/ops-update/ops-toggle)"
      contains: 'case "ops-create"'
    - path: "tests/smoke/tbm_start_v2.sh"
      provides: "tbm-start 3 시나리오 (work_scope 분기 + UNIQUE 3-튜플 + is_active 가드)"
      contains: "work_scope"
    - path: "tests/smoke/tbm_end_v2.sh"
      provides: "tbm-end 3 시나리오 (key_hazard_id + feedback_notes 회의록 양식)"
      contains: "key_hazard_id"
    - path: "tests/smoke/tbm_missed_v2.sh"
      provides: "tbm-missed 3 시나리오 (work_scope 무관, Phase 9 변경 0 확인)"
      contains: "tbm-missed"
    - path: "tests/smoke/ops_create.sh"
      provides: "ops-create 3 시나리오 (정상 / hazards 빈 400 / controls 빈 400)"
      contains: "ops-create"
    - path: "tests/smoke/ops_update.sh"
      provides: "ops-update 3 시나리오 (정상 / 필드 누락 400 / unknown template 404)"
      contains: "ops-update"
    - path: "tests/smoke/ops_toggle.sh"
      provides: "ops-toggle 3 시나리오 (비활성화 200 / 재활성화 200 / 400)"
      contains: "ops-toggle"
  key_links:
    - from: "Edge Function tbm-start (amend)"
      to: "tbm_templates.hazards + controls + is_active"
      via: "supabase.from('tbm_templates').select('checklist, title, hazards, controls, is_active').eq('work_type', wt)"
      pattern: "hazards.*controls.*is_active"
    - from: "Edge Function tbm-start (amend)"
      to: "tbm_sessions.hazards_snapshot + controls_snapshot"
      via: "INSERT body.hazards ?? tmpl.hazards, body.controls ?? tmpl.controls (D-01 + T-12-05)"
      pattern: "hazards_snapshot"
    - from: "Edge Function ops-create / update"
      to: "tbm_templates (is_custom=true)"
      via: "supabase.from('tbm_templates').insert({is_custom: true, ...})"
      pattern: "is_custom: true"
    - from: "Edge Function ops-toggle"
      to: "tbm_templates.is_active UPDATE"
      via: "supabase.from('tbm_templates').update({is_active}).eq('template_id', id)"
      pattern: "is_active.*update"
    - from: "FCM data.work_scope"
      to: "Android MyFirebaseMessagingService tbm_alert 분기 (Plan 12-03)"
      via: "FCM data payload 의 신규 work_scope 키 (P-NEW-5)"
      pattern: "work_scope.*data"
---

<objective>
Phase 12 Wave 2 (parallel with Plan 12-03) — Edge Function `notifications/index.ts` amend +
신규 3 case 추가. Phase 9 의 4 case 모두 014 schema (work_scope + hazards/controls snapshot +
회의록 양식 필드 + tbm_templates 확장) 에 맞춰 갱신하고, 신규 ops-create / ops-update /
ops-toggle 3 case 로 관리자 OPS 카탈로그 UI (Plan 12-03) 가 호출할 endpoints 제공. deploy +
21 smoke (4 amend × 3 + 3 신규 × 3) 로 회귀 가드 검증.

Purpose: Plan 12-03 (Android UI) 가 호출하는 tbm-start (work_scope + hazards) + ops-create/
update/toggle (관리자 OPS 카탈로그) 의 server-side 계약 확정. Plan 12-01 의 cron 이 호출하는
tbm-missed 는 body 변경 0 (work_scope 무관, 그대로 동작). Pitfall P-NEW-1 (23505 shape 유지) +
P-NEW-4 (is_active race) + P-NEW-5 (FCM disambiguation) 모두 검증.

Output:
- supabase/functions/notifications/index.ts (4 amend + 3 신규 + deploy)
- tests/smoke/{tbm_start_v2,tbm_end_v2,tbm_missed_v2,ops_create,ops_update,ops_toggle}.sh (21 smoke)
- D-09 회귀 가드: public.notifications row delta = 0
- T-12-01·02·03·06 mitigation 검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-tbm-redesign-kosha-guide/12-CONTEXT.md
@.planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md
@.planning/phases/09-tbm-worker-guide/09-CONTEXT.md
@.planning/phases/09-tbm-worker-guide/09-02-PLAN.md
@.planning/phases/09-tbm-worker-guide/09-02-SUMMARY.md
@.planning/phases/12-tbm-redesign-kosha-guide/12-01-PLAN.md
@supabase/functions/notifications/index.ts
@supabase/functions/_shared/fcm.ts
@supabase/functions/_shared/supabase.ts
@supabase/functions/_shared/response.ts
@supabase/functions/_shared/cors.ts

<interfaces>
<!-- Plan 12-01 의 014 schema (이미 운영 DB 적용) — Edge Function 본문이 직접 SELECT/INSERT/UPDATE -->

tbm_templates (014 확장):
  template_id, work_type UNIQUE, title, description,
  hazards JSONB, controls JSONB, key_actions JSONB, checks JSONB,
  target_detector, is_active, is_custom, created_at

tbm_sessions (014 확장):
  session_id, group_id, session_date, work_scope NOT NULL,
  started_at, ended_at, expected_end_at, leader_user_id, work_type,
  location, notes, missed_alert_at,
  hazards_snapshot JSONB, controls_snapshot JSONB,
  key_hazard_id, feedback_notes, created_at
  UNIQUE (group_id, session_date, work_scope)  -- 3-튜플

tbm_checklists / tbm_participants — 013 그대로 (014 RECREATE 후 동일 shape)

<!-- 기존 _shared 헬퍼 (재사용, 변경 0) -->
_shared/fcm.ts:
  sendPushToUsers(supabase, userIds: string[], payload): Promise<{sent, failed, skipped}>
  -- Pitfall 9 dedup: [...new Set(userIds)] line 253

_shared/supabase.ts: createServiceClient()
_shared/response.ts: ok(body), err(message, status=400)

<!-- 기존 notifications/index.ts switch 구조 (Phase 7·8·9 검증) -->
serve(async (req) => {
  switch (action) {
    case "watch-ack": ...      // Phase 7, 변경 0
    case "watch-pair": ...     // Phase 7, 변경 0
    case "camera-down": ...    // Phase 8, 변경 0
    case "camera-recovered": ...  // Phase 8, 변경 0
    case "tbm-start": ...      // Phase 9, ★ amend (work_scope + hazards snapshot + FCM)
    case "tbm-checkin": ...    // Phase 9, 변경 0
    case "tbm-end": ...        // Phase 9, ★ amend (key_hazard_id + feedback_notes 옵션)
    case "tbm-missed": ...     // Phase 9, 변경 0 (work_scope 무관)
    case "ops-create": ...     // Phase 12 ★ 신규
    case "ops-update": ...     // Phase 12 ★ 신규
    case "ops-toggle": ...     // Phase 12 ★ 신규
    default: return err(`unknown action: ${action}`, 400);
  }
})

<!-- 신규 3 case 의 payload 계약 (Plan 12-03 의 SettingOpsCatalogRepository 가 사용) -->

ops-create request:
{ action: "ops-create", user_id: string,  // manager 권한 검증용
  work_type: string, title: string, description?: string,
  hazards: [{id, text}],                  // ≥1 (D-07)
  controls: [{id, text, hazard_id, level}],  // ≥1 (D-07)
  key_actions?: [{text}], checks?: string[],
  target_detector?: string,
  is_active?: boolean (default true) }
→ 200 { ok: true, template_id, work_type, is_custom: true }
→ 400 "manager only" / "work_type already exists" / "hazards/controls required"
→ 401 "user_id required" / 403 "manager only"

ops-update request:
{ action: "ops-update", user_id, template_id: number,
  title?, description?, hazards?, controls?, key_actions?, checks?, target_detector? }
→ 200 { ok: true, template_id }
→ 404 "template not found"
→ 400 "hazards/controls required (when provided, length>=1)"

ops-toggle request:
{ action: "ops-toggle", user_id, template_id: number, is_active: boolean }
→ 200 { ok: true, template_id, is_active }
→ 404 "template not found"
→ 400 "is_active required" / 403 "manager only"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifications/index.ts amend — 4 case (tbm-start/end/checkin/missed) + 3 신규 case (ops-create/update/toggle) + deploy</name>
  <read_first>
    - supabase/functions/notifications/index.ts (전체 — Phase 9 4 case 본문 amend 위치 확인)
    - supabase/functions/_shared/fcm.ts (sendPushToUsers + dedup line 253)
    - supabase/functions/_shared/response.ts (ok/err 헬퍼)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md §Edge Function line 134-180 (4 amend 본문 + 신규 case 권장)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md §Pitfalls P-NEW-1·4·5 (23505 shape / is_active race / FCM disambiguation)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-CONTEXT.md §D-07 (필수 필드) + §D-08 (manager 권한) + §D-01 (is_custom)
    - .planning/phases/09-tbm-worker-guide/09-02-PLAN.md Task 1 (Phase 9 4 case 본문 — 1:1 base)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-01-PLAN.md (014 schema interfaces — work_scope, hazards/controls/snapshot, is_active)
  </read_first>
  <action>
    1. **사전 가드 — 014 적용 확인**:
       ```bash
       supabase migration list --linked 2>/dev/null | grep 014
       ```
       기대: `014` row 등장. 없으면 즉시 중단 + Plan 12-01 재실행 안내.

    2. **`supabase/functions/notifications/index.ts` amend — tbm-start (D-01 OPS prefill + is_active 가드 + FCM work_scope)**:

       **변경 항목** (Phase 9 본문에서):
       ```typescript
       case "tbm-start": {
         const {
           leader_user_id, group_id, work_type, work_scope,           // ★ work_scope 신규 필수
           expected_end_at, location, notes,
           hazards, controls,                                          // ★ D-01 사용자 수정 옵션
         } = body;
         if (!leader_user_id || !group_id || !work_type || !expected_end_at) {
           return err("leader_user_id, group_id, work_type, expected_end_at are required", 400);
         }
         if (!work_scope) return err("work_scope is required", 400);   // ★ 신규
         if (typeof work_scope !== "string" || work_scope.length > 80) {
           return err("work_scope must be string <= 80 chars", 400);   // ★ T-12-02 DoS
         }

         // 1. work_type 검증 + 확장 컬럼 SELECT (★ hazards + controls + is_active 추가)
         const { data: tmpl, error: tErr } = await supabase
           .from("tbm_templates")
           .select("checklist:checks, title, hazards, controls, is_active")  // ★ checks 가 Phase 9 의 checklist 후신
           .eq("work_type", work_type).maybeSingle();
         if (tErr) return err(tErr.message, 500);
         if (!tmpl) return err(`unknown work_type: ${work_type}`, 400);
         if (tmpl.is_active === false) {                                // ★ P-NEW-4 race mitigation
           return err("OPS 비활성화됨 — 다른 work_type 선택", 400);
         }

         // 2. session insert (UNIQUE 3-튜플 23505 → 409, P-NEW-1 shape 유지)
         const today = new Date().toISOString().slice(0, 10);
         const { data: session, error: sErr } = await supabase
           .from("tbm_sessions").insert({
             group_id, session_date: today,
             work_scope,                                                // ★ 신규
             expected_end_at, leader_user_id, work_type,
             location: location ?? null, notes: notes ?? null,
             // ★ D-01 + T-12-05: 사용자 수정값 우선, 없으면 template snapshot
             hazards_snapshot:  hazards  ?? tmpl.hazards  ?? [],
             controls_snapshot: controls ?? tmpl.controls ?? [],
           }).select().maybeSingle();
         if (sErr) {
           if (sErr.code === "23505") {
             return err(`이미 오늘 ${work_scope} 세션이 존재합니다`, 409);  // ★ 응답 텍스트만 갱신
           }
           return err(sErr.message, 500);
         }
         if (!session) return err("session insert returned null", 500);

         // 3. checklists bulk insert (tmpl.checks 가 Phase 9 의 checklist 후신, 동일 shape)
         const checks = (tmpl.checklist as string[]) ?? [];   // alias 로 SELECT 됨
         const items = checks.map((text, idx) => ({
           session_id: session.session_id, item_idx: idx, item_text: text,
         }));
         if (items.length > 0) {
           const { error: cErr } = await supabase.from("tbm_checklists").insert(items);
           if (cErr) return err(cErr.message, 500);
         }

         // 4. group worker SELECT + push (Phase 9 패턴 그대로)
         const { data: workers, error: wErr } = await supabase
           .from("profiles").select("user_id")
           .eq("group_id", group_id)
           .in("user_role", ["worker", "general_manager"])
           .neq("user_id", leader_user_id);
         if (wErr) return err(wErr.message, 500);

         const workerIds = (workers ?? []).map((w: { user_id: string }) => w.user_id);
         const r = await sendPushToUsers(supabase, workerIds, {
           title: "TBM 세션 시작",
           body: `${tmpl.title} (${work_scope}) — ${workerIds.length}명 대상`,
           data: {
             type: "tbm_alert", action_in_app: "tbm-started",
             session_id: String(session.session_id),
             work_type,
             work_scope,                                                // ★ P-NEW-5
           },
         });
         return ok({ ok: true, session_id: session.session_id, work_scope,
                     checklist_count: items.length, notified_count: r.sent });
       }
       ```

       **NOTE**: `select("checklist:checks, ...")` 는 Supabase PostgREST 의 column alias 문법.
       Phase 9 의 코드가 `tmpl.checklist` 로 access 하던 것을 014 의 컬럼명 `checks` 와 호환되게
       유지. (alias 미사용 시 line 245 의 `tmpl.checklist as string[]` → `tmpl.checks as string[]`
       로 교체해도 됨 — executor 가 결정, 변경 line 수 적은 쪽 권장.)

    3. **`tbm-checkin` — 변경 0 확인**:
       Phase 9 본문 그대로. session_id 가 work_scope 정보 보유, profile.group_id ownership 검증
       (T-9-03) 동일, 23505 idempotent 동일. **git diff 0 line** (case 본문).

    4. **`tbm-end` amend — key_hazard_id + feedback_notes 옵션 인자 추가 (TBM-05)**:

       Phase 9 본문에서 변경:
       ```typescript
       case "tbm-end": {
         const { session_id, leader_user_id, key_hazard_id, feedback_notes } = body;  // ★ 신규 옵션
         if (!session_id || !leader_user_id) {
           return err("session_id, leader_user_id are required", 400);
         }

         const updateBody: Record<string, unknown> = {
           ended_at: new Date().toISOString(),
         };
         if (key_hazard_id !== undefined) updateBody.key_hazard_id = key_hazard_id;   // ★ TBM-05
         if (feedback_notes !== undefined) updateBody.feedback_notes = feedback_notes; // ★ TBM-05

         const { data, error } = await supabase
           .from("tbm_sessions")
           .update(updateBody)
           .eq("session_id", session_id)
           .eq("leader_user_id", leader_user_id)
           .is("ended_at", null)
           .select().maybeSingle();
         if (error) return err(error.message, 500);
         if (!data) return err("session not found or already ended or not led by user", 404);

         const { count } = await supabase
           .from("tbm_participants").select("*", { count: "exact", head: true })
           .eq("session_id", session_id);
         return ok({ ok: true, ended_at: data.ended_at, participant_count: count ?? 0 });
       }
       ```

    5. **`tbm-missed` — 변경 0 확인**:
       Phase 9 본문 그대로. work_scope 무관. body 변경 0. **git diff 0 line**.

    6. **신규 case `ops-create`** (manager 가 새 OPS 추가 — D-07 + T-12-01·02·03):
       ```typescript
       case "ops-create": {
         const {
           user_id, work_type, title, description,
           hazards, controls, key_actions, checks, target_detector,
           is_active,
         } = body;
         if (!user_id) return err("user_id required", 400);
         // ★ T-12-01 manager 권한 가드
         const { data: prof } = await supabase
           .from("profiles").select("user_role")
           .eq("user_id", user_id).maybeSingle();
         if (!prof) return err("user not found", 404);
         if (!["manager", "general_manager"].includes(prof.user_role)) {
           return err("manager only", 403);
         }

         // ★ D-07 필수 검증
         if (!work_type || typeof work_type !== "string" || work_type.length > 40) {
           return err("work_type required (<=40 chars)", 400);
         }
         if (!title || typeof title !== "string") return err("title required", 400);
         if (!Array.isArray(hazards) || hazards.length < 1) {
           return err("hazards must be array with >=1 items", 400);
         }
         if (!Array.isArray(controls) || controls.length < 1) {
           return err("controls must be array with >=1 items", 400);
         }

         // ★ T-12-03 JSONB schema 화이트리스트 (hazards: {id, text} 만, controls: {id, text, hazard_id, level} 만)
         const cleanHazards = hazards.map((h: any) => ({
           id: String(h.id ?? ""), text: String(h.text ?? ""),
         })).filter((h) => h.id && h.text);
         const cleanControls = controls.map((c: any) => ({
           id: String(c.id ?? ""), text: String(c.text ?? ""),
           hazard_id: String(c.hazard_id ?? ""),
           level: ["제거","대체","통제"].includes(c.level) ? c.level : "통제",
         })).filter((c) => c.id && c.text);
         if (cleanHazards.length < 1 || cleanControls.length < 1) {
           return err("hazards/controls must have valid {id, text} entries", 400);
         }

         const { data, error } = await supabase
           .from("tbm_templates").insert({
             work_type, title,
             description: description ?? null,
             hazards: cleanHazards, controls: cleanControls,
             key_actions: Array.isArray(key_actions) ? key_actions : [],
             checks: Array.isArray(checks) ? checks : [],
             target_detector: target_detector ?? null,
             is_active: is_active ?? true,
             is_custom: true,           // ★ D-01 강제
           }).select().maybeSingle();
         if (error) {
           if (error.code === "23505") return err("work_type already exists", 409);
           return err(error.message, 500);
         }
         return ok({ ok: true, template_id: data!.template_id, work_type, is_custom: true });
       }
       ```

    7. **신규 case `ops-update`** (manager 가 기존 OPS 수정):
       ```typescript
       case "ops-update": {
         const {
           user_id, template_id, title, description,
           hazards, controls, key_actions, checks, target_detector,
         } = body;
         if (!user_id) return err("user_id required", 400);
         if (!template_id) return err("template_id required", 400);

         // manager 가드
         const { data: prof } = await supabase
           .from("profiles").select("user_role").eq("user_id", user_id).maybeSingle();
         if (!prof || !["manager","general_manager"].includes(prof.user_role)) {
           return err("manager only", 403);
         }

         // 존재 확인
         const { data: existing } = await supabase
           .from("tbm_templates").select("template_id")
           .eq("template_id", template_id).maybeSingle();
         if (!existing) return err("template not found", 404);

         const updateBody: Record<string, unknown> = {};
         if (title !== undefined) updateBody.title = title;
         if (description !== undefined) updateBody.description = description;
         if (hazards !== undefined) {
           if (!Array.isArray(hazards) || hazards.length < 1) {
             return err("hazards must be array with >=1 items", 400);
           }
           // T-12-03 화이트리스트
           updateBody.hazards = hazards.map((h: any) => ({
             id: String(h.id ?? ""), text: String(h.text ?? ""),
           })).filter((h: any) => h.id && h.text);
         }
         if (controls !== undefined) {
           if (!Array.isArray(controls) || controls.length < 1) {
             return err("controls must be array with >=1 items", 400);
           }
           updateBody.controls = controls.map((c: any) => ({
             id: String(c.id ?? ""), text: String(c.text ?? ""),
             hazard_id: String(c.hazard_id ?? ""),
             level: ["제거","대체","통제"].includes(c.level) ? c.level : "통제",
           })).filter((c: any) => c.id && c.text);
         }
         if (key_actions !== undefined) updateBody.key_actions = key_actions;
         if (checks !== undefined) updateBody.checks = checks;
         if (target_detector !== undefined) updateBody.target_detector = target_detector;

         const { error } = await supabase
           .from("tbm_templates").update(updateBody).eq("template_id", template_id);
         if (error) return err(error.message, 500);
         return ok({ ok: true, template_id });
       }
       ```

    8. **신규 case `ops-toggle`** (manager 가 is_active 토글 — Option B Hybrid 의 Option A 측):
       ```typescript
       case "ops-toggle": {
         const { user_id, template_id, is_active } = body;
         if (!user_id) return err("user_id required", 400);
         if (!template_id) return err("template_id required", 400);
         if (typeof is_active !== "boolean") return err("is_active must be boolean", 400);

         const { data: prof } = await supabase
           .from("profiles").select("user_role").eq("user_id", user_id).maybeSingle();
         if (!prof || !["manager","general_manager"].includes(prof.user_role)) {
           return err("manager only", 403);
         }

         const { data, error } = await supabase
           .from("tbm_templates")
           .update({ is_active })
           .eq("template_id", template_id)
           .select("template_id, is_active").maybeSingle();
         if (error) return err(error.message, 500);
         if (!data) return err("template not found", 404);
         return ok({ ok: true, template_id: data.template_id, is_active: data.is_active });
       }
       ```

    9. **D-09 회귀 가드 (Phase 9 패턴 유지)**: 7 case (4 amend + 3 신규) 모두 본문에
       `notifications.insert(` 등장 0회. grep 검증.

    10. **deploy**: `supabase functions deploy notifications` 실행. bundle size 기록 (Phase 9
        ~70-80kB 대비 +5-10kB 예상 — 3 신규 case 추가). esm.sh 522 transient 시 5초 후 재시도.

    11. **회귀 가드 — 기존 case 무변경 (Phase 7/8 case + Phase 9 의 tbm-checkin/missed)**:
        `case "watch-ack"`, `case "watch-pair"`, `case "camera-down"`, `case "camera-recovered"`,
        `case "tbm-checkin"`, `case "tbm-missed"` 6 case 의 body line 변경 0. grep count 그대로.
  </action>
  <verify>
    <automated>grep -c 'case "tbm-start"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-checkin"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-end"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-missed"' supabase/functions/notifications/index.ts && grep -c 'case "ops-create"' supabase/functions/notifications/index.ts && grep -c 'case "ops-update"' supabase/functions/notifications/index.ts && grep -c 'case "ops-toggle"' supabase/functions/notifications/index.ts && grep -c 'work_scope' supabase/functions/notifications/index.ts && grep -c 'hazards_snapshot' supabase/functions/notifications/index.ts && grep -c 'controls_snapshot' supabase/functions/notifications/index.ts && grep -c 'is_active === false' supabase/functions/notifications/index.ts && grep -c 'is_custom: true' supabase/functions/notifications/index.ts && grep -c 'manager only' supabase/functions/notifications/index.ts && [ "$(grep -c 'notifications\.insert' supabase/functions/notifications/index.ts)" = "0" ] && grep -c 'case "watch-ack"' supabase/functions/notifications/index.ts && grep -c 'case "camera-down"' supabase/functions/notifications/index.ts && echo OK</automated>
  </verify>
  <done>
    - notifications/index.ts 에 4 amend (tbm-start work_scope + hazards snapshot + is_active 가드 + FCM data.work_scope / tbm-end key_hazard_id + feedback_notes) + 3 신규 case (ops-create/update/toggle)
    - P-NEW-1 검증: tbm-start 의 23505 catch + 응답 텍스트 `이미 오늘 \${work_scope} 세션` 형식
    - P-NEW-4 검증: tbm-start 의 `is_active === false` 가드 (400 "OPS 비활성화됨")
    - P-NEW-5 검증: tbm-start FCM data.work_scope 추가
    - T-12-01 검증: ops-create/update/toggle 모두 manager 권한 가드 (`manager only` 응답 텍스트 3회)
    - T-12-02 검증: work_scope length cap 80
    - T-12-03 검증: ops-create/update 의 hazards/controls JSON schema 화이트리스트 (id/text/hazard_id/level 만 통과)
    - D-09 회귀: `notifications.insert` 0건
    - 기존 case 무변경 (watch-* / camera-* / tbm-checkin / tbm-missed) — body git diff 0
    - `supabase functions deploy notifications` 성공 + bundle size SUMMARY 기록
  </done>
</task>

<task type="auto">
  <name>Task 2: 21 smoke (4 case amend × 3 + 3 신규 case × 3) + D-09 회귀 가드 검증</name>
  <read_first>
    - supabase/functions/notifications/index.ts (Task 1 산출물)
    - .planning/phases/09-tbm-worker-guide/09-02-PLAN.md Task 2 (12 smoke 패턴 — 1:1 base)
    - .planning/phases/09-tbm-worker-guide/09-02-SUMMARY.md (Phase 9 smoke 결과)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md §Edge Function line 172-180 (신규 smoke 시나리오)
    - .env (SUPABASE_URL + SUPABASE_ANON_KEY)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-01-PLAN.md (014 schema interfaces — 3 OPS 시드 work_type 확인)
  </read_first>
  <action>
    1. **사전 가드 — 014 시드 확인 + worker 시드 보존 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_templates?select=work_type,is_active" \
         -H "apikey: $SUPABASE_ANON_KEY"
       ```
       기대: 3 rows (forklift/chemical/hot_work, is_active=true).
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/profiles?group_id=eq.1&user_role=eq.worker&select=user_id" \
         -H "apikey: $SUPABASE_ANON_KEY"
       ```
       기대: 3 rows (testuser_w1·w2·w3, Plan 12-01 의 Phase 9 산출물 보존 확인).

    2. **`tests/smoke/tbm_start_v2.sh` (3 시나리오)**:

       **시나리오 1 — 정상 200 (work_scope='산세')**:
       ```bash
       NOW_PLUS_15=$(date -d '+15 minutes' --iso-8601=seconds)
       curl -s -w "\n%{http_code}\n" -X POST "$SUPABASE_URL/functions/v1/notifications" \
         -H "Authorization: Bearer $SUPABASE_ANON_KEY" -H "apikey: $SUPABASE_ANON_KEY" \
         -H "Content-Type: application/json" \
         -d "{\"action\":\"tbm-start\",\"leader_user_id\":\"testuser1\",\"group_id\":1,\"work_type\":\"chemical\",\"work_scope\":\"산세\",\"expected_end_at\":\"$NOW_PLUS_15\"}"
       ```
       기대: HTTP 200, body `{"ok":true,"session_id":<N>,"work_scope":"산세","checklist_count":10,...}`.
       → SESSION_ID_1 변수 보관.

       **시나리오 2 — 200 다른 work_scope='도금조' (UNIQUE 완화 확인, P-NEW-1)**:
       같은 group/date/leader, work_scope 만 다름.
       기대: HTTP 200, 새 session_id 발급 (3-튜플 UNIQUE 통과).

       **시나리오 3 — 409 같은 work_scope='산세' 중복 (P-NEW-1 23505 shape 유지)**:
       시나리오 1 과 동일 페이로드 재호출.
       기대: HTTP 409, body 에 "이미 오늘 산세 세션" 포함.

       **(옵션) 시나리오 4 — 400 is_active=false (P-NEW-4)**:
       사전: SQL 으로 tbm_templates SET is_active=false WHERE work_type='chemical', 호출 후 다시 true 로 복원.
       기대: HTTP 400, body "OPS 비활성화됨". 옵션 (smoke 시 cleanup 부담 — 권장하지만 필수는 아님).

    3. **`tests/smoke/tbm_end_v2.sh` (3 시나리오)**:

       **시나리오 1 — 200 + key_hazard_id + feedback_notes 모두 포함**:
       위 SESSION_ID_2 (또는 새 session 생성):
       ```bash
       curl -s -w "\n%{http_code}\n" -X POST "$SUPABASE_URL/functions/v1/notifications" \
         -H "..." -d "{\"action\":\"tbm-end\",\"session_id\":$SID,\"leader_user_id\":\"testuser1\",\"key_hazard_id\":\"h1\",\"feedback_notes\":\"환류: 작업 중 보호구 일부 누락 확인\"}"
       ```
       기대: HTTP 200, body 의 ended_at 채움. 추가 PostgREST GET tbm_sessions?session_id=$SID 로 key_hazard_id='h1' + feedback_notes 채움 확인 (TBM-05).

       **시나리오 2 — 200 + key_hazard_id 미포함 (회의록 미작성 OK)**:
       기대: HTTP 200, ended_at 채움. key_hazard_id NULL 유지.

       **시나리오 3 — 404 leader 불일치**:
       기대: HTTP 404 (Phase 9 패턴 동일).

    4. **`tests/smoke/tbm_missed_v2.sh` (3 시나리오)**:
       Phase 9 의 tbm_missed.sh 와 본문 동일 — work_scope 무관 확인이 핵심.

       **시나리오 1 — 200 (missed worker + leader 모두 push)**:
       prep: 새 session 또는 SQL 으로 expected_end_at-1hour UPDATE + ended_at=NULL UPDATE.
       기대: HTTP 200, body `{"ok":true,"missed_count":<N>,"notified_count":<N+1>}` (leader 포함).

       **시나리오 2 — 200 leader-only (모든 worker checkin 후)**:
       기대: HTTP 200, missed_count=0, notified_count=1.

       **시나리오 3 — 400 payload 누락**:
       기대: HTTP 400.

    5. **`tests/smoke/ops_create.sh` (3 시나리오)**:

       **시나리오 1 — 200 (정상 신규 OPS)**:
       ```bash
       curl -s -w "\n%{http_code}\n" -X POST "$SUPABASE_URL/functions/v1/notifications" \
         -H "..." -d '{
           "action":"ops-create","user_id":"testuser1",
           "work_type":"crane_test","title":"크레인 작업 (테스트)",
           "hazards":[{"id":"h1","text":"낙하"},{"id":"h2","text":"흔들림"}],
           "controls":[{"id":"c1","text":"줄걸이 점검","hazard_id":"h1","level":"통제"}]
         }'
       ```
       기대: HTTP 200, body `{"ok":true,"template_id":<N>,"work_type":"crane_test","is_custom":true}`.

       **시나리오 2 — 400 hazards 빈 (D-07)**:
       기대: HTTP 400, body "hazards must be array with >=1 items".

       **시나리오 3 — 400 controls 빈 (D-07)**:
       기대: HTTP 400, body "controls must be array with >=1 items".

       **cleanup**: 시나리오 1 의 'crane_test' row 는 시연 후 SQL `DELETE FROM tbm_templates WHERE work_type='crane_test'`.

    6. **`tests/smoke/ops_update.sh` (3 시나리오)**:

       **시나리오 1 — 200 정상 (forklift 의 title 만 수정)**:
       template_id 는 시드된 forklift 의 PK (PostgREST 로 사전 조회).
       기대: HTTP 200.

       **시나리오 2 — 400 hazards.length=0 (제공했지만 빈 array)**:
       기대: HTTP 400.

       **시나리오 3 — 404 unknown template_id**:
       기대: HTTP 404.

    7. **`tests/smoke/ops_toggle.sh` (3 시나리오)**:

       **시나리오 1 — 200 비활성화 (forklift 의 is_active=false)**:
       기대: HTTP 200, body `{"ok":true,"template_id":<N>,"is_active":false}`.

       **시나리오 2 — 200 재활성화 (forklift is_active=true)**:
       기대: HTTP 200, is_active=true.

       **시나리오 3 — 400 payload 누락 (template_id 또는 is_active)**:
       기대: HTTP 400.

       **manager 가드 시나리오 (옵션, 4번째)**:
       worker (testuser_w1) 가 호출 → HTTP 403 "manager only" (T-12-01 검증).

    8. **D-09 회귀 가드 (Phase 9 동일)**: 21 smoke 실행 전후 `public.notifications` row 수 변화 0:
       ```bash
       BEFORE=$(curl -s "$SUPABASE_URL/rest/v1/notifications?select=count" -H "Prefer: count=exact" ...)
       # ... 21 smoke ...
       AFTER=$(curl -s ...)
       echo "delta=$((AFTER - BEFORE))"
       ```
       기대: delta=0 (push-only).

    9. **cleanup (21 smoke 종료 후)**:
       ```sql
       DELETE FROM tbm_sessions WHERE leader_user_id='testuser1' AND session_date=CURRENT_DATE;
       DELETE FROM tbm_templates WHERE work_type='crane_test';
       -- forklift 의 title 변경은 시연 영향 없음 (또는 SQL 으로 원복)
       ```
       단 demo 시연 흐름에서 데이터 잔존 가치 있으면 cleanup 생략 (SUMMARY 에 명시).

    10. **smoke 결과 SUMMARY 표** — 각 smoke 의 status / body / 검증 결과를 표로 정리 (Phase 9 패턴).
  </action>
  <verify>
    <automated>test -f tests/smoke/tbm_start_v2.sh && test -f tests/smoke/tbm_end_v2.sh && test -f tests/smoke/tbm_missed_v2.sh && test -f tests/smoke/ops_create.sh && test -f tests/smoke/ops_update.sh && test -f tests/smoke/ops_toggle.sh && grep -c 'work_scope' tests/smoke/tbm_start_v2.sh && grep -c 'key_hazard_id' tests/smoke/tbm_end_v2.sh && grep -c 'ops-create' tests/smoke/ops_create.sh && grep -c 'ops-update' tests/smoke/ops_update.sh && grep -c 'ops-toggle' tests/smoke/ops_toggle.sh && echo OK</automated>
  </verify>
  <done>
    - 6 smoke shell scripts 작성 (tbm_start_v2.sh / tbm_end_v2.sh / tbm_missed_v2.sh / ops_create.sh / ops_update.sh / ops_toggle.sh)
    - 21 smoke 시나리오 ALL PASS (4 amend × 3 + 3 신규 × 3)
    - P-NEW-1 검증: tbm-start 시나리오 2 (다른 work_scope 200) + 시나리오 3 (같은 work_scope 409 + 응답 텍스트 "이미 오늘 산세 세션")
    - T-12-05 snapshot 검증: tbm-start 200 후 PostgREST GET tbm_sessions 의 hazards_snapshot/controls_snapshot 채움 확인
    - TBM-05 검증: tbm-end 시나리오 1 의 key_hazard_id + feedback_notes 가 tbm_sessions row 에 PATCH 됨
    - T-12-01 검증: ops-toggle worker 호출 시 HTTP 403 "manager only"
    - D-07 검증: ops-create hazards/controls 빈 → 400
    - D-09 회귀: `public.notifications` row delta = 0 (21 smoke 전후)
    - SUMMARY 에 21 smoke status 표 + delta 모두 기록
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon)→Edge Function | 7 case (4 TBM + 3 OPS) — anon key 인증, 검증은 함수 본문 책임 |
| Edge Function→PostgREST (service_role) | UNIQUE 충돌 catch + manager 권한 가드 + tbm_templates UPDATE |
| Edge Function→FCM HTTP v1 | sendPushToUsers + work_scope payload (P-NEW-5) |
| pg_cron→Edge Function (tbm-missed) | Vault sr_key Bearer — Phase 9 패턴 그대로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | T | tbm_sessions UNIQUE bypass | mitigate | DB-level UNIQUE 트리거가 3-튜플로 변경 (Plan 12-01). Edge Function 의 23505 catch shape 동일 (P-NEW-1). smoke tbm_start_v2 시나리오 2·3 검증. |
| T-9-03 | E | 다른 group worker 가 tbm-checkin spoof | mitigate | Phase 9 의 `profile.group_id !== session.group_id → 403` 변경 0. body git diff 0. |
| T-9-04 | E | leader 가 아닌 user 가 tbm-end | mitigate | Phase 9 의 `.eq("leader_user_id", leader_user_id) → 404` 변경 0. smoke tbm_end_v2 시나리오 3 검증. |
| T-9-09 | I | leader push 중복 (자기 자신에게 미참여 알림 2건) | mitigate | sendPushToUsers Set dedup (fcm.ts:253) 그대로. Phase 9 패턴. |
| **T-12-01** | E (Elevation of Privilege) | worker 가 ops-create/update/toggle 호출 시도 | mitigate | 3 신규 case 모두 본문 첫 줄 `profile.user_role != manager → 403`. smoke ops_toggle 의 manager 가드 시나리오 검증. **추가 클라이언트 측 가드** (Plan 12-03 의 SettingOpsCatalogActivity onCreate 권한 가드) 가 deep-link 우회 차단. |
| **T-12-02** | T | work_scope SQL injection / DoS | mitigate | (a) Postgrest parameterized query 강제. (b) length cap 80자 (Edge Function 검증). (c) 014 schema 의 VARCHAR(80) DB-level enforcement. |
| **T-12-03** | T | JSONB nested injection (hazards/controls 에 임의 키 주입) | mitigate | ops-create/update 의 화이트리스트 — `hazards.map(h => ({id: String(h.id), text: String(h.text)}))` 만 통과. 추가 키 (admin: true 등) 무시. |
| **T-12-04** | T | is_active 토글 race | accept v1.0 | last-writer-wins. ops-toggle 의 UPDATE 단일 statement, race 시 마지막 write 승. v1.2 optimistic locking. |
| **T-12-05** | I | OPS 비활성 시 진행중 세션 hazards 손실 | mitigate | tbm-start 가 hazards_snapshot/controls_snapshot 으로 INSERT (Plan 12-01 schema + 본 plan amend). 토글이 진행중 세션 영향 0. |
| **T-12-06** | T | tbm_templates anon UPDATE 남용 | mitigate (partial) | Edge Function 의 ops-toggle 만 manager 권한 검증. Plan 12-01 의 RLS UPDATE 정책 `USING(true)` 은 임시 v1.0 (Plan 12-03 의 클라이언트가 is_active 컬럼만 PATCH 하도록 코드 검증). v1.2 RLS narrowing 권장. |
</threat_model>

<verification>
- notifications/index.ts 에 4 amend (tbm-start + tbm-end) + 3 신규 case (ops-create/update/toggle) 추가
- 기존 6 case (watch-* + camera-* + tbm-checkin + tbm-missed) 본문 무변경
- `supabase functions deploy notifications` 성공 (bundle size ~80-90kB 예상)
- 21 smoke ALL PASS (status code + body 검증)
- D-09 회귀: `public.notifications` row delta = 0
- T-12-01·02·03 mitigation 모두 smoke 로 검증 (manager 가드 / length cap / JSON 화이트리스트)
- ai_agent/tests/ 31/31 PASS (Python 코드 변경 0)
- j2208a/tests/ 43/43 PASS (워치 코드 변경 0)
</verification>

<success_criteria>
- TBM-04 충족 (Edge Function 측) — tbm-start 의 work_scope NOT NULL + 23505 3-튜플 catch + 응답 텍스트 갱신
- TBM-05 충족 — tbm-start 의 hazards_snapshot/controls_snapshot INSERT (D-01 + snapshot) + tbm-end 의 key_hazard_id/feedback_notes PATCH
- TBM-08 충족 (Edge Function 측) — ops-create/update/toggle 3 endpoints + manager 권한 가드 + D-07 필수 검증 + is_custom=true 강제
- Plan 12-03 (Android UI) 가 7 case (tbm-start v2 + tbm-checkin + tbm-end v2 + ops-create/update/toggle) 호출 가능
- T-12-01·02·03·05·06 mitigation 충족
- D-09 알림 전이 원칙 일관 (Phase 9 패턴 유지)
</success_criteria>

<deviations>
<!-- Executor 작성 칸. -->
</deviations>

<rollback_plan>
**Rollback 시 (deploy 후 critical 결함)**:

1. **Edge Function 측**: `git revert <Plan 12-02 commit>` + `supabase functions deploy notifications` 재실행 (Phase 9 case 본문 복원).
2. **smoke 측**: smoke shell scripts 는 revert 영향 없음 (테스트 실패만 발생).
3. **DB 측**: 014 schema 는 그대로 (Plan 12-01 rollback 별도 path).
4. **Android 측**: Plan 12-03 의 변경은 Plan 12-02 와 독립 — 단 tbm-start v2 호출 시 work_scope 누락 → 400, 일시적 사용자 영향.

**Rollback 신호**: 21 smoke 중 50%+ 실패 또는 운영 중 7 case 의 critical query 동작 불가.
</rollback_plan>

<output>
After completion, create `.planning/phases/12-tbm-redesign-kosha-guide/12-02-SUMMARY.md` documenting:
- 4 amend case (tbm-start v2 / tbm-end v2) + 3 신규 case (ops-create/update/toggle) 의 line ranges + git diff stat
- `supabase functions deploy notifications` 출력 + bundle size (~85kB 예상)
- 21 smoke 결과 표 (case / scenario / status / body 발췌 / 검증 결과)
- P-NEW-1·4·5 + T-12-01·02·03·05·06 검증 결과
- D-09 회귀 가드 (notifications row delta = 0)
- 기존 6 case (watch-* / camera-* / tbm-checkin / tbm-missed) 무변경 확인 (git diff 0 line)
- ai_agent + j2208a 무회귀 확인 (31/31 + 43/43)
- Deviations (Rule 1/2/3 별 분류)
</output>
</content>
</invoke>