---
phase: 12-tbm-redesign-kosha-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/014_tbm_v2_schema.sql
  - tests/sql/test_014_tbm_v2_isolation.sql
autonomous: true
requirements: [TBM-04, TBM-05, TBM-07]
tags: [phase-12, tbm, supabase, migration, schema-recreate, blocking, pg-cron, vault, storage, rls, realtime-publication, ops-catalog, kosha-guide, T-9-02, T-12-02, T-12-04, T-12-05, T-12-06]

user_setup:
  - service: supabase
    why: "014 마이그레이션 운영 DB 적용 ([BLOCKING] supabase db push --linked --yes) — DROP+RECREATE destructive (D-04). Phase 9 의 013 schema 4 테이블 + 5 generic templates + Phase 9 시드 세션 row 모두 제거 후 신규 schema (work_scope + hazards_snapshot 등) 로 RECREATE. Vault sr_key/edge_function_base_url 은 Phase 8 02 에서 시드됨 — push 자체는 sr_key 부재여도 OK (graceful skip)."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token (Phase 4·7·8·9 동일 작성됨)"
      - name: SUPABASE_DB_PASSWORD
        source: ".env (Phase 4 010 에서 작성됨)"
    dashboard_config: []

must_haves:
  truths:
    - "014_tbm_v2_schema.sql 가 013 의 4 테이블 (tbm_sessions / tbm_templates / tbm_checklists / tbm_participants) 을 DROP + RECREATE (D-04 destructive)"
    - "tbm_sessions 에 work_scope VARCHAR(80) NOT NULL 컬럼 추가 (D-03 — TBM ↔ AI work_scope 기록만, v1.2 detector 변화 0)"
    - "tbm_sessions UNIQUE 제약 — `UNIQUE (group_id, session_date, work_scope)` (D-09 — 같은 group·date 의 다른 work_scope 다중 세션 허용)"
    - "tbm_sessions 에 hazards_snapshot JSONB + controls_snapshot JSONB + key_hazard_id VARCHAR(20) + feedback_notes TEXT 신규 (TBM-05 회의록 양식 핵심 필드 + snapshot 패턴 T-12-05 mitigation)"
    - "tbm_templates 확장 — hazards JSONB + controls JSONB + key_actions JSONB + checks JSONB (Phase 9 checklist 후신) + target_detector VARCHAR(20) + is_active BOOLEAN + is_custom BOOLEAN (D-01 + D-02 + D-03)"
    - "tbm_participants UNIQUE (session_id, user_id) 유지 (D-09 — 같은 세션 중복 참여만 차단)"
    - "RLS 4 테이블 모두 ENABLE + SELECT USING (true) v1.0 PoC 패턴 + WRITE 정책 미등록 = service_role 만 (Phase 9 패턴 미러)"
    - "tbm_templates 추가 정책: anon UPDATE (is_active 토글만) 허용 — `FOR UPDATE TO anon, authenticated USING (true) WITH CHECK (true)` (Option B hybrid — Plan 02 의 ops-create/update 는 Edge Function 경유)"
    - "ALTER PUBLICATION supabase_realtime ADD TABLE 4 (DROP TABLE 의 side effect 로 publication 자동 제거 → RECREATE 후 재등록 필수)"
    - "Storage 버킷 'tbm-signatures' 는 Phase 9 그대로 유지 (DROP TABLE 영향 없음) — orphan signature PNG 잔존 acceptable (cheap, RESEARCH Open Q6 권장)"
    - "도금 도메인 OPS 3종 시드 INSERT — work_type IN ('forklift','chemical','hot_work'), hazards/controls/key_actions/checks JSONB 모두 채움, target_detector 채움, is_active=true, is_custom=false (TBM-07)"
    - "pg_cron 'tbm_missed_attendance_minute' 1분 주기 재등록 — tbm_missed_attendance_check() body 변경 0 (work_scope 무관, expected_end_at + 30min 임계 그대로)"
    - "tbm_missed_attendance_check() SECURITY DEFINER + SET search_path 잠금 + Vault sr_key/base_url NULL 가드 (Phase 9 패턴 1:1 미러)"
    - "[BLOCKING] supabase db push --linked --yes 운영 DB 적용 성공 — supabase migration list 에 014 등장"
    - "tests/sql/test_014_tbm_v2_isolation.sql 8 assertions — (1) 4 ENABLE ROW LEVEL SECURITY, (2) UNIQUE (group_id, session_date, work_scope) 등록, (3) 3 OPS 시드 (forklift/chemical/hot_work) 확인, (4) tbm_templates 의 hazards/controls/key_actions/checks 컬럼 존재, (5) cron.job 등록 확인, (6) tbm_missed_attendance_check 의 prosecdef=t, (7) tbm-signatures bucket 존재 (Phase 9 보존), (8) Realtime publication 4 테이블 등록"
    - "service_role JWT hardcode 0건 (T-9-07 회귀 가드, 코멘트 제외 후 `eyJ` count == 0)"
  artifacts:
    - path: "supabase/migrations/014_tbm_v2_schema.sql"
      provides: "DROP+RECREATE 4 테이블 + work_scope/hazards/controls 신규 컬럼 + 3 도금 OPS 시드 + RLS + Realtime publication + cron 재등록"
      contains: "DROP TABLE IF EXISTS public.tbm_sessions"
    - path: "tests/sql/test_014_tbm_v2_isolation.sql"
      provides: "Phase 12 schema 회귀 가드 (8 assertions)"
      contains: "SET LOCAL ROLE anon"
  key_links:
    - from: "tbm_sessions.hazards_snapshot"
      to: "tbm_templates.hazards (snapshot at start)"
      via: "Edge Function tbm-start 본문 (Plan 12-02): hazards_snapshot = body.hazards ?? tmpl.hazards"
      pattern: "hazards_snapshot"
    - from: "pg_cron 'tbm_missed_attendance_minute'"
      to: "public.tbm_missed_attendance_check() 함수"
      via: "cron.schedule('* * * * *', $$SELECT public.tbm_missed_attendance_check();$$)"
      pattern: "cron.schedule.*tbm_missed_attendance_minute"
    - from: "tbm_missed_attendance_check() 함수"
      to: "notifications/index.ts case 'tbm-missed' (Plan 12-02 amend)"
      via: "net.http_post + Vault sr_key + body.action='tbm-missed'"
      pattern: "'tbm-missed'"
    - from: "supabase_realtime publication"
      to: "4 tbm_* 테이블 (RECREATE 후 재등록 필수)"
      via: "ALTER PUBLICATION supabase_realtime ADD TABLE"
      pattern: "ALTER PUBLICATION supabase_realtime ADD TABLE"
---

<objective>
Phase 12 Wave 1 — TBM v2 schema 운영 DB 적용. 단일 마이그레이션 `014_tbm_v2_schema.sql` 안에
(a) Phase 9 의 4 테이블 DROP + 신규 schema RECREATE (work_scope NOT NULL + hazards_snapshot/
controls_snapshot/key_hazard_id/feedback_notes + tbm_templates 확장 6 컬럼), (b) RLS + Realtime
publication 재등록 (DROP TABLE side effect 보정), (c) 도금 도메인 OPS 3종 시드 (forklift /
chemical / hot_work), (d) pg_cron + tbm_missed_attendance_check() 재등록, 그리고 [BLOCKING]
`supabase db push --linked --yes` 로 운영 DB 적용. 추가로 회귀 가드용 SQL isolation test.

Purpose: Wave 2 (Plan 12-02 Edge Function amend + Plan 12-03 Android refactor) 가 모두 014
schema 적용을 가정으로 진행됨. 본 plan 의 핵심 gate = supabase db push 성공 + Realtime
publication 4 테이블 재등록 (RECREATE 후 자동 제거된 publication 복구 필수, Phase 9 P11 의 변형).

Output:
- supabase/migrations/014_tbm_v2_schema.sql (운영 DB 적용 완료)
- tests/sql/test_014_tbm_v2_isolation.sql (8 assertions 회귀 가드)
- supabase migration list 에 014 등장 + cron.job 1행 + 3 OPS 시드 + Realtime publication 4 테이블 등록 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-tbm-redesign-kosha-guide/12-CONTEXT.md
@.planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md
@.planning/phases/09-tbm-worker-guide/09-CONTEXT.md
@.planning/phases/09-tbm-worker-guide/09-01-PLAN.md
@.planning/phases/09-tbm-worker-guide/09-01-SUMMARY.md
@supabase/migrations/013_tbm_schema.sql
@supabase/migrations/004_storage.sql
@supabase/migrations/010_watch_pipeline.sql
@supabase/migrations/011_watch_app_rls.sql
@supabase/migrations/012_cameras_health.sql

<interfaces>
<!-- 013 schema (현재 운영 DB) 가 DROP 대상 — 모든 컬럼/UNIQUE/RLS/publication 제거됨 -->

013 tbm_sessions (DROP 대상):
  session_id, group_id, session_date, started_at, ended_at, expected_end_at,
  leader_user_id, work_type, location, notes, missed_alert_at, created_at
  UNIQUE (group_id, session_date)  -- ★ 제거 대상, 014 에서 3-튜플로 교체

013 tbm_templates (DROP 대상):
  template_id, work_type UNIQUE, title, checklist JSONB, created_at
  -- 5종 시드 'fire'·'electric'·'height'·'heavy'·'general' 모두 제거

013 tbm_checklists / tbm_participants (DROP 대상, 014 에서 동일 shape RECREATE)

<!-- 014 schema 신규 shape (RESEARCH §Schema line 62-108 그대로) -->

014 tbm_sessions:
  session_id BIGSERIAL PK
  group_id INTEGER FK
  session_date DATE DEFAULT CURRENT_DATE
  work_scope VARCHAR(80) NOT NULL   -- ★ D-03 신규
  started_at TIMESTAMPTZ DEFAULT now()
  ended_at TIMESTAMPTZ
  expected_end_at TIMESTAMPTZ NOT NULL
  leader_user_id VARCHAR(50) NOT NULL
  work_type VARCHAR(40) NOT NULL  -- OPS 참조
  location VARCHAR(255)
  notes TEXT
  missed_alert_at TIMESTAMPTZ
  hazards_snapshot JSONB DEFAULT '[]'  -- TBM-05
  controls_snapshot JSONB DEFAULT '[]'  -- TBM-05
  key_hazard_id VARCHAR(20)            -- TBM-05 중점위험요인
  feedback_notes TEXT                  -- TBM-05 환류 조치
  created_at TIMESTAMPTZ DEFAULT now()
  UNIQUE (group_id, session_date, work_scope)  -- ★ D-09 3-튜플

014 tbm_templates:
  template_id SERIAL PK
  work_type VARCHAR(40) UNIQUE
  title VARCHAR(100)
  description TEXT
  hazards JSONB           -- [{id, text}] D-02
  controls JSONB          -- [{id, text, hazard_id, level}] D-02
  key_actions JSONB       -- [{text}] 핵심 안전조치 3개 (선택)
  checks JSONB            -- ["…"] 자율점검 list (Phase 9 checklist 후신)
  target_detector VARCHAR(20)  -- D-03 hint metadata
  is_active BOOLEAN DEFAULT true  -- D-08 토글
  is_custom BOOLEAN DEFAULT false  -- D-01
  created_at TIMESTAMPTZ DEFAULT now()

014 tbm_checklists / tbm_participants — 013 그대로 RECREATE

<!-- Vault secrets (Phase 8 02 시드 — 본 plan 은 SELECT 만) -->
vault.decrypted_secrets:
  name='service_role_key' + 'edge_function_base_url'

<!-- Plan 12-02 의 Edge Function tbm-missed 가 받을 payload (Phase 9 그대로) -->
tbm_missed_attendance_check() net.http_post body:
{action:'tbm-missed', session_id, group_id, leader_user_id}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 014_tbm_v2_schema.sql 작성 (DROP+RECREATE 4 테이블 + 신규 컬럼 + 3 OPS 시드 + RLS + Realtime + cron)</name>
  <read_first>
    - supabase/migrations/013_tbm_schema.sql (전체 — DROP 대상 + RECREATE base)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md §Schema line 60-130 (DDL fragments)
    - .planning/phases/12-tbm-redesign-kosha-guide/12-RESEARCH.md §OPS Seed Content line 218-356 (3 OPS INSERT 본문)
    - .planning/phases/09-tbm-worker-guide/09-01-PLAN.md Task 1 (013 schema base — 1:1 미러할 패턴)
    - supabase/migrations/012_cameras_health.sql line 100-180 (Vault sr_key + pg_cron + SECURITY DEFINER + EXCEPTION OTHERS — 그대로 미러)
    - supabase/migrations/011_watch_app_rls.sql (ALTER PUBLICATION ADD TABLE EXCEPTION 흡수 패턴)
  </read_first>
  <action>
    1. **신규 파일** `supabase/migrations/014_tbm_v2_schema.sql`. 정확한 골격:

       **(A) DROP 단계 — destructive D-04**:
       ```sql
       -- Phase 12 / 014 TBM v2 schema — KOSHA 가이드 흡수
       -- D-04 destructive: 013 의 4 테이블 + 5 generic templates 시드 모두 제거 후 RECREATE
       --
       -- 사전 자동 제거 (DROP CASCADE 의 side effect):
       --   - tbm_sessions/templates/checklists/participants → supabase_realtime publication 자동 탈락
       --   - tbm_checklists/participants → ON DELETE CASCADE 로 자동 정리
       --   - storage.objects (tbm-signatures bucket) → bucket row 영향 없음, orphan PNG 잔존 (RESEARCH Open Q6 accept)
       --
       -- DROP cron 먼저 (함수 변경 0 이지만 RECREATE 의 idempotent 패턴)
       DO $$ BEGIN
         PERFORM cron.unschedule('tbm_missed_attendance_minute');
       EXCEPTION WHEN OTHERS THEN NULL; END $$;

       DROP FUNCTION IF EXISTS public.tbm_missed_attendance_check();

       DROP TABLE IF EXISTS public.tbm_participants CASCADE;
       DROP TABLE IF EXISTS public.tbm_checklists  CASCADE;
       DROP TABLE IF EXISTS public.tbm_sessions    CASCADE;
       DROP TABLE IF EXISTS public.tbm_templates   CASCADE;
       ```

       **(B) RECREATE 4 테이블 — RESEARCH §Schema 그대로**:
       ```sql
       -- tbm_sessions: work_scope + hazards/controls snapshot + 회의록 양식 필드
       CREATE TABLE public.tbm_sessions (
         session_id        BIGSERIAL PRIMARY KEY,
         group_id          INTEGER NOT NULL REFERENCES public.groups(group_id),
         session_date      DATE NOT NULL DEFAULT CURRENT_DATE,
         work_scope        VARCHAR(80) NOT NULL,             -- D-03 신규
         started_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
         ended_at          TIMESTAMPTZ,
         expected_end_at   TIMESTAMPTZ NOT NULL,
         leader_user_id    VARCHAR(50) NOT NULL,
         work_type         VARCHAR(40) NOT NULL,             -- OPS template 참조
         location          VARCHAR(255),
         notes             TEXT,
         missed_alert_at   TIMESTAMPTZ,
         -- TBM-05 회의록 양식 핵심 필드 (snapshot 패턴 T-12-05 mitigation)
         hazards_snapshot  JSONB NOT NULL DEFAULT '[]'::jsonb,
         controls_snapshot JSONB NOT NULL DEFAULT '[]'::jsonb,
         key_hazard_id     VARCHAR(20),                       -- 중점위험요인 1개
         feedback_notes    TEXT,                              -- 환류 조치
         created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
         CONSTRAINT tbm_sessions_group_date_scope_uq
           UNIQUE (group_id, session_date, work_scope)
       );

       -- tbm_templates: OPS 카탈로그로 확장 (D-01 + D-02 + D-03)
       CREATE TABLE public.tbm_templates (
         template_id     SERIAL PRIMARY KEY,
         work_type       VARCHAR(40) NOT NULL UNIQUE,
         title           VARCHAR(100) NOT NULL,
         description     TEXT,
         hazards         JSONB NOT NULL DEFAULT '[]'::jsonb,  -- D-02 잠재위험
         controls        JSONB NOT NULL DEFAULT '[]'::jsonb,  -- D-02 대책
         key_actions     JSONB NOT NULL DEFAULT '[]'::jsonb,  -- 핵심 안전조치 3개 (선택)
         checks          JSONB NOT NULL DEFAULT '[]'::jsonb,  -- 자율점검 (Phase 9 checklist 후신)
         target_detector VARCHAR(20),                          -- D-03 hint metadata
         is_active       BOOLEAN NOT NULL DEFAULT true,        -- D-08 토글
         is_custom       BOOLEAN NOT NULL DEFAULT false,       -- D-01
         created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
       );

       -- tbm_checklists: 013 그대로 RECREATE
       CREATE TABLE public.tbm_checklists (
         checklist_id  BIGSERIAL PRIMARY KEY,
         session_id    BIGINT NOT NULL REFERENCES public.tbm_sessions(session_id) ON DELETE CASCADE,
         item_idx      INTEGER NOT NULL,
         item_text     TEXT NOT NULL,
         is_checked    BOOLEAN NOT NULL DEFAULT false,
         note          TEXT,
         checked_at    TIMESTAMPTZ,
         CONSTRAINT tbm_checklists_session_item_uq UNIQUE (session_id, item_idx)
       );

       -- tbm_participants: 013 그대로 RECREATE (D-09 UNIQUE 유지)
       CREATE TABLE public.tbm_participants (
         participant_id  BIGSERIAL PRIMARY KEY,
         session_id      BIGINT NOT NULL REFERENCES public.tbm_sessions(session_id) ON DELETE CASCADE,
         user_id         VARCHAR(50) NOT NULL,
         signed_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
         signature_url   TEXT,
         method          VARCHAR(20) NOT NULL DEFAULT 'signature'
           CHECK (method IN ('signature','nfc','qr','manual')),
         CONSTRAINT tbm_participants_session_user_uq UNIQUE (session_id, user_id)
       );

       CREATE INDEX IF NOT EXISTS idx_tbm_sessions_group_date
         ON public.tbm_sessions (group_id, session_date DESC);
       CREATE INDEX IF NOT EXISTS idx_tbm_sessions_work_scope
         ON public.tbm_sessions (work_scope);
       CREATE INDEX IF NOT EXISTS idx_tbm_participants_session
         ON public.tbm_participants (session_id);
       CREATE INDEX IF NOT EXISTS idx_tbm_checklists_session
         ON public.tbm_checklists (session_id, item_idx);
       CREATE INDEX IF NOT EXISTS idx_tbm_templates_active
         ON public.tbm_templates (is_active) WHERE is_active = true;
       ```

       **(C) RLS — v1.0 PoC pattern (Phase 9 미러 + tbm_templates 부분 anon UPDATE)**:
       ```sql
       ALTER TABLE public.tbm_sessions     ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_templates    ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_checklists   ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_participants ENABLE ROW LEVEL SECURITY;

       DO $$ BEGIN
         CREATE POLICY "tbm_sessions_select_v1_poc"     ON public.tbm_sessions
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_templates_select_v1_poc"    ON public.tbm_templates
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_checklists_select_v1_poc"   ON public.tbm_checklists
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_participants_select_v1_poc" ON public.tbm_participants
           FOR SELECT TO anon, authenticated USING (true);

         -- D-06/D-08 + RESEARCH Option B hybrid — tbm_templates UPDATE 정책
         -- (SettingOpsCatalogActivity 가 is_active 토글 직접 PATCH).
         -- 신규 INSERT (ops-create) + UPDATE 본문 (ops-update) 는 Edge Function (Option A) 경유.
         -- T-12-06 mitigation — 본 정책은 토글 한정 (Plan 12-03 의 SettingOpsCatalogRepository
         -- 가 PATCH 시 is_active 만 보냄, 코드 검증 필수).
         CREATE POLICY "tbm_templates_update_anon_active_only" ON public.tbm_templates
           FOR UPDATE TO anon, authenticated
           USING (true) WITH CHECK (true);
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;
       ```

       **(D) Realtime publication 재등록 (RECREATE 후 자동 탈락 보정, P-NEW-1 인접)**:
       ```sql
       DO $$ BEGIN
         ALTER PUBLICATION supabase_realtime ADD TABLE
           public.tbm_sessions, public.tbm_templates,
           public.tbm_checklists, public.tbm_participants;
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;
       ```

       **(E) 도금 도메인 OPS 3종 시드 (TBM-07 — RESEARCH §OPS Seed Content line 218-356 그대로)**:
       ```sql
       -- Template 1: 지게차 (CITED KOSHA 가이드 28페이지)
       INSERT INTO public.tbm_templates (
         work_type, title, description, hazards, controls, key_actions, checks, target_detector
       ) VALUES (
         'forklift', '지게차 작업',
         '도금 라인 자재·완제품 운반 시 사용. 운전자격·출입통제·안전벨트 3대 핵심조치.',
         '[
           {"id":"h1","text":"보행자와 충돌 (좁은 통로·시야 사각)"},
           {"id":"h2","text":"적재물 낙하 (포크 과적·불균형 적재)"},
           {"id":"h3","text":"전도 (속도 과다·급정거·경사로)"},
           {"id":"h4","text":"협착 (포크·마스트·차체와의 끼임)"}
         ]'::jsonb,
         '[
           {"id":"c1","text":"좁은 통로에 지게차 진입 금지 (대체 통로 사용)","hazard_id":"h1","level":"대체"},
           {"id":"c2","text":"보행자 출입통제 라인 및 유도자 배치","hazard_id":"h1","level":"통제"},
           {"id":"c3","text":"포크 적재 한도 표시 + 운전자 적재 전 확인","hazard_id":"h2","level":"통제"},
           {"id":"c4","text":"제한속도 5km/h 이하 + 급정거 금지","hazard_id":"h3","level":"통제"},
           {"id":"c5","text":"안전벨트 착용 확인 (전도 시 차체 이탈 방지)","hazard_id":"h3","level":"통제"},
           {"id":"c6","text":"포크 작업반경 1m 내 출입금지선 표시","hazard_id":"h4","level":"통제"}
         ]'::jsonb,
         '[
           {"text":"운전자격 확인 (지게차 면허 + 사업장 내 운전 허가)"},
           {"text":"출입통제 라인 + 유도자 배치 (보행자 분리)"},
           {"text":"안전벨트 착용 확인 (전도 시 차체 이탈 방지)"}
         ]'::jsonb,
         '[
           "운전자격 확인 (면허 + 사내 허가증)",
           "지게차 작업계획서 비치",
           "안전수칙 미준수 시 작업중지 권한 명시",
           "후방경광등 / 후진경보음 동작 확인",
           "포크·체인·마스트 외관 점검",
           "안전벨트 동작 확인",
           "타이어 마모·공기압 점검",
           "작업장 통로 폭 확보 + 노면 평탄",
           "출입통제 라인·표지 설치",
           "유도자 1명 이상 배치 (사각지대 보행자 안내)",
           "적재물 결속·균형 확인",
           "포크 1~2단 (지면 가까이) 이동",
           "운전자 안전벨트 착용",
           "급정거·급회전 금지"
         ]'::jsonb,
         'forklift'
       );

       -- Template 2: 화학물질 (ASSUMED — 도금 도메인 자체 정의)
       INSERT INTO public.tbm_templates (
         work_type, title, description, hazards, controls, key_actions, checks, target_detector
       ) VALUES (
         'chemical', '화학물질 취급 (산세·도금액)',
         '황산·염산·질산 등 산세조, 니켈·크롬·시안화물 도금조 취급 시 노출·튐·증기 흡입 위험 관리.',
         '[
           {"id":"h1","text":"산·알칼리 튐 → 피부·눈 손상"},
           {"id":"h2","text":"유증기 흡입 → 호흡기·점막 손상"},
           {"id":"h3","text":"시안화물 누출 → 청산 가스 생성 (산 접촉 시)"},
           {"id":"h4","text":"폐액 혼합 → 발열·돌비"}
         ]'::jsonb,
         '[
           {"id":"c1","text":"무인 자동공급 라인으로 대체 (인력 투입 제거)","hazard_id":"h1","level":"제거"},
           {"id":"c2","text":"산·도금조 분리 격벽 + 펌프 자동공급","hazard_id":"h1","level":"대체"},
           {"id":"c3","text":"내산 장갑·고글·앞치마·안면보호구 착용","hazard_id":"h1","level":"통제"},
           {"id":"c4","text":"국소배기장치 후드 위치·풍속 점검 (0.4m/s 이상)","hazard_id":"h2","level":"통제"},
           {"id":"c5","text":"시안조와 산조 동일 동선 금지 (혼입 사고 차단)","hazard_id":"h3","level":"통제"},
           {"id":"c6","text":"폐액 분류 보관·혼합 작업 금지 (지정 처리업체 위탁)","hazard_id":"h4","level":"통제"}
         ]'::jsonb,
         '[
           {"text":"MSDS 비치 + 작업자 교육 이수 확인"},
           {"text":"보호구 완전착용 (내산 장갑·고글·앞치마·호흡보호구)"},
           {"text":"비상샤워·세안기 가동 확인 + 5m 이내 위치"}
         ]'::jsonb,
         '[
           "MSDS 게시 + 작업자 숙지",
           "보호구 (내산 장갑·고글·앞치마·호흡보호구) 착용",
           "비상샤워·세안기 동작 확인 + 5m 이내 위치",
           "국소배기장치 가동 (후드 풍속 0.4m/s 이상)",
           "산조·도금조·시안조 동선 분리",
           "약품 농도·온도 표시판 부착",
           "폐액 분리 보관 + 라벨링",
           "환기 상태 (체류 가스 측정 50ppm 이하)",
           "비상연락망 + 응급조치 절차 게시",
           "출입통제 + 외부인 진입 차단"
         ]'::jsonb,
         'fire'
       );

       -- Template 3: 고온·열처리 (ASSUMED — 도금 도메인 자체 정의)
       INSERT INTO public.tbm_templates (
         work_type, title, description, hazards, controls, key_actions, checks, target_detector
       ) VALUES (
         'hot_work', '고온·열처리 작업',
         '전기도금 라인 발열·열처리로·건조로 운영 시 화상·화재·증기 위험 관리.',
         '[
           {"id":"h1","text":"고온 표면 접촉 → 화상"},
           {"id":"h2","text":"가연성 도금액 인접 → 발화·화재"},
           {"id":"h3","text":"고온 증기·미스트 흡입 → 호흡기 손상"},
           {"id":"h4","text":"열처리로 폭발 (가스 누출·과압)"}
         ]'::jsonb,
         '[
           {"id":"c1","text":"열처리로 자동 인터록 (도어 열림 시 가열 차단)","hazard_id":"h4","level":"제거"},
           {"id":"c2","text":"고온부 단열재 피복 + 안전망 설치","hazard_id":"h1","level":"대체"},
           {"id":"c3","text":"내열 장갑·앞치마·안면보호구 착용","hazard_id":"h1","level":"통제"},
           {"id":"c4","text":"열처리로 주변 2m 가연물 비치 금지","hazard_id":"h2","level":"통제"},
           {"id":"c5","text":"강제배기 + 외부 토출 (실내 농도 25ppm 이하)","hazard_id":"h3","level":"통제"},
           {"id":"c6","text":"가스 누출 감지기 + 자동 차단밸브","hazard_id":"h4","level":"통제"}
         ]'::jsonb,
         '[
           {"text":"가연물 격리 + 소화기 5m 이내 비치"},
           {"text":"내열 보호구 완전착용 (장갑·앞치마·안면보호구)"},
           {"text":"가스 누출 감지기 정상 + 비상정지 버튼 위치 숙지"}
         ]'::jsonb,
         '[
           "열처리로·건조로 외관·인터록 점검",
           "고온부 단열 피복 + 안전망 상태",
           "내열 장갑·앞치마·안면보호구 착용",
           "주변 2m 가연물 제거",
           "소화기·소화전 5m 이내 비치 + 점검필 확인",
           "가스 누출 감지기 동작 (가연성 25%LEL 이하)",
           "강제배기·국소배기 가동",
           "비상정지 버튼 위치·동작 확인",
           "온도 표시판 + 작업 한계 온도 게시",
           "통신수단 + 비상연락망"
         ]'::jsonb,
         'fire'
       );
       ```

       **(F) tbm_missed_attendance_check() — Phase 9 그대로 RECREATE (body 변경 0)**:
       ```sql
       CREATE OR REPLACE FUNCTION public.tbm_missed_attendance_check() RETURNS void
       LANGUAGE plpgsql SECURITY DEFINER
       SET search_path = public, extensions, net
       AS $$
       DECLARE
         r        RECORD;
         sr_key   TEXT;
         base_url TEXT;
       BEGIN
         SELECT decrypted_secret INTO sr_key
           FROM vault.decrypted_secrets WHERE name = 'service_role_key';
         SELECT decrypted_secret INTO base_url
           FROM vault.decrypted_secrets WHERE name = 'edge_function_base_url';

         IF sr_key IS NULL OR base_url IS NULL THEN
           RAISE WARNING 'tbm_missed_attendance_check: vault secret missing — skip';
           RETURN;
         END IF;

         FOR r IN
           SELECT session_id, group_id, expected_end_at, leader_user_id
           FROM public.tbm_sessions
           WHERE expected_end_at + interval '30 minutes' < now()
             AND missed_alert_at IS NULL
             AND ended_at IS NULL
         LOOP
           UPDATE public.tbm_sessions
             SET missed_alert_at = now()
             WHERE session_id = r.session_id;

           PERFORM net.http_post(
             url := base_url || '/notifications',
             headers := jsonb_build_object(
               'Authorization', 'Bearer ' || sr_key,
               'Content-Type',  'application/json'
             ),
             body := jsonb_build_object(
               'action',         'tbm-missed',
               'session_id',     r.session_id,
               'group_id',       r.group_id,
               'leader_user_id', r.leader_user_id
             )
           );
         END LOOP;
       END;
       $$;

       -- pg_cron 재등록
       SELECT cron.schedule(
         'tbm_missed_attendance_minute',
         '* * * * *',
         $$SELECT public.tbm_missed_attendance_check();$$
       );
       ```

       **(G) NOTE — Storage 'tbm-signatures' bucket 보존**: Phase 9 의 storage.buckets row 는
       그대로 유지 (DROP TABLE 영향 없음). 014 본문에 storage 관련 INSERT 없음. 단 코멘트로
       명시 — 마이그 본문 상단 또는 RECREATE 후:
       ```sql
       -- NOTE (RESEARCH Open Q6): Storage 'tbm-signatures' bucket 은 storage.buckets 안 — 014 의
       -- DROP TABLE public.tbm_* 영향 없음. Phase 9 시드 세션의 서명 PNG 가 orphan 으로 잔존하나
       -- cost 작고 격리됨. 014 안에서 storage.objects cleanup 안 함 (D-04 destructive 의 단순성 우선).
       ```

    2. **회귀 가드 — service_role hardcode 0건**: 마이그 본문에 `eyJ` (JWT prefix) 등장 0회.
       코멘트 (`--` line) 제외 후 grep 검증.

    3. **컬럼명 일관**: `work_scope`, `hazards_snapshot`, `controls_snapshot`, `key_hazard_id`,
       `feedback_notes`, `is_active`, `is_custom`, `target_detector` 모두 마이그 + Plan 12-02
       의 Edge Function payload + Plan 12-03 의 Kotlin model SerialName 과 1:1 일관. grep 검증.

    4. **Pitfall 11 mitigation (JSONB array 순서)**: hazards/controls/key_actions/checks 의 JSONB
       array 입력 순서가 SELECT 시에도 보장됨 (Postgres JSONB 순서 보존 — Phase 9 P11 그대로 적용).
       단 hazards 의 id ("h1","h2",...) 는 명시적 — Edge Function 또는 Android 가 id 기반 lookup
       시 정합성 보장 (이름 충돌 0).
  </action>
  <verify>
    <automated>test -f supabase/migrations/014_tbm_v2_schema.sql && grep -c "DROP TABLE IF EXISTS public.tbm_sessions" supabase/migrations/014_tbm_v2_schema.sql && grep -c "DROP TABLE IF EXISTS public.tbm_templates" supabase/migrations/014_tbm_v2_schema.sql && grep -c "CREATE TABLE public.tbm_sessions" supabase/migrations/014_tbm_v2_schema.sql && grep -c "CREATE TABLE public.tbm_templates" supabase/migrations/014_tbm_v2_schema.sql && grep -c "work_scope VARCHAR" supabase/migrations/014_tbm_v2_schema.sql && grep -c "hazards_snapshot JSONB" supabase/migrations/014_tbm_v2_schema.sql && grep -c "controls_snapshot JSONB" supabase/migrations/014_tbm_v2_schema.sql && grep -c "key_hazard_id" supabase/migrations/014_tbm_v2_schema.sql && grep -c "feedback_notes" supabase/migrations/014_tbm_v2_schema.sql && grep -c "is_active BOOLEAN" supabase/migrations/014_tbm_v2_schema.sql && grep -c "is_custom BOOLEAN" supabase/migrations/014_tbm_v2_schema.sql && grep -c "target_detector" supabase/migrations/014_tbm_v2_schema.sql && grep -c "UNIQUE (group_id, session_date, work_scope)" supabase/migrations/014_tbm_v2_schema.sql && grep -c "'forklift'" supabase/migrations/014_tbm_v2_schema.sql && grep -c "'chemical'" supabase/migrations/014_tbm_v2_schema.sql && grep -c "'hot_work'" supabase/migrations/014_tbm_v2_schema.sql && grep -c "ALTER PUBLICATION supabase_realtime ADD TABLE" supabase/migrations/014_tbm_v2_schema.sql && grep -c "SECURITY DEFINER" supabase/migrations/014_tbm_v2_schema.sql && grep -c "cron.schedule" supabase/migrations/014_tbm_v2_schema.sql && [ "$(grep -v '^--' supabase/migrations/014_tbm_v2_schema.sql | grep -c 'eyJ')" = "0" ] && echo OK</automated>
  </verify>
  <done>
    - 신규 파일 `supabase/migrations/014_tbm_v2_schema.sql` 존재
    - DROP 단계: `DROP TABLE IF EXISTS public.tbm_{sessions,templates,checklists,participants} CASCADE` 모두 등장 (D-04)
    - RECREATE 단계: 4 `CREATE TABLE public.tbm_*` 모두 등장
    - `work_scope VARCHAR(80) NOT NULL` 1회 (D-03 신규)
    - `hazards_snapshot JSONB` + `controls_snapshot JSONB` + `key_hazard_id` + `feedback_notes` 모두 등장 (TBM-05)
    - tbm_templates 확장 컬럼 — `hazards JSONB`, `controls JSONB`, `key_actions JSONB`, `checks JSONB`, `target_detector`, `is_active`, `is_custom` 모두 등장
    - `UNIQUE (group_id, session_date, work_scope)` 1회 (D-09)
    - 3 OPS 시드 — 'forklift', 'chemical', 'hot_work' INSERT 본문 모두 등장 (TBM-07)
    - `ALTER PUBLICATION supabase_realtime ADD TABLE` 1회 with 4 tables (RECREATE 후 재등록)
    - `SECURITY DEFINER` 1회 (Phase 9 미러)
    - `cron.schedule.*tbm_missed_attendance_minute` 1회
    - service_role JWT hardcode 0건 (코멘트 제외 후 `eyJ` count == 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push --linked --yes + tests/sql/test_014_tbm_v2_isolation.sql + PostgREST 검증</name>
  <read_first>
    - supabase/migrations/014_tbm_v2_schema.sql (Task 1 산출물)
    - .planning/phases/09-tbm-worker-guide/09-01-PLAN.md Task 2 (push + PostgREST + cron + SQL test 패턴 — 1:1 미러)
    - .planning/phases/09-tbm-worker-guide/09-01-SUMMARY.md (Phase 9 push 결과 evidence)
    - tests/sql/test_013_tbm_isolation.sql (Phase 9 isolation test 패턴 — 7 assertions, 014 가 8 assertions 으로 확장)
    - .env (SUPABASE_DB_PASSWORD + SUPABASE_ACCESS_TOKEN — Phase 4·7·8·9 작성됨)
  </read_first>
  <action>
    1. **[BLOCKING] `supabase db push --linked --yes`** 실행 — 014 마이그 운영 DB 적용.
       NOTICE 출력 확인 — (a) 4 테이블 DROP, (b) 4 테이블 RECREATE, (c) 4 ENABLE RLS, (d) 4 정책 +
       tbm_templates UPDATE 정책, (e) publication ADD TABLE, (f) 3 OPS 시드 INSERT, (g) 함수 정의,
       (h) cron job 등록. 실패 시 즉시 중단 + Wave 2 진입 차단.

       Non-TTY 환경 (Phase 8·9 패턴): `SUPABASE_ACCESS_TOKEN` env 만 export 후 push.
       psql 미설치 환경에서는 PostgREST + Management API NOTICE 조합 우회 (09-01 SUMMARY 패턴 동일).

       **⚠ Destructive 경고**: 014 가 013 의 tbm_sessions / tbm_participants 등 row 모두 제거.
       Phase 9 시연 데이터 잔존 가능 (옵션 C 단축 시연 시) → DROP 후 사라짐. PPT 시연 재시도 필요 시
       Plan 12-04 의 합성 검증 시나리오로 새 데이터 생성.

    2. **PostgREST 검증 — 3 OPS 시드 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_templates?select=work_type,title,is_active,is_custom&order=template_id" \
         -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY"
       ```
       기대: 3 rows with work_type IN ('forklift','chemical','hot_work'), is_active=true, is_custom=false 모두.

    3. **PostgREST 검증 — tbm_templates 의 JSONB 컬럼 채워짐**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_templates?work_type=eq.forklift&select=hazards,controls,key_actions,checks,target_detector" \
         -H "apikey: $ANON_KEY"
       ```
       기대: hazards array 4개 + controls array 6개 + key_actions array 3개 + checks array 14개 + target_detector='forklift'.

    4. **PostgREST 검증 — tbm_sessions 빈 확인 (anon SELECT 200 empty)**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_sessions?select=session_id,work_scope" \
         -H "apikey: $ANON_KEY"
       ```
       기대: `[]` (RECREATE 직후, 데이터 0).

    5. **RPC sanity call** (Phase 9 패턴):
       ```bash
       curl -s -X POST "$SUPABASE_URL/rest/v1/rpc/tbm_missed_attendance_check" \
         -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY" \
         -H "Content-Type: application/json" -d '{}'
       ```
       기대: HTTP 204 No Content + Vault sr_key 미시드 시 RAISE WARNING (graceful skip, Phase 9 검증 패턴).

    6. **`tests/sql/test_014_tbm_v2_isolation.sql` 작성 (8 assertions, Phase 9 test_013 미러)**:
       ```sql
       -- (1) 4 tables 의 ROW LEVEL SECURITY 활성 검증
       DO $$ BEGIN
         IF NOT (SELECT bool_and(rowsecurity) FROM pg_class
                 WHERE relname IN ('tbm_sessions','tbm_templates','tbm_checklists','tbm_participants')) THEN
           RAISE EXCEPTION 'RLS not enabled on all 4 tbm tables';
         END IF;
       END $$;

       -- (2) UNIQUE (group_id, session_date, work_scope) 등록 검증 (D-09 3-튜플)
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM pg_constraint
                        WHERE conname = 'tbm_sessions_group_date_scope_uq') THEN
           RAISE EXCEPTION 'UNIQUE (group_id, session_date, work_scope) not registered';
         END IF;
       END $$;

       -- (3) 3 OPS 시드 검증 (TBM-07)
       DO $$ BEGIN
         IF (SELECT count(*) FROM public.tbm_templates
             WHERE work_type IN ('forklift','chemical','hot_work')) <> 3 THEN
           RAISE EXCEPTION '3 OPS seed missing (forklift/chemical/hot_work)';
         END IF;
       END $$;

       -- (4) tbm_templates 확장 컬럼 존재 검증 (D-02)
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM information_schema.columns
                        WHERE table_schema='public' AND table_name='tbm_templates'
                          AND column_name IN ('hazards','controls','key_actions','checks','target_detector','is_active','is_custom')
                        GROUP BY table_schema HAVING count(*) = 7) THEN
           RAISE EXCEPTION 'tbm_templates extension columns missing';
         END IF;
       END $$;

       -- (5) cron.job 등록 검증
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM cron.job
                        WHERE jobname='tbm_missed_attendance_minute' AND active=true) THEN
           RAISE EXCEPTION 'tbm_missed_attendance_minute cron not registered';
         END IF;
       END $$;

       -- (6) SECURITY DEFINER 검증
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM pg_proc p JOIN pg_namespace n ON p.pronamespace=n.oid
                        WHERE n.nspname='public' AND p.proname='tbm_missed_attendance_check'
                        AND p.prosecdef=true) THEN
           RAISE EXCEPTION 'tbm_missed_attendance_check should be SECURITY DEFINER';
         END IF;
       END $$;

       -- (7) tbm-signatures bucket 존재 검증 (Phase 9 보존)
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM storage.buckets WHERE id='tbm-signatures' AND public=false) THEN
           RAISE EXCEPTION 'tbm-signatures bucket missing or public (should be preserved from Phase 9)';
         END IF;
       END $$;

       -- (8) Realtime publication 4 테이블 재등록 검증 (P-NEW-1 인접)
       DO $$ BEGIN
         IF (SELECT count(*) FROM pg_publication_tables
             WHERE pubname='supabase_realtime'
               AND tablename IN ('tbm_sessions','tbm_templates','tbm_checklists','tbm_participants')) <> 4 THEN
           RAISE EXCEPTION 'Realtime publication 4 tables not re-registered after RECREATE';
         END IF;
       END $$;

       SELECT 'test_014_tbm_v2_isolation: 8/8 assertions PASS' AS result;
       ```
       Dashboard SQL Editor 또는 supabase db remote commit 으로 1회 실행.

    7. **회귀 가드 — Phase 9 worker 시드 보존 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/profiles?group_id=eq.1&user_role=eq.worker&select=user_id" \
         -H "apikey: $ANON_KEY"
       ```
       기대: 3 rows (testuser_w1·w2·w3) — `profiles` 테이블 변경 0 이므로 그대로 잔존 (Phase 9 의 seed_tbm_demo.py 산출물).

    8. **Phase 9 코드 검증 — TbmRepository 의 `decodeSingleOrNull` 등이 단일 row 가정** (P-NEW-2):
       Plan 12-03 에서 모두 `decodeList` 로 교체 예정 — 본 plan 의 SUMMARY 에서 "Plan 12-03 가
       shape 변경 필수" 명시 (executor 가 push 직후 Android 실행하면 일부 Realtime callback crash
       가능 — Wave 2 가 양쪽 다 amend 완료 후 시연).
  </action>
  <verify>
    <automated>test -f tests/sql/test_014_tbm_v2_isolation.sql && grep -c "tbm-signatures" tests/sql/test_014_tbm_v2_isolation.sql && grep -c "tbm_missed_attendance_minute" tests/sql/test_014_tbm_v2_isolation.sql && grep -c "ROW LEVEL SECURITY\|rowsecurity" tests/sql/test_014_tbm_v2_isolation.sql && grep -c "tbm_sessions_group_date_scope_uq" tests/sql/test_014_tbm_v2_isolation.sql && grep -c "forklift\|chemical\|hot_work" tests/sql/test_014_tbm_v2_isolation.sql && echo OK</automated>
  </verify>
  <done>
    - `supabase db push --linked --yes` 성공 (커밋 또는 SUMMARY 에 운영 DB 적용 시각 + NOTICE 출력 기록)
    - `supabase migration list` 에 014 등장
    - PostgREST anon GET `/rest/v1/tbm_templates?select=work_type,title,is_active` 3 rows ('forklift','chemical','hot_work', is_active=true)
    - PostgREST GET `/rest/v1/tbm_templates?work_type=eq.forklift` hazards 4개 + controls 6개 + key_actions 3개 + checks 14개 + target_detector='forklift' 모두 채움
    - PostgREST anon GET `/rest/v1/tbm_sessions` `[]` (RECREATE 직후 빈 배열)
    - cron.job 의 'tbm_missed_attendance_minute' active=true 등록 확인 (재등록)
    - storage.buckets 'tbm-signatures' (public=false) 그대로 잔존 (Phase 9 보존)
    - `tests/sql/test_014_tbm_v2_isolation.sql` 작성 (8 assertions) + Dashboard SQL Editor 에서 ALL PASS
    - profiles 시드 잔존 — testuser_w1·w2·w3 3 rows (Phase 9 산출물 보존)
    - SUMMARY 에 destructive 명시 — 013 의 tbm_sessions row 모두 사라짐, Plan 12-04 합성 검증 시 새 데이터 생성
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon)→PostgREST | 운영 DB read/write — RLS 가 default deny + USING(true) SELECT + tbm_templates UPDATE 정책 (Option B hybrid) |
| pg_cron→Edge Function | net.http_post + Vault sr_key — Postgres 내부, Phase 9 패턴 그대로 |
| client (anon)→Storage | tbm-signatures bucket INSERT (Phase 9 보존, 014 영향 없음) |
| Dashboard (manual)→Vault | sr_key 시드 (Phase 8 02 prerequisite, 9 재사용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | T (Tampering) | tbm_sessions UNIQUE bypass | mitigate | UNIQUE 트리거 컬럼이 (group_id, session_date) → (group_id, session_date, work_scope) 3-튜플로 변경 (D-09). DB-level enforcement. Plan 12-02 의 Edge Function 23505 catch shape 동일 (P-NEW-1). |
| T-9-05 | D | Vault sr_key 미시드 시 cron 무한 RAISE WARNING | accept (graceful skip) | tbm_missed_attendance_check() body 변경 0 — Phase 9 패턴 그대로. silent skip. |
| T-9-07 | T | Vault secret git 노출 | mitigate | 014 마이그 본문 `eyJ` 0건 (코멘트 제외). vault.decrypted_secrets SELECT 만 사용. |
| T-9-08 | E (Elevation of Privilege) | anon 이 tbm_participants 임의 INSERT | mitigate | RLS write 정책 미등록 = default deny. Plan 12-02 의 tbm-checkin Edge Function 만 service_role 로 INSERT. |
| **T-12-02** | T | work_scope SQL injection | mitigate | VARCHAR(80) length cap (DoS 차단). Supabase Postgrest parameterized query 강제. raw SQL 미사용. |
| **T-12-04** | T | is_active 토글 race (두 manager 동시 토글) | accept v1.0 | Phase 9 D-09 last-writer-wins 패턴. 안전 critical 아님 (운영 중 OPS 비활성화는 rare event). v1.2 optimistic locking 검토. |
| **T-12-05** | I | OPS 비활성화 → 진행중 세션의 hazards/controls 손실 | mitigate | tbm_sessions.hazards_snapshot/controls_snapshot 으로 자연 mitigation (snapshot 패턴, Phase 9 tbm_checklists.item_text 미러). 014 schema 에 명시. |
| **T-12-06** | T | tbm_templates anon UPDATE 남용 (Option B hybrid) | accept v1.0 | RLS UPDATE 정책 `USING(true) WITH CHECK(true)` — anon 이 임의 row 토글 가능. v1.0 PoC 한정. **Plan 12-03 의 SettingOpsCatalogActivity 에서 클라이언트가 is_active 컬럼만 PATCH 하도록 코드 검증** (다른 컬럼 변경 시 v1.2 RLS narrowing). v1.2 mitigation: WITH CHECK 조건식에 row diff 비교 추가 또는 service_role-only Option A 전환. |
</threat_model>

<verification>
- 신규 파일 `supabase/migrations/014_tbm_v2_schema.sql` 존재 + 모든 `<done>` 조건 충족
- 신규 파일 `tests/sql/test_014_tbm_v2_isolation.sql` 존재
- `supabase db push --linked --yes` 운영 DB 적용 성공
- `supabase migration list` 에 014 등장
- PostgREST 3 OPS 시드 + tbm_sessions 빈 응답 + cron.job 재등록 + tbm-signatures bucket 보존 모두 확인
- `tests/sql/test_014_tbm_v2_isolation.sql` 의 8 assertions Dashboard SQL Editor 에서 모두 PASS
- service_role JWT hardcode 0건
- ai_agent/tests/ 무회귀 (31/31 PASS — capture/scheduler 코드 0 변경)
- j2208a/tests/ 무회귀 (43/43 PASS — watch 코드 0 변경)
</verification>

<success_criteria>
- TBM-04 충족 (Edge Function 측은 Plan 12-02) — `tbm_sessions UNIQUE (group_id, session_date, work_scope)` 3-튜플 + `work_scope NOT NULL` 컬럼 운영 DB 적용
- TBM-05 충족 (schema 측) — `hazards_snapshot` + `controls_snapshot` + `key_hazard_id` + `feedback_notes` 회의록 양식 핵심 필드 schema mapping
- TBM-07 충족 — 도금 도메인 OPS 3종 (forklift/chemical/hot_work) seed + target_detector hint
- Plan 12-02 (Edge Function amend) + Plan 12-03 (Android refactor) 가 014 schema 적용을 가정으로 진행 가능
- T-12-02·04·05·06 mitigation 충족
- Phase 9 worker 시드 (testuser_w1·w2·w3) 보존 (profiles 변경 0)
</success_criteria>

<deviations>
<!-- Executor 작성 칸. Rule 1 (plan 정정) / Rule 2 (워크어라운드) / Rule 3 (가드) 별로 기록. -->
</deviations>

<rollback_plan>
**Rollback 시 (014 적용 후 치명적 결함 발견)**:

1. **DB 측 rollback**: `015_tbm_v2_rollback.sql` 마이그 작성 — 013 schema 로 역전. 단 013 의 시드 데이터 (5 generic templates + Phase 9 시연 세션) 는 복원 불가 (DROP CASCADE 의 side effect).
2. **Code 측 rollback**: `git revert <Phase 12 commits>` — Plan 12-02·03 의 변경 (Edge Function + Android) 모두 되돌림.
3. **Realtime publication**: rollback 마이그에서 `ALTER PUBLICATION supabase_realtime ADD TABLE` 4 테이블 재등록 필수.
4. **Storage**: orphan signature PNG 는 그대로 잔존 — 별도 cleanup 불필요.

**Rollback 신호**: Plan 12-04 의 합성 검증에서 catastrophic 실패 시 (예: 회귀 가드 위반 25%+ 또는 운영 DB 의 critical query 동작 불가).
</rollback_plan>

<output>
After completion, create `.planning/phases/12-tbm-redesign-kosha-guide/12-01-SUMMARY.md` documenting:
- supabase db push --linked --yes 실행 결과 (NOTICE 출력 발췌 — DROP 4 + CREATE 4 + RLS + publication ADD + cron register)
- supabase migration list 에 014 등장 확인
- PostgREST 3 OPS 시드 응답 (forklift/chemical/hot_work + is_active=true + JSONB 컬럼 채움)
- PostgREST tbm_sessions 빈 응답 (RECREATE 직후)
- cron.job 재등록 응답
- storage.buckets 'tbm-signatures' 보존 확인 (Phase 9 산출물)
- profiles testuser_w1·w2·w3 잔존 확인 (Phase 9 worker 시드)
- test_014_tbm_v2_isolation.sql 8/8 PASS 응답
- T-12-02·04·05·06 mitigation 검증 결과
- Vault sr_key 시드 prerequisite (Phase 8 02 SUMMARY User Setup Required 재인용)
- Deviations (Rule 1/2/3 별 분류)
- **⚠ Destructive 영향 명시** — Phase 9 시연 세션 row 모두 사라짐 (Plan 12-04 합성 검증 시 새 데이터 생성)
</output>
</content>
</invoke>