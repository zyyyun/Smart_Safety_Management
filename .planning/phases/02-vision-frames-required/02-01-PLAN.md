---
phase: 02-vision-frames-required
plan: 01
type: execute
wave: 1
depends_on: [1]
files_modified:
  - ai_agent/detector_configs.py
  - ai_agent/scheduler.py
  - ai_agent/tests/__init__.py
  - ai_agent/tests/test_scheduler_buffer.py
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "DETECTOR_CONFIGS 의 4 detector (fire/helmet/forklift/person) 모두에 frames_required 키가 존재하고 값이 5/3/1/1 이다 (MODEL-01 / ROADMAP SC #1)"
    - "scheduler.py 의 _process_detection_for_camera 가 단일 frame is_detected=True spike 입력 시 알람을 발사하지 않는다 (MODEL-02 / ROADMAP SC #2)"
    - "scheduler.py 의 _process_detection_for_camera 가 N=frames_required 연속 is_detected=True 입력 시 정확히 1회 알람을 발사한다 (MODEL-02 / ROADMAP SC #2)"
    - "알람 발사 후 buffer.clear() + cooldown 갱신이 일어나, 다음 알람은 cooldown 만료 + 재누적 N 연속 둘 다 충족 시에만 발생한다 (D-02 / D-07)"
    - "cooldown 활성 cycle 에서는 detect 가 호출되지 않으므로 buffer push 가 발생하지 않는다 (D-07)"
    - "forklift/person (N=1) 의 동작이 단일 cycle 즉시 알람으로 유지된다 (분기 없이 동일 코드 경로, D-04)"
    - "pytest ai_agent/tests/test_scheduler_buffer.py -v 가 0 exit code 로 6개 이상 테스트 모두 PASS (D-03 / ROADMAP SC #2)"
  artifacts:
    - path: "ai_agent/detector_configs.py"
      provides: "DETECTOR_CONFIGS dict + frames_required 키 4개 (fire=5, helmet=3, forklift=1, person=1)"
      contains: "frames_required"
    - path: "ai_agent/scheduler.py"
      provides: "_detection_buffer 모듈 상태 + buffer push + N 연속 검사 + clear 로직"
      contains: "_detection_buffer"
    - path: "ai_agent/tests/__init__.py"
      provides: "테스트 패키지 마커 (pytest 발견용)"
      min_lines: 0
    - path: "ai_agent/tests/test_scheduler_buffer.py"
      provides: "_process_detection_for_camera buffer 동작 단위 테스트 (mocked detector + bridge)"
      min_lines: 100
  key_links:
    - from: "ai_agent/scheduler.py"
      to: "ai_agent/detector_configs.py"
      via: "cfg['frames_required'] 읽기"
      pattern: "cfg\\[.frames_required.\\]"
    - from: "ai_agent/scheduler.py"
      to: "ai_agent/yolo_detector.py"
      via: "result.is_detected → buffer.append"
      pattern: "_detection_buffer.*append"
    - from: "ai_agent/tests/test_scheduler_buffer.py"
      to: "ai_agent/scheduler.py"
      via: "_process_detection_for_camera 호출 + _detection_buffer / _detection_cooldown 직접 조작"
      pattern: "_process_detection_for_camera|_detection_buffer"
---

<objective>
Phase 2 — frames 연속 룰 (단일 PLAN, 단일 wave, 3 sequential tasks).

`ai_agent/scheduler.py` 의 일반 detection 사이클에 N 프레임 연속 룰을 추가하여, 단일
frame conf 변동 (조명·그림자·NMS 노이즈, fire D-19 fallback 의 conf 0.10) 으로 인한
false positive 알람을 흡수한다. fire 는 5 cycle / helmet 은 3 cycle / forklift·person
은 1 cycle (즉시) 연속 검출일 때만 알람을 발사한다.

Purpose:
- MODEL-01: `DETECTOR_CONFIGS` 4 detector 에 `frames_required: int` 키 추가
- MODEL-02: `_process_detection_for_camera` 의 buffer push + N 연속 검사 분기 + 단위 테스트
- MODEL-03: Phase 1 에서 흡수 완료 (D-08 — fire D-19 fallback + frames_required 결합으로 의미 완성). Phase 2 에서는 no-op (재진행 X).

Output:
- `ai_agent/detector_configs.py` 의 fire/helmet/forklift/person 4 항목에 frames_required 키
- `ai_agent/scheduler.py` 의 `_detection_buffer` 모듈 dict + 6단계 흐름 (D-07 순서)
- `ai_agent/tests/__init__.py` + `ai_agent/tests/test_scheduler_buffer.py` (≥6 tests, mocked detector + bridge)
- pytest 통과 (단일 명령 exit 0)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-vision-frames-required/02-CONTEXT.md
@.planning/phases/01-vision-demo-videos/01-CONTEXT.md
@.planning/phases/01-vision-demo-videos/01-SUMMARY.md
@ai_agent/scheduler.py
@ai_agent/detector_configs.py
@ai_agent/yolo_detector.py

<interfaces>
<!-- 코드 변경에 필요한 핵심 contracts. 실행자는 코드베이스 추가 탐색 없이 이 정보로 작업한다. -->

From `ai_agent/yolo_detector.py` (line 29-40):
```python
@dataclass
class DetectResult:
    is_detected: bool
    confidence: float | None = None
    bbox: tuple[float, float, float, float] | None = None
    label: str | None = None
    inference_ms: float = 0.0
```

From `ai_agent/scheduler.py` (line 42-45) — 모듈 상태 dict 패턴 (그대로 따라 추가):
```python
_fall_cooldown: dict[int, float] = {}
_detection_cooldown: dict[tuple[int, str], float] = {}
# Phase 2 신규 추가 위치:
# _detection_buffer: dict[tuple[int, str], "deque[bool]"] = {}
```

From `ai_agent/scheduler.py` (line 202-264) — 변경 대상 함수 시그니처:
```python
def _process_detection_for_camera(
    bridge: SupabaseBridge,
    settings: Settings,
    event_key: str,
    detector: GenericYoloDetector,
    cfg: dict[str, Any],
    camera: dict[str, Any],
) -> str:
    # cooldown skip → snapshot → detect → upload + register_ai_event → return
```

From `ai_agent/detector_configs.py` — 변경 대상 dict (4 항목 모두에 신규 키 1개 추가):
```python
DETECTOR_CONFIGS: dict[str, dict] = {
    "fire": {..., "conf_thres": 0.10, "target_classes": None, ...},   # add frames_required: 5
    "helmet": {..., "conf_thres": 0.5, "target_classes": ["head"], ...},  # add frames_required: 3
    "forklift": {..., "conf_thres": 0.25, "target_classes": None, ...},   # add frames_required: 1
    "person": {..., "conf_thres": 0.30, "target_classes": ["person"], ...},  # add frames_required: 1
}
```

CONTEXT.md 의 D-decisions (AUTHORITATIVE — 모두 honor):
- D-01: scheduler 모듈 상태 dict (`_detection_cooldown` 옆 라인 ~45)
- D-02: 알람 후 `buffer.clear()` reset
- D-03: pytest unit test (mocked detector)
- D-04: frames_required = 5/3/1/1 (fire/helmet/forklift/person)
- D-05: `result.is_detected` 사용
- D-06: 휘발성 OK (재시작 시 reset)
- D-07: cooldown skip 시 push 안 함, detect 후 push + 검사 + clear
- D-08: Phase 1 D-19 fallback (fire 0.10) 의 의미 완성 — frames_required 5 와 결합 시 운영급
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: detector_configs.py 의 4 detector 에 frames_required 키 추가 (MODEL-01)</name>
  <files>ai_agent/detector_configs.py</files>

  <read_first>
    - ai_agent/detector_configs.py (전체 파일 — 4 항목 dict 구조 확인)
    - .planning/phases/02-vision-frames-required/02-CONTEXT.md (D-04, D-08)
  </read_first>

  <action>
fire / helmet / forklift / person 4 항목 각각에 `frames_required: int` 키를 추가한다.
값은 D-04 정확히: fire=5, helmet=3, forklift=1, person=1.

배치 위치는 각 dict 내 `iou_thres` 와 `img_size` 사이 (가독성 — 룰 임계 그룹). 기존
키는 손대지 않는다 (Phase 1 에서 lock 된 conf_thres, target_classes 등 그대로 유지).

D-08 의 fire 주석 보강도 1줄 추가: 기존 `# Phase 2 frames_required (fire 5 연속) 결합
시 운영급 의미 확보.` 옆에 frames_required 키가 실제로 들어왔음을 가리키는 마무리
주석은 불필요 (값 자체가 D-04 의 5).

```python
DETECTOR_CONFIGS: dict[str, dict] = {
    "fire": {
        "framework": "yolov5",
        "weights": r"D:\2025_산업안전\산업안전\모델 7종\화재 탐지\fire_best.pt",
        "event_name": "화재",
        "risk_level": "DANGER",
        "camera_ids": [1],
        # ... 기존 D-19 주석 그대로 ...
        "conf_thres": 0.10,
        "iou_thres": 0.45,
        # Phase 2 MODEL-01 / D-04: 5 cycle 연속 fire 검출 시에만 알람 (false positive 흡수).
        # D-19 fallback (conf 0.10) 의 운영급 의미는 frames_required 와 결합으로 완성됨 (D-08).
        "frames_required": 5,
        "img_size": 640,
        "target_classes": None,
        "storage_prefix": "fire",
    },
    "helmet": {
        # ... 기존 키 그대로 (conf_thres 0.5, target_classes ['head']) ...
        "iou_thres": 0.45,
        # Phase 2 MODEL-01 / D-04: 3 cycle 연속 head 검출 시에만 알람 (일시적 가림 흡수).
        "frames_required": 3,
        "img_size": 640,
        "target_classes": ["head"],
        "storage_prefix": "helmet",
    },
    "forklift": {
        # ... 기존 키 그대로 ...
        "iou_thres": 0.45,
        # Phase 2 MODEL-01 / D-04: 지게차 진입 1회 검출만으로도 알람 (시간 누적 불필요).
        "frames_required": 1,
        "img_size": 640,
        "target_classes": None,
        "storage_prefix": "forklift",
    },
    "person": {
        # ... 기존 키 그대로 ...
        "iou_thres": 0.45,
        # Phase 2 MODEL-01 / D-04: 사람 등장 1회 검출만으로도 알람.
        "frames_required": 1,
        "img_size": 640,
        "target_classes": ["person"],
        "storage_prefix": "person",
    },
}
```

**구현 노트:**
- 기존 `# D-19 fallback (2026-05-04): ...` 의 fire 주석 블록은 손대지 않는다 (Phase 1 lock).
- helmet 주석의 운영급 검증 텍스트도 손대지 않는다.
- 신규 추가는 오직 `frames_required: N,` 한 줄 + 위 주석 한 줄씩.
- 키 추가 위치는 `iou_thres` 다음 (위 코드처럼 `img_size` 위) — 룰 임계 그룹 일관성.
  </action>

  <acceptance_criteria>
    - `grep -c "frames_required" ai_agent/detector_configs.py` >= 4 (4 detector 모두)
    - `grep -E '"frames_required":\s*5' ai_agent/detector_configs.py` 1건 (fire)
    - `grep -E '"frames_required":\s*3' ai_agent/detector_configs.py` 1건 (helmet)
    - `grep -cE '"frames_required":\s*1' ai_agent/detector_configs.py` >= 2 (forklift, person)
    - `python -c "from ai_agent.detector_configs import DETECTOR_CONFIGS; assert {k: v['frames_required'] for k,v in DETECTOR_CONFIGS.items()} == {'fire':5,'helmet':3,'forklift':1,'person':1}, DETECTOR_CONFIGS"` exit 0
      (cwd 는 프로젝트 루트, 또는 `cd ai_agent && python -c "from detector_configs import DETECTOR_CONFIGS; ..."`)
    - 기존 conf_thres 값 무손상: `grep -E '"conf_thres":\s*0\.10' ai_agent/detector_configs.py` 1건 (fire), `grep -E '"conf_thres":\s*0\.5' ai_agent/detector_configs.py` 1건 (helmet)
  </acceptance_criteria>

  <verify>
    <automated>cd ai_agent &amp;&amp; python -c "from detector_configs import DETECTOR_CONFIGS; req={k:v['frames_required'] for k,v in DETECTOR_CONFIGS.items()}; assert req=={'fire':5,'helmet':3,'forklift':1,'person':1}, req; print('OK', req)"</automated>
  </verify>

  <done>
    DETECTOR_CONFIGS 의 4 detector 모두 frames_required 키를 가지며 값이 D-04 와 정확히
    일치 (fire=5, helmet=3, forklift=1, person=1). Phase 1 의 conf_thres / target_classes
    값은 무손상 유지.
  </done>
</task>

<task type="auto">
  <name>Task 2: scheduler.py 의 _detection_buffer + N 연속 검사 분기 추가 (MODEL-02 코어)</name>
  <files>ai_agent/scheduler.py</files>

  <read_first>
    - ai_agent/scheduler.py (line 1-50 imports + 모듈 상태 dict / line 200-265 _process_detection_for_camera)
    - ai_agent/detector_configs.py (Task 1 결과 — frames_required 키 확인)
    - .planning/phases/02-vision-frames-required/02-CONTEXT.md (D-01, D-02, D-05, D-06, D-07)
  </read_first>

  <action>
3 가지 변경:

**1. imports 보강 (line 17-23 근처):**
```python
from __future__ import annotations

import logging
import time
from collections import deque                # 신규 추가
from concurrent.futures import ThreadPoolExecutor, as_completed
from pathlib import Path
from typing import Any, Optional
```

**2. 모듈 상태 dict 추가 (line 42-45 근처, `_detection_cooldown` 바로 아래):**
```python
# (camera_id, event_key) 별 마지막 일반 detector 감지 타임스탬프.
_detection_cooldown: dict[tuple[int, str], float] = {}

# (camera_id, event_key) 별 최근 N 사이클 is_detected 결과 buffer (Phase 2 / MODEL-02).
# - maxlen = 5 (DETECTOR_CONFIGS 의 max frames_required = fire 5).
# - cooldown skip cycle 에서는 push 하지 않음 (D-07).
# - 알람 발사 시 buffer.clear() + cooldown 갱신 (D-02).
# - 1프로세스 PoC 휘발성 OK — 재시작 시 자동 reset (D-06).
_detection_buffer: dict[tuple[int, str], deque[bool]] = {}
```

**3. `_process_detection_for_camera` 의 detect 분기 변경 (line 202-264 전체 교체).**
변경 흐름은 D-07:
1. cooldown 활성 → `[detect_skip_cooldown]` return (현재 동작 유지, push 안 함)
2. capture + cv2.imread (현재 동작 유지)
3. detector.detect(img) → result
4. **신규**: buffer 가져오기 + `buffer.append(result.is_detected)` (cooldown skip 아닐 때만)
5. `if not result.is_detected: return [no_detect]` (현재 동작 유지)
6. **신규**: `n = cfg["frames_required"]`; `recent = list(buffer)[-n:]`;
   `if len(buffer) < n or not all(recent): return [no_alert_yet] f={count}/{n}`
7. 기존 알람 발사 (upload_detection_snapshot + register_ai_event)
8. **신규**: `buffer.clear()` + 기존 cooldown 갱신

전체 교체 코드:
```python
def _process_detection_for_camera(
    bridge: SupabaseBridge,
    settings: Settings,
    event_key: str,
    detector: GenericYoloDetector,
    cfg: dict[str, Any],
    camera: dict[str, Any],
) -> str:
    camera_id = int(camera["camera_id"])
    rtsp_url = camera["live_url_detail"]

    cooldown_key = (camera_id, event_key)
    last_ts = _detection_cooldown.get(cooldown_key)
    now = time.time()
    if last_ts and (now - last_ts) < settings.detectors_cooldown_min * 60:
        # cooldown 활성 — detect 자체를 skip 하므로 buffer push 도 하지 않음 (D-07).
        return f"[detect_skip_cooldown] camera_id={camera_id} event={event_key}"

    tmp_name = f"detect_{event_key}_{camera_id}_{int(now * 1000)}.jpg"
    tmp_path = settings.snapshot_tmp_dir / tmp_name
    try:
        capture(
            rtsp_url,
            tmp_path,
            ffmpeg_bin=settings.ffmpeg_bin,
            seek_seconds=settings.detectors_demo_seek_sec,
        )
        img = cv2.imread(str(tmp_path))
        if img is None:
            return f"[DETECT_ERR] camera_id={camera_id} event={event_key}: cv2.imread failed"

        result = detector.detect(img)

        # Phase 2 MODEL-02: buffer push (cooldown skip 이 아닌 모든 detect 결과를 누적).
        # maxlen 은 DETECTOR_CONFIGS 의 max frames_required (현재 fire 5).
        # buffer 미존재 시 lazy 생성. is_detected=False 도 push (연속성 깨뜨리는 신호).
        frames_required = int(cfg.get("frames_required", 1))
        buffer = _detection_buffer.get(cooldown_key)
        if buffer is None:
            buffer = deque(maxlen=max(5, frames_required))
            _detection_buffer[cooldown_key] = buffer
        buffer.append(bool(result.is_detected))

        if not result.is_detected:
            return (
                f"[no_detect] camera_id={camera_id} event={event_key} "
                f"inf={result.inference_ms:.0f}ms"
            )

        # Phase 2 MODEL-02: N 연속 검사 — 최근 N 결과가 모두 True 일 때만 알람 발사.
        # forklift/person 의 N=1 도 동일 코드 경로 (D-04, 분기 없이 즉시 통과).
        recent = list(buffer)[-frames_required:]
        if len(buffer) < frames_required or not all(recent):
            return (
                f"[no_alert_yet] camera_id={camera_id} event={event_key} "
                f"frames={sum(recent)}/{frames_required} "
                f"conf={result.confidence:.2f}"
            )

        public_url, _path = bridge.upload_detection_snapshot(
            camera_id, cfg.get("storage_prefix", event_key), tmp_path
        )
        event = bridge.register_ai_event(
            camera_id=camera_id,
            event_name=cfg["event_name"],
            risk_level=cfg["risk_level"],
            accuracy=float(result.confidence or 0.0),
            image_url=public_url,
        )
        # Phase 2 MODEL-02: 알람 발사 후 buffer reset (D-02).
        # 다음 알람은 cooldown 만료 + buffer 재누적 N 연속 둘 다 충족 시에만 발생.
        buffer.clear()
        _detection_cooldown[cooldown_key] = now
        event_id = event.get("event_id")
        return (
            f"[DETECT] camera_id={camera_id} event={event_key} "
            f"label={result.label} conf={result.confidence:.2f} "
            f"event_id={event_id} url={public_url}"
        )
    except SnapshotError as e:
        return f"[DETECT_SNAPSHOT_ERR] camera_id={camera_id} event={event_key}: {e}"
    except Exception as e:
        return f"[DETECT_ERR] camera_id={camera_id} event={event_key}: {e!r}"
    finally:
        try:
            tmp_path.unlink(missing_ok=True)
        except OSError:
            pass
```

또한 `run_detection_cycle` (line 267-307) 의 log 분기에 `[no_alert_yet]` 도 DEBUG 로 분류
한다 (line ~303 근처):
```python
if msg.startswith(("[no_detect]", "[detect_skip_cooldown]", "[no_alert_yet]")):
    log.debug(msg)
else:
    log.info(msg)
```

**구현 노트:**
- `deque[bool]` 타입 어노테이션은 Python 3.9+ 의 `from __future__ import annotations`
  덕분에 그대로 동작 (문자열 평가). 필요시 `"deque[bool]"` 문자열 형태로도 OK.
- buffer.maxlen 은 항상 5 이상 (현재 fire=5 가 최대). 향후 N>5 detector 추가 시
  `max(5, frames_required)` 가 동적 보장.
- `bool(result.is_detected)` — DetectResult.is_detected 는 dataclass field 이지만
  방어적으로 명시 cast. mock 가 truthy 객체 반환해도 buffer 는 진짜 bool 만 담음.
- `[no_alert_yet]` log prefix 신규 — scheduler 컨벤션 따름.
- `_fall_cooldown` / `run_fall_cycle` 등 쓰러짐 경로는 손대지 않음 (Phase 2 범위 밖).
  </action>

  <acceptance_criteria>
    - `grep -c "_detection_buffer" ai_agent/scheduler.py` >= 4 (선언 1 + 함수 내 사용 3+)
    - `grep -c "from collections import deque" ai_agent/scheduler.py` == 1
    - `grep -c "no_alert_yet" ai_agent/scheduler.py` >= 2 (return 메시지 1 + run_detection_cycle 분기 1)
    - `grep -c "buffer.clear()" ai_agent/scheduler.py` >= 1
    - `grep -c 'cfg\.get("frames_required"' ai_agent/scheduler.py` >= 1 (또는 `cfg["frames_required"]`)
    - `python -c "import ast; tree=ast.parse(open('ai_agent/scheduler.py',encoding='utf-8').read()); print('OK syntax')"` exit 0
    - 기존 동작 무손상: `grep -c "register_ai_event" ai_agent/scheduler.py` >= 2 (fall 1 + detect 1)
  </acceptance_criteria>

  <verify>
    <automated>python -c "import ast; ast.parse(open('ai_agent/scheduler.py',encoding='utf-8').read()); print('OK syntax')" &amp;&amp; grep -c "_detection_buffer" ai_agent/scheduler.py</automated>
  </verify>

  <done>
    scheduler.py 의 imports 에 `deque` 추가, 모듈 상태에 `_detection_buffer` dict 추가,
    `_process_detection_for_camera` 가 D-07 6단계 흐름 (cooldown skip → capture → detect →
    buffer push → no_detect early return → N 연속 검사 → 알람 + clear) 으로 동작.
    `run_detection_cycle` log 분기에 `[no_alert_yet]` 추가. 파이썬 syntax 정상.
  </done>
</task>

<task type="auto">
  <name>Task 3: ai_agent/tests/ 패키지 + test_scheduler_buffer.py 단위 테스트 (MODEL-02 검증)</name>
  <files>
    ai_agent/tests/__init__.py,
    ai_agent/tests/test_scheduler_buffer.py
  </files>

  <read_first>
    - ai_agent/scheduler.py (Task 2 결과 — _detection_buffer / _detection_cooldown / _process_detection_for_camera)
    - ai_agent/detector_configs.py (Task 1 결과 — frames_required 키)
    - .planning/phases/02-vision-frames-required/02-CONTEXT.md (D-03 의 5 시퀀스 케이스)
  </read_first>

  <action>
**1. `ai_agent/tests/__init__.py` 빈 파일 생성** (pytest 패키지 발견 마커):
```python
```
(완전 빈 파일. 0 byte 또는 한 줄 docstring `"""ai_agent unit tests."""` OK.)

**2. `ai_agent/tests/test_scheduler_buffer.py` 신규 작성.**

핵심 전략:
- `_process_detection_for_camera` 는 외부 의존 (capture, cv2.imread, bridge, detector) 다수 → mocking 필수.
- pytest fixture 로 모듈 상태 dict (`_detection_buffer`, `_detection_cooldown`) 초기화.
- `monkeypatch` 로 외부 의존 stub.
- mock detector 는 `result.is_detected` boolean 만 변화시키며 confidence/label 은 고정.
- D-03 의 5 시퀀스 케이스 모두 테스트 + N=1 forklift 즉시 알람 + cooldown 상호작용.

테스트 케이스 (총 8개):
1. `test_n5_consecutive_true_fires_one_alert` — fire (N=5) 시퀀스 [T,T,T,T,T] → register_ai_event 1회.
2. `test_n5_breaks_no_alert` — fire (N=5) 시퀀스 [T,T,T,T,F] → register_ai_event 0회.
3. `test_n5_late_consecutive_fires_alert` — fire (N=5) 시퀀스 [F,T,T,T,T,T] → 1회 (last 5 모두 True).
4. `test_n5_post_alert_clear_then_re_accumulate` — [T,T,T,T,T,T] (cooldown 우회) 후 [T] → 2번째 cycle 은 알람 X (clear 후 재누적).
5. `test_n1_forklift_fires_immediately` — forklift (N=1) 시퀀스 [T] → 1회.
6. `test_n1_no_detect_no_alert` — forklift (N=1) 시퀀스 [F] → 0회 (buffer 에 False 가 들어가도 is_detected=False 면 early return).
7. `test_cooldown_active_skips_buffer_push` — cooldown 활성 (last_ts 직전) 시 buffer push 없음 + return prefix == `[detect_skip_cooldown]`.
8. `test_n3_helmet_consecutive` — helmet (N=3) 시퀀스 [T,T,T] → 1회. [T,T] → 0회.

**구현 코드:**

```python
"""Phase 2 MODEL-02 — _process_detection_for_camera 의 frames_required buffer 동작 테스트.

D-03 (CONTEXT.md): pytest unit test (mocked detector + bridge + capture).
시퀀스 입력별 알람 발사 횟수 검증. cooldown 상호작용 포함.
"""

from __future__ import annotations

import sys
import time
from pathlib import Path
from types import SimpleNamespace
from unittest.mock import MagicMock

import pytest

# ai_agent 패키지 import 보장 (cwd 가 프로젝트 루트일 때).
AGENT_DIR = Path(__file__).resolve().parents[1]
if str(AGENT_DIR) not in sys.path:
    sys.path.insert(0, str(AGENT_DIR))

import scheduler  # noqa: E402  (sys.path 조정 후)


# ──────────────────────────────────────────────
# fixtures
# ──────────────────────────────────────────────
@pytest.fixture(autouse=True)
def reset_module_state():
    """매 테스트마다 _detection_buffer / _detection_cooldown 초기화."""
    scheduler._detection_buffer.clear()
    scheduler._detection_cooldown.clear()
    yield
    scheduler._detection_buffer.clear()
    scheduler._detection_cooldown.clear()


@pytest.fixture
def stub_external(monkeypatch, tmp_path):
    """capture / cv2.imread 를 no-op stub 으로 대체.

    capture() 는 tmp_path 에 빈 jpg 를 만들지 않아도 cv2.imread 를 stub 했으므로 OK.
    """
    monkeypatch.setattr(scheduler, "capture", lambda *a, **kw: None)

    fake_img = MagicMock(name="fake_bgr_ndarray")
    monkeypatch.setattr(scheduler.cv2, "imread", lambda _path: fake_img)
    return fake_img


def make_settings(tmp_path):
    return SimpleNamespace(
        snapshot_tmp_dir=tmp_path,
        ffmpeg_bin="ffmpeg",
        detectors_demo_seek_sec=0.0,
        detectors_cooldown_min=10,
    )


def make_bridge():
    """register_ai_event / upload_detection_snapshot mock 로 호출 횟수 카운트."""
    bridge = MagicMock(name="bridge")
    bridge.upload_detection_snapshot.return_value = ("https://example.test/snap.jpg", "obj/path")
    bridge.register_ai_event.return_value = {"event_id": 999}
    return bridge


def make_detector(sequence: list[bool]):
    """sequence 에서 한 번에 하나씩 is_detected 를 꺼내는 detector mock."""
    iterator = iter(sequence)

    def _detect(_img):
        try:
            flag = next(iterator)
        except StopIteration:
            flag = False
        return SimpleNamespace(
            is_detected=flag,
            confidence=0.42 if flag else None,
            bbox=None,
            label="x" if flag else None,
            inference_ms=12.3,
        )

    detector = MagicMock(name="detector")
    detector.detect.side_effect = _detect
    return detector


def fire_cfg():
    return {
        "event_name": "화재",
        "risk_level": "DANGER",
        "frames_required": 5,
        "storage_prefix": "fire",
    }


def helmet_cfg():
    return {
        "event_name": "안전모 미착용",
        "risk_level": "WARNING",
        "frames_required": 3,
        "storage_prefix": "helmet",
    }


def forklift_cfg():
    return {
        "event_name": "지게차 진입",
        "risk_level": "WARNING",
        "frames_required": 1,
        "storage_prefix": "forklift",
    }


CAM = {"camera_id": 1, "live_url_detail": "rtsp://stub"}


def run_n_cycles(n, *, bridge, settings, event_key, detector, cfg):
    results = []
    for _ in range(n):
        msg = scheduler._process_detection_for_camera(
            bridge, settings, event_key, detector, cfg, CAM
        )
        results.append(msg)
    return results


# ──────────────────────────────────────────────
# tests — D-03 5 시퀀스 + 추가 3개
# ──────────────────────────────────────────────
def test_n5_consecutive_true_fires_one_alert(stub_external, tmp_path):
    bridge = make_bridge()
    detector = make_detector([True, True, True, True, True])
    msgs = run_n_cycles(
        5, bridge=bridge, settings=make_settings(tmp_path),
        event_key="fire", detector=detector, cfg=fire_cfg(),
    )
    assert bridge.register_ai_event.call_count == 1
    assert msgs[-1].startswith("[DETECT]"), msgs
    # 첫 4 cycle 은 [no_alert_yet]
    for m in msgs[:4]:
        assert m.startswith("[no_alert_yet]"), m


def test_n5_breaks_no_alert(stub_external, tmp_path):
    bridge = make_bridge()
    detector = make_detector([True, True, True, True, False])
    msgs = run_n_cycles(
        5, bridge=bridge, settings=make_settings(tmp_path),
        event_key="fire", detector=detector, cfg=fire_cfg(),
    )
    assert bridge.register_ai_event.call_count == 0
    assert msgs[-1].startswith("[no_detect]"), msgs


def test_n5_late_consecutive_fires_alert(stub_external, tmp_path):
    """[F,T,T,T,T,T] — last 5 모두 True 면 6번째 cycle 에 알람 발사."""
    bridge = make_bridge()
    detector = make_detector([False, True, True, True, True, True])
    msgs = run_n_cycles(
        6, bridge=bridge, settings=make_settings(tmp_path),
        event_key="fire", detector=detector, cfg=fire_cfg(),
    )
    assert bridge.register_ai_event.call_count == 1
    assert msgs[-1].startswith("[DETECT]"), msgs


def test_n5_post_alert_clear_then_re_accumulate(stub_external, tmp_path, monkeypatch):
    """알람 발사 후 buffer.clear() — 직후 [T] 단일 cycle 은 알람 0회.

    cooldown 도 갱신되므로 cooldown 우회 위해 detectors_cooldown_min=0 설정.
    """
    bridge = make_bridge()
    detector = make_detector([True] * 6)
    settings = SimpleNamespace(
        snapshot_tmp_dir=tmp_path,
        ffmpeg_bin="ffmpeg",
        detectors_demo_seek_sec=0.0,
        detectors_cooldown_min=0,  # cooldown 우회
    )
    msgs = run_n_cycles(
        6, bridge=bridge, settings=settings,
        event_key="fire", detector=detector, cfg=fire_cfg(),
    )
    # 5번째 cycle 에 1회 알람 + clear → 6번째 cycle 은 buffer=[True], len<5 → no_alert_yet
    assert bridge.register_ai_event.call_count == 1
    assert msgs[4].startswith("[DETECT]"), msgs
    assert msgs[5].startswith("[no_alert_yet]"), msgs[5]


def test_n1_forklift_fires_immediately(stub_external, tmp_path):
    bridge = make_bridge()
    detector = make_detector([True])
    msgs = run_n_cycles(
        1, bridge=bridge, settings=make_settings(tmp_path),
        event_key="forklift", detector=detector, cfg=forklift_cfg(),
    )
    assert bridge.register_ai_event.call_count == 1
    assert msgs[0].startswith("[DETECT]"), msgs


def test_n1_no_detect_no_alert(stub_external, tmp_path):
    bridge = make_bridge()
    detector = make_detector([False])
    msgs = run_n_cycles(
        1, bridge=bridge, settings=make_settings(tmp_path),
        event_key="forklift", detector=detector, cfg=forklift_cfg(),
    )
    assert bridge.register_ai_event.call_count == 0
    assert msgs[0].startswith("[no_detect]"), msgs


def test_cooldown_active_skips_buffer_push(stub_external, tmp_path):
    """cooldown 활성 cycle 에서는 detect 실행조차 안 함 → buffer push 없음 (D-07)."""
    bridge = make_bridge()
    detector = make_detector([True, True])  # 두 번째 호출은 일어나지 않아야 함
    settings = make_settings(tmp_path)

    # 사전 cooldown 설정 — 방금 알람 발사한 것처럼 위장
    scheduler._detection_cooldown[(1, "fire")] = time.time()

    msg = scheduler._process_detection_for_camera(
        bridge, settings, "fire", detector, fire_cfg(), CAM,
    )
    assert msg.startswith("[detect_skip_cooldown]"), msg
    # detector.detect 는 호출되지 않아야 함
    assert detector.detect.call_count == 0
    # buffer 도 비어 있어야 함
    assert (1, "fire") not in scheduler._detection_buffer or len(scheduler._detection_buffer[(1, "fire")]) == 0
    # bridge 호출 없음
    assert bridge.register_ai_event.call_count == 0


def test_n3_helmet_consecutive(stub_external, tmp_path):
    bridge = make_bridge()
    detector = make_detector([True, True, True])
    msgs = run_n_cycles(
        3, bridge=bridge, settings=make_settings(tmp_path),
        event_key="helmet", detector=detector, cfg=helmet_cfg(),
    )
    assert bridge.register_ai_event.call_count == 1
    assert msgs[-1].startswith("[DETECT]"), msgs
    # 첫 2 cycle 은 [no_alert_yet]
    assert msgs[0].startswith("[no_alert_yet]"), msgs[0]
    assert msgs[1].startswith("[no_alert_yet]"), msgs[1]
```

**구현 노트:**
- `sys.path.insert` 트릭 — `ai_agent/tests/test_*.py` 가 cwd=프로젝트 루트에서도 cwd=`ai_agent/`
  에서도 모두 import 가능하도록. `from scheduler import ...` 는 `ai_agent/` 가 sys.path 에 있어야 함.
- `monkeypatch.setattr(scheduler, "capture", ...)` — scheduler 모듈이 import 한 함수를 직접 교체.
  `from snapshot import capture` 가 scheduler 안의 `scheduler.capture` 로 노출됨.
- `cv2.imread` 는 `scheduler.cv2.imread` 로 monkeypatch — `import cv2` 후 `cv2.imread` 는
  scheduler 모듈 내에서 `cv2.imread` 로 접근되므로 `scheduler.cv2.imread` 로도 patch 가능.
- `detectors_cooldown_min=0` — Test 4 에서 알람 후 즉시 다음 cycle 도 cooldown skip 안 되도록.
- `time.time()` 직접 호출 — Test 7 에서 cooldown 활성 시뮬레이션. mock time 까지는 D-03 범위 밖
  (CONTEXT 의 "cooldown 와 상호작용도 unit test 에서 검증 (mock time)" 은 권장이며 단순 last_ts
  주입으로 cover 가능).
- `MagicMock` 의 `bridge.register_ai_event.call_count` — D-03 의 "알람 발사 횟수" 검증 핵심.

**3. pytest 실행:**
```bash
cd ai_agent && python -m pytest tests/test_scheduler_buffer.py -v
```

또는 (프로젝트 루트):
```bash
python -m pytest ai_agent/tests/test_scheduler_buffer.py -v
```

cwd 를 `ai_agent/` 로 두는 것이 import 경로 보장. 둘 다 동작해야 정답 (sys.path 트릭 덕).
  </action>

  <acceptance_criteria>
    - `ls ai_agent/tests/__init__.py` 존재
    - `ls ai_agent/tests/test_scheduler_buffer.py` 존재
    - `cd ai_agent && python -m pytest tests/test_scheduler_buffer.py -v` exit 0
    - `cd ai_agent && python -m pytest tests/test_scheduler_buffer.py -v` 출력에 `passed` 토큰 + 8 tests 모두 PASS
    - `grep -cE "^def test_" ai_agent/tests/test_scheduler_buffer.py` >= 6 (D-03 의 5 시퀀스 케이스 + 추가)
    - `grep -c "register_ai_event.call_count" ai_agent/tests/test_scheduler_buffer.py` >= 4
  </acceptance_criteria>

  <verify>
    <automated>cd ai_agent &amp;&amp; python -m pytest tests/test_scheduler_buffer.py -v --tb=short</automated>
  </verify>

  <done>
    `ai_agent/tests/__init__.py` 와 `ai_agent/tests/test_scheduler_buffer.py` 가 존재
    하며 pytest 가 6개 이상 테스트를 모두 PASS 한다 (exit 0). D-03 의 5 시퀀스 케이스
    (N=5 모두 True / 4-True+1-False / 늦은 5 연속 / 알람 후 reset+재누적 / N=1 즉시)
    가 모두 cover 됨. cooldown 활성 cycle 에서 buffer push 가 일어나지 않음을 확인하는
    테스트 1개 포함.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Phase 2 는 순수 코드 변경 — `ai_agent/scheduler.py` 의 inline 로직 + `ai_agent/detector_configs.py`
의 dict 추가 + 신규 단위 테스트. 새로운 trust boundary 도입 없음.)

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 2 는 외부 입출력 / 인증 / 네트워크 / Storage / DB 스키마 변경 없음. 기존 detection 사이클 내부 로직만 수정. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | `_detection_buffer` 모듈 상태 | accept | 1 프로세스 PoC 휘발 메모리. 다른 코드 경로에서 직접 조작 가능하나, ai_agent 프로세스 자체가 이미 trusted boundary 안. v1.1 다중 프로세스 진입 시 영속화/네임스페이스 격리 검토 (D-06 deferred). |
| T-02-02 | D (DoS) | buffer 무한 성장 | mitigate | `deque(maxlen=max(5, frames_required))` 로 maxlen 강제 (Task 2 코드). 카메라 × event_key 조합당 최대 5 bool = 5 byte. 활성 detector 4 × 활성 카메라 5 = 20 deque × 5 bool = 100 byte 상한. |
| T-02-03 | I (Information disclosure) | `[no_alert_yet]` log message | accept | 새 log prefix 가 frames N/N + conf 노출. 현재 `[DETECT]` log 도 conf + event_id 이미 노출 중 (line 254). 동일 trust 레벨. |

(S/R/E categories: Phase 2 가 인증·서명·권한 경계 변경 없음 — 해당 없음.)

**No new threats from Phase 2.** D-08 의 "fire conf 0.10 + frames_required 5 결합" 은
오히려 false positive 알람 폭증 (= I/D 카테고리 위험) 을 *완화* 하는 방향.
</threat_model>

<verification>

## Phase-level checks (3 tasks 모두 완료 후)

1. **DETECTOR_CONFIGS 키 정합성** — Task 1 acceptance:
   `cd ai_agent && python -c "from detector_configs import DETECTOR_CONFIGS; req={k:v['frames_required'] for k,v in DETECTOR_CONFIGS.items()}; assert req=={'fire':5,'helmet':3,'forklift':1,'person':1}, req; print('OK', req)"`

2. **scheduler.py syntax + 키 사용** — Task 2 acceptance:
   `python -c "import ast; ast.parse(open('ai_agent/scheduler.py',encoding='utf-8').read()); print('OK')"` +
   `grep -c "_detection_buffer" ai_agent/scheduler.py` >= 4

3. **pytest 통과** — Task 3 acceptance:
   `cd ai_agent && python -m pytest tests/test_scheduler_buffer.py -v` exit 0, 8 tests PASS

4. **MODEL-03 흡수 확인** (no-op) — Phase 1 SUMMARY 가 이미 `helmet conf_thres=0.5 +
   target_classes=['head']` lock 한 것을 재확인:
   `grep -E '"conf_thres":\s*0\.5' ai_agent/detector_configs.py` 1건 (helmet) +
   `grep "\['head'\]" ai_agent/detector_configs.py` 1건. **재진행 없음** (D-08 — fire 는 D-19 fallback).

5. **선택 검증 (옵션, Phase 5 EVAL 에서 본격)** — `--once-detect` 1회 실행 시 신규
   `[no_alert_yet] frames=1/5 ...` log 가 fire 사이클에 보이는지 (Phase 1 의 새 영상 기준).
   본 Plan 의 SC 는 unit test 로 한정 (D-03).

</verification>

<success_criteria>

ROADMAP Phase 2 의 4 SC 중 본 Plan 이 cover:

- [x] **SC #1 (MODEL-01)**: `DETECTOR_CONFIGS` 에 `frames_required` 키 + fire=5/helmet=3/forklift=1/person=1 — Task 1 grep + python assert
- [x] **SC #2 (MODEL-02)**: scheduler 의 frame buffer + N 연속 룰 + 단일 spike 입력 시 알람 0회 / N 연속 입력 시 정확히 1회 — Task 3 의 8 pytest tests
- [x] **SC #3 (MODEL-03)**: helmet conf_thres=0.5 + target_classes=['head'] 복원 — **이미 Phase 1 에서 lock 완료**, fire 는 D-19 fallback (D-08). 본 Plan 은 no-op (재진행 없음). Phase 1 SUMMARY 의 `01-SUMMARY.md` MODEL-03 partial PASS 가 evidence.
- [ ] **SC #4 (E2E `--once-detect` 검증)**: Phase 5 EVAL 에서 본격. 본 Plan 의 unit test 가 의미 동등성 입증 (D-03).

본 Plan 완료 조건:
- [x] Task 1 verify 통과 (frames_required 4개 키 정확)
- [x] Task 2 verify 통과 (scheduler syntax + grep)
- [x] Task 3 verify 통과 (pytest exit 0, 8 tests PASS)
- [x] threat_model 의 신규 위협 없음 확인 (T-02-01·02·03 모두 accept/mitigate)

</success_criteria>

<output>
After completion, create `.planning/phases/02-vision-frames-required/02-01-SUMMARY.md`
following the template at `~/.claude/get-shit-done/templates/summary.md`.

SUMMARY 에 포함할 핵심:
- DETECTOR_CONFIGS 변경 diff (4 항목 × frames_required 키)
- scheduler.py 변경 diff (imports + _detection_buffer + _process_detection_for_camera 6단계 흐름 + run_detection_cycle log 분기)
- pytest 결과 (8 tests PASS evidence)
- D-08 의 fire D-19 fallback + frames_required 5 결합 효과 — Phase 1 SUMMARY 의
  `event_id 22-25 baseline` 과 비교 (단일 cycle 알람 → 5 cycle 누적 알람으로 동작 변경됨)
- MODEL-03 no-op 확인 — Phase 1 SUMMARY 의 partial PASS 가 그대로 유효
- 다음 Phase 3 (FUSION-01·02) 진입 가능 여부 — frames_required 룰이 IoU/공간 매칭 fusion
  에도 동일하게 재사용될 예정 (ROADMAP Phase 3 SC #3)
</output>
