---
phase: 04-watch-j2208a-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/010_watch_pipeline.sql
autonomous: true
requirements: [WATCH-01]
must_haves:
  truths:
    - "supabase/migrations/010_watch_pipeline.sql 가 적용되어 devices 테이블에 mac_address/firmware_version/last_comm_at 3 컬럼이 추가된다"
    - "raw_events / wear_state_events / minute_summary / safety_alerts 4 테이블이 생성된다"
    - "raw_events 에 (device_id, ts_truncated_to_second, raw_hash) UNIQUE 제약이 존재해 1초 내 동일 raw 가 ON CONFLICT DO NOTHING 으로 무시된다"
    - "pg_cron 1시간 주기 cleanup job (cleanup_raw_events_hourly) 이 등록되어 7일 초과 raw_events 행을 삭제한다"
    - "4 신규 테이블 모두 RLS ENABLED 이며 정책은 등록되지 않아 service_role 만 접근 가능하다 (D-03 시스템 테이블 패턴)"
    - "testuser1 의 J2208A 워치 디바이스 시드 1행이 devices 테이블에 존재한다 (mac_address='21:02:02:06:01:69', user_id='testuser1', device_type='WATCH')"
  artifacts:
    - path: "supabase/migrations/010_watch_pipeline.sql"
      provides: "ALTER devices + 4 신규 테이블 + RLS + pg_cron + testuser1 시드"
      contains: "ALTER TABLE public.devices ADD COLUMN mac_address VARCHAR(17)"
    - path: "supabase/migrations/010_watch_pipeline.sql"
      provides: "raw_events 테이블 (UNIQUE 생성 컬럼 + ON CONFLICT DO NOTHING 가능)"
      contains: "CREATE TABLE IF NOT EXISTS public.raw_events"
    - path: "supabase/migrations/010_watch_pipeline.sql"
      provides: "pg_cron cleanup job"
      contains: "cron.schedule('cleanup_raw_events_hourly'"
  key_links:
    - from: "raw_events.device_id"
      to: "devices.device_id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES public\\.devices\\(device_id\\) ON DELETE CASCADE"
    - from: "raw_events"
      to: "pg_cron"
      via: "cleanup_raw_events_hourly job"
      pattern: "DELETE FROM .*raw_events WHERE ts <"
    - from: "devices.mac_address"
      to: "testuser1 시드 행"
      via: "INSERT ... ON CONFLICT (serial_number) DO UPDATE"
      pattern: "21:02:02:06:01:69"
---

<objective>
J2208A 워치 1인 파이프라인이 적재할 Supabase 스키마 (`010_watch_pipeline.sql`) 를
1개 마이그레이션 파일로 작성하고 적용한다. CONTEXT.md D-01·D-02·D-03 의 결정에
따라 `devices` 를 ALTER 하고, `raw_events`/`wear_state_events`/`minute_summary`/
`safety_alerts` 4 신규 테이블을 만들고, RLS 를 시스템 패턴 (정책 X) 으로 설정하며,
pg_cron 1시간 주기 cleanup job 과 testuser1 시드를 추가한다.

**Important — 6 vs 5 table 차이:** ROADMAP Phase 4 SC #1 은 "6 테이블" 을 언급하지만
CONTEXT.md D-01 가 권위 있는 결정 — `workers` 별도 테이블은 만들지 않고 기존
`profiles` + `devices.user_id` 매핑으로 충족한다. 본 phase 산출물은 ALTER devices +
4 신규 테이블 = "확장 + 신규 4" 구조.

Purpose: WATCH-02~05 가 적재할 영구 스키마를 먼저 확정해야 04-02 (Python 패키지
unit test 의 컬럼·타입 모킹) 와 04-03 (BLE 클라이언트의 service_role insert) 이
의존할 수 있다.

Output:
- `supabase/migrations/010_watch_pipeline.sql` 1 파일 (ALTER + CREATE 4 + RLS +
  pg_cron + 시드)
- `supabase db push` 또는 `psql` 적용 결과 = 4 테이블 + 1 cron job 등록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@docs/J2208A_안전관리_시스템_PLAN.md
@supabase/migrations/001_extensions.sql
@supabase/migrations/002_tables.sql
@supabase/migrations/003_rls_policies.sql

<interfaces>
<!-- 기존 devices 테이블 (002_tables.sql:142-150) — ALTER 대상 -->
```sql
CREATE TABLE IF NOT EXISTS public.devices (
    device_id       SERIAL PRIMARY KEY,
    device_type     VARCHAR(20) NOT NULL,
    serial_number   VARCHAR(50) UNIQUE NOT NULL,
    battery_level   INTEGER DEFAULT 100,
    gps_status      VARCHAR(20) DEFAULT 'OFF',
    user_id         VARCHAR(50),
    updated_at      TIMESTAMP DEFAULT now()
);
```

<!-- 기존 RLS 시스템 테이블 패턴 (003_rls_policies.sql) -->
ENABLE ROW LEVEL SECURITY 만 + 정책 미등록 → service_role 만 접근.

<!-- pg_cron 활성화됨 (001_extensions.sql) → cron.schedule() 즉시 사용 가능 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 010_watch_pipeline.sql 작성 — ALTER devices + raw_events + dedup 인프라</name>
  <files>supabase/migrations/010_watch_pipeline.sql</files>
  <read_first>
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-01·D-02 정확 컬럼·타입)
    - supabase/migrations/002_tables.sql lines 142-157 (devices/device_watches 기존 스키마)
    - supabase/migrations/001_extensions.sql (pg_cron 활성화 확인)
    - docs/J2208A_안전관리_시스템_PLAN.md §5 (저장 스키마 reference)
  </read_first>
  <action>
    `supabase/migrations/010_watch_pipeline.sql` 신규 파일 생성. 헤더 주석에
    "Phase 4 — Watch J2208A 1-person pipeline schema (per CONTEXT.md D-01/D-02/D-03)"
    명시. 다음 SQL 을 정확히 이 순서로 포함:

    **(A) ALTER devices (D-01):**
    ```sql
    ALTER TABLE public.devices
        ADD COLUMN IF NOT EXISTS mac_address      VARCHAR(17) UNIQUE,
        ADD COLUMN IF NOT EXISTS firmware_version VARCHAR(20),
        ADD COLUMN IF NOT EXISTS last_comm_at     TIMESTAMPTZ;
    ```

    **(B) raw_events (D-01·D-02 — 7일 TTL, 1초 dedup):**
    ```sql
    CREATE TABLE IF NOT EXISTS public.raw_events (
        raw_id      BIGSERIAL PRIMARY KEY,
        device_id   INTEGER NOT NULL REFERENCES public.devices(device_id) ON DELETE CASCADE,
        ts          TIMESTAMPTZ NOT NULL DEFAULT now(),
        cmd         SMALLINT NOT NULL,
        raw_hex     TEXT NOT NULL,
        parsed      JSONB,
        ts_truncated_to_second TIMESTAMPTZ
            GENERATED ALWAYS AS (date_trunc('second', ts)) STORED,
        raw_hash    TEXT
            GENERATED ALWAYS AS (md5(raw_hex)) STORED,
        UNIQUE (device_id, ts_truncated_to_second, raw_hash)
    );
    CREATE INDEX IF NOT EXISTS idx_raw_events_device_ts
        ON public.raw_events (device_id, ts DESC);
    ```
    Generated columns + UNIQUE 조합으로 INSERT 시 `ON CONFLICT DO NOTHING` 가능
    (per D-02). 동일 1초 내 같은 raw_hex = duplicate 로 간주.

    검증: 파일을 작성한 후 `grep -c "GENERATED ALWAYS AS" supabase/migrations/010_watch_pipeline.sql`
    가 ≥ 2 를 반환해야 한다.
  </action>
  <verify>
    <automated>grep -c "GENERATED ALWAYS AS" supabase/migrations/010_watch_pipeline.sql | grep -q "^[2-9]" &amp;&amp; grep -q "ALTER TABLE public.devices" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "mac_address VARCHAR(17) UNIQUE" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "REFERENCES public.devices(device_id) ON DELETE CASCADE" supabase/migrations/010_watch_pipeline.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ALTER TABLE public.devices" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "mac_address VARCHAR(17)" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "firmware_version VARCHAR(20)" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "last_comm_at" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "CREATE TABLE.*public.raw_events" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "GENERATED ALWAYS AS (date_trunc('second', ts)) STORED" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "GENERATED ALWAYS AS (md5(raw_hex)) STORED" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "UNIQUE (device_id, ts_truncated_to_second, raw_hash)" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
  </acceptance_criteria>
  <done>
    `010_watch_pipeline.sql` 에 D-01 의 ALTER devices + D-02 의 raw_events 테이블
    (generated columns + UNIQUE constraint) 가 정확한 SQL 로 들어가 있다.
  </done>
</task>

<task type="auto">
  <name>Task 2: wear_state_events / minute_summary / safety_alerts 3 테이블 추가 (per D-01)</name>
  <files>supabase/migrations/010_watch_pipeline.sql</files>
  <read_first>
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-01 4 테이블 컬럼 정의)
    - supabase/migrations/010_watch_pipeline.sql (Task 1 산출물)
    - docs/J2208A_안전관리_시스템_PLAN.md §5 (저장 스키마 reference)
  </read_first>
  <action>
    Task 1 산출물에 다음 SQL 을 append 한다:

    **(C) wear_state_events (D-01 — 영구):**
    ```sql
    CREATE TABLE IF NOT EXISTS public.wear_state_events (
        event_id   BIGSERIAL PRIMARY KEY,
        device_id  INTEGER NOT NULL REFERENCES public.devices(device_id) ON DELETE CASCADE,
        ts         TIMESTAMPTZ NOT NULL DEFAULT now(),
        from_state TEXT NOT NULL,
        to_state   TEXT NOT NULL,
        reason     JSONB,
        CHECK (from_state IN ('OFF','WARMUP','TRANSIENT','WORN','ABNORMAL')),
        CHECK (to_state IN ('OFF','WARMUP','TRANSIENT','WORN','ABNORMAL'))
    );
    CREATE INDEX IF NOT EXISTS idx_wear_state_events_device_ts
        ON public.wear_state_events (device_id, ts DESC);
    ```

    **(D) minute_summary (D-01·D-08 — 1인 × 1분 × 1행):**
    ```sql
    CREATE TABLE IF NOT EXISTS public.minute_summary (
        device_id      INTEGER NOT NULL REFERENCES public.devices(device_id) ON DELETE CASCADE,
        minute_ts      TIMESTAMPTZ NOT NULL,
        hr_median      INTEGER,
        temp_median    NUMERIC(4,1),
        temp_iqr       NUMERIC(4,1),
        steps_delta    INTEGER,
        dominant_state TEXT,
        good_ratio     NUMERIC(3,2),
        PRIMARY KEY (device_id, minute_ts),
        CHECK (dominant_state IS NULL OR dominant_state IN ('OFF','WARMUP','TRANSIENT','WORN','ABNORMAL'))
    );
    ```

    **(E) safety_alerts (D-01·D-09 — 영구):**
    ```sql
    CREATE TABLE IF NOT EXISTS public.safety_alerts (
        alert_id    BIGSERIAL PRIMARY KEY,
        device_id   INTEGER NOT NULL REFERENCES public.devices(device_id) ON DELETE CASCADE,
        alert_type  TEXT NOT NULL,
        severity    TEXT NOT NULL,
        raised_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
        resolved_at TIMESTAMPTZ,
        ack_at      TIMESTAMPTZ,
        reason      JSONB,
        CHECK (alert_type IN ('TACHY','REMOVED','COMMS_LOST')),
        CHECK (severity IN ('CAUTION','WARNING','DANGER'))
    );
    CREATE INDEX IF NOT EXISTS idx_safety_alerts_device_raised
        ON public.safety_alerts (device_id, raised_at DESC);
    ```

    `alert_type` / `severity` / state 컬럼은 CHECK constraint (per CONTEXT.md
    "Claude's Discretion": CHECK 로 시작) 으로 enforce.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS public.wear_state_events" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "CREATE TABLE IF NOT EXISTS public.minute_summary" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "CREATE TABLE IF NOT EXISTS public.safety_alerts" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "PRIMARY KEY (device_id, minute_ts)" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "alert_type IN ('TACHY','REMOVED','COMMS_LOST')" supabase/migrations/010_watch_pipeline.sql</automated>
  </verify>
  <acceptance_criteria>
    - 4 개 CREATE TABLE 문이 모두 존재 (raw_events + 3 신규)
    - minute_summary 의 PK = (device_id, minute_ts) — 1인 × 1분 × 1행 enforce
    - safety_alerts.alert_type CHECK = TACHY/REMOVED/COMMS_LOST 3 값
    - safety_alerts.severity CHECK = CAUTION/WARNING/DANGER 3 값
    - wear_state_events.from_state/to_state CHECK = 5 wear-state 값
  </acceptance_criteria>
  <done>
    D-01 의 4 신규 테이블 정의가 정확한 컬럼·타입·CHECK constraint 으로
    `010_watch_pipeline.sql` 에 들어가 있다.
  </done>
</task>

<task type="auto">
  <name>Task 3: RLS ENABLE + pg_cron cleanup job + testuser1 시드 (per D-02·D-03)</name>
  <files>supabase/migrations/010_watch_pipeline.sql</files>
  <read_first>
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-02·D-03)
    - supabase/migrations/003_rls_policies.sql lines 1-60 (RLS 시스템 테이블 패턴)
    - supabase/migrations/010_watch_pipeline.sql (Task 1·2 산출물)
  </read_first>
  <action>
    Task 2 산출물에 다음 SQL 을 append:

    **(F) RLS ENABLE — 4 테이블 모두 (D-03 시스템 테이블 패턴, 정책 X):**
    ```sql
    ALTER TABLE public.raw_events         ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.wear_state_events  ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.minute_summary     ENABLE ROW LEVEL SECURITY;
    ALTER TABLE public.safety_alerts      ENABLE ROW LEVEL SECURITY;
    -- 정책 미등록 → service_role 전용 (003_rls_policies.sql 의 시스템 테이블 패턴 동일)
    ```

    **(G) pg_cron cleanup job (D-02 — 1시간 주기, 7일 TTL):**
    ```sql
    -- 1시간 주기 raw_events cleanup (per D-02). 001_extensions.sql 에서 pg_cron 활성화됨.
    SELECT cron.schedule(
        'cleanup_raw_events_hourly',
        '0 * * * *',
        $$DELETE FROM public.raw_events WHERE ts < now() - INTERVAL '7 days'$$
    );
    ```
    동일 이름 job 가 이미 있으면 cron.schedule 가 unique violation 을 던지므로,
    안전하게 다음 패턴으로 감싼다:
    ```sql
    DO $$
    BEGIN
        PERFORM cron.unschedule('cleanup_raw_events_hourly');
    EXCEPTION WHEN OTHERS THEN
        NULL;
    END $$;
    SELECT cron.schedule(
        'cleanup_raw_events_hourly',
        '0 * * * *',
        $$DELETE FROM public.raw_events WHERE ts < now() - INTERVAL '7 days'$$
    );
    ```

    **(H) testuser1 J2208A 시드 (D-13 검증을 위한 1행):**
    ```sql
    -- testuser1 의 J2208A 워치 디바이스 시드. serial_number 는 MAC 그대로 사용.
    INSERT INTO public.devices (device_type, serial_number, mac_address, firmware_version, user_id)
    VALUES ('WATCH', 'J2208A-21:02:02:06:01:69', '21:02:02:06:01:69', '0.6.3.9', 'testuser1')
    ON CONFLICT (serial_number) DO UPDATE
        SET mac_address      = EXCLUDED.mac_address,
            firmware_version = EXCLUDED.firmware_version,
            user_id          = EXCLUDED.user_id;
    ```

    파일 끝에 주석으로 적용 명령 명시:
    ```sql
    -- 적용: supabase db push  (또는)  psql $DATABASE_URL -f 010_watch_pipeline.sql
    ```

    그 다음 마이그레이션을 실제로 적용 (`supabase db push` 또는 `psql -f`). 적용
    실패 시 (e.g. supabase CLI 미로그인) STATE.md 의 컨벤션에 따라 명령 + 출력만
    SUMMARY 에 기록하고 Task 는 "파일 작성 완료, 적용은 04-04 직전 수동 적용"
    상태로 close 한다.
  </action>
  <verify>
    <automated>grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "cleanup_raw_events_hourly" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "INTERVAL '7 days'" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -q "21:02:02:06:01:69" supabase/migrations/010_watch_pipeline.sql &amp;&amp; grep -c "ALTER TABLE public\\..*ENABLE ROW LEVEL SECURITY" supabase/migrations/010_watch_pipeline.sql | grep -q "^4$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ALTER TABLE public\\..*ENABLE ROW LEVEL SECURITY" supabase/migrations/010_watch_pipeline.sql` = 4
    - `grep "cron.schedule.*cleanup_raw_events_hourly" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "INTERVAL '7 days'" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - `grep "21:02:02:06:01:69" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1 (testuser1 시드)
    - `grep "ON CONFLICT (serial_number) DO UPDATE" supabase/migrations/010_watch_pipeline.sql` returns ≥ 1
    - 적용 후 (선택, 환경에 따라): `psql -c "\d+ public.raw_events"` 가 generated 컬럼 + UNIQUE 표시
    - 적용 후 (선택): `psql -c "SELECT jobname FROM cron.job WHERE jobname='cleanup_raw_events_hourly'"` 1행 반환
  </acceptance_criteria>
  <done>
    RLS 4 테이블 모두 ENABLE, pg_cron cleanup_raw_events_hourly 등록, testuser1
    워치 디바이스 시드 INSERT 가 마이그레이션 파일에 정확히 들어가 있다.
    가능한 환경에서는 마이그레이션이 실제 DB 에 적용되었다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE 클라이언트 (Python) → Supabase | service_role key 로 raw_events / wear_state_events / minute_summary / safety_alerts 에 직접 INSERT (04-03 산출). 본 plan 은 스키마만 — RLS = service_role 전용으로 격리. |
| 외부 사용자 → 4 신규 테이블 | RLS ENABLE + 정책 미등록 → 차단됨 (D-03). 대시보드는 v1.1 (Phase 6 DEMO-03) 에서 Edge Function 경유로 우회. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | raw_events.device_id | mitigate | FK ON DELETE CASCADE (per 002_tables.sql 패턴). 무효 device_id INSERT 시 FK 위반으로 거부. |
| T-04-02 | I (Information disclosure) | raw_events.raw_hex / parsed JSONB | accept | raw 패킷에 PII 없음 (HR·temp·steps 만). 1인 PoC 단계, v1.1 다중작업자 진입 시 재검토. |
| T-04-03 | I | safety_alerts.reason JSONB | accept | 알림 컨텍스트 (HR median 값 등) 만 저장. PII 없음. |
| T-04-04 | D (DoS) | raw_events 무한 증가 | mitigate | pg_cron 1h cleanup + 7일 TTL (per D-02). dedup UNIQUE 로 1초 내 spam 차단. |
| T-04-05 | E (Elevation) | 4 신규 테이블 외부 노출 | mitigate | RLS ENABLE + 정책 미등록 = service_role 만 접근 (per D-03). |
| T-04-06 | S (Spoofing) | testuser1 시드 행의 mac_address | accept | v1.0 1인 PoC, MAC 고정 매핑. v1.1 매일 QR/NFC 체크인 재검토 (J2208A §11). |

전체 severity = LOW (1인 PoC, 로컬 BLE LAN, PII 없음).
</threat_model>

<verification>
- 4 테이블 + 1 cron job + 1 testuser1 시드 행이 마이그레이션에 정의되어 있다.
- `grep "REFERENCES public.devices(device_id)" supabase/migrations/010_watch_pipeline.sql`
  가 ≥ 4 (4 자식 테이블 모두 device_id FK).
- 마이그레이션 적용 후 (가능한 환경): `psql -c "select count(*) from devices where serial_number='J2208A-21:02:02:06:01:69'"` = 1.
</verification>

<success_criteria>
- ROADMAP Phase 4 SC #1 충족 (CONTEXT.md D-01 가중 — 5 테이블 = devices+4신규 으로
  재해석. workers 별도 X)
- WATCH-01 의 모든 컬럼·TTL·dedup·RLS 명세가 코드로 표현됨
- 04-02 / 04-03 가 의존할 수 있는 테이블·컬럼 계약이 확정됨
</success_criteria>

<output>
완료 후 `.planning/phases/04-watch-j2208a-pipeline/04-01-SUMMARY.md` 작성.
</output>
