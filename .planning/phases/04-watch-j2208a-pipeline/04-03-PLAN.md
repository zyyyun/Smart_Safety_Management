---
phase: 04-watch-j2208a-pipeline
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - scripts/j2208a_sensor_reader.py
  - supabase/functions/notifications/index.ts
  - j2208a/runtime.py
  - j2208a/tests/test_runtime_integration.py
  - .env.example
  - .gitignore
autonomous: true
requirements: [WATCH-05]
must_haves:
  truths:
    - "scripts/j2208a_sensor_reader.py 의 _on_notify 가 j2208a 패키지의 validate→aggregate→state_machine→derive 체인을 호출한다 (D-04)"
    - "raw_events 가 모든 BLE notify 시점에 INSERT 되며 ON CONFLICT DO NOTHING 으로 1초 dedup 가 자동 적용된다"
    - "1분 경계마다 minute_summary 1행이 INSERT 된다 (good_ratio<0.5 인 경우 집계는 NULL, 행 자체는 존재)"
    - "wear-state 전이 시점에만 wear_state_events 1행이 INSERT 된다 (동일 상태 지속 중 중복 X)"
    - "safety_alerts INSERT 직후 notifications Edge Function 의 watch-alert action 이 호출되어 _shared/fcm.ts 로 푸시 발송 (D-11·D-12)"
    - "notifications/index.ts 에 case 'watch-alert' 가 추가되어 testuser1 의 fcm_token 으로 sendPushToUser 호출"
    - "service_role key 는 .env 에서 로드되며 .env 파일은 .gitignore 차단 — .env.example 만 커밋"
  artifacts:
    - path: "supabase/functions/notifications/index.ts"
      provides: "watch-alert action 케이스 추가 (action-routing 패턴 유지)"
      contains: "case \"watch-alert\""
    - path: "scripts/j2208a_sensor_reader.py"
      provides: "_on_notify 가 j2208a.runtime.process_sample 호출"
      contains: "from j2208a.runtime import"
    - path: "j2208a/runtime.py"
      provides: "BLE notify → pipeline 통합 진입점 (RuntimeState + process_sample)"
      contains: "def process_sample"
    - path: ".env.example"
      provides: "환경변수 템플릿 (실제 .env 는 .gitignore)"
      contains: "SUPABASE_SERVICE_ROLE_KEY="
  key_links:
    - from: "scripts/j2208a_sensor_reader.py:_on_notify"
      to: "j2208a.runtime.process_sample"
      via: "callback 내부 호출"
      pattern: "process_sample"
    - from: "j2208a.runtime.process_sample"
      to: "supabase_writer.insert_raw_event"
      via: "service_role client"
      pattern: "insert_raw_event"
    - from: "j2208a.runtime.process_sample"
      to: "supabase_writer.call_watch_alert_edge_function"
      via: "safety_alerts 발생 시 호출"
      pattern: "call_watch_alert_edge_function"
    - from: "supabase/functions/notifications/index.ts watch-alert"
      to: "_shared/fcm.ts sendPushToUser"
      via: "action 케이스 내부 호출"
      pattern: "sendPushToUser"
---

<objective>
04-01 의 스키마 + 04-02 의 순수 로직 패키지를 결합한다. `scripts/j2208a_sensor_reader.py`
의 `_on_notify` 를 BLE 데몬 골격만 남기고, 모든 S2~S4 + DB write + Edge Function
호출을 `j2208a.runtime.process_sample` 로 위임한다. `supabase/functions/notifications/
index.ts` 에 `case "watch-alert"` 를 추가해 service_role 호출자가 testuser1 의
FCM 으로 알림을 보낼 수 있게 한다.

본 plan 의 종점 = 자동 검증 가능한 통합 (mocked notify → writer 체인 + Edge
Function curl 200) 까지. **실제 24h BLE 착용 검증은 04-04 의 책임**. 04-03 단독
으로는 BLE 디바이스 없이도 deploy + curl + pytest 로 acceptance 충족 가능해야
한다.

CONTEXT.md 결정 매핑:
- D-04: BLE 클라이언트 인라인 통합. 모듈 분리는 04-02 에서 완료, 04-03 가 wiring.
- D-11: FCM only — `_shared/fcm.ts` 재사용 (notifications Edge Function 경유).
- D-12: BLE 클라이언트가 service_role 로 Edge Function 호출 (pg trigger 아님).
  notifications/index.ts 가 action-routing 패턴이므로 새 Edge Function 신설
  대신 case 'watch-alert' 추가.

Purpose: WATCH-05 의 wiring 절반 (logic + DB integration + FCM trigger). 24h
검증 (04-04) 직전 단계.

Output:
- `scripts/j2208a_sensor_reader.py` 의 `_on_notify` = `j2208a.runtime.process_sample`
  delegation
- `j2208a/runtime.py` 신규 — RuntimeState + process_sample
- `supabase/functions/notifications/index.ts` 에 case 'watch-alert' 추가
- `.env.example` 신규 + `.gitignore` 에 `.env` 등록
- pytest mocked integration test + curl-based Edge Function smoke test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@.planning/phases/04-watch-j2208a-pipeline/04-01-SUMMARY.md
@.planning/phases/04-watch-j2208a-pipeline/04-02-SUMMARY.md
@scripts/j2208a_sensor_reader.py
@supabase/functions/notifications/index.ts
@supabase/functions/_shared/fcm.ts
@supabase/functions/_shared/supabase.ts
@supabase/functions/_shared/response.ts

<interfaces>
04-02 가 export 한 j2208a 함수 시그니처 (계약):

```python
from j2208a.validate       import Sample, validate_sample
from j2208a.aggregate      import aggregate_minute, MinuteResult
from j2208a.state_machine  import StateMachine, classify_state, STATES
from j2208a.derive         import DeriveContext, derive_alerts, AlertEvent, TEST_USER_AGE
from j2208a.supabase_writer import (
    get_client, insert_raw_event, insert_minute_summary,
    insert_wear_state_event, insert_safety_alert,
    update_safety_alert_resolved, call_watch_alert_edge_function,
)
from j2208a.decode import parse_packet  # S1
```

notifications/index.ts 기존 action-routing 패턴:

```
switch (action) {
  case "list": ...
  case "mark_read": ...
  case "send_group": ...
  case "send_individual": ...
  default: return err(`Unknown action: ${action}`);
}
// → 'watch-alert' case 추가
```

_shared/fcm.ts public API:

```
sendPushToUser(admin, userId, {title, body, data?}) -> Promise<boolean>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifications/index.ts 에 watch-alert action 추가 + curl smoke test</name>
  <files>supabase/functions/notifications/index.ts</files>
  <read_first>
    - supabase/functions/notifications/index.ts (기존 action 5개 — list/mark_read/send_group/send_individual)
    - supabase/functions/_shared/fcm.ts (sendPushToUser 시그니처 + ANDROID_CHANNEL_ID)
    - supabase/functions/_shared/response.ts (ok/err helpers)
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-11·D-12)
  </read_first>
  <action>
    `supabase/functions/notifications/index.ts` 의 switch 문에 default 직전
    `case "watch-alert"` 를 추가한다. payload 계약 (j2208a/supabase_writer.py 의
    call_watch_alert_edge_function 와 매칭).

    추가할 case 본문:

    ```typescript
    // ──────────────────────────────────────────
    // WATCH_ALERT — J2208A 워치 위험 알림 (per CONTEXT.md D-11·D-12)
    // ──────────────────────────────────────────
    case "watch-alert": {
      const { user_id, alert_type, severity, alert_id, title, body: alertBody } = body;
      if (!user_id || !alert_type || !severity || !title || !alertBody) {
        return err("user_id, alert_type, severity, title, body are required");
      }

      // alert_type / severity 화이트리스트 (DB CHECK constraint 와 일치)
      const ALLOWED_TYPES = new Set(["TACHY", "REMOVED", "COMMS_LOST"]);
      const ALLOWED_SEV   = new Set(["CAUTION", "WARNING", "DANGER", "NORMAL"]);
      if (!ALLOWED_TYPES.has(alert_type)) {
        return err(`Invalid alert_type: ${alert_type}`);
      }
      if (!ALLOWED_SEV.has(severity)) {
        return err(`Invalid severity: ${severity}`);
      }

      // 알림 트레이용 DB 인서트 (UI 알림 목록 표시 — 기존 패턴과 동일)
      const { error: insErr } = await supabase
        .from("notifications")
        .insert({ user_id, title, content: alertBody });
      if (insErr) {
        console.error("[watch-alert] notifications insert failed:", insErr.message);
      }

      // FCM 푸시 — _shared/fcm.ts 재사용
      sendPushToUser(supabase, user_id, {
        title,
        body: alertBody,
        data: {
          type: "watch_alert",
          alert_type,
          severity,
          alert_id: String(alert_id ?? ""),
        },
      }).catch((e) =>
        console.error("[FCM] watch-alert push failed:", e)
      );

      return ok({ success: true });
    }
    ```

    Deploy 단계:
    1. `supabase functions deploy notifications` 실행 (로그인 안 되어 있을 시
       `supabase login` 먼저).
    2. 실패 시 (e.g. 토큰 만료) 명령 + stderr 를 SUMMARY 에 기록하고 04-04
       직전 재시도하도록 인계.

    Smoke test (deploy 성공한 경우만):

    ```bash
    curl -X POST "${SUPABASE_URL}/functions/v1/notifications" \
      -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
      -H "Content-Type: application/json" \
      -d '{"action":"watch-alert","user_id":"testuser1","alert_type":"TACHY","severity":"WARNING","alert_id":0,"title":"[TEST] 빈맥 의심","body":"테스트 메시지"}'
    ```
    기대: HTTP 200 + body `{"success":true}`. 실기기 트레이 푸시 도착은 04-04
    검증 항목.
  </action>
  <verify>
    <automated>grep -q 'case "watch-alert"' supabase/functions/notifications/index.ts && grep -q 'ALLOWED_TYPES' supabase/functions/notifications/index.ts && grep -q 'sendPushToUser(supabase, user_id' supabase/functions/notifications/index.ts && grep -q '"type": "watch_alert"' supabase/functions/notifications/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'case "watch-alert"' supabase/functions/notifications/index.ts` returns 1
    - `grep 'ALLOWED_TYPES = new Set' supabase/functions/notifications/index.ts` returns 1
    - watch-alert case 내부에 `sendPushToUser(supabase, user_id` 호출 존재
    - `grep '"type": "watch_alert"' supabase/functions/notifications/index.ts` returns 1
    - 환경 가능 시: `supabase functions deploy notifications` 성공
    - 환경 가능 시: 위 curl 명령이 HTTP 200 반환
  </acceptance_criteria>
  <done>
    notifications Edge Function 에 watch-alert action 이 추가되어 alert_type +
    severity 검증 후 sendPushToUser 호출. 환경 허용 시 deploy + curl smoke 통과.
  </done>
</task>

<task type="auto">
  <name>Task 2: j2208a/runtime.py — BLE notify → pipeline 통합 진입점 + .env.example + .gitignore</name>
  <files>j2208a/runtime.py, .env.example, .gitignore, j2208a/tests/test_runtime_integration.py</files>
  <read_first>
    - j2208a/validate.py / aggregate.py / state_machine.py / derive.py / supabase_writer.py (04-02 산출물)
    - scripts/j2208a_sensor_reader.py:326-330 (_on_notify 현재 구현)
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-04 인라인 통합 + D-09 전이 알림)
    - .gitignore (현재 .env 등록 여부 확인)
  </read_first>
  <action>
    (A) `j2208a/runtime.py` 신규. 1분 버퍼 + 상태 + DB write 통합 진입점.
    `_on_notify` 가 sync callback 이므로 process_sample 도 sync (supabase-py 는
    내부 sync). 모듈 본문:

    ```python
    """BLE notify -> pipeline 통합 (per CONTEXT.md D-04 인라인 통합)."""

    import os
    import time
    from dataclasses import dataclass, field
    from datetime import datetime, timezone
    from typing import Optional

    from .validate import Sample, validate_sample
    from .aggregate import aggregate_minute
    from .state_machine import StateMachine
    from .derive import DeriveContext, derive_alerts, AlertEvent
    from .supabase_writer import (
        insert_raw_event, insert_minute_summary, insert_wear_state_event,
        insert_safety_alert, update_safety_alert_resolved,
        call_watch_alert_edge_function,
    )

    DEVICE_ID = int(os.environ.get("TEST_DEVICE_ID", "0"))
    USER_ID   = os.environ.get("TEST_USER_ID", "testuser1")

    @dataclass
    class RuntimeState:
        sm: StateMachine = field(default_factory=StateMachine)
        derive_ctx: DeriveContext = field(default_factory=DeriveContext)
        prev_validate_sample: Optional[Sample] = None
        current_minute_floor: Optional[int] = None
        minute_buffer: list = field(default_factory=list)
        last_state: str = "TRANSIENT"
        last_raw_ts: Optional[float] = None
        last_alert_ids: dict = field(default_factory=lambda: {
            "TACHY": None, "REMOVED": None, "COMMS_LOST": None,
        })

    _runtime = RuntimeState()
    _client = None

    def _get_client():
        global _client
        if _client is None:
            from .supabase_writer import get_client
            _client = get_client()
        return _client

    def _to_iso(ts: float) -> str:
        return datetime.fromtimestamp(ts, tz=timezone.utc).isoformat()

    def _alert_message(alert_type, severity, reason, is_resolution):
        if is_resolution:
            return (f"[정상 복귀] {alert_type}", f"{alert_type} 해소됨")
        if alert_type == "TACHY":
            return ("[주의] 빈맥 의심",
                    f"심박 60s median={reason.get('hr_60s_median')} "
                    f"(threshold={reason.get('threshold')})")
        if alert_type == "REMOVED":
            return ("[주의] 시계 탈착 감지",
                    f"OFF {reason.get('off_duration_sec', 0):.0f}s 지속")
        if alert_type == "COMMS_LOST":
            return ("[주의] 통신 두절",
                    f"마지막 raw 수신 {reason.get('last_raw_age_sec', 0):.0f}s 전")
        return (f"[{severity}] {alert_type}", str(reason))

    def process_sample(parsed, raw_hex, cmd, now_ts=None,
                        client=None, alert_caller=None):
        """BLE notify 1건 처리. parsed = j2208a.decode.parse_packet 출력."""
        if now_ts is None:
            now_ts = time.time()
        client = client or _get_client()
        alert_caller = alert_caller or call_watch_alert_edge_function
        rt = _runtime

        ts_iso = _to_iso(now_ts)
        try:
            insert_raw_event(client, DEVICE_ID, ts_iso, cmd, raw_hex, parsed)
        except Exception as e:
            print(f"[runtime] insert_raw_event failed: {e}")
        # NOTE: rt.last_raw_ts 갱신은 derive_alerts 호출 "이후" 로 미룬다.
        # process_sample 안의 derive_alerts 가 직전 raw_ts 와 비교하도록 보장.
        # COMMS_LOST 의 실제 트리거는 별도 tick() 헬퍼가 담당 (raw 없는 경우에도 호출되어야 함).

        hr = parsed.get("heart_rate")
        temp = parsed.get("temperature_c")
        if hr is None and temp is None:
            rt.last_raw_ts = now_ts  # raw 만 적재하고 종료 — last_raw_ts 갱신 필요
            return

        sample = Sample(ts=now_ts, heart_rate=hr, temperature_c=temp)
        quality = validate_sample(sample, rt.prev_validate_sample)
        rt.prev_validate_sample = sample

        new_state = rt.sm.update(now_ts,
                                   hr if hr is not None else 0,
                                   temp if temp is not None else 0.0)

        if new_state != rt.last_state:
            try:
                insert_wear_state_event(client, DEVICE_ID, ts_iso,
                                          rt.last_state, new_state,
                                          {"hr": hr, "temp": temp})
            except Exception as e:
                print(f"[runtime] insert_wear_state_event failed: {e}")
            rt.last_state = new_state

        minute_floor = int(now_ts // 60)
        if rt.current_minute_floor is None:
            rt.current_minute_floor = minute_floor
        if minute_floor != rt.current_minute_floor:
            _flush_minute(rt, client)
            rt.current_minute_floor = minute_floor
            rt.minute_buffer = []

        rt.minute_buffer.append({
            "ts": now_ts, "hr": hr, "temp": temp,
            "steps": parsed.get("steps"),
            "quality_hr": quality.get("heart_rate"),
            "quality_temp": quality.get("temperature_c"),
        })

        good_hr = [s["hr"] for s in rt.minute_buffer
                   if s.get("quality_hr") == "GOOD" and s.get("hr") is not None]
        hr_60s_median = int(sum(good_hr) / len(good_hr)) if good_hr else None

        events = derive_alerts(rt.derive_ctx, now_ts, new_state,
                                hr_60s_median, rt.last_raw_ts)
        for ev in events:
            _emit_alert(client, alert_caller, ev, ts_iso, rt)

        # 모든 처리 후에 last_raw_ts 갱신 (derive_alerts 가 직전 raw_ts 를 보도록)
        rt.last_raw_ts = now_ts

    def tick(now_ts=None, client=None, alert_caller=None):
        """주기적 heartbeat — raw 가 없는 동안에도 derive_alerts 를 호출해
        COMMS_LOST 와 REMOVED 5분 만료 같은 시간 기반 전이를 잡아낸다.
        sensor_reader.py 의 _run_session 가 ~10초 주기로 호출."""
        if now_ts is None:
            now_ts = time.time()
        client = client or _get_client()
        alert_caller = alert_caller or call_watch_alert_edge_function
        rt = _runtime

        # 마지막 1분 버퍼의 GOOD HR median (process_sample 과 동일 계산)
        good_hr = [s["hr"] for s in rt.minute_buffer
                   if s.get("quality_hr") == "GOOD" and s.get("hr") is not None]
        hr_60s_median = int(sum(good_hr) / len(good_hr)) if good_hr else None

        events = derive_alerts(rt.derive_ctx, now_ts, rt.last_state,
                                hr_60s_median, rt.last_raw_ts)
        if not events:
            return
        ts_iso = _to_iso(now_ts)
        for ev in events:
            _emit_alert(client, alert_caller, ev, ts_iso, rt)

    def _flush_minute(rt, client):
        if not rt.minute_buffer:
            return
        result = aggregate_minute(rt.minute_buffer, rt.last_state)
        minute_ts = datetime.fromtimestamp(
            rt.current_minute_floor * 60, tz=timezone.utc
        ).isoformat()
        try:
            insert_minute_summary(
                client, DEVICE_ID, minute_ts,
                result.hr_median, result.temp_median, result.temp_iqr,
                result.steps_delta, result.dominant_state, result.good_ratio,
            )
        except Exception as e:
            print(f"[runtime] insert_minute_summary failed: {e}")

    def _emit_alert(client, alert_caller, ev, ts_iso, rt):
        if ev.is_resolution:
            try:
                update_safety_alert_resolved(client, DEVICE_ID, ev.alert_type, ts_iso)
            except Exception as e:
                print(f"[runtime] update_safety_alert_resolved failed: {e}")
            title, body = _alert_message(ev.alert_type, ev.severity, ev.reason, True)
            alert_id = rt.last_alert_ids.get(ev.alert_type) or 0
        else:
            try:
                alert_id = insert_safety_alert(
                    client, DEVICE_ID, ev.alert_type,
                    ev.severity, ev.reason, ts_iso,
                )
                rt.last_alert_ids[ev.alert_type] = alert_id
            except Exception as e:
                print(f"[runtime] insert_safety_alert failed: {e}")
                alert_id = -1
            title, body = _alert_message(ev.alert_type, ev.severity, ev.reason, False)

        try:
            sev_for_edge = "NORMAL" if ev.is_resolution else ev.severity
            alert_caller(USER_ID, ev.alert_type, sev_for_edge, alert_id, title, body)
        except Exception as e:
            print(f"[runtime] call_watch_alert_edge_function failed: {e}")
    ```

    (B) `.env.example` 신규 (실제 .env 는 .gitignore 차단):

    ```
    # J2208A BLE 클라이언트 환경변수 (per CONTEXT.md D-12)
    # 실제 .env 는 .gitignore — 절대 커밋 금지
    SUPABASE_URL=https://YOUR-PROJECT.supabase.co
    SUPABASE_SERVICE_ROLE_KEY=eyJ...
    TEST_DEVICE_ID=1
    TEST_USER_ID=testuser1
    ```

    (C) `.gitignore` 검사 — `.env` 라인이 없으면 추가:

    ```
    # 환경변수 (절대 커밋 금지)
    .env
    ```

    (D) `j2208a/tests/test_runtime_integration.py` — mocked client + alert_caller
    로 process_sample 통합 흐름 검증 (DB 미연결 환경에서도 통과해야 함):

    - Test RT-1: 정상 sample 처리 시 client.table('raw_events').upsert(...).execute() 호출됨
    - Test RT-2: 분 경계 넘을 시 client.table('minute_summary').upsert(...).execute() 호출됨
    - Test RT-3: WORN 60s 이상 + hr_60s_median 이 Karvonen 임계 초과 →
      alert_caller 가 호출 (alert_type='TACHY', severity='WARNING')
    - Test RT-4: 같은 TACHY 가 연속해도 alert_caller 는 1회만 호출 (전이 시점)
    - Test RT-5: 상태 전이 (OFF → WARMUP) 시 client.table('wear_state_events').insert
      호출됨
    - **Test RT-6 (COMMS_LOST 입증)**: process_sample 으로 t=0 에 1건 적재 후
      runtime.tick(now_ts=130.0) 호출 → alert_caller 가 alert_type='COMMS_LOST',
      severity='WARNING' 로 1회 호출됨. 같은 tick 을 다시 호출해도 (이미 WARNING
      상태) 추가 호출 X.
    - **Test RT-7 (RT-6 의 회귀 가드)**: process_sample 단독 (raw 도착) 으로는
      COMMS_LOST 가 발생하지 않음 — 직전 raw_ts 와 now_ts 차이가 0 에 가깝기
      때문. tick() 만이 시간 기반 전이를 잡는다는 contract 검증.

    Mock 패턴 예시:

    ```python
    from unittest.mock import MagicMock
    from j2208a import runtime

    def test_raw_event_insert_on_every_sample():
        client = MagicMock()
        alert_caller = MagicMock(return_value=True)
        runtime._runtime = runtime.RuntimeState()  # reset
        parsed = {"cmd": "0x28", "heart_rate": 70, "temperature_c": 36.5}
        runtime.process_sample(parsed, "28 02 46 ...", 0x28,
                                now_ts=1700000000.0,
                                client=client, alert_caller=alert_caller)
        assert any(call_args[0][0] == "raw_events"
                   for call_args in client.table.call_args_list)

    def test_minute_boundary_triggers_minute_summary_insert():
        client = MagicMock()
        runtime._runtime = runtime.RuntimeState()
        # 같은 분에 sample 2개
        runtime.process_sample({"heart_rate": 70, "temperature_c": 36.5},
                                "raw1", 0x28, now_ts=1700000010.0,
                                client=client, alert_caller=MagicMock())
        runtime.process_sample({"heart_rate": 71, "temperature_c": 36.6},
                                "raw2", 0x28, now_ts=1700000020.0,
                                client=client, alert_caller=MagicMock())
        # 다음 분으로 진입
        runtime.process_sample({"heart_rate": 72, "temperature_c": 36.7},
                                "raw3", 0x28, now_ts=1700000061.0,
                                client=client, alert_caller=MagicMock())
        assert any(call_args[0][0] == "minute_summary"
                   for call_args in client.table.call_args_list)
    ```

    검증: `pytest j2208a/tests/test_runtime_integration.py -v` 통과.
  </action>
  <verify>
    <automated>cd D:/2026_산업안전/Smart_Safety_Management && python -m pytest j2208a/tests/test_runtime_integration.py -v --tb=short && grep -q "from .validate import" j2208a/runtime.py && grep -q "from .supabase_writer import" j2208a/runtime.py && grep -q "SUPABASE_SERVICE_ROLE_KEY=" .env.example && grep -q "^\.env$" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest j2208a/tests/test_runtime_integration.py` 종료코드 = 0, ≥ 5 tests passed
    - `grep "def process_sample" j2208a/runtime.py` returns 1
    - `grep "from .supabase_writer import" j2208a/runtime.py` returns 1
    - `grep "from .derive import" j2208a/runtime.py` returns 1
    - `grep "SUPABASE_SERVICE_ROLE_KEY=" .env.example` returns 1
    - `grep "^\.env$" .gitignore` returns 1 (또는 .env 가 다른 패턴으로 차단되는지 검증)
    - `python -c "from j2208a import runtime; print(runtime.RuntimeState())"` 종료 0
  </acceptance_criteria>
  <done>
    j2208a/runtime.py 가 process_sample 진입점 제공. mocked test 5종 통과.
    .env.example 커밋, .env 는 .gitignore 차단.
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/j2208a_sensor_reader.py 의 _on_notify 를 runtime.process_sample 호출로 전환</name>
  <files>scripts/j2208a_sensor_reader.py</files>
  <read_first>
    - scripts/j2208a_sensor_reader.py (전체 — 특히 line 326-330 _on_notify, line 219-318 J2208AClient)
    - j2208a/runtime.py (Task 2 산출물)
    - j2208a/decode.py (04-02 산출물 — parse_packet 동일 동작 재확인)
    - .planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md (D-04 — 데몬 골격만 유지)
  </read_first>
  <action>
    sensor_reader.py 의 S1 구현 (parse_packet 등) 은 04-02 가 j2208a/decode.py 로
    이전했지만, 본 plan 단계에서는 **점진적 전환** 으로 안전하게 처리한다:

    1. 파일 상단에 import 추가:
       ```python
       try:
           from j2208a.runtime import process_sample
           _RUNTIME_OK = True
       except ImportError as _imp_err:
           print(f"[!] j2208a.runtime import 실패 — DB write/알림 비활성화: {_imp_err}")
           process_sample = None
           _RUNTIME_OK = False
       ```
       파일이 어디에서든 (PYTHONPATH=. 또는 repo root) 실행 가능하도록 sys.path
       hack 은 추가하지 않는다. 사용자가 `python -m scripts.j2208a_sensor_reader`
       또는 `cd repo && python scripts/j2208a_sensor_reader.py` 로 실행할 것을
       README 주석으로 명시.

    2. `_on_notify` 메서드 변경 (line 326-330) — 기존 print 출력은 유지하되,
       process_sample 호출을 추가:
       ```python
       def _on_notify(self, _sender, data: bytearray) -> None:
           raw = bytes(data)
           info = parse_packet(raw)
           # 한 줄 진단 출력 (기존 동작 보존)
           kv = " ".join(f"{k}={v}" for k, v in info.items() if k not in ("raw",))
           print(f"[<] {kv}    (raw: {info['raw']})")

           # j2208a 파이프라인 진입 (per CONTEXT.md D-04 인라인 통합)
           if _RUNTIME_OK and process_sample is not None:
               try:
                   import time as _time
                   cmd = raw[0] if raw else 0
                   process_sample(info, info.get("raw", ""), cmd,
                                   now_ts=_time.time())
               except Exception as e:
                   # 데몬 안전 — process_sample 실패해도 BLE 데몬은 계속 동작
                   print(f"[!] j2208a.runtime.process_sample 실패: {e}")
       ```

    3. parse_packet / CRC 등 S1 함수의 중복 정의는 본 plan 에서 제거하지 않는다
       (sensor_reader 가 standalone 으로도 동작 — j2208a 패키지 미설치 환경
       fallback). 04-02 의 j2208a/decode.py 가 동일 코드를 보유해 long-term
       drift 우려가 있지만, v1.0 1인 PoC 단계에서는 허용. v1.1 모듈 분리 강화
       시 sensor_reader 가 `from j2208a.decode import parse_packet` 으로
       전환하는 안건을 SUMMARY 에 todo 로 기록.

    4. 데몬 골격 (`demo`, `_run_session`, `J2208AClient.connect`/`discover` 등)
       은 그대로 유지. 3s→30s 백오프, 자동 재연결, _on_disconnect 콜백 모두
       기존 동작 보존.

    5. **Heartbeat 추가 (per advisor 가이드 — COMMS_LOST 트리거)**: process_sample
       단독으로는 COMMS_LOST 가 발생하지 않는다 (raw 도착 시점에만 derive_alerts
       가 호출되기 때문). 별도 heartbeat 코루틴으로 10초 주기 `runtime.tick()`
       을 호출해 시간 기반 전이 (REMOVED 5분 만료, COMMS_LOST 120s 부재) 를
       잡는다. 구체 패턴:

       sensor_reader.py 의 `_run_session` 위에 helper 추가:
       ```python
       async def _heartbeat_loop(disconnected: asyncio.Event,
                                  interval_sec: float = 10.0) -> None:
           """주기적 j2208a.runtime.tick 호출 — raw 없어도 COMMS_LOST 검출."""
           if not _RUNTIME_OK:
               return
           try:
               from j2208a.runtime import tick as _tick
           except ImportError:
               return
           while not disconnected.is_set():
               try:
                   await asyncio.sleep(interval_sec)
                   if disconnected.is_set():
                       break
                   _tick()
               except Exception as e:
                   print(f"[!] j2208a.runtime.tick 실패: {e}")
       ```

       `_run_session` 본문 시작에 task spawn:
       ```python
       hb_task = asyncio.create_task(_heartbeat_loop(disconnected, 10.0))
       ```

       `_run_session` 의 `finally:` (또는 함수 종료 직전 새로 추가) 에서 cancel:
       ```python
       hb_task.cancel()
       try:
           await hb_task
       except (asyncio.CancelledError, Exception):
           pass
       ```

       기존 `while not disconnected.is_set():` 루프는 그대로 유지 — heartbeat 는
       병렬 task. _on_notify 가 호출하는 process_sample 과 heartbeat 의 tick 모두
       동일한 _runtime 인스턴스를 공유한다 (모듈 전역).

    검증:
    - `python -c "import scripts.j2208a_sensor_reader as m; print(m._RUNTIME_OK)"`
      가 True 출력 (PYTHONPATH=. 가정).
    - `grep "from j2208a.runtime import process_sample" scripts/j2208a_sensor_reader.py`
      이 1회 등장.
    - `grep "process_sample(info" scripts/j2208a_sensor_reader.py` 이 _on_notify
      안에서 1회 등장.
    - sensor_reader 의 기존 `parse_packet` import 와 데몬 entry (`if __name__
      == "__main__": main()`) 는 변경되지 않음.
  </action>
  <verify>
    <automated>grep -q "from j2208a.runtime import process_sample" scripts/j2208a_sensor_reader.py && grep -q "process_sample(info" scripts/j2208a_sensor_reader.py && grep -q "_RUNTIME_OK" scripts/j2208a_sensor_reader.py && python -c "import sys; sys.path.insert(0, '.'); import importlib.util; spec = importlib.util.spec_from_file_location('m', 'scripts/j2208a_sensor_reader.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); print('_RUNTIME_OK=', mod._RUNTIME_OK)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from j2208a.runtime import process_sample" scripts/j2208a_sensor_reader.py` returns 1
    - `grep "_RUNTIME_OK" scripts/j2208a_sensor_reader.py` returns ≥ 2 (변수 정의 + 사용)
    - `grep "process_sample(info" scripts/j2208a_sensor_reader.py` returns 1
    - `grep "_heartbeat_loop" scripts/j2208a_sensor_reader.py` returns ≥ 2 (정의 + create_task 호출)
    - `grep "asyncio.create_task(_heartbeat_loop" scripts/j2208a_sensor_reader.py` returns 1
    - `grep "hb_task.cancel" scripts/j2208a_sensor_reader.py` returns 1
    - 모듈 import 시 _RUNTIME_OK 가 True (PYTHONPATH 정상)
    - 기존 `def main():` 와 `def demo(...)` 함수가 변경 없이 존재 — `grep -c "^async def demo\|^def main" scripts/j2208a_sensor_reader.py` ≥ 2
    - 04-02 의 unit test 는 여전히 모두 통과 (회귀 없음): `python -m pytest j2208a/tests/ -q` 종료 0
    - **회귀 테스트 (RT-6)**: `python -m pytest j2208a/tests/test_runtime_integration.py -k comms_lost` 통과 — tick 으로만 COMMS_LOST 트리거 가능함을 입증
  </acceptance_criteria>
  <done>
    sensor_reader 의 _on_notify 가 runtime.process_sample 호출로 통합되어, BLE
    notify 마다 raw_events INSERT + 1분 minute_summary + state 전이 + safety_alerts
    발생 시 Edge Function 호출까지 흐름이 닫혔다. 04-04 의 24h 실측 직전 상태.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BLE 디바이스 → Python 클라이언트 | 로컬 BLE LAN, 페어링 없음 (J2208A 4.0 LE). MITM 위험 LOW (작업장 내부, 공격자 근접 필요). |
| Python 클라이언트 → Supabase | service_role key — `.env` 에서 로드, .gitignore 차단. 키 유출 시 모든 테이블 RW. |
| Edge Function watch-alert → FCM | 기존 `_shared/fcm.ts` 재사용 — RS256 self-sign, 04-02 단계에서 검증된 흐름. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | I (Information disclosure) | service_role key in .env | mitigate | .gitignore 에 .env 추가 + .env.example 만 커밋. python-dotenv 또는 OS env 로 로드. |
| T-04-11 | S (Spoofing) | watch-alert Edge Function 호출자 | mitigate | Authorization: Bearer SERVICE_ROLE_KEY 필수 — 토큰 없는 호출 거부 (Supabase 기본 동작). |
| T-04-12 | T (Tampering) | watch-alert payload 의 alert_type/severity | mitigate | ALLOWED_TYPES / ALLOWED_SEV 화이트리스트로 unknown 값 거부. |
| T-04-13 | R (Replay) | BLE notify 패킷 | accept | 로컬 LAN 1인 PoC, replay 가치 낮음. v1.1 다중작업자 시 BLE secure pairing 재검토. |
| T-04-14 | D (DoS) | watch-alert FCM spam | mitigate | derive 의 전이 시점 1회 룰 (D-09) — 같은 등급 지속 중 알림 X. |
| T-04-15 | I | FCM token leak via watch-alert response | mitigate | Edge Function 은 success/fail 만 반환, fcm_token 은 응답에 포함 X (기존 `_shared/fcm.ts` 패턴). |

severity = LOW (1인 PoC, 로컬 LAN, 기존 검증된 FCM 흐름 재사용).
</threat_model>

<verification>
- `pytest j2208a/tests/` 전체 통과 (≥ 28 tests — 04-02 의 23 + RT 5)
- sensor_reader.py 가 j2208a.runtime import 성공 + `process_sample(info, ...)` 호출 라인 존재
- notifications/index.ts 의 watch-alert case 에 ALLOWED_TYPES/ALLOWED_SEV +
  sendPushToUser 호출 존재
- 환경 가능 시: `supabase functions deploy notifications` 성공 + curl 200
- `.env` 는 .gitignore, `.env.example` 만 커밋
</verification>

<success_criteria>
- WATCH-05 의 wiring 완료 (logic + DB write + Edge Function trigger). 24h 실측은
  04-04 책임.
- 04-02 의 회귀 없음 (전체 pytest 통과).
- Edge Function 1개 추가, BLE 클라이언트 1개 수정, 신규 모듈 1개, 신규 테스트 1개.
</success_criteria>

<output>
완료 후 `.planning/phases/04-watch-j2208a-pipeline/04-03-SUMMARY.md` 작성.
</output>
