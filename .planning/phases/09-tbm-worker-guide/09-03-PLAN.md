---
phase: 09-tbm-worker-guide
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - app/build.gradle.kts
  - app/proguard-rules.pro
  - app/src/main/java/com/example/smart_safety_management/MyApp.kt
  - app/src/main/java/com/example/smart_safety_management/HomeActivity.kt
  - app/src/main/java/com/example/smart_safety_management/HomeWorkerActivity.kt
  - app/src/main/java/com/example/smart_safety_management/TbmDashboardActivity.kt
  - app/src/main/java/com/example/smart_safety_management/TbmWorkerActivity.kt
  - app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmModels.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmDashboardCardComposable.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmWorkerCardComposable.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmDashboardScreen.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmWorkerScreen.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/SignatureCanvas.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmStartSection.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/WorkTypeValidator.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidator.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmParticipantsReducer.kt
  - app/src/main/java/com/example/smart_safety_management/tbm/TbmRetrofitApi.kt
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/layout/main_home.xml
  - app/src/main/res/layout/main_home_worker.xml
  - app/src/test/java/com/example/smart_safety_management/tbm/WorkTypeValidatorTest.kt
  - app/src/test/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidatorTest.kt
  - app/src/test/java/com/example/smart_safety_management/tbm/TbmParticipantsReducerTest.kt
  - app/src/test/java/com/example/smart_safety_management/tbm/SignatureStateTest.kt
autonomous: true
requirements: [TBM-02]
tags: [phase-9, tbm, android, compose, supabase-kt, realtime, storage-kt, signature-canvas, fcm, ui, tdd-tasks, T-9-01, T-9-06, T-9-12, T-9-13]

must_haves:
  truths:
    - "app/build.gradle.kts 에 `io.github.jan-tennert.supabase:storage-kt:2.2.0` 의존성 추가 (기존 realtime-kt + postgrest-kt 2.2.0 옆) — Pitfall 10 (gotrue-kt transitive) acceptable APK +50KB"
    - "MyApp.supabase 싱글톤 (Phase 7 작성됨) 가 Storage plugin install 호출 추가 — `install(Storage)` (변경 1줄, by-lazy 동일 인스턴스)"
    - "신규 `tbm/` 패키지 13 main 파일 + 4 test 파일 — watch/ 9 파일 1:1 미러 + SignatureCanvas + WorkTypeValidator + ExpectedEndAtValidator + TbmStartSection + TbmRetrofitApi"
    - "TbmRepository 가 dynamic session_id 2-stage subscription 패턴 구현 (RESEARCH Pattern 2 line 412-525) — Stage A: todaySessionFlow(groupId), Stage B: participantsFlow(sessionId) + checklistsFlow(sessionId)"
    - "TbmRepository 가 4 채널 Realtime — tbm_sessions / tbm_templates 미구독 (anti-pattern RESEARCH line 805, seed-only) → 실제 3 채널 구독 (tbm_sessions / tbm_checklists / tbm_participants), tbm_templates 는 PostgREST 1회 fetch + 캐시"
    - "TbmModels — TbmSessionRow / TbmTemplateRow / TbmChecklistRow / TbmParticipantRow @Serializable data class (snake_case column → @SerialName)"
    - "SignatureCanvas — Compose Canvas + Path 누적 + drawPath + ImageBitmap 오프스크린 → PNG ByteArray (RESEARCH Pattern 1 line 296-410). Pitfall 1 (Bitmap.recycle finally) + Pitfall 2 (mutableStateOf<Path?> setter 강제) 모두 명시 적용"
    - "SignatureCanvas → Supabase Storage 업로드 — `supabase.storage.from('tbm-signatures').upload(path, bytes)` (storage-kt 2.2.0 ABI), path = `{session_id}/{user_id}_{epoch_ms}.png` (013 RLS Option A 의 key prefix 가드 충족)"
    - "WorkTypeValidator — 5종 enum ('fire','electric','height','heavy','general') + normalize (lowercase + trim) — RESEARCH Example A. **tdd=true** (단위 테스트 5+ cases)"
    - "ExpectedEndAtValidator — ZonedDateTime ISO-8601 OFFSET 형식 검증 (Pitfall 8 timezone). Android 측 `ZonedDateTime.now(ZoneId.of('Asia/Seoul'))` + ISO_OFFSET_DATE_TIME. **tdd=true** (3+ cases — UTC offset 포함 / 누락 거부 / now 보다 과거 거부)"
    - "TbmParticipantsReducer — pure object, PostgresAction.Insert/Update/Delete 처리 (RESEARCH Example B) — Phase 7 SafetyAlertReducer 미러. **tdd=true** (4+ cases — insert / dup id update / unrelated id 보존 / delete)"
    - "SignatureState.toPngBytes() — 단위 테스트 (canvasSize=0 → ByteArray(0) / 1 path → PNG bytes>0). **tdd=true** (2+ cases)"
    - "HomeActivity.setupTbmDashboardCard() — main_home.xml 에 ComposeView (`@+id/tbm_dashboard_compose`) 추가 + ComposeView setContent { Smart_Safety_ManagementTheme { TbmDashboardCardComposable(groupId, onClickDashboard={ startActivity(TbmDashboardActivity) }) } } (Pitfall 12 — Theme 래핑 강제, Phase 7 setupWatchCard 1:1 미러)"
    - "HomeWorkerActivity.setupTbmCard() — main_home_worker.xml 의 watch_card_compose 아래 ComposeView (`@+id/tbm_card_compose`) 추가 + setContent { Smart_Safety_ManagementTheme { TbmWorkerCardComposable(groupId, userId, onClickGuide={ startActivity(TbmWorkerActivity, EXTRA_SESSION_ID) }) } } + DisposeOnLifecycleDestroyed"
    - "TbmDashboardActivity (신규 ComponentActivity, manager 권한 가드) — UserSession.userRole == MANAGER 확인 후 진입, worker deep-link 진입 시 Toast + finish(). 4 상태 UI (세션 없음 → start 버튼 / 진행 중 / 완료 / 미참여 알림 발사 후)"
    - "TbmWorkerActivity (신규 ComponentActivity) — paired session_id 조회 → TbmWorkerScreen (체크리스트 read-only + SignatureCanvas + 참여 확인 버튼). FCM extras 의 session_id 만 신뢰 + DB 재조회 (Phase 7 D-02 패턴 — anti-pattern RESEARCH line 801)"
    - "MyFirebaseMessagingService — 기존 watch_alert 분기 옆 `data.type == 'tbm_alert'` 분기 1개 추가 → showTbmAlertNotification + pendingIntent → TbmWorkerActivity (worker) 또는 TbmDashboardActivity (manager) — UserSession.userRole 분기"
    - "AndroidManifest.xml 에 TbmDashboardActivity + TbmWorkerActivity 등록 (`android:exported=\"false\"`, parentActivity 미지정 — back stack default)"
    - "compileDebugKotlin BUILD SUCCESSFUL (Phase 7 Korean repo path workaround `layout.buildDirectory.set('D:/ssm-app-build')` 재사용)"
    - "회귀 가드 — `acknowledged_at` count = 0 in tbm/, `realtime-kt:3.` count = 0 in build.gradle.kts (Pitfall 1 supabase-kt 3.x 거부 유지)"
    - "회귀 가드 — watch/ 패키지 git diff 0 (Phase 7 보존, SC #4 코드 경로 분리)"
    - "4 unit test files PASS — WorkTypeValidatorTest (5+) + ExpectedEndAtValidatorTest (3+) + TbmParticipantsReducerTest (4+) + SignatureStateTest (2+) — 합산 14+ cases, JVM unit test Korean path workaround 적용"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "storage-kt 2.2.0 의존성 추가"
      contains: "storage-kt:2.2.0"
    - path: "app/src/main/java/com/example/smart_safety_management/MyApp.kt"
      provides: "supabase 싱글톤에 Storage plugin install"
      contains: "install(Storage)"
    - path: "app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt"
      provides: "Dynamic session_id 2-stage Realtime 구독 (3 채널, RESEARCH Pattern 2)"
      contains: "todaySessionFlow"
    - path: "app/src/main/java/com/example/smart_safety_management/tbm/SignatureCanvas.kt"
      provides: "Compose Canvas 수기 서명 + PNG ByteArray export (Pitfall 1·2 mitigation)"
      contains: "fun toPngBytes"
    - path: "app/src/main/java/com/example/smart_safety_management/tbm/WorkTypeValidator.kt"
      provides: "5종 work_type enum 검증 + normalize"
      contains: "ALLOWED"
    - path: "app/src/main/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidator.kt"
      provides: "ZonedDateTime ISO-8601 OFFSET 형식 검증 (Pitfall 8 timezone)"
      contains: "ISO_OFFSET_DATE_TIME"
    - path: "app/src/main/java/com/example/smart_safety_management/tbm/TbmParticipantsReducer.kt"
      provides: "Realtime INSERT/UPDATE/DELETE 리듀서 (Phase 7 SafetyAlertReducer 미러)"
      contains: "fun apply"
    - path: "app/src/main/java/com/example/smart_safety_management/TbmDashboardActivity.kt"
      provides: "manager only TBM 대시보드 Activity (TBM-03)"
      contains: "TbmDashboardActivity"
    - path: "app/src/main/java/com/example/smart_safety_management/TbmWorkerActivity.kt"
      provides: "worker TBM 가이드 Activity (TBM-02)"
      contains: "TbmWorkerActivity"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "TbmDashboardActivity + TbmWorkerActivity 등록"
      contains: "TbmDashboardActivity"
    - path: "app/src/main/res/layout/main_home.xml"
      provides: "manager 홈에 tbm_dashboard_compose ComposeView (첫 임베드)"
      contains: "tbm_dashboard_compose"
    - path: "app/src/main/res/layout/main_home_worker.xml"
      provides: "worker 홈에 tbm_card_compose ComposeView (watch_card_compose 아래)"
      contains: "tbm_card_compose"
    - path: "app/src/test/java/com/example/smart_safety_management/tbm/WorkTypeValidatorTest.kt"
      provides: "WorkTypeValidator 단위 테스트 (TDD)"
      contains: "@Test"
    - path: "app/src/test/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidatorTest.kt"
      provides: "ExpectedEndAtValidator 단위 테스트 (TDD)"
      contains: "@Test"
    - path: "app/src/test/java/com/example/smart_safety_management/tbm/TbmParticipantsReducerTest.kt"
      provides: "TbmParticipantsReducer 단위 테스트 (TDD)"
      contains: "@Test"
    - path: "app/src/test/java/com/example/smart_safety_management/tbm/SignatureStateTest.kt"
      provides: "SignatureState.toPngBytes 단위 테스트 (TDD)"
      contains: "@Test"
  key_links:
    - from: "HomeActivity.setupTbmDashboardCard"
      to: "TbmDashboardCardComposable"
      via: "ComposeView.setContent { Smart_Safety_ManagementTheme { ... } }"
      pattern: "TbmDashboardCardComposable"
    - from: "HomeWorkerActivity.setupTbmCard"
      to: "TbmWorkerCardComposable"
      via: "ComposeView.setContent { Smart_Safety_ManagementTheme { ... } }"
      pattern: "TbmWorkerCardComposable"
    - from: "TbmWorkerActivity (참여 확인 버튼)"
      to: "tbm-checkin Edge Function"
      via: "TbmRetrofitApi.checkin(...) → POST /functions/v1/notifications action=tbm-checkin"
      pattern: "tbm-checkin"
    - from: "TbmDashboardActivity (start 버튼)"
      to: "tbm-start Edge Function"
      via: "TbmRetrofitApi.start(...) → POST /functions/v1/notifications action=tbm-start"
      pattern: "tbm-start"
    - from: "SignatureCanvas (참여 시 PNG 업로드)"
      to: "Supabase Storage tbm-signatures bucket"
      via: "supabase.storage.from('tbm-signatures').upload(path, bytes)"
      pattern: "tbm-signatures"
    - from: "MyFirebaseMessagingService"
      to: "TbmWorkerActivity / TbmDashboardActivity"
      via: "data.type='tbm_alert' 분기 + UserSession.userRole 분기 + pendingIntent"
      pattern: "tbm_alert"
    - from: "TbmRepository.todaySessionFlow"
      to: "supabase_realtime publication 의 tbm_sessions"
      via: "supabase.channel(...).postgresChangeFlow + filter eq group_id (Stage A)"
      pattern: "todaySessionFlow"
---

<objective>
Phase 9 Wave 2 (parallel with Plan 09-02) — Android 클라이언트 본체. (1) `tbm/` 신규 패키지
13 main + 4 test 파일 (watch/ 9 파일 1:1 미러 + SignatureCanvas + 2 validator + TbmStartSection),
(2) HomeActivity + HomeWorkerActivity 에 ComposeView 카드 1개씩 임베드 (Pitfall 12 Theme 래핑),
(3) TbmDashboardActivity + TbmWorkerActivity 신규 (manager / worker 권한 가드),
(4) MyFirebaseMessagingService `tbm_alert` 분기 추가, (5) storage-kt:2.2.0 의존성 + MyApp 의
Storage plugin install (변경 1줄), (6) 4 unit test files (TDD) — WorkTypeValidator +
ExpectedEndAtValidator + TbmParticipantsReducer + SignatureState.

Purpose: TBM-02 의 작업자/관리자 화면 완성. Plan 09-01 의 schema + Plan 09-02 의 Edge Function
4 case 와 합쳐서 Phase 9 SC #1·#2·#3 모두 합성 검증 가능 (Plan 09-04 의 시연만 남음).
Phase 7 의 watch/ 패키지 + ComposeView 패턴 + SafetyAlertReducer 1:1 미러 — 신규 작성은
Compose Canvas (SignatureCanvas) + Dynamic session_id 2-stage subscription + 2 validator 만.

Output:
- 13 main 파일 in `app/.../tbm/` + 4 test 파일
- 2 신규 Activity (TbmDashboardActivity + TbmWorkerActivity) + 등록
- HomeActivity + HomeWorkerActivity setup{Tbm*}Card 추가 + main_home.xml + main_home_worker.xml ComposeView 추가
- MyFirebaseMessagingService 분기 1개 추가
- app/build.gradle.kts: storage-kt:2.2.0 추가
- MyApp.kt: install(Storage) 1줄 추가
- compileDebugKotlin BUILD SUCCESSFUL
- 4 test files PASS (14+ cases)
- 회귀: watch/ git diff 0 (Phase 7 보존, SC #4 코드 경로 분리)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tbm-worker-guide/09-CONTEXT.md
@.planning/phases/09-tbm-worker-guide/09-RESEARCH.md
@.planning/phases/07-watch-app-bridge/07-03-PLAN.md
@.planning/phases/07-watch-app-bridge/07-03-SUMMARY.md
@app/build.gradle.kts
@app/src/main/java/com/example/smart_safety_management/MyApp.kt
@app/src/main/java/com/example/smart_safety_management/HomeActivity.kt
@app/src/main/java/com/example/smart_safety_management/HomeWorkerActivity.kt
@app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt
@app/src/main/java/com/example/smart_safety_management/UserSession.kt
@app/src/main/java/com/example/smart_safety_management/watch/SupabaseModule.kt
@app/src/main/java/com/example/smart_safety_management/watch/WatchRealtimeRepository.kt
@app/src/main/java/com/example/smart_safety_management/watch/WatchCardComposable.kt
@app/src/main/java/com/example/smart_safety_management/watch/SafetyAlertsScreen.kt
@app/src/main/java/com/example/smart_safety_management/watch/PairWatchSection.kt
@app/src/main/AndroidManifest.xml
@app/src/main/res/layout/main_home.xml
@app/src/main/res/layout/main_home_worker.xml

<interfaces>
<!-- Plan 09-01 의 schema (013_tbm_schema.sql) + Plan 09-02 의 Edge Function 계약 -->

<!-- 013 schema (TbmModels @Serializable 의 SerialName 기준) -->
tbm_sessions (snake_case columns):
  session_id, group_id, session_date, started_at, ended_at, expected_end_at,
  leader_user_id, work_type, location, notes, missed_alert_at, created_at

tbm_templates: template_id, work_type, title, checklist (JSONB array), created_at

tbm_checklists: checklist_id, session_id, item_idx, item_text, is_checked, note, checked_at

tbm_participants: participant_id, session_id, user_id, signed_at, signature_url, method

<!-- Plan 09-02 의 Edge Function 4 actions (Android Retrofit POST /functions/v1/notifications) -->
tbm-start request: {action, leader_user_id, group_id, work_type, expected_end_at, location?, notes?}
  → 200 {ok, session_id, checklist_count, notified_count}
  → 409 (UNIQUE 23505 — "이미 오늘 세션")
  → 400 (payload 누락)

tbm-checkin request: {action, session_id, user_id, signature_url?}
  → 200 {ok, participant_id, signed_at} (정상 또는 idempotent:true)
  → 403 "user not in session group" (T-9-03)
  → 404 "session not found" / 410 "already ended"

tbm-end request: {action, session_id, leader_user_id}
  → 200 {ok, ended_at, participant_count}
  → 404 (leader 불일치 또는 already ended — T-9-04)
  → 400 (payload 누락)

<!-- Phase 7 SupabaseModule.kt (재사용 — install(Storage) 만 추가) -->
object SupabaseModule {
    val client: SupabaseClient by lazy {
        createSupabaseClient(SUPABASE_URL, SUPABASE_ANON_KEY) {
            install(Postgrest) { /* ... */ }
            install(Realtime)  { /* ... */ }
            install(Storage)   { /* Phase 9 신규 1줄 — RESEARCH §Pattern 3 */ }
            httpEngine = createKtorCioEngine()
        }
    }
}

<!-- Phase 7 HomeWorkerActivity.setupWatchCard (Phase 9 가 setupTbmCard 동일 패턴 추가) -->
private fun setupWatchCard() {
    val composeView = findViewById<ComposeView>(R.id.watch_card_compose)
    composeView.setViewCompositionStrategy(ViewCompositionStrategy.DisposeOnLifecycleDestroyed(this))
    composeView.setContent {
        Smart_Safety_ManagementTheme {
            // ... WatchCardComposable
        }
    }
}

<!-- 컬러 / 라벨 / status badge 패턴 (Phase 7 WearStateLabel) -->
색상: Color(0xFF4CAF50) 초록 / Color(0xFFFFC107) 노랑 / Color(0xFF9E9E9E) 회색 / Color(0xFFF44336) 빨강
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/build.gradle.kts + MyApp.kt — storage-kt 2.2.0 의존성 + Storage plugin install (Wave 0 build infra)</name>
  <read_first>
    - app/build.gradle.kts (line 155-165, supabase-kt 2.2.0 의존성 블록)
    - app/src/main/java/com/example/smart_safety_management/MyApp.kt (전체 — 기존 by-lazy 싱글톤)
    - app/src/main/java/com/example/smart_safety_management/watch/SupabaseModule.kt (기존 createSupabaseClient 호출 패턴)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pattern 3 line 526-592 (storage-kt 업로드 + signed URL 코드 + Pitfall 10 gotrue-kt transitive)
    - .planning/phases/07-watch-app-bridge/07-01-SUMMARY.md (supabase-kt 2.2.0 lock 검증 + Pitfall 1 3.x 거부 회귀 가드)
  </read_first>
  <action>
    1. **`app/build.gradle.kts` dependencies 블록에 추가** (line 158 의 postgrest-kt 옆):
       ```kotlin
       // Phase 9 / 09-03 TBM-02 — Storage 모듈 (수기 서명 PNG 업로드용).
       // ABI 호환: realtime-kt + postgrest-kt 2.2.0 와 동일 버전 강제 (Pitfall 1 — 3.x 거부 유지).
       // Transitive: gotrue-kt:2.2.0 + ktor-server-core/cio:2.3.9 자동 (Pitfall 10 acceptable, APK +50KB).
       implementation("io.github.jan-tennert.supabase:storage-kt:2.2.0")
       ```

    2. **`app/proguard-rules.pro` 에 gotrue-kt + ktor-server transitive 무시 룰 추가** (Pitfall 10):
       ```pro
       # Phase 9 / 09-03 — storage-kt 2.2.0 transitive (gotrue-kt + ktor-server-*).
       -dontwarn io.github.jan.supabase.gotrue.**
       -dontwarn io.ktor.server.**
       ```

    3. **`MyApp.kt` 의 supabase by-lazy 블록에 `install(Storage)` 1줄 추가** (변경 1줄):
       기존:
       ```kotlin
       val supabase: SupabaseClient by lazy {
           createSupabaseClient(BuildConfig.SUPABASE_URL, BuildConfig.SUPABASE_ANON_KEY) {
               install(Postgrest)
               install(Realtime)
               httpEngine = engine(CIO)
           }
       }
       ```
       추가 후:
       ```kotlin
       val supabase: SupabaseClient by lazy {
           createSupabaseClient(BuildConfig.SUPABASE_URL, BuildConfig.SUPABASE_ANON_KEY) {
               install(Postgrest)
               install(Realtime)
               install(Storage)  // Phase 9 / 09-03 TBM-02 — 수기 서명 PNG 업로드용
               httpEngine = engine(CIO)
           }
       }
       ```
       import 추가: `import io.github.jan.supabase.storage.Storage`

    4. **회귀 가드 — supabase-kt 3.x 거부**: `app/build.gradle.kts` 본문에 `realtime-kt:3.`,
       `postgrest-kt:3.`, `storage-kt:3.` 모두 0회 (Pitfall 1 회귀 가드 유지). grep 검증.

    5. **Gradle resolve 검증** (Wave 0 ground truth):
       ```bash
       cd app && ./gradlew app:dependencies --configuration releaseRuntimeClasspath | grep -E "supabase|ktor|gotrue" | head -20
       ```
       기대: realtime-kt:2.2.0 + postgrest-kt:2.2.0 + storage-kt:2.2.0 + gotrue-kt:2.2.0
       + ktor-client-cio:2.3.9 + ktor-server-core/cio:2.3.9 transitive 모두 등장.
  </action>
  <verify>
    <automated>grep -c 'storage-kt:2\.2\.0' app/build.gradle.kts && grep -c 'install(Storage)' app/src/main/java/com/example/smart_safety_management/MyApp.kt && grep -c 'import io.github.jan.supabase.storage.Storage' app/src/main/java/com/example/smart_safety_management/MyApp.kt && [ "$(grep -c 'realtime-kt:3\.' app/build.gradle.kts)" = "0" ] && [ "$(grep -c 'storage-kt:3\.' app/build.gradle.kts)" = "0" ] && echo OK</automated>
  </verify>
  <done>
    - `app/build.gradle.kts` 에 `storage-kt:2.2.0` 1회 추가
    - `app/proguard-rules.pro` 에 gotrue + ktor-server dontwarn 추가
    - `MyApp.kt` 에 `install(Storage)` 1줄 + import
    - 회귀: realtime-kt:3.x / storage-kt:3.x 등장 0건
    - Gradle resolve 통과 (storage-kt:2.2.0 + gotrue-kt:2.2.0 + ktor-server-* transitive 등장)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tbm/ 패키지 신규 + Models + Validator + Reducer + TDD 4 test files</name>
  <read_first>
    - app/src/main/java/com/example/smart_safety_management/watch/WatchModels.kt (@Serializable + @SerialName snake_case 패턴)
    - app/src/main/java/com/example/smart_safety_management/watch/MacAddressValidator.kt (Validator object 패턴)
    - app/src/main/java/com/example/smart_safety_management/watch/WearStateLabel.kt (5 상태 enum + 색상 mapper 패턴)
    - app/src/test/java/com/example/smart_safety_management/watch/MacAddressValidatorTest.kt (JUnit 5 단위 테스트 패턴)
    - app/src/test/java/com/example/smart_safety_management/watch/WatchRealtimeRepositoryTest.kt (PostgresAction Reducer 테스트 — applyDirect 패턴)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Example A line 928-948 (WorkTypeValidator + test)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Example B line 950-980 (TbmParticipantsReducer + applyDirect 패턴)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pitfall 8 (TIMESTAMPTZ + ZonedDateTime ISO_OFFSET_DATE_TIME)
  </read_first>
  <behavior>
    **WorkTypeValidator (5+ test cases):**
    - isValid("fire") → true
    - isValid("welding") → false (unknown)
    - isValid("") → false
    - isValid("FIRE") → false (대소문자 sensitive — normalize 전)
    - normalize("FIRE ") → "fire" (lowercase + trim)
    - normalize("Electric") + isValid → true

    **ExpectedEndAtValidator (3+ test cases):**
    - validate("2026-05-18T09:15:00+09:00") → Ok(ZonedDateTime)
    - validate("2026-05-18T09:15:00") → Err("offset required") — Pitfall 8
    - validate("invalid") → Err
    - validate("1999-01-01T00:00:00+09:00") → Err("must be future") (옵션 — 과거 거부)

    **TbmParticipantsReducer (4+ test cases):**
    - apply(empty, INSERT row1) → [row1]
    - apply([row1], INSERT row1 dup) → [row1] (id 같으면 update)
    - apply([row1, row2], UPDATE row1 with new signed_at) → [updated row1, row2]
    - apply([row1, row2], INSERT row3) → [row1, row2, row3]
    - apply([row1], DELETE row1) → [] (옵션)

    **SignatureState (2+ test cases):**
    - toPngBytes() when canvasSize=0 → ByteArray(0)
    - 1 path 추가 + canvasSize 설정 후 toPngBytes() → bytes.size > 0 + PNG magic header (8 bytes: 0x89 0x50 0x4E 0x47 ...)
  </behavior>
  <action>
    1. **TDD RED — 4 test files 먼저 작성 (failing)**:

       **`app/src/test/java/com/example/smart_safety_management/tbm/WorkTypeValidatorTest.kt`** (5+ cases, JUnit5):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import org.junit.jupiter.api.Test
       import org.junit.jupiter.api.Assertions.*

       class WorkTypeValidatorTest {
         @Test fun `valid fire`() = assertTrue(WorkTypeValidator.isValid("fire"))
         @Test fun `valid all 5 types`() {
           listOf("fire","electric","height","heavy","general").forEach {
             assertTrue(WorkTypeValidator.isValid(it), "$it should be valid")
           }
         }
         @Test fun `invalid empty`() = assertFalse(WorkTypeValidator.isValid(""))
         @Test fun `invalid unknown`() = assertFalse(WorkTypeValidator.isValid("welding"))
         @Test fun `case sensitive before normalize`() = assertFalse(WorkTypeValidator.isValid("FIRE"))
         @Test fun `normalize uppercase to lower`() = assertEquals("fire", WorkTypeValidator.normalize("FIRE "))
         @Test fun `normalize trims whitespace`() = assertEquals("electric", WorkTypeValidator.normalize("  Electric  "))
       }
       ```

       **`ExpectedEndAtValidatorTest.kt`** (3+ cases):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import org.junit.jupiter.api.Test
       import org.junit.jupiter.api.Assertions.*

       class ExpectedEndAtValidatorTest {
         @Test fun `valid ISO offset`() {
           assertTrue(ExpectedEndAtValidator.parse("2026-05-18T09:15:00+09:00").isSuccess)
         }
         @Test fun `reject no offset`() {
           assertTrue(ExpectedEndAtValidator.parse("2026-05-18T09:15:00").isFailure)
         }
         @Test fun `reject invalid string`() {
           assertTrue(ExpectedEndAtValidator.parse("not a timestamp").isFailure)
         }
         @Test fun `accept Z UTC marker`() {
           assertTrue(ExpectedEndAtValidator.parse("2026-05-18T00:15:00Z").isSuccess)
         }
       }
       ```

       **`TbmParticipantsReducerTest.kt`** (4+ cases — Phase 7 WatchRealtimeRepositoryTest applyDirect 패턴):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import org.junit.jupiter.api.Test
       import org.junit.jupiter.api.Assertions.*

       class TbmParticipantsReducerTest {
         private fun row(id: Long, uid: String, signedAt: String = "2026-05-18T09:00:00Z") =
           TbmParticipantRow(participantId = id, sessionId = 1, userId = uid, signedAt = signedAt,
                             signatureUrl = null, method = "signature")

         @Test fun `insert into empty`() {
           val r = TbmParticipantsReducer.applyDirect(emptyList(), ChangeKind.INSERT, row(1, "a"))
           assertEquals(1, r.size); assertEquals(1L, r[0].participantId)
         }
         @Test fun `insert duplicate id updates in place`() {
           val initial = listOf(row(1, "a"))
           val r = TbmParticipantsReducer.applyDirect(initial, ChangeKind.INSERT,
                                                     row(1, "a", "2026-05-18T09:01:00Z"))
           assertEquals(1, r.size); assertEquals("2026-05-18T09:01:00Z", r[0].signedAt)
         }
         @Test fun `update preserves unrelated`() {
           val initial = listOf(row(1, "a"), row(2, "b"))
           val r = TbmParticipantsReducer.applyDirect(initial, ChangeKind.UPDATE, row(1, "a", "2026-05-18T09:02:00Z"))
           assertEquals(2, r.size); assertEquals("2026-05-18T09:02:00Z", r.first { it.participantId == 1L }.signedAt)
         }
         @Test fun `insert appends new`() {
           val initial = listOf(row(1, "a"))
           val r = TbmParticipantsReducer.applyDirect(initial, ChangeKind.INSERT, row(2, "b"))
           assertEquals(2, r.size)
         }
         @Test fun `delete removes`() {
           val initial = listOf(row(1, "a"), row(2, "b"))
           val r = TbmParticipantsReducer.applyDirect(initial, ChangeKind.DELETE, row(1, "a"))
           assertEquals(1, r.size); assertEquals(2L, r[0].participantId)
         }
       }
       ```

       **`SignatureStateTest.kt`** (2+ cases — JVM unit test 환경에서 Android Bitmap mock):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import org.junit.jupiter.api.Test
       import org.junit.jupiter.api.Assertions.*
       import androidx.compose.ui.unit.IntSize

       class SignatureStateTest {
         @Test fun `empty when no paths`() {
           val s = SignatureState()
           assertTrue(s.isEmpty)
         }
         @Test fun `canvasSize zero yields empty bytes`() {
           val s = SignatureState()
           // canvasSize remains IntSize.Zero
           val bytes = s.toPngBytes()
           assertEquals(0, bytes.size)
         }
         // NOTE: PNG bytes 검증은 instrumented test (Bitmap 의 native heap 필요) — JVM unit 에선 skip.
         // SignatureState.isEmpty + canvasSize=Zero 가드 만 단위 테스트로 검증.
       }
       ```

    2. **TDD GREEN — main 파일 작성**:

       **`tbm/WorkTypeValidator.kt`** (RESEARCH Example A 그대로):
       ```kotlin
       package com.example.smart_safety_management.tbm
       object WorkTypeValidator {
         val ALLOWED: Set<String> = setOf("fire","electric","height","heavy","general")
         fun isValid(workType: String): Boolean = workType in ALLOWED
         fun normalize(input: String): String = input.lowercase().trim()
       }
       ```

       **`tbm/ExpectedEndAtValidator.kt`** (Pitfall 8 timezone):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import java.time.ZonedDateTime
       import java.time.format.DateTimeFormatter
       import java.time.format.DateTimeParseException

       object ExpectedEndAtValidator {
         fun parse(input: String): Result<ZonedDateTime> = runCatching {
           ZonedDateTime.parse(input, DateTimeFormatter.ISO_OFFSET_DATE_TIME)
         }.recoverCatching {
           ZonedDateTime.parse(input, DateTimeFormatter.ISO_ZONED_DATE_TIME)
         }
         fun nowKst(): ZonedDateTime =
           ZonedDateTime.now(java.time.ZoneId.of("Asia/Seoul"))
         fun formatForServer(z: ZonedDateTime): String =
           z.format(DateTimeFormatter.ISO_OFFSET_DATE_TIME)
       }
       ```

       **`tbm/TbmModels.kt`** (RESEARCH Example B + WatchModels 패턴 미러):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import kotlinx.serialization.SerialName
       import kotlinx.serialization.Serializable

       @Serializable
       data class TbmSessionRow(
         @SerialName("session_id") val sessionId: Long,
         @SerialName("group_id") val groupId: Int,
         @SerialName("session_date") val sessionDate: String,
         @SerialName("started_at") val startedAt: String,
         @SerialName("ended_at") val endedAt: String? = null,
         @SerialName("expected_end_at") val expectedEndAt: String,
         @SerialName("leader_user_id") val leaderUserId: String,
         @SerialName("work_type") val workType: String,
         val location: String? = null,
         val notes: String? = null,
         @SerialName("missed_alert_at") val missedAlertAt: String? = null,
       )

       @Serializable
       data class TbmTemplateRow(
         @SerialName("template_id") val templateId: Int,
         @SerialName("work_type") val workType: String,
         val title: String,
         val checklist: List<String>,
       )

       @Serializable
       data class TbmChecklistRow(
         @SerialName("checklist_id") val checklistId: Long,
         @SerialName("session_id") val sessionId: Long,
         @SerialName("item_idx") val itemIdx: Int,
         @SerialName("item_text") val itemText: String,
         @SerialName("is_checked") val isChecked: Boolean,
         val note: String? = null,
         @SerialName("checked_at") val checkedAt: String? = null,
       )

       @Serializable
       data class TbmParticipantRow(
         @SerialName("participant_id") val participantId: Long,
         @SerialName("session_id") val sessionId: Long,
         @SerialName("user_id") val userId: String,
         @SerialName("signed_at") val signedAt: String,
         @SerialName("signature_url") val signatureUrl: String? = null,
         val method: String = "signature",
       )

       enum class ChangeKind { INSERT, UPDATE, DELETE }
       ```

       **`tbm/TbmParticipantsReducer.kt`** (RESEARCH Example B 그대로 + applyDirect):
       ```kotlin
       package com.example.smart_safety_management.tbm
       import io.github.jan.supabase.realtime.PostgresAction

       object TbmParticipantsReducer {
         fun apply(current: List<TbmParticipantRow>, action: PostgresAction): List<TbmParticipantRow> = when (action) {
           is PostgresAction.Insert -> {
             val row = action.decodeRecord<TbmParticipantRow>()
             if (current.any { it.participantId == row.participantId })
               current.map { if (it.participantId == row.participantId) row else it }
             else current + listOf(row)
           }
           is PostgresAction.Update -> {
             val u = action.decodeRecord<TbmParticipantRow>()
             current.map { if (it.participantId == u.participantId) u else it }
           }
           is PostgresAction.Delete -> {
             val key = action.oldRecord["participant_id"]?.toString()?.toLongOrNull()
             if (key == null) current else current.filterNot { it.participantId == key }
           }
           else -> current
         }

         fun applyDirect(current: List<TbmParticipantRow>, kind: ChangeKind, row: TbmParticipantRow): List<TbmParticipantRow> =
           when (kind) {
             ChangeKind.INSERT -> if (current.any { it.participantId == row.participantId })
               current.map { if (it.participantId == row.participantId) row else it }
             else current + listOf(row)
             ChangeKind.UPDATE -> current.map { if (it.participantId == row.participantId) row else it }
             ChangeKind.DELETE -> current.filterNot { it.participantId == row.participantId }
           }
       }
       ```

       **`tbm/SignatureCanvas.kt`** (RESEARCH Pattern 1 line 296-410 그대로 — Pitfall 1·2 적용 verbatim):
       — class SignatureState (paths + currentPath + canvasSize + isEmpty + clear + toPngBytes — Bitmap.recycle finally)
       — @Composable fun SignatureCanvas (detectDragGestures + onDragStart Path moveTo + onDrag lineTo + state.currentPath = state.currentPath setter 강제)

    3. **TDD VERIFY — 4 test files RED→GREEN**:
       ```bash
       cd app && ./gradlew app:testDebugUnitTest --tests "com.example.smart_safety_management.tbm.*"
       # Korean repo path workaround: layout.buildDirectory.set('D:/ssm-app-build') 적용됨 (Phase 7)
       ```
       기대: 4 test files 모두 PASS, 14+ cases total.

    4. **회귀**: 기존 watch/ test 26 cases 모두 PASS 유지 (`testDebugUnitTest --tests "...watch.*"`).
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/example/smart_safety_management/tbm/WorkTypeValidator.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidator.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmModels.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmParticipantsReducer.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/SignatureCanvas.kt && test -f app/src/test/java/com/example/smart_safety_management/tbm/WorkTypeValidatorTest.kt && test -f app/src/test/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidatorTest.kt && test -f app/src/test/java/com/example/smart_safety_management/tbm/TbmParticipantsReducerTest.kt && test -f app/src/test/java/com/example/smart_safety_management/tbm/SignatureStateTest.kt && grep -c "ISO_OFFSET_DATE_TIME" app/src/main/java/com/example/smart_safety_management/tbm/ExpectedEndAtValidator.kt && grep -c "if (!androidBitmap.isRecycled)" app/src/main/java/com/example/smart_safety_management/tbm/SignatureCanvas.kt && grep -c "state.currentPath = state.currentPath" app/src/main/java/com/example/smart_safety_management/tbm/SignatureCanvas.kt && echo OK</automated>
  </verify>
  <done>
    - 5 main files (WorkTypeValidator + ExpectedEndAtValidator + TbmModels + TbmParticipantsReducer + SignatureCanvas) 작성
    - 4 test files (WorkTypeValidatorTest + ExpectedEndAtValidatorTest + TbmParticipantsReducerTest + SignatureStateTest) 작성
    - `./gradlew app:testDebugUnitTest --tests "*.tbm.*"` 14+ cases ALL PASS
    - Pitfall 1 적용: SignatureCanvas 의 `if (!androidBitmap.isRecycled) androidBitmap.recycle()` finally 블록
    - Pitfall 2 적용: SignatureCanvas 의 `state.currentPath = state.currentPath` setter 강제
    - Pitfall 8 적용: ExpectedEndAtValidator 의 ISO_OFFSET_DATE_TIME 강제 + offset 누락 거부
    - 회귀: watch/ 26 cases 유지
  </done>
</task>

<task type="auto">
  <name>Task 3: TbmRepository (Dynamic 2-stage Realtime, 3 채널) + TbmRetrofitApi + TbmCardComposables + TbmStartSection</name>
  <read_first>
    - app/src/main/java/com/example/smart_safety_management/watch/WatchRealtimeRepository.kt (3 채널 postgresChangeFlow + Status.CONNECTED 분기 + polling fallback)
    - app/src/main/java/com/example/smart_safety_management/watch/WatchAckRetrofitApi.kt (Retrofit POST /functions/v1/notifications 패턴)
    - app/src/main/java/com/example/smart_safety_management/watch/WatchCardComposable.kt (status badge 3-색상 + Realtime collectAsState 패턴)
    - app/src/main/java/com/example/smart_safety_management/watch/PairWatchSection.kt (Edge Function 호출 + status badge)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pattern 2 line 412-525 (Dynamic session_id 2-stage subscription)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Anti-patterns line 805 (tbm_templates 채널 구독 X — seed only, 3 채널만)
    - .planning/phases/09-tbm-worker-guide/09-CONTEXT.md §D-06·D-07 (manager 카드 3줄 / worker 카드 4 상태)
    - .planning/phases/09-tbm-worker-guide/09-02-PLAN.md (Edge Function 4 actions request/response 계약)
  </read_first>
  <action>
    1. **`tbm/TbmRetrofitApi.kt`** (Phase 7 WatchAckRetrofitApi 패턴 미러 — 4 actions):
       - interface `TbmRetrofitApi` with @POST("functions/v1/notifications") 메서드 4개
       - request body data class 4 (TbmStartRequest / TbmCheckinRequest / TbmEndRequest / TbmMissedRequest) with `action: String` lit
       - response: 200 → ApiResponse data class + 4xx → IOException
       - Builder: Retrofit + ScalarsConverterFactory + Bearer anon key header

    2. **`tbm/TbmRepository.kt`** (RESEARCH Pattern 2 line 412-525 + Phase 7 WatchRealtimeRepository 패턴):

       **3 채널 (anti-pattern: tbm_templates 채널 X — seed only)**:
       - `todaySessionFlow(groupId: Int): Flow<TbmSessionRow?>` — Stage A
         초기 PostgREST fetch + supabase.channel("tbm_sessions:group_$groupId").postgresChangeFlow + filter("group_id", EQ, groupId).
         session_date == today 인 row 만 emit, ended_at IS NOT NULL 인 row 도 emit (worker 가 종료 상태 표시).
       - `participantsFlow(sessionId: Long): Flow<List<TbmParticipantRow>>` — Stage B
         초기 PostgREST select WHERE session_id=$1 ORDER BY signed_at + channel filter("session_id", EQ, sessionId).
         TbmParticipantsReducer.apply 사용.
       - `checklistsFlow(sessionId: Long): Flow<List<TbmChecklistRow>>` — Stage B
         동일 패턴, filter("session_id", EQ, sessionId).

       **tbm_templates**: `suspend fun fetchTemplates(): List<TbmTemplateRow>` PostgREST 1회 select (Realtime 구독 X — research C2 anti-pattern).

       **status flow**: Phase 7 Realtime.Status.CONNECTED 분기 + 5초 polling fallback 동일 패턴 (manager 가 디버깅 가능하도록).

    3. **`tbm/TbmDashboardCardComposable.kt`** (manager HomeActivity 카드 — D-06):
       - 3줄 layout:
         (a) **상태 badge**: 회색 "세션 없음" / 노랑 "진행 중 ({checked}/{total})" / 초록 "완료" / 빨강 "⚠ 미참여 알림 발사됨"
         (b) **참여 카운트**: `{참여}/{대상}` (예: `5/8 명`)
         (c) **미참여 알림 상태**: missed_alert_at NOT NULL 시 "⚠ 미참여 X명 알림 발송 — {time}"
       - 클릭 시 `onClickDashboard()` 콜백 → TbmDashboardActivity 진입
       - 데이터: groupId + supabase 받아서 todaySessionFlow + participantsFlow collectAsState
       - Phase 7 WatchCardComposable 의 status badge 3-색상 패턴 직접 미러

    4. **`tbm/TbmWorkerCardComposable.kt`** (worker HomeWorkerActivity 카드 — D-07):
       - 4 상태 (CONTEXT D-07):
         - "세션 없음" (오늘 group 의 tbm_sessions row 부재) — 회색 + 클릭 비활성
         - "⚠ TBM 참여 필요 (예정 종료 {time})" — 노랑 + 클릭 → TbmWorkerActivity
         - "✓ TBM 참여 완료 {time}" — 초록 + 클릭 → 참여 내역 표시
         - "오늘 TBM 종료 ({time})" — 회색 + 클릭 → 참여 내역 표시
       - 데이터: groupId + userId + supabase + todaySessionFlow + participantsFlow 의 본인 row 검색

    5. **`tbm/TbmStartSection.kt`** (manager 작업유형 선택 + expected_end_at 입력):
       - DropdownMenu: 5 work_type (fire/electric/height/heavy/general) — title 표시 (TbmTemplateRow.title)
       - TimePicker 또는 OutlinedTextField with ISO format placeholder — default "현재시각 + 15분" (CONTEXT Claude's Discretion + ExpectedEndAtValidator.nowKst().plusMinutes(15))
       - "세션 시작" 버튼 → TbmRetrofitApi.start(...) → 200 시 onSuccess(session_id), 409 시 Toast "이미 오늘 세션", 400 시 Toast "필수 항목 누락"
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmRetrofitApi.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmDashboardCardComposable.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmWorkerCardComposable.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmStartSection.kt && grep -c "todaySessionFlow" app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt && grep -c "participantsFlow" app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt && grep -c "checklistsFlow" app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt && [ "$(grep -c 'channel.*tbm_templates' app/src/main/java/com/example/smart_safety_management/tbm/TbmRepository.kt)" = "0" ] && echo OK</automated>
  </verify>
  <done>
    - 5 신규 main 파일 (TbmRetrofitApi + TbmRepository + TbmDashboardCardComposable + TbmWorkerCardComposable + TbmStartSection)
    - TbmRepository: 3 채널 구독 (todaySessionFlow + participantsFlow + checklistsFlow) + tbm_templates 채널 구독 0건 (anti-pattern)
    - TbmDashboardCardComposable: 3줄 layout + 4 상태 badge + 클릭 콜백
    - TbmWorkerCardComposable: 4 상태 badge + 클릭 콜백 (CONTEXT D-07)
    - TbmStartSection: 5 work_type Dropdown + expected_end_at TimePicker + Retrofit start 호출
  </done>
</task>

<task type="auto">
  <name>Task 4: TbmDashboardActivity + TbmWorkerActivity + TbmDashboardScreen + TbmWorkerScreen + HomeActivity·HomeWorkerActivity ComposeView 임베드 + Manifest + layout</name>
  <read_first>
    - app/src/main/java/com/example/smart_safety_management/SafetyAlertsActivity.kt (Phase 7 ComponentActivity + ComposeView root + Smart_Safety_ManagementTheme 패턴)
    - app/src/main/java/com/example/smart_safety_management/HomeWorkerActivity.kt (line 47-79, setupWatchCard 패턴 — ComposeView findViewById + setContent { Smart_Safety_ManagementTheme { ... } } + DisposeOnLifecycleDestroyed)
    - app/src/main/java/com/example/smart_safety_management/HomeActivity.kt (전체 — 첫 ComposeView 임베드 위치 결정)
    - app/src/main/AndroidManifest.xml (기존 Activity 등록 패턴)
    - app/src/main/res/layout/main_home.xml (manager 홈 layout, TBM 카드 추가 위치)
    - app/src/main/res/layout/main_home_worker.xml (worker 홈 layout, watch_card_compose 아래 추가)
    - app/src/main/java/com/example/smart_safety_management/UserSession.kt + UserRole.kt (manager 권한 가드)
    - app/src/main/java/com/example/smart_safety_management/watch/SafetyAlertsScreen.kt (Compose Screen 패턴)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pitfall 12 (HomeActivity 첫 ComposeView Theme 래핑 강제)
    - .planning/phases/09-tbm-worker-guide/09-CONTEXT.md §D-06·D-07 (Dashboard / Worker 화면 spec)
  </read_first>
  <action>
    1. **`tbm/TbmDashboardScreen.kt`** (Compose Screen — TbmDashboardActivity 본체, D-06):
       - 상단: 오늘 세션 정보 카드 (work_type title, leader 이름, expected_end_at, location)
       - 세션 없음 시: TbmStartSection 표시 (Task 3) — start 버튼 → 성공 시 session_id 보관 후 dashboard 로 전환
       - 세션 active 시: 체크리스트 LazyColumn (read-only — manager 가 입력한 is_checked 상태 표시) + manager 가 추가 체크 가능
       - 참여자 grid: TbmParticipantsReducer 의 결과 List → profile_image (Glide 또는 Coil) + 이름 + 체크인 시각 + 서명 thumbnail (클릭 → 모달 다이얼로그 + supabase.storage.from('tbm-signatures').createSignedUrl(path, 60.seconds) 발급 — Pitfall 4 mitigation: 모달 dispose 시 새 URL 재발급)
       - 미참여자 grid: 회색 표시 + (옵션) "수동 알림" 버튼 — v1.0 한정 비활성, v1.1 TODO comment
       - 하단: "세션 종료" 버튼 (manager only) → TbmRetrofitApi.end(...)

    2. **`tbm/TbmWorkerScreen.kt`** (TbmWorkerActivity 본체, D-07):
       - 상단: 세션 정보 (work_type title, leader, expected_end_at)
       - 중단: 체크리스트 LazyColumn read-only — 각 항목 ✓/✗ icon + note
       - 하단: 본인 참여 여부 확인
         - 미참여: SignatureCanvas + "지우기" / "참여 확인" 버튼 → (1) SignatureState.toPngBytes() → ByteArray, (2) supabase.storage.from('tbm-signatures').upload("$sessionId/${userId}_${System.currentTimeMillis()}.png", bytes), (3) signatureUrl 받아서 TbmRetrofitApi.checkin(...) (사진 path 만 전송 — Edge Function 이 path 받음), (4) 200 시 Toast "참여 완료" + UI 본인 참여 row 표시.
         - 참여 완료: 서명 thumbnail + "참여 완료 {time}" + 버튼 disable

    3. **`TbmDashboardActivity.kt`** (신규 ComponentActivity, manager only):
       ```kotlin
       package com.example.smart_safety_management
       import androidx.activity.ComponentActivity
       import androidx.activity.compose.setContent
       import com.example.smart_safety_management.ui.theme.Smart_Safety_ManagementTheme
       import com.example.smart_safety_management.tbm.TbmDashboardScreen

       class TbmDashboardActivity : ComponentActivity() {
         override fun onCreate(savedInstanceState: Bundle?) {
           super.onCreate(savedInstanceState)
           // 권한 가드 — manager only
           if (UserSession.userRole != UserRole.MANAGER && UserSession.userRole != UserRole.GENERAL_MANAGER) {
             Toast.makeText(this, "관리자만 접근 가능합니다", Toast.LENGTH_SHORT).show()
             finish(); return
           }
           val groupId = UserSession.groupId ?: run { finish(); return }
           setContent { Smart_Safety_ManagementTheme {
             TbmDashboardScreen(groupId = groupId, supabase = (application as MyApp).supabase)
           } }
         }
       }
       ```

    4. **`TbmWorkerActivity.kt`** (신규 ComponentActivity):
       ```kotlin
       class TbmWorkerActivity : ComponentActivity() {
         override fun onCreate(savedInstanceState: Bundle?) {
           super.onCreate(savedInstanceState)
           val groupId = UserSession.groupId ?: run { finish(); return }
           val userId = UserSession.userId ?: run { finish(); return }
           // FCM extras 의 session_id 만 신뢰 + DB 재조회 (Phase 7 D-02 anti-pattern 회피)
           val sessionIdFromExtras = intent.getLongExtra(EXTRA_SESSION_ID, -1L).takeIf { it > 0 }
           setContent { Smart_Safety_ManagementTheme {
             TbmWorkerScreen(
               groupId = groupId, userId = userId,
               supabase = (application as MyApp).supabase,
               sessionHintFromFcm = sessionIdFromExtras,  // optional — DB 재조회로 검증
             )
           } }
         }
         companion object { const val EXTRA_SESSION_ID = "extra_session_id" }
       }
       ```

    5. **`HomeActivity.setupTbmDashboardCard()`** (manager 의 첫 ComposeView — Pitfall 12 Theme 래핑 강제):
       - onCreate 또는 setContentView 후 `setupTbmDashboardCard()` 호출
       - findViewById<ComposeView>(R.id.tbm_dashboard_compose) + setViewCompositionStrategy(DisposeOnLifecycleDestroyed(this))
       - composeView.setContent { Smart_Safety_ManagementTheme { TbmDashboardCardComposable(groupId, onClickDashboard = { startActivity(Intent(this, TbmDashboardActivity::class.java)) }) } }

    6. **`HomeWorkerActivity.setupTbmCard()`** (Phase 7 setupWatchCard 1:1 미러):
       - findViewById<ComposeView>(R.id.tbm_card_compose)
       - DisposeOnLifecycleDestroyed
       - setContent { Smart_Safety_ManagementTheme { TbmWorkerCardComposable(groupId, userId, onClickGuide = { startActivity(Intent(this, TbmWorkerActivity::class.java)) }) } }
       - onCreate 안 setupWatchCard 호출 옆에 setupTbmCard 호출 추가

    7. **`main_home.xml` 수정**: 기존 카드 영역에 ComposeView 추가:
       ```xml
       <androidx.compose.ui.platform.ComposeView
           android:id="@+id/tbm_dashboard_compose"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:layout_marginTop="16dp" />
       ```
       정확한 위치 = manager 홈 의 기존 daily_safety_check 카드 위 (CONTEXT D-06 "planner UX 결정") — 사용자가 manager 홈 진입 시 첫 화면에서 TBM 상태 즉시 시인.

    8. **`main_home_worker.xml` 수정**: watch_card_compose 아래에 추가:
       ```xml
       <androidx.compose.ui.platform.ComposeView
           android:id="@+id/tbm_card_compose"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:layout_marginTop="16dp" />
       ```

    9. **`AndroidManifest.xml` 수정**: TbmDashboardActivity + TbmWorkerActivity 등록:
       ```xml
       <activity android:name=".TbmDashboardActivity" android:exported="false" />
       <activity android:name=".TbmWorkerActivity" android:exported="false" />
       ```

    10. **회귀 가드** — `app/src/main/java/com/example/smart_safety_management/watch/` 전체 git diff 0
        (Phase 7 보존, SC #4 코드 경로 분리). grep 검증 — Phase 9 작업 후
        `git diff --stat app/src/main/java/com/example/smart_safety_management/watch/` 빈 출력.
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/example/smart_safety_management/TbmDashboardActivity.kt && test -f app/src/main/java/com/example/smart_safety_management/TbmWorkerActivity.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmDashboardScreen.kt && test -f app/src/main/java/com/example/smart_safety_management/tbm/TbmWorkerScreen.kt && grep -c "TbmDashboardActivity" app/src/main/AndroidManifest.xml && grep -c "TbmWorkerActivity" app/src/main/AndroidManifest.xml && grep -c "tbm_dashboard_compose" app/src/main/res/layout/main_home.xml && grep -c "tbm_card_compose" app/src/main/res/layout/main_home_worker.xml && grep -c "setupTbmDashboardCard\|setupTbmCard" app/src/main/java/com/example/smart_safety_management/HomeActivity.kt && grep -c "setupTbmCard" app/src/main/java/com/example/smart_safety_management/HomeWorkerActivity.kt && grep -c "Smart_Safety_ManagementTheme" app/src/main/java/com/example/smart_safety_management/TbmDashboardActivity.kt && grep -c "Smart_Safety_ManagementTheme" app/src/main/java/com/example/smart_safety_management/TbmWorkerActivity.kt && echo OK</automated>
  </verify>
  <done>
    - 4 신규 파일 (TbmDashboardActivity + TbmWorkerActivity + TbmDashboardScreen + TbmWorkerScreen)
    - HomeActivity 의 첫 ComposeView 임베드 (Pitfall 12 — Theme 래핑 적용)
    - HomeWorkerActivity 의 두 번째 ComposeView 임베드 (watch_card_compose 아래)
    - main_home.xml + main_home_worker.xml 의 ComposeView 태그 신규
    - AndroidManifest.xml 의 2 Activity 등록 (android:exported="false")
    - manager 권한 가드 (TbmDashboardActivity 의 UserRole.MANAGER 검증)
    - FCM extras session_id DB 재조회 (Phase 7 D-02 anti-pattern 회피)
    - 회귀: watch/ 패키지 git diff 0
  </done>
</task>

<task type="auto">
  <name>Task 5: MyFirebaseMessagingService tbm_alert 분기 + compileDebugKotlin BUILD SUCCESSFUL + 단위 테스트 14+ PASS</name>
  <read_first>
    - app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt (기존 watch_alert 분기 — Phase 7)
    - .planning/phases/09-tbm-worker-guide/09-CONTEXT.md §D-09 (data.type='tbm_alert' + action_in_app 분기 + UserRole 분기)
    - .planning/phases/07-watch-app-bridge/07-03-SUMMARY.md (Korean repo path workaround layout.buildDirectory.set 패턴)
  </read_first>
  <action>
    1. **`MyFirebaseMessagingService.kt` 의 onMessageReceived 분기 추가**:
       기존 watch_alert 분기 옆에 tbm_alert 분기 1개 추가:
       ```kotlin
       when (data["type"]) {
         "watch_alert" -> showWatchAlertNotification(data)   // Phase 7
         "camera_alert" -> showCameraAlertNotification(data) // Phase 8 (or default trayed)
         "tbm_alert" -> showTbmAlertNotification(data)        // Phase 9 / D-09
         else -> // default tray
       }
       ```

    2. **`showTbmAlertNotification(data: Map<String, String>)`** 함수 신규:
       - notification id = `data["session_id"]?.toIntOrNull() ?: 0` (같은 session_id 알림 갱신 — D-09 알림 전이)
       - channel_id = `fcm_default_channel` (Option B, D-09, Android 코드 변경 0 원칙)
       - title = `data["title"] ?: "TBM 알림"`, body = `data["body"] ?: ""`
       - pendingIntent → UserSession.userRole 분기:
         - MANAGER / GENERAL_MANAGER → TbmDashboardActivity (extras 부재 — Activity 가 자체 fetch)
         - WORKER → TbmWorkerActivity (extras `EXTRA_SESSION_ID = data["session_id"]?.toLongOrNull()`)
       - data["action_in_app"] ∈ {"tbm-started", "tbm-missed", "tbm-ended"} — 본문 차별화 옵션 (logging only v1.0)

    3. **compileDebugKotlin 빌드**:
       ```bash
       cd app && ./gradlew app:compileDebugKotlin
       # Korean repo path workaround (Phase 7 검증): layout.buildDirectory.set('D:/ssm-app-build')
       # 이미 build.gradle.kts 에 적용됨 — 그대로 진행. 미적용 환경이라면 추가.
       ```
       기대: `BUILD SUCCESSFUL`. error 시 즉시 fix.

    4. **단위 테스트 14+ cases 실행**:
       ```bash
       cd app && ./gradlew app:testDebugUnitTest --tests "com.example.smart_safety_management.tbm.*"
       ```
       기대: 4 test classes, 14+ tests, 0 failure.

    5. **회귀 — watch/ 26 cases 유지**:
       ```bash
       cd app && ./gradlew app:testDebugUnitTest --tests "com.example.smart_safety_management.watch.*"
       ```
       기대: 4 test classes, 26 tests, 0 failure (Phase 7 보존).

    6. **회귀 — ai_agent/tests/ 28/28 PASS**:
       ```bash
       cd ai_agent && pytest -v
       ```
       기대: 28/28 PASS (Phase 8 SC #4 보존, Python 코드 0 변경).

    7. **TBM 카드 visual smoke** (옵션 — assembleDebug APK 시 추가):
       ```bash
       cd app && ./gradlew app:assembleDebug
       ```
       기대: BUILD SUCCESSFUL + APK 생성. 실기기 시연은 Plan 09-04 의 책임.
  </action>
  <verify>
    <automated>grep -c '"tbm_alert"' app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt && grep -c 'showTbmAlertNotification' app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt && grep -c 'TbmDashboardActivity\|TbmWorkerActivity' app/src/main/java/com/example/smart_safety_management/MyFirebaseMessagingService.kt && echo OK</automated>
  </verify>
  <done>
    - MyFirebaseMessagingService 에 `"tbm_alert"` 분기 1개 추가 + showTbmAlertNotification 함수
    - UserRole 분기 (manager → TbmDashboardActivity, worker → TbmWorkerActivity)
    - extras `EXTRA_SESSION_ID` 전달 (worker 측)
    - `./gradlew app:compileDebugKotlin` BUILD SUCCESSFUL
    - 14+ tbm/ unit tests PASS
    - 26 watch/ unit tests 유지
    - ai_agent/tests/ 28/28 PASS (Phase 8 보존)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker app (anon)→Storage tbm-signatures | PNG 업로드 — Option A RLS (bucket_id + key prefix 가드) |
| Worker app→Edge Function tbm-checkin | signature_url path 전송 — Edge Function 이 ownership 검증 |
| Manager app (anon)→Edge Function tbm-start/tbm-end | Retrofit POST + Bearer anon — 서버 검증 |
| FCM extras→Activity Intent | extras session_id 만 신뢰 + DB 재조회 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | I (Information Disclosure) | Worker upload to tbm-signatures bucket | mitigate (client-side) | SignatureCanvas 가 path = `{session_id}/{user_id}_{epoch_ms}.png` 강제 (013 Storage RLS Option A 의 prefix 가드 충족). 클라이언트는 본인 user_id 만 prefix 로 사용 (server-side 추가 검증은 v1.1 Option B 마이그). |
| T-9-06 | I | Signed URL 60s 만료 후 expired URL 노출 (Pitfall 4) | mitigate | TbmDashboardScreen 의 thumbnail 클릭 시 모달 다이얼로그 → 닫으면 dispose → 재진입 시 새 URL 발급. AsyncImage 사용 시 `key(System.currentTimeMillis() / 30000)` 30초 cache invalidate. |
| T-9-12 | T | FCM extras session_id spoofing (Activity intent extras 신뢰) | mitigate | TbmWorkerActivity 가 extras session_id 만 hint 로 받고 DB 재조회 (group_id ownership + ended_at 검증). Phase 7 D-02 anti-pattern 회피 1:1 미러. |
| T-9-13 | E | worker 가 manager TbmDashboardActivity deep-link 진입 | mitigate | TbmDashboardActivity onCreate 의 `UserSession.userRole != MANAGER && != GENERAL_MANAGER` 가드 → Toast + finish(). Worker app 의 manifest 등록은 `android:exported="false"` (외부 deep-link 차단). |
| T-9-14 | I | Compose Canvas Bitmap memory leak (Pitfall 1) | mitigate | SignatureState.toPngBytes() finally 블록의 `if (!androidBitmap.isRecycled) androidBitmap.recycle()`. minSdk 24 + desugar 환경에서 명시 호출. |
| T-9-15 | T | Realtime SUBSCRIBED 가짜 (Pitfall 7 publication 미등록) | mitigate (by plan 09-01) | 013 마이그가 supabase_realtime publication 에 4 테이블 ADD (Plan 09-01 의 must_have). TbmRepository 가 Realtime.Status.CONNECTED 분기 + 5초 polling fallback (Phase 7 검증 패턴). |
</threat_model>

<verification>
- 22 신규/수정 파일 모두 존재 + 모든 `<done>` 조건 충족
- `./gradlew app:compileDebugKotlin` BUILD SUCCESSFUL
- `./gradlew app:testDebugUnitTest --tests "*.tbm.*"` 14+ cases ALL PASS
- 회귀: watch/ 26 cases + ai_agent/tests/ 28 cases 유지 (Phase 7·8 무변경)
- 회귀: watch/ 패키지 git diff 0 (SC #4 코드 경로 분리)
- 회귀: build.gradle.kts 의 realtime-kt:3.x / storage-kt:3.x 등장 0 (Pitfall 1)
- Pitfall 1·2·8·12 모두 명시 적용 + grep 검증
</verification>

<success_criteria>
- TBM-02 충족 — Android TBM 가이드 화면 (worker + manager) 완성
- HomeActivity 첫 ComposeView 임베드 (manager) + HomeWorkerActivity 두 번째 ComposeView (worker)
- 2 신규 Activity (TbmDashboardActivity + TbmWorkerActivity) 등록 + 권한 가드
- MyFirebaseMessagingService tbm_alert 분기 1개 추가 (UserRole 분기 + extras 신뢰 X)
- Storage 업로드 + signed URL 패턴 (T-9-01·06 mitigation, Pitfall 1·4)
- Dynamic session_id 2-stage Realtime subscription (RESEARCH Pattern 2)
- 4 TDD unit test files (14+ cases) ALL PASS
- 회귀 가드: watch/ 무변경 (SC #4), ai_agent/tests/ 28/28 (Phase 8 보존)
- T-9-01·06·12·13·14·15 mitigation 충족
</success_criteria>

<output>
After completion, create `.planning/phases/09-tbm-worker-guide/09-03-SUMMARY.md` documenting:
- 13 main + 4 test 신규 파일 list + lines (Korean repo path workaround 명시)
- compileDebugKotlin BUILD SUCCESSFUL + APK size (옵션)
- 14+ unit tests PASS (test class / case 분류)
- 회귀: watch/ 26 + ai_agent/tests/ 28 PASS
- Pitfall 1·2·8·12 적용 evidence (grep count)
- T-9-01·06·12·13·14·15 mitigation evidence
- HomeActivity ComposeView 임베드 위치 결정 (manager 의 daily_safety_check 위/아래)
- Deviations (Rule 1/2/3 별 분류 — Korean repo path workaround 재현)
</output>
