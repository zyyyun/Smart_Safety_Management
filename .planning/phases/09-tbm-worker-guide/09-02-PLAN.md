---
phase: 09-tbm-worker-guide
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - supabase/functions/notifications/index.ts
  - tests/smoke/tbm_start.sh
  - tests/smoke/tbm_checkin.sh
  - tests/smoke/tbm_end.sh
  - tests/smoke/tbm_missed.sh
autonomous: true
requirements: [TBM-03]
tags: [phase-9, tbm, edge-function, supabase, deno, fcm, send-push-to-users, action-routing, T-9-02, T-9-03, T-9-04, T-9-09]

user_setup:
  - service: supabase
    why: "supabase functions deploy notifications — 4 case 추가 (tbm-start / tbm-checkin / tbm-end / tbm-missed) 활성화"
    env_vars:
      - name: SUPABASE_URL
        source: ".env (Phase 4·7·8 작성됨)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: ".env (Phase 4·7·8 작성됨)"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token"
      - name: SUPABASE_ANON_KEY
        source: ".env (curl smoke 용)"
    dashboard_config: []

must_haves:
  truths:
    - "notifications/index.ts 에 4 case 추가 — case 'tbm-start' / 'tbm-checkin' / 'tbm-end' / 'tbm-missed' (default 위, Phase 4·7·8 action-routing 패턴 일관)"
    - "tbm-start: tbm_templates 조회 → tbm_sessions INSERT (UNIQUE 23505 → 409 'already exists') → tbm_checklists bulk INSERT → group worker SELECT (leader 제외) → sendPushToUsers (plural, fcm.ts:239 재사용)"
    - "tbm-checkin: tbm_sessions 존재 + ended_at NULL 검증 (404/410) → profiles.group_id == session.group_id ownership 검증 (T-9-03 spoofing 차단, 403) → tbm_participants INSERT (UNIQUE 23505 → 200 idempotent + existing row 반환)"
    - "tbm-end: leader_user_id == session.leader_user_id 검증 (T-9-04, 404) → UPDATE tbm_sessions SET ended_at=now() WHERE ended_at IS NULL (already-ended → 404)"
    - "tbm-missed: pg_cron 호출 전용 — missed worker 계산 (profiles.group_id == session.group_id AND user_role IN ('worker','general_manager') AND user_id NOT IN tbm_participants AND user_id != leader_user_id) → sendPushToUsers (missedIds + leader_user_id) — Pitfall 9 dedup 검증 (sendPushToUsers 내부 Set dedup, fcm.ts:253)"
    - "4 case 모두 data.type='tbm_alert', action_in_app ∈ {tbm-started, tbm-checkin-ok (옵션), tbm-ended (옵션), tbm-missed} — D-09 채널 분리 X (fcm_default_channel 재사용 Option B Phase 8 일관)"
    - "D-09 회귀 가드: 4 case 호출 후 public.notifications row 수 변화 = 0 (push-only, 상태 전이 책임은 cron 의 missed_alert_at + worker checkin 의 tbm_participants — research C1)"
    - "Edge Function 배포: `supabase functions deploy notifications` 성공 (Phase 8 03 패턴, 약 70~80kB)"
    - "12 curl smoke ALL PASS — tbm-start 3 (정상 200 / 23505 409 / 누락 400) + tbm-checkin 3 (정상 200 / ownership 403 / idempotent 200) + tbm-end 3 (정상 200 / leader 불일치 404 / 누락 400) + tbm-missed 3 (정상 200 / no-missed-workers leader-only 200 / 누락 400)"
    - "Pitfall 5 검증: 23505 catch 의 `if (sErr.code === '23505') return err(...409)` 분기가 tbm-start 와 tbm-checkin 모두 처리"
    - "Pitfall 9 검증: tbm-missed 의 recipientIds 가 leader 중복 0 (sendPushToUsers Set dedup + D-04 SQL leader 자연 제외)"
  artifacts:
    - path: "supabase/functions/notifications/index.ts"
      provides: "case 'tbm-start' + 'tbm-checkin' + 'tbm-end' + 'tbm-missed' (Edge Function 4 action-routing)"
      contains: 'case "tbm-start"'
    - path: "tests/smoke/tbm_start.sh"
      provides: "tbm-start 3 curl smoke (정상 200 / UNIQUE 23505 → 409 / payload 누락 400)"
      contains: "tbm-start"
    - path: "tests/smoke/tbm_checkin.sh"
      provides: "tbm-checkin 3 curl smoke (정상 200 / ownership 403 / idempotent 200)"
      contains: "tbm-checkin"
    - path: "tests/smoke/tbm_end.sh"
      provides: "tbm-end 3 curl smoke (정상 200 / leader 불일치 404 / payload 누락 400)"
      contains: "tbm-end"
    - path: "tests/smoke/tbm_missed.sh"
      provides: "tbm-missed 3 curl smoke (정상 200 / no-missed leader-only 200 / payload 누락 400)"
      contains: "tbm-missed"
  key_links:
    - from: "Edge Function tbm-start"
      to: "tbm_templates.checklist (JSONB array)"
      via: "supabase.from('tbm_templates').select('checklist, title').eq('work_type', wt)"
      pattern: "tbm_templates"
    - from: "Edge Function tbm-start"
      to: "tbm_checklists (bulk insert)"
      via: "items.map((text, idx) => ({session_id, item_idx: idx, item_text: text}))"
      pattern: "tbm_checklists.*insert"
    - from: "Edge Function tbm-checkin"
      to: "profiles.group_id ownership 검증"
      via: "if (profile.group_id !== session.group_id) return err(403)"
      pattern: "group_id !== session.group_id"
    - from: "Edge Function tbm-missed"
      to: "sendPushToUsers (plural)"
      via: "sendPushToUsers(supabase, [...missedIds, leader_user_id], payload)"
      pattern: "sendPushToUsers"
    - from: "pg_cron tbm_missed_attendance_check (plan 09-01)"
      to: "Edge Function case 'tbm-missed'"
      via: "net.http_post + Vault sr_key + body.action='tbm-missed'"
      pattern: "'tbm-missed'"
---

<objective>
Phase 9 Wave 2 (parallel with Plan 09-03) — Edge Function `notifications/index.ts` 에 TBM 4 case
추가 (tbm-start / tbm-checkin / tbm-end / tbm-missed) + deploy + 12 curl smoke. Phase 4·7·8 의
action-routing 패턴 1:1 미러 + Phase 8 03 의 sendPushToUsers (plural) 재사용.

Purpose: Plan 09-03 (Android UI) 가 호출하는 4 endpoints + Plan 09-01 의 pg_cron 이 호출하는
`tbm-missed` endpoint 가 운영 환경에서 동작. Edge Function 본문에 `notifications.insert()`
부재 = push-only — 상태 전이 책임은 cron 의 `missed_alert_at` UPDATE + tbm_participants
INSERT (worker checkin) 이 가짐 (research C1 amendment, Phase 8 D-09 일관).

Output:
- supabase/functions/notifications/index.ts (4 case 추가 + 배포)
- tests/smoke/tbm_{start,checkin,end,missed}.sh (12 smoke, ALL PASS)
- D-09 회귀 가드: public.notifications 0 변화
- Pitfall 5 (23505 catch) + Pitfall 9 (leader dedup) 모두 검증
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tbm-worker-guide/09-CONTEXT.md
@.planning/phases/09-tbm-worker-guide/09-RESEARCH.md
@.planning/phases/07-watch-app-bridge/07-02-PLAN.md
@.planning/phases/07-watch-app-bridge/07-02-SUMMARY.md
@.planning/phases/08-rtsp-camera/08-03-PLAN.md
@.planning/phases/08-rtsp-camera/08-03-SUMMARY.md
@supabase/functions/notifications/index.ts
@supabase/functions/_shared/fcm.ts
@supabase/functions/_shared/supabase.ts
@supabase/functions/_shared/response.ts
@supabase/functions/_shared/cors.ts

<interfaces>
<!-- Plan 09-01 의 schema (013_tbm_schema.sql) 가 이미 정의 — Edge Function 이 직접 사용 -->

tbm_templates:
  template_id  SERIAL PK
  work_type    VARCHAR(40) UNIQUE
  title        VARCHAR(100)
  checklist    JSONB  -- ["인화성 물질 격리 확인", ...]

tbm_sessions:
  session_id        BIGSERIAL PK
  group_id          INTEGER
  session_date      DATE DEFAULT CURRENT_DATE
  started_at        TIMESTAMPTZ DEFAULT now()
  ended_at          TIMESTAMPTZ
  expected_end_at   TIMESTAMPTZ NOT NULL
  leader_user_id    VARCHAR(50)
  work_type         VARCHAR(40)
  location          VARCHAR(255)
  notes             TEXT
  missed_alert_at   TIMESTAMPTZ
  UNIQUE (group_id, session_date)  -- 23505 catch 대상

tbm_checklists:
  checklist_id  BIGSERIAL PK
  session_id    BIGINT FK
  item_idx      INTEGER
  item_text     TEXT
  is_checked    BOOLEAN DEFAULT false
  note          TEXT
  checked_at    TIMESTAMPTZ
  UNIQUE (session_id, item_idx)

tbm_participants:
  participant_id  BIGSERIAL PK
  session_id      BIGINT FK
  user_id         VARCHAR(50)
  signed_at       TIMESTAMPTZ DEFAULT now()
  signature_url   TEXT
  method          VARCHAR(20) DEFAULT 'signature' CHECK ENUM
  UNIQUE (session_id, user_id)  -- 23505 catch 대상 (idempotent)

<!-- 기존 _shared 헬퍼 (재사용, 변경 0) -->
_shared/fcm.ts:
  sendPushToUsers(supabase, userIds: string[], payload): Promise<{sent, failed, skipped}>
  -- line 253: const uniqueIds = [...new Set(userIds)] (Set dedup — Pitfall 9 보장)

_shared/supabase.ts:
  createServiceClient(): SupabaseClient (service_role JWT)

_shared/response.ts:
  ok(body): Response (200 JSON)
  err(message, status=400): Response

<!-- 기존 notifications/index.ts 의 switch 구조 (Phase 7·8 검증) -->
serve(async (req) => {
  switch (action) {
    case "watch-ack": ...      // Phase 7
    case "watch-pair": ...     // Phase 7
    case "camera-down": ...    // Phase 8
    case "camera-recovered": ...  // Phase 8
    // *** Phase 9 추가: tbm-start / tbm-checkin / tbm-end / tbm-missed ***
    default: return err(`unknown action: ${action}`, 400);
  }
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifications/index.ts 에 4 TBM case 추가 + 013 적용 확인 + deploy</name>
  <read_first>
    - supabase/functions/notifications/index.ts (전체 파일 — switch 구조 + watch-* / camera-* 패턴)
    - supabase/functions/_shared/fcm.ts (sendPushToUsers line 239 + dedup line 253)
    - supabase/functions/_shared/supabase.ts (createServiceClient)
    - supabase/functions/_shared/response.ts (ok / err 헬퍼)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pattern 4 line 593-750 (4 case skeleton 코드)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pitfall 5·9·11 (UNIQUE catch / leader dedup / JSONB 순서)
    - .planning/phases/08-rtsp-camera/08-03-PLAN.md Task 1 (deploy + 4 smoke 패턴 — 1:1 미러)
    - .planning/phases/07-watch-app-bridge/07-02-PLAN.md (watch-pair ownership 검증 패턴 — T-9-03 미러)
    - .planning/phases/09-tbm-worker-guide/09-01-PLAN.md (013 schema interfaces 확인 — 4 테이블 컬럼명)
  </read_first>
  <action>
    1. **사전 가드 — 013 적용 확인**:
       ```bash
       supabase migration list --linked 2>/dev/null | grep 013
       ```
       기대: `013` row 등장. 없으면 즉시 중단 + Plan 09-01 재실행 안내. (Plan 09-01 의 `[BLOCKING] supabase db push` 가 통과해야 본 plan 진행 — depends_on: [09-01] 의 workflow 신호 + 본 task 의 명시 가드.)

    2. **`supabase/functions/notifications/index.ts` 의 switch 안 4 case 추가** (default 위, Phase 7·8 case 들 옆 — line 정확히는 execute-plan 단계에서 file diff 로 확인).

       정확한 코드 (RESEARCH §Pattern 4 line 599-750 그대로):

       **(A) tbm-start**:
       ```typescript
       case "tbm-start": {
         const { leader_user_id, group_id, work_type, expected_end_at, location, notes } = body;
         if (!leader_user_id || !group_id || !work_type || !expected_end_at) {
           return err("leader_user_id, group_id, work_type, expected_end_at are required", 400);
         }
         // 1. work_type 검증 + checklist 조회
         const { data: tmpl, error: tErr } = await supabase
           .from("tbm_templates").select("checklist, title")
           .eq("work_type", work_type).maybeSingle();
         if (tErr) return err(tErr.message, 500);
         if (!tmpl) return err(`unknown work_type: ${work_type}`, 400);

         // 2. session insert (UNIQUE 23505 → 409, Pitfall 5)
         const today = new Date().toISOString().slice(0, 10);
         const { data: session, error: sErr } = await supabase
           .from("tbm_sessions").insert({
             group_id, session_date: today,
             expected_end_at, leader_user_id, work_type,
             location: location ?? null, notes: notes ?? null,
           }).select().maybeSingle();
         if (sErr) {
           if (sErr.code === "23505") return err("이미 오늘 세션이 존재합니다", 409);
           return err(sErr.message, 500);
         }
         if (!session) return err("session insert returned null", 500);

         // 3. checklists bulk insert (JSONB array order 보장 — Pitfall 11)
         const items = (tmpl.checklist as string[]).map((text, idx) => ({
           session_id: session.session_id, item_idx: idx, item_text: text,
         }));
         const { error: cErr } = await supabase.from("tbm_checklists").insert(items);
         if (cErr) return err(cErr.message, 500);

         // 4. group worker 전원 SELECT (leader 제외)
         const { data: workers, error: wErr } = await supabase
           .from("profiles").select("user_id")
           .eq("group_id", group_id)
           .in("user_role", ["worker", "general_manager"])
           .neq("user_id", leader_user_id);
         if (wErr) return err(wErr.message, 500);

         const workerIds = (workers ?? []).map((w: { user_id: string }) => w.user_id);
         const r = await sendPushToUsers(supabase, workerIds, {
           title: "TBM 세션 시작",
           body: `${tmpl.title} — ${workerIds.length}명 대상`,
           data: {
             type: "tbm_alert", action_in_app: "tbm-started",
             session_id: String(session.session_id), work_type,
           },
         });
         return ok({ ok: true, session_id: session.session_id,
                     checklist_count: items.length, notified_count: r.sent });
       }
       ```

       **(B) tbm-checkin** (ownership 검증 = T-9-03 미러):
       ```typescript
       case "tbm-checkin": {
         const { session_id, user_id, signature_url } = body;
         if (!session_id || !user_id) return err("session_id, user_id are required", 400);

         // 1. session 유효성
         const { data: session, error: sErr } = await supabase
           .from("tbm_sessions").select("session_id, group_id, ended_at")
           .eq("session_id", session_id).maybeSingle();
         if (sErr) return err(sErr.message, 500);
         if (!session) return err("session not found", 404);
         if (session.ended_at) return err("session already ended", 410);

         // 2. ownership 검증 (T-9-03 spoofing 차단)
         const { data: profile, error: pErr } = await supabase
           .from("profiles").select("group_id").eq("user_id", user_id).maybeSingle();
         if (pErr) return err(pErr.message, 500);
         if (!profile) return err("user not found", 404);
         if (profile.group_id !== session.group_id) {
           return err("user not in session group", 403);
         }

         // 3. participant insert (UNIQUE 23505 → 200 idempotent)
         const { data: p, error: insErr } = await supabase
           .from("tbm_participants").insert({
             session_id, user_id,
             signature_url: signature_url ?? null,
             method: "signature",
           }).select().maybeSingle();
         if (insErr) {
           if (insErr.code === "23505") {
             const { data: existing } = await supabase
               .from("tbm_participants")
               .select("participant_id, signed_at")
               .eq("session_id", session_id).eq("user_id", user_id).maybeSingle();
             return ok({ ok: true,
                         participant_id: existing?.participant_id,
                         signed_at: existing?.signed_at,
                         idempotent: true });
           }
           return err(insErr.message, 500);
         }
         return ok({ ok: true, participant_id: p!.participant_id, signed_at: p!.signed_at });
       }
       ```

       **(C) tbm-end** (T-9-04 — leader 검증):
       ```typescript
       case "tbm-end": {
         const { session_id, leader_user_id } = body;
         if (!session_id || !leader_user_id) {
           return err("session_id, leader_user_id are required", 400);
         }

         const { data, error } = await supabase
           .from("tbm_sessions")
           .update({ ended_at: new Date().toISOString() })
           .eq("session_id", session_id)
           .eq("leader_user_id", leader_user_id)
           .is("ended_at", null)
           .select().maybeSingle();
         if (error) return err(error.message, 500);
         if (!data) return err("session not found or already ended or not led by user", 404);

         const { count } = await supabase
           .from("tbm_participants").select("*", { count: "exact", head: true })
           .eq("session_id", session_id);
         return ok({ ok: true, ended_at: data.ended_at, participant_count: count ?? 0 });
       }
       ```

       **(D) tbm-missed** (cron 호출 + Pitfall 9 dedup):
       ```typescript
       case "tbm-missed": {
         const { session_id, group_id, leader_user_id } = body;
         if (!session_id || group_id === undefined || group_id === null || !leader_user_id) {
           return err("session_id, group_id, leader_user_id are required", 400);
         }

         // 1. session 존재 + active 확인 (옵션, defensive)
         const { data: session } = await supabase
           .from("tbm_sessions").select("session_id, ended_at")
           .eq("session_id", session_id).maybeSingle();
         if (!session) return err("session not found", 404);

         // 2. missed worker 계산 (D-04 SQL — RPC 또는 직접 query)
         //    직접 query 채택 (RPC 미생성, deploy 단순화):
         const { data: groupWorkers, error: gErr } = await supabase
           .from("profiles").select("user_id, user_name")
           .eq("group_id", group_id)
           .in("user_role", ["worker", "general_manager"])
           .neq("user_id", leader_user_id);
         if (gErr) return err(gErr.message, 500);

         const { data: joined } = await supabase
           .from("tbm_participants").select("user_id")
           .eq("session_id", session_id);
         const joinedSet = new Set((joined ?? []).map((j: {user_id: string}) => j.user_id));
         const missedIds = (groupWorkers ?? [])
           .map((w: {user_id: string}) => w.user_id)
           .filter((uid: string) => !joinedSet.has(uid));

         // 3. recipients = missedIds + leader (sendPushToUsers 내부 Set dedup, Pitfall 9)
         const recipientIds = [...missedIds, leader_user_id];
         const r = await sendPushToUsers(supabase, recipientIds, {
           title: "TBM 미참여 작업자 알림",
           body: `예정 종료 + 30분 — ${missedIds.length}명 미참여`,
           data: {
             type: "tbm_alert", action_in_app: "tbm-missed",
             session_id: String(session_id),
             missed_count: String(missedIds.length),
           },
         });
         return ok({ ok: true, missed_count: missedIds.length, notified_count: r.sent });
       }
       ```

    3. **D-09 회귀 가드 (research C1)**: 4 case 모두 본문에 `notifications.insert(` 등장 0회.
       grep 검증 — 상태 전이 책임은 cron 의 `missed_alert_at` UPDATE (plan 09-01) + worker checkin
       의 tbm_participants INSERT.

    4. **deploy**: `supabase functions deploy notifications` 실행. 성공 응답 + bundle size
       기록 (약 70~80kB, Phase 8 03 비슷). esm.sh 522 transient 시 5초 후 재시도 (Phase 8 03
       Deviation 패턴 일관).

    5. **회귀 가드 — 기존 case 무변경**: `case "watch-ack"`, `case "watch-pair"`, `case "camera-down"`,
       `case "camera-recovered"` 4 case 모두 git diff 0 (Phase 7·8 보존). grep count 확인.
  </action>
  <verify>
    <automated>grep -c 'case "tbm-start"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-checkin"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-end"' supabase/functions/notifications/index.ts && grep -c 'case "tbm-missed"' supabase/functions/notifications/index.ts && grep -c 'sendPushToUsers' supabase/functions/notifications/index.ts && grep -c 'group_id !== session.group_id' supabase/functions/notifications/index.ts && [ "$(grep -c 'notifications\.insert' supabase/functions/notifications/index.ts)" = "0" ] && grep -c 'case "watch-ack"' supabase/functions/notifications/index.ts && grep -c 'case "camera-down"' supabase/functions/notifications/index.ts && echo OK</automated>
  </verify>
  <done>
    - notifications/index.ts 에 4 case 추가 (`case "tbm-start"` / `case "tbm-checkin"` / `case "tbm-end"` / `case "tbm-missed"`)
    - 23505 catch 분기가 tbm-start (409) + tbm-checkin (200 idempotent) 모두 처리 (Pitfall 5)
    - ownership 검증 `profile.group_id !== session.group_id` 1회 (T-9-03)
    - leader 검증 `eq("leader_user_id", leader_user_id)` 1회 (tbm-end, T-9-04)
    - sendPushToUsers 2회 호출 (tbm-start + tbm-missed)
    - notifications.insert 0건 (research C1, D-09 push-only)
    - 기존 4 case (watch-ack / watch-pair / camera-down / camera-recovered) 무변경
    - `supabase functions deploy notifications` 성공 (bundle size SUMMARY 기록)
  </done>
</task>

<task type="auto">
  <name>Task 2: 12 curl smoke (4 case × 3 scenarios) + D-09 회귀 가드 검증</name>
  <read_first>
    - supabase/functions/notifications/index.ts (Task 1 산출물)
    - .planning/phases/08-rtsp-camera/08-03-PLAN.md Task 2 (4 smoke 패턴 — 1:1 미러)
    - .planning/phases/07-watch-app-bridge/07-02-SUMMARY.md (8 smoke 패턴 — UNIQUE catch + ownership 검증)
    - .planning/phases/09-tbm-worker-guide/09-01-PLAN.md (013 schema — testuser1 manager + testuser_w1·w2·w3 worker 시드 가정)
    - .env (SUPABASE_URL + SUPABASE_ANON_KEY — curl 인증 헤더용)
  </read_first>
  <action>
    1. **사전 가드 — 시드 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/profiles?group_id=eq.1&user_role=eq.worker&select=user_id" \
         -H "apikey: $SUPABASE_ANON_KEY" -H "Authorization: Bearer $SUPABASE_ANON_KEY"
       ```
       기대: 3 rows (testuser_w1·w2·w3). 0~2 rows 면 plan 09-01 의 seed_tbm_demo.py 미실행 → 즉시 중단 + 09-01 재실행.

    2. **`tests/smoke/tbm_start.sh` 작성 (3 scenarios)**:

       **시나리오 1 — 정상 200**:
       ```bash
       # leader=testuser1, group=1, work_type=electric, expected_end_at=now+15min (KST offset)
       NOW_PLUS_15=$(date -d '+15 minutes' --iso-8601=seconds)
       curl -s -w "\n%{http_code}\n" -X POST "$SUPABASE_URL/functions/v1/notifications" \
         -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
         -H "apikey: $SUPABASE_ANON_KEY" \
         -H "Content-Type: application/json" \
         -d "{\"action\":\"tbm-start\",\"leader_user_id\":\"testuser1\",\"group_id\":1,\"work_type\":\"electric\",\"expected_end_at\":\"$NOW_PLUS_15\"}"
       ```
       기대: HTTP 200, body `{"ok":true,"session_id":<N>,"checklist_count":5,"notified_count":<>=0>}`.
       → session_id 변수 보관 (이후 smoke 에서 사용).

       **시나리오 2 — UNIQUE 23505 → 409**: 같은 day 두 번째 호출.
       기대: HTTP 409, body 에 "이미 오늘 세션" 포함.

       **시나리오 3 — payload 누락 → 400**: `expected_end_at` 생략.
       기대: HTTP 400.

    3. **`tests/smoke/tbm_checkin.sh` 작성 (3 scenarios)**:

       **시나리오 1 — 정상 200**: 위 session_id + user_id=testuser_w1, signature_url=null.
       기대: HTTP 200, body `{"ok":true,"participant_id":<N>,"signed_at":"..."}`.

       **시나리오 2 — ownership 위반 → 403**: 동일 session_id + user_id=<다른 group 의 user>.
       — group_id=99 PRESET worker 시드 없으면 fallback: profiles 에 직접 SQL 으로 testuser_other (group_id=99) 1회 시드 (smoke 시작 시 자동 fixture, 종료 시 cleanup).
       기대: HTTP 403, body "user not in session group".

       **시나리오 3 — idempotent → 200**: 위 시나리오 1 과 동일 페이로드 재호출.
       기대: HTTP 200, body 에 `"idempotent":true`.

    4. **`tests/smoke/tbm_end.sh` 작성 (3 scenarios)**:

       **시나리오 1 — 정상 200**: session_id + leader_user_id=testuser1.
       기대: HTTP 200, body `{"ok":true,"ended_at":"...","participant_count":<N>}`.

       **시나리오 2 — leader 불일치 → 404**: session_id + leader_user_id=testuser_w1 (worker, leader 아님).
       기대: HTTP 404.

       **시나리오 3 — payload 누락 → 400**: leader_user_id 생략.
       기대: HTTP 400.

       — 시나리오 1 실행 후 session.ended_at != NULL → 본 smoke 의 cleanup. **이후 다른 smoke
       에서 같은 session_id 사용 시 ended (410) 응답이 정상**. 단 tbm-missed smoke 는 별도
       session 생성 (시나리오 1 의 새 session 또는 SQL 으로 ended_at=NULL UPDATE — cleanup).

    5. **`tests/smoke/tbm_missed.sh` 작성 (3 scenarios)**:

       **prep**: 새 session 생성 (tbm-start, 다른 group 또는 SQL 으로 직접 INSERT — UNIQUE
       제약 회피용). 또는 기존 session 의 ended_at=NULL UPDATE + missed_alert_at=NULL UPDATE
       후 expected_end_at-1hour 로 UPDATE → 30분 임계 통과 + cron 자연 발사 대신 직접 호출.

       **시나리오 1 — 정상 200**: session_id + group_id=1 + leader_user_id=testuser1.
       기대: HTTP 200, body `{"ok":true,"missed_count":<N>,"notified_count":<>=1>}` (leader 포함).
       — **Pitfall 9 검증**: 응답 본문 또는 fcm.ts log 에서 leader 중복 push 0건 확인 (sendPushToUsers 내부 Set dedup).

       **시나리오 2 — no-missed-workers leader-only**: 모든 worker 가 tbm-checkin 완료 후 호출.
       기대: HTTP 200, body `{"ok":true,"missed_count":0,"notified_count":1}` (leader 만).

       **시나리오 3 — payload 누락 → 400**: session_id 생략.
       기대: HTTP 400.

    6. **D-09 회귀 가드** (research C1): 12 smoke 실행 전후 `public.notifications` row 수 변화 0:
       ```bash
       BEFORE=$(curl -s "$SUPABASE_URL/rest/v1/notifications?select=count" -H "apikey: $SR_KEY" -H "Prefer: count=exact" | jq '.[0].count')
       # ... 12 smoke 실행
       AFTER=$(curl -s ...)
       echo "before=$BEFORE after=$AFTER delta=$((AFTER - BEFORE))"
       ```
       기대: `delta=0` (push-only, Phase 8 03 동일 패턴). delta>0 이면 Task 1 의 4 case 중
       하나에 잘못된 notifications.insert 가 추가됨 → 즉시 fix.

    7. **smoke 결과 SUMMARY 작성용 표** — 각 smoke 의 status / body / 검증 결과를 표로
       정리하여 SUMMARY 단계에서 그대로 인용 가능 (Phase 8 03 SUMMARY 패턴).

    8. **cleanup**: 12 smoke 종료 후 SQL 으로 cleanup (테스트 session row 삭제 + 테스트
       profiles 삭제):
       ```sql
       DELETE FROM tbm_sessions WHERE leader_user_id='testuser1' AND session_date=CURRENT_DATE;
       -- (ON DELETE CASCADE 가 checklists / participants 자동 정리)
       DELETE FROM profiles WHERE user_id='testuser_other';
       ```
       단 demo 시연 흐름에서 데이터 잔존이 필요하면 cleanup 생략 가능 (SUMMARY 에 명시).
  </action>
  <verify>
    <automated>test -f tests/smoke/tbm_start.sh && test -f tests/smoke/tbm_checkin.sh && test -f tests/smoke/tbm_end.sh && test -f tests/smoke/tbm_missed.sh && grep -c 'tbm-start' tests/smoke/tbm_start.sh && grep -c 'tbm-checkin' tests/smoke/tbm_checkin.sh && grep -c 'tbm-end' tests/smoke/tbm_end.sh && grep -c 'tbm-missed' tests/smoke/tbm_missed.sh && echo OK</automated>
  </verify>
  <done>
    - 4 smoke shell scripts 작성 (tbm_start.sh / tbm_checkin.sh / tbm_end.sh / tbm_missed.sh)
    - 12 smoke ALL PASS — tbm-start 3 (200 / 409 / 400) + tbm-checkin 3 (200 / 403 / 200 idempotent) + tbm-end 3 (200 / 404 / 400) + tbm-missed 3 (200 / 200 leader-only / 400)
    - Pitfall 5 검증: 23505 catch tbm-start 의 409 응답 + tbm-checkin 의 200 idempotent 응답
    - Pitfall 9 검증: tbm-missed 정상 200 응답에서 sent count = unique(missedIds + leader) 와 일치 (leader 중복 X)
    - T-9-03 검증: tbm-checkin 시나리오 2 의 ownership 위반 → 403 응답
    - T-9-04 검증: tbm-end 시나리오 2 의 leader 불일치 → 404 응답
    - D-09 회귀 가드: `public.notifications` row delta = 0 (12 smoke 전후)
    - SUMMARY 에 12 smoke status 표 + sent count + delta 모두 기록
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon)→Edge Function | 모든 4 case — anon key 인증, 검증은 함수 본문 책임 |
| Edge Function→PostgREST (service_role) | UNIQUE 충돌 catch + ownership 검증 + UPDATE |
| Edge Function→FCM HTTP v1 | sendPushToUsers RS256 JWT self-sign (_shared/fcm.ts) |
| pg_cron→Edge Function (tbm-missed) | Vault sr_key Bearer — Phase 8 02 패턴 일관 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | T (Tampering) | tbm-start UNIQUE (group_id, session_date) bypass | mitigate | DB-level UNIQUE 제약 + Edge Function `if (sErr.code === "23505") return err(..., 409)`. Pitfall 5 검증 smoke 1 시나리오 2 (HTTP 409). |
| T-9-03 | E (Elevation of Privilege) | 다른 group worker 가 tbm-checkin spoof | mitigate | tbm-checkin 의 `profile.group_id !== session.group_id → 403`. smoke 2 시나리오 2 (testuser_other 의 group_id=99 → HTTP 403). Phase 7 watch-pair T-7-03 1:1 미러. |
| T-9-04 | E | leader 가 아닌 user 가 tbm-end | mitigate | tbm-end 의 `.eq("leader_user_id", leader_user_id)` + `.is("ended_at", null)` → 미일치 시 404. smoke 3 시나리오 2 (testuser_w1 → HTTP 404). |
| T-9-09 | I | leader push 중복 (자기 자신에게 미참여 알림 2건) | mitigate | sendPushToUsers 내부 `[...new Set(userIds)]` Set dedup (fcm.ts:253). D-04 SQL 의 `!= leader_user_id` 가 자연 제외. smoke 4 시나리오 1 의 sent count 검증. |
| T-9-10 | T | Edge Function 응답 fabrication (idempotent 거짓 응답) | accept | tbm-checkin idempotent 응답은 existing row 의 participant_id + signed_at 을 DB 에서 SELECT 후 반환. 거짓 응답 0 가능. accept. |
| T-9-11 | D (DoS) | Edge Function rate limit 부재 | accept v1.0 | Supabase Edge Function 자체 rate limit (1000 req/s) 가 v1.0 PoC 한정 충분. v1.1 별도 dedup/throttle 검토. |
</threat_model>

<verification>
- notifications/index.ts 에 4 case 추가 + 기존 4 case 무변경 (Phase 7·8 보존)
- `supabase functions deploy notifications` 성공 (bundle ~70~80kB)
- 12 curl smoke ALL PASS (status code + body 검증)
- D-09 회귀 가드: `public.notifications` row delta = 0
- Pitfall 5 (23505 catch) + Pitfall 9 (leader dedup) 모두 smoke 로 검증
- ai_agent/tests/ 무회귀 (28/28 PASS — Python 코드 0 변경)
</verification>

<success_criteria>
- TBM-03 의 Edge Function 4 actions 충족 (관리자/작업자/cron 모두 호출 가능)
- Plan 09-03 (Android UI) 가 tbm-start / tbm-checkin / tbm-end 3 actions 호출 가능
- Plan 09-01 의 cron 이 tbm-missed action 호출 가능 (운영 환경 sr_key 시드 후 자연 동작)
- T-9-02·03·04·09 mitigation 충족
- D-09 알림 전이 원칙 일관 (Phase 4·8 패턴 미러)
</success_criteria>

<output>
After completion, create `.planning/phases/09-tbm-worker-guide/09-02-SUMMARY.md` documenting:
- 4 case 추가 line ranges + git diff stat
- `supabase functions deploy notifications` 출력 + bundle size
- 12 smoke 결과 표 (action / scenario / status / body 발췌 / 검증 결과)
- D-09 회귀 가드 (notifications row delta)
- Pitfall 5·9 + T-9-02·03·04·09 검증 결과
- Deviations (Rule 1/2/3 별 분류)
</output>
