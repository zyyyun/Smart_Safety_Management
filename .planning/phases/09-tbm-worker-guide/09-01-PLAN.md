---
phase: 09-tbm-worker-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/013_tbm_schema.sql
  - tests/sql/test_013_tbm_isolation.sql
  - scripts/seed_tbm_demo.py
autonomous: true
requirements: [TBM-01]
tags: [phase-9, tbm, supabase, migration, schema-push, blocking, pg-cron, vault, storage, rls, realtime-publication, T-9-01, T-9-02, T-9-05]

user_setup:
  - service: supabase
    why: "013 마이그레이션 운영 DB 적용 ([BLOCKING] supabase db push --linked --yes) — Vault sr_key + edge_function_base_url 은 Phase 8 02 에서 이미 시드됨, 신규 시드 0개. 단 cron 의 round-trip 검증 시점에만 sr_key 필요 — push 자체는 sr_key 부재여도 OK (graceful skip, Phase 8 04 검증된 패턴)."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Non-TTY 환경: supabase CLI login token (Phase 4·7·8 동일 작성됨)"
      - name: SUPABASE_DB_PASSWORD
        source: ".env (Phase 4 010 에서 작성됨)"
    dashboard_config: []

must_haves:
  truths:
    - "013_tbm_schema.sql 가 4 신규 테이블 생성 — tbm_sessions / tbm_templates / tbm_checklists / tbm_participants (D-01)"
    - "tbm_sessions 에 UNIQUE (group_id, session_date) 제약 (D-02 — 1 group × 1 day × 1 session v1.0)"
    - "tbm_participants 에 UNIQUE (session_id, user_id) 제약 (idempotent 체크인)"
    - "tbm_participants.method CHECK IN ('signature','nfc','qr','manual') (v1.0 'signature' default)"
    - "tbm_sessions.missed_alert_at TIMESTAMPTZ 컬럼 존재 (D-05 알림 전이 1회 dedup, Phase 4 D-09 패턴)"
    - "RLS 4 테이블 모두 ENABLE + SELECT USING (true) v1.0 PoC (Phase 7 011 패턴 미러) + WRITE 정책 미등록 = service_role 만 (D-08 Edge Function 경유)"
    - "ALTER PUBLICATION supabase_realtime ADD TABLE 4 (tbm_sessions, tbm_templates, tbm_checklists, tbm_participants) — research C2 amendment, Phase 7 011 패턴 1:1 미러"
    - "Storage 버킷 'tbm-signatures' 신규 생성 — public=false, allowed_mime_types ARRAY['image/png'] (D-03 + research C3)"
    - "Storage RLS Option A — anon INSERT 허용 + bucket_id='tbm-signatures' + (storage.foldername(name))[1] regex '^[0-9]+$' (key prefix 가드, T-9-01 mitigation, research C3)"
    - "5 templates 시드 INSERT — work_type IN ('fire','electric','height','heavy','general'), checklist JSONB array 5 items 각각 (D-01 시드)"
    - "pg_cron 'tbm_missed_attendance_minute' 1분 주기 (* * * * *) 등록 — public.tbm_missed_attendance_check() 호출 (D-05)"
    - "tbm_missed_attendance_check() SECURITY DEFINER + SET search_path = public, extensions, net (Pitfall 7 보강) + Vault sr_key/base_url NULL 가드 (RAISE WARNING + RETURN graceful skip — Phase 8 검증 패턴)"
    - "expected_end_at + 30분 임계 SQL — `WHERE expected_end_at + interval '30 minutes' < now() AND missed_alert_at IS NULL AND ended_at IS NULL`"
    - "[BLOCKING] supabase db push --linked --yes 운영 DB 적용 성공 — supabase migration list 에 013 등장"
    - "scripts/seed_tbm_demo.py 가 group_id=1 에 worker 2~3명 시드 (testuser_w1/w2/w3) — RESEARCH Runtime State Inventory 의 시드 강제 prerequisite (미시드 시 tbm-start 0명 push, tbm-missed 0명 대상)"
    - "tests/sql/test_013_tbm_isolation.sql 가 7 assertions — (1) 4 ENABLE ROW LEVEL SECURITY, (2) anon UPDATE tbm_sessions 차단, (3) anon INSERT tbm_signatures 정상 path 200, (4) anon INSERT 비정상 path 차단, (5) 5 templates 시드 확인, (6) cron.job 등록 확인, (7) tbm_missed_attendance_check 의 prosecdef=t"
  artifacts:
    - path: "supabase/migrations/013_tbm_schema.sql"
      provides: "4 신규 테이블 + RLS + Realtime publication + pg_cron + Storage 버킷 + 5 templates 시드"
      contains: "CREATE TABLE public.tbm_sessions"
    - path: "tests/sql/test_013_tbm_isolation.sql"
      provides: "RLS isolation + 시드 + cron 등록 회귀 가드 (7 assertions)"
      contains: "SET LOCAL ROLE anon"
    - path: "scripts/seed_tbm_demo.py"
      provides: "group_id=1 worker 2~3명 시드 (PoC prerequisite)"
      contains: "testuser_w1"
  key_links:
    - from: "pg_cron job 'tbm_missed_attendance_minute'"
      to: "public.tbm_missed_attendance_check() 함수"
      via: "cron.schedule('* * * * *', $$SELECT public.tbm_missed_attendance_check();$$)"
      pattern: "cron.schedule.*tbm_missed_attendance_minute"
    - from: "tbm_missed_attendance_check() 함수"
      to: "notifications/index.ts case 'tbm-missed' (plan 09-02 신규)"
      via: "net.http_post + Vault sr_key + body.action='tbm-missed'"
      pattern: "'tbm-missed'"
    - from: "supabase_realtime publication"
      to: "tbm_sessions / tbm_templates / tbm_checklists / tbm_participants"
      via: "ALTER PUBLICATION supabase_realtime ADD TABLE"
      pattern: "ALTER PUBLICATION supabase_realtime ADD TABLE"
    - from: "storage.buckets row 'tbm-signatures'"
      to: "storage.objects RLS policy 'tbm_signatures_insert_anon'"
      via: "WITH CHECK (bucket_id='tbm-signatures' AND prefix regex)"
      pattern: "tbm_signatures_insert_anon"
---

<objective>
Phase 9 Wave 1 — TBM 스키마 + Storage + cron 인프라 운영 DB 적용. 단일 마이그레이션
`013_tbm_schema.sql` 안에 (a) 4 신규 테이블 + 인덱스 + RLS + Realtime publication,
(b) `tbm-signatures` Storage 버킷 + Option A RLS, (c) 5 templates 시드, (d)
`tbm_missed_attendance_check()` SECURITY DEFINER + pg_cron 1분 주기 등록 — 그리고
[BLOCKING] `supabase db push --linked --yes` 로 운영 DB 적용. 추가로 PoC 시연
prerequisite 인 group_id=1 worker 시드 스크립트 + RLS isolation SQL 테스트.

Purpose: Wave 2 (Plan 02 Edge Function + Plan 03 Android UI) 가 모두 013 스키마 적용을
가정으로 진행됨. 본 plan 의 핵심 gate = supabase db push 성공. cron 의 net.http_post
round-trip 은 Vault sr_key 미시드 시 graceful skip (Phase 8 04 검증 패턴 그대로).

Output:
- supabase/migrations/013_tbm_schema.sql (운영 DB 적용 완료)
- scripts/seed_tbm_demo.py (group_id=1 worker 시드)
- tests/sql/test_013_tbm_isolation.sql (7 assertions 회귀 가드)
- supabase migration list 에 013 등장 + cron.job 1행 + storage.buckets +1 + 5 templates 시드 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-tbm-worker-guide/09-CONTEXT.md
@.planning/phases/09-tbm-worker-guide/09-RESEARCH.md
@.planning/phases/04-watch-j2208a-pipeline/04-CONTEXT.md
@.planning/phases/07-watch-app-bridge/07-CONTEXT.md
@.planning/phases/08-rtsp-camera/08-02-PLAN.md
@.planning/phases/08-rtsp-camera/08-02-SUMMARY.md
@.planning/phases/08-rtsp-camera/08-04-SUMMARY.md
@supabase/migrations/004_storage.sql
@supabase/migrations/010_watch_pipeline.sql
@supabase/migrations/011_watch_app_rls.sql
@supabase/migrations/012_cameras_health.sql

<interfaces>
<!-- 010·011·012 에 이미 존재하는 column / 패턴 (executor 가 013 작성 시 직접 참조) -->

profiles (002_tables.sql):
  user_id      VARCHAR(50) PRIMARY KEY
  user_role    VARCHAR (worker / manager / general_manager)
  group_id     INTEGER FK → public.groups(group_id)
  user_name    VARCHAR

groups (002_tables.sql):
  group_id     INTEGER PRIMARY KEY

vault.decrypted_secrets (Phase 8 12 시드):
  name             TEXT
  decrypted_secret TEXT
  → 'service_role_key' + 'edge_function_base_url' 2 entries

cron.schedule(jobname, schedule, command) → returns BIGINT (010 패턴):
  010 의 unschedule + schedule idempotent 패턴 사용 — DO $$ ... EXCEPTION WHEN OTHERS THEN NULL; END $$

storage.buckets (004_storage.sql 의 4 버킷 + 본 plan 신규 1):
  id / name / public / file_size_limit / allowed_mime_types

storage.objects RLS pattern (004_storage.sql line 30-50):
  policy WITH CHECK 가 bucket_id 검증 + storage.foldername(name) 추출

<!-- Plan 09-02 의 Edge Function tbm-missed 가 받을 payload 계약 -->
tbm_missed_attendance_check() 가 net.http_post 으로 보내는 body:
```json
{
  "action": "tbm-missed",
  "session_id": <bigint>,
  "group_id": <int>,
  "leader_user_id": "<varchar>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 013_tbm_schema.sql 작성 (4 테이블 + RLS + Realtime publication + Storage 버킷 + 5 templates 시드 + cron + 함수)</name>
  <read_first>
    - supabase/migrations/010_watch_pipeline.sql (4 테이블 + RLS + Realtime publication 패턴 1:1 미러)
    - supabase/migrations/011_watch_app_rls.sql (USING (true) v1.0 PoC + ALTER PUBLICATION ADD TABLE EXCEPTION 흡수 패턴)
    - supabase/migrations/012_cameras_health.sql (line 100-180, Vault sr_key + base_url + pg_cron + SECURITY DEFINER + search_path 잠금 + EXCEPTION OTHERS — 1:1 미러)
    - supabase/migrations/004_storage.sql (Storage 버킷 INSERT + RLS 정책 패턴)
    - .planning/phases/09-tbm-worker-guide/09-CONTEXT.md §D-01 (4 테이블 SQL + 인덱스 + RLS + 시드) + §D-05 (cron + 함수) + §D-03 (Storage 버킷)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pattern 5 line 754-797 (Storage Option A RLS — bucket_id + key prefix 가드)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §Pitfall 5·6·7·8·11 (UNIQUE 23505 / Vault 미시드 / publication 미등록 / TIMESTAMPTZ / JSONB 순서)
  </read_first>
  <action>
    1. **신규 파일** `supabase/migrations/013_tbm_schema.sql`. 정확한 골격 (CONTEXT.md D-01 + D-05 + D-03 1:1 적용, 012 마이그 1:1 미러):

       **(A) 4 신규 테이블** (CONTEXT D-01 SQL 그대로):
       ```sql
       -- (1) tbm_sessions: 일자 × 작업장 × 리더 × 작업유형
       CREATE TABLE IF NOT EXISTS public.tbm_sessions (
         session_id        BIGSERIAL PRIMARY KEY,
         group_id          INTEGER NOT NULL REFERENCES public.groups(group_id),
         session_date      DATE NOT NULL DEFAULT CURRENT_DATE,
         started_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
         ended_at          TIMESTAMPTZ,
         expected_end_at   TIMESTAMPTZ NOT NULL,
         leader_user_id    VARCHAR(50) NOT NULL,
         work_type         VARCHAR(40) NOT NULL,
         location          VARCHAR(255),
         notes             TEXT,
         missed_alert_at   TIMESTAMPTZ,
         created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
         CONSTRAINT tbm_sessions_group_date_uq UNIQUE (group_id, session_date)
       );

       -- (2) tbm_templates: 작업유형별 체크리스트 템플릿
       CREATE TABLE IF NOT EXISTS public.tbm_templates (
         template_id   SERIAL PRIMARY KEY,
         work_type     VARCHAR(40) NOT NULL UNIQUE,
         title         VARCHAR(100) NOT NULL,
         checklist     JSONB NOT NULL,
         created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
       );

       -- (3) tbm_checklists: 세션별 체크 항목 (snapshot)
       CREATE TABLE IF NOT EXISTS public.tbm_checklists (
         checklist_id  BIGSERIAL PRIMARY KEY,
         session_id    BIGINT NOT NULL REFERENCES public.tbm_sessions(session_id) ON DELETE CASCADE,
         item_idx      INTEGER NOT NULL,
         item_text     TEXT NOT NULL,
         is_checked    BOOLEAN NOT NULL DEFAULT false,
         note          TEXT,
         checked_at    TIMESTAMPTZ,
         CONSTRAINT tbm_checklists_session_item_uq UNIQUE (session_id, item_idx)
       );

       -- (4) tbm_participants: 참여 작업자 + 서명 + 체크인
       CREATE TABLE IF NOT EXISTS public.tbm_participants (
         participant_id  BIGSERIAL PRIMARY KEY,
         session_id      BIGINT NOT NULL REFERENCES public.tbm_sessions(session_id) ON DELETE CASCADE,
         user_id         VARCHAR(50) NOT NULL,
         signed_at       TIMESTAMPTZ NOT NULL DEFAULT now(),
         signature_url   TEXT,
         method          VARCHAR(20) NOT NULL DEFAULT 'signature'
           CHECK (method IN ('signature','nfc','qr','manual')),
         CONSTRAINT tbm_participants_session_user_uq UNIQUE (session_id, user_id)
       );

       CREATE INDEX IF NOT EXISTS idx_tbm_sessions_group_date
         ON public.tbm_sessions (group_id, session_date DESC);
       CREATE INDEX IF NOT EXISTS idx_tbm_participants_session
         ON public.tbm_participants (session_id);
       CREATE INDEX IF NOT EXISTS idx_tbm_checklists_session
         ON public.tbm_checklists (session_id, item_idx);
       ```

       **(B) RLS — v1.0 PoC pattern (011 미러)**:
       ```sql
       ALTER TABLE public.tbm_sessions     ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_templates    ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_checklists   ENABLE ROW LEVEL SECURITY;
       ALTER TABLE public.tbm_participants ENABLE ROW LEVEL SECURITY;

       -- v1.0: SELECT USING (true) — Phase 7 011 패턴, write 정책 미등록 = service_role 만
       DO $$ BEGIN
         CREATE POLICY "tbm_sessions_select_v1_poc"     ON public.tbm_sessions
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_templates_select_v1_poc"    ON public.tbm_templates
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_checklists_select_v1_poc"   ON public.tbm_checklists
           FOR SELECT TO anon, authenticated USING (true);
         CREATE POLICY "tbm_participants_select_v1_poc" ON public.tbm_participants
           FOR SELECT TO anon, authenticated USING (true);
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;
       ```

       **(C) Realtime publication (research C2 — 4 테이블 모두 ADD)**:
       ```sql
       DO $$ BEGIN
         ALTER PUBLICATION supabase_realtime ADD TABLE
           public.tbm_sessions, public.tbm_templates,
           public.tbm_checklists, public.tbm_participants;
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;
       ```

       **(D) Storage 버킷 'tbm-signatures' (research C3 Option A — private + anon INSERT + key prefix 가드)**:
       ```sql
       INSERT INTO storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
       VALUES ('tbm-signatures', 'tbm-signatures', false, 524288, ARRAY['image/png'])
       ON CONFLICT (id) DO NOTHING;

       DO $$ BEGIN
         CREATE POLICY "tbm_signatures_insert_anon"
           ON storage.objects FOR INSERT
           TO anon, authenticated
           WITH CHECK (
             bucket_id = 'tbm-signatures'
             AND (storage.foldername(name))[1] ~ '^[0-9]+$'
             AND lower(right(name, 4)) = '.png'
           );
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;
       -- SELECT 정책 미등록 = default deny — 모든 read 는 service_role signed URL 60s
       ```

       **(E) 5 templates 시드 (CONTEXT D-01)**:
       ```sql
       INSERT INTO public.tbm_templates (work_type, title, checklist) VALUES
         ('fire',     '화재 위험 작업',
          '["인화성 물질 격리 확인","소화기 위치 확인","비상 대피로 확인","화재 감지기 동작 확인","불티 비산 방지포 설치"]'::jsonb),
         ('electric', '전기 작업',
          '["전원 차단 확인 (LOTO)","검전기 확인","절연 장갑/매트 사용","접지 상태 확인","아크 차단기 점검"]'::jsonb),
         ('height',   '고소 작업',
          '["안전대 착용 확인","안전모 턱끈 체결","발판/사다리 견고성","낙하물 방지망 설치","2m 이상 작업 시 추락 방지 조치"]'::jsonb),
         ('heavy',    '중량물 취급',
          '["지게차 운행 경로 확인","호이스트/슬링 점검","적재 안정성 확인","작업 반경 출입 통제","협착 위험 부위 확인"]'::jsonb),
         ('general',  '일반 작업',
          '["안전모 착용","안전화 착용","보안경 착용 (필요 시)","작업장 정리정돈","비상 연락망 확인"]'::jsonb)
       ON CONFLICT (work_type) DO NOTHING;
       ```

       **(F) tbm_missed_attendance_check() 함수 (CONTEXT D-05 SQL + 012 미러 — Pitfall 6·7 보강)**:
       ```sql
       CREATE OR REPLACE FUNCTION public.tbm_missed_attendance_check() RETURNS void
       LANGUAGE plpgsql SECURITY DEFINER
       SET search_path = public, extensions, net
       AS $$
       DECLARE
         r        RECORD;
         sr_key   TEXT;
         base_url TEXT;
       BEGIN
         SELECT decrypted_secret INTO sr_key
           FROM vault.decrypted_secrets WHERE name = 'service_role_key';
         SELECT decrypted_secret INTO base_url
           FROM vault.decrypted_secrets WHERE name = 'edge_function_base_url';

         IF sr_key IS NULL OR base_url IS NULL THEN
           RAISE WARNING 'tbm_missed_attendance_check: vault secret missing — skip';
           RETURN;
         END IF;

         FOR r IN
           SELECT session_id, group_id, expected_end_at, leader_user_id
           FROM public.tbm_sessions
           WHERE expected_end_at + interval '30 minutes' < now()
             AND missed_alert_at IS NULL
             AND ended_at IS NULL
         LOOP
           UPDATE public.tbm_sessions
             SET missed_alert_at = now()
             WHERE session_id = r.session_id;

           PERFORM net.http_post(
             url := base_url || '/notifications',
             headers := jsonb_build_object(
               'Authorization', 'Bearer ' || sr_key,
               'Content-Type',  'application/json'
             ),
             body := jsonb_build_object(
               'action',         'tbm-missed',
               'session_id',     r.session_id,
               'group_id',       r.group_id,
               'leader_user_id', r.leader_user_id
             )
           );
         END LOOP;
       END;
       $$;
       ```

       **(G) pg_cron 1분 주기 등록 (010·012 idempotent 패턴)**:
       ```sql
       DO $$ BEGIN
         PERFORM cron.unschedule('tbm_missed_attendance_minute');
       EXCEPTION WHEN OTHERS THEN NULL; END $$;
       SELECT cron.schedule(
         'tbm_missed_attendance_minute',
         '* * * * *',
         $$SELECT public.tbm_missed_attendance_check();$$
       );
       ```

    2. **회귀 가드 — service_role hardcode 0건**: 마이그 본문에 `eyJ` (JWT prefix) 또는
       raw service_role JWT 등장 0회. 코멘트 제외 후 grep 검증.

    3. **컬럼명 일관**: `missed_alert_at`, `expected_end_at`, `signature_url`, `work_type`
       모두 마이그 + 함수 본문 + Edge Function payload (plan 09-02 의 case 와 1:1)
       일관. grep 검증.
  </action>
  <verify>
    <automated>test -f supabase/migrations/013_tbm_schema.sql && grep -c "CREATE TABLE IF NOT EXISTS public.tbm_sessions" supabase/migrations/013_tbm_schema.sql && grep -c "CREATE TABLE IF NOT EXISTS public.tbm_templates" supabase/migrations/013_tbm_schema.sql && grep -c "CREATE TABLE IF NOT EXISTS public.tbm_checklists" supabase/migrations/013_tbm_schema.sql && grep -c "CREATE TABLE IF NOT EXISTS public.tbm_participants" supabase/migrations/013_tbm_schema.sql && grep -c "UNIQUE (group_id, session_date)" supabase/migrations/013_tbm_schema.sql && grep -c "UNIQUE (session_id, user_id)" supabase/migrations/013_tbm_schema.sql && grep -c "SECURITY DEFINER" supabase/migrations/013_tbm_schema.sql && grep -c "ALTER PUBLICATION supabase_realtime ADD TABLE" supabase/migrations/013_tbm_schema.sql && grep -c "tbm-signatures" supabase/migrations/013_tbm_schema.sql && grep -c "cron.schedule" supabase/migrations/013_tbm_schema.sql && grep -c "tbm-missed" supabase/migrations/013_tbm_schema.sql && [ "$(grep -v '^--' supabase/migrations/013_tbm_schema.sql | grep -c 'eyJ')" = "0" ] && echo OK</automated>
  </verify>
  <done>
    - 신규 파일 `supabase/migrations/013_tbm_schema.sql` 존재
    - 4 `CREATE TABLE IF NOT EXISTS public.tbm_{sessions,templates,checklists,participants}` 모두 등장
    - `UNIQUE (group_id, session_date)` 1회 (D-02)
    - `UNIQUE (session_id, user_id)` 1회 (idempotent 체크인)
    - `SECURITY DEFINER` 1회 (Pitfall 7)
    - `SET search_path = public, extensions, net` 1회 (search_path 잠금)
    - `ALTER PUBLICATION supabase_realtime ADD TABLE` 1회 with 4 tables (research C2)
    - `INSERT INTO storage.buckets` 1회 with `'tbm-signatures'` + `public=false` + `'image/png'`
    - `CREATE POLICY "tbm_signatures_insert_anon"` with `storage.foldername` regex
    - 5 templates 시드 INSERT — work_type 'fire','electric','height','heavy','general' 모두 등장
    - `cron.schedule.*tbm_missed_attendance_minute` 1회
    - `'tbm-missed'` 1회 (Edge Function action — plan 09-02 의 case 와 1:1)
    - `expected_end_at + interval '30 minutes'` 1회 (D-05 임계)
    - `missed_alert_at IS NULL` 1회 (알림 전이 dedup)
    - service_role JWT hardcode 0건 (코멘트 제외 후 `eyJ` count == 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push --linked --yes + scripts/seed_tbm_demo.py + tests/sql/test_013_tbm_isolation.sql + PostgREST 검증</name>
  <read_first>
    - supabase/migrations/013_tbm_schema.sql (Task 1 산출물)
    - .planning/phases/08-rtsp-camera/08-02-PLAN.md Task 2 (push + Vault 시드 검증 + cron 등록 검증 + RPC sanity call 패턴)
    - .planning/phases/08-rtsp-camera/08-02-SUMMARY.md (PostgREST + Management API NOTICE 우회 패턴 — psql 미설치 환경)
    - .planning/phases/07-watch-app-bridge/07-01-PLAN.md (scripts/seed_watch_demo.py 패턴 — urllib + service_role key)
    - .planning/phases/04-watch-j2208a-pipeline/04-01-SUMMARY.md (supabase db push 패턴)
    - .env (SUPABASE_DB_PASSWORD + SUPABASE_ACCESS_TOKEN — Phase 4·7·8 에서 작성됨)
    - .planning/phases/09-tbm-worker-guide/09-RESEARCH.md §"Runtime State Inventory" line 832-840 (worker 시드 강제 prerequisite)
  </read_first>
  <action>
    1. **[BLOCKING] `supabase db push --linked --yes`** 실행 — 013 마이그 운영 DB 적용.
       NOTICE 출력 확인 — (a) 4 테이블 생성, (b) 4 ENABLE RLS, (c) 4 정책 + storage 정책,
       (d) publication ADD TABLE, (e) storage.buckets +1, (f) 5 templates 시드 INSERT,
       (g) 함수 정의, (h) cron job 등록. 실패 시 즉시 중단 + Wave 2 진입 차단.

       Non-TTY 환경 (Phase 8 02 패턴): `SUPABASE_ACCESS_TOKEN` env 만 export 후 push.
       psql 미설치 환경에서는 PostgREST + Management API NOTICE 조합 우회 (08-02-SUMMARY 패턴).

    2. **PostgREST 검증 — 5 templates 시드 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_templates?select=work_type,title" \
         -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY"
       ```
       기대 응답: 5 rows with work_type IN ('fire','electric','height','heavy','general').

    3. **PostgREST 검증 — tbm_sessions 빈 확인 (anon SELECT 200 empty)**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/tbm_sessions?select=session_id" \
         -H "apikey: $ANON_KEY" -H "Authorization: Bearer $ANON_KEY"
       ```
       기대: `[]` (RLS USING(true) + 데이터 0).

    4. **cron.job 등록 확인** (Management API SQL 또는 Dashboard SQL Editor):
       ```sql
       SELECT jobname, schedule, active FROM cron.job WHERE jobname='tbm_missed_attendance_minute';
       ```
       기대: 1 row, schedule='* * * * *', active=true.

    5. **storage.buckets +1 확인**:
       ```bash
       curl -s "$SUPABASE_URL/rest/v1/buckets?select=id,public,allowed_mime_types" \
         -H "apikey: $SERVICE_ROLE_KEY" -H "Authorization: Bearer $SERVICE_ROLE_KEY"
       ```
       또는 Dashboard Storage → Buckets 에서 'tbm-signatures' (private) 확인.

    6. **`scripts/seed_tbm_demo.py` 작성** (Phase 7 seed_watch_demo.py 패턴 미러):
       - urllib + service_role key 사용 (Supabase REST `/rest/v1/profiles?on_conflict=user_id` POST + Prefer: resolution=merge-duplicates).
       - group_id=1 에 worker 3명 시드 — `testuser_w1`, `testuser_w2`, `testuser_w3` (user_role='worker', user_name='작업자 1·2·3', password=bcrypt placeholder 또는 NULL 허용).
       - testuser1 (manager) 의 group_id=1 정합 확인 (Phase 7·8 에서 검증됨, 재확인 only).
       - 멱등 (이미 존재 시 ON CONFLICT DO NOTHING).
       - 실행: `python scripts/seed_tbm_demo.py` → 표준출력 'seeded N rows (skipped M)'.

       핵심 골격:
       ```python
       import os, json, urllib.request
       SUPABASE_URL = os.environ["SUPABASE_URL"]
       SR_KEY = os.environ["SUPABASE_SERVICE_ROLE_KEY"]

       workers = [
           {"user_id": "testuser_w1", "user_name": "작업자 1",
            "user_role": "worker", "group_id": 1},
           {"user_id": "testuser_w2", "user_name": "작업자 2",
            "user_role": "worker", "group_id": 1},
           {"user_id": "testuser_w3", "user_name": "작업자 3",
            "user_role": "worker", "group_id": 1},
       ]
       req = urllib.request.Request(
           f"{SUPABASE_URL}/rest/v1/profiles?on_conflict=user_id",
           data=json.dumps(workers).encode(),
           headers={
               "apikey": SR_KEY,
               "Authorization": f"Bearer {SR_KEY}",
               "Content-Type": "application/json",
               "Prefer": "resolution=merge-duplicates,return=minimal",
           }, method="POST")
       resp = urllib.request.urlopen(req)
       print(f"seed_tbm_demo: status={resp.status}, workers={len(workers)}")
       ```
       
       **NOTE — profiles 컬럼 추가 요건 확인**: profiles 테이블의 NOT NULL 컬럼 확인 후
       (e.g. password_hash) bcrypt placeholder 또는 schema 호환 default 처리. 002_tables.sql
       확인 필수. 부재 컬럼이 NOT NULL 이면 PostgreSQL 400 — 그 경우 execute-plan 단계에서
       SQL fallback 으로 직접 INSERT.

    7. **`tests/sql/test_013_tbm_isolation.sql` 작성** (test_012 패턴 미러, 7 assertions):
       ```sql
       -- (a) 4 tables 의 ROW LEVEL SECURITY 활성 검증
       DO $$ BEGIN
         IF NOT (SELECT bool_and(rowsecurity) FROM pg_class
                 WHERE relname IN ('tbm_sessions','tbm_templates','tbm_checklists','tbm_participants')) THEN
           RAISE EXCEPTION 'RLS not enabled on all 4 tbm tables';
         END IF;
       END $$;

       -- (b) anon UPDATE tbm_sessions 차단 검증 (정책 미등록 = default deny)
       DO $$
       DECLARE rows_affected INTEGER;
       BEGIN
         SET LOCAL ROLE anon;
         UPDATE public.tbm_sessions SET ended_at = now() WHERE session_id = -1;
         GET DIAGNOSTICS rows_affected = ROW_COUNT;
         IF rows_affected > 0 THEN
           RAISE EXCEPTION 'anon UPDATE should be blocked';
         END IF;
         RESET ROLE;
       END $$;

       -- (c) 5 templates 시드 검증
       DO $$ BEGIN
         IF (SELECT count(*) FROM public.tbm_templates
             WHERE work_type IN ('fire','electric','height','heavy','general')) <> 5 THEN
           RAISE EXCEPTION '5 templates seed missing';
         END IF;
       END $$;

       -- (d) cron.job 등록 검증
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM cron.job
                        WHERE jobname='tbm_missed_attendance_minute' AND active=true) THEN
           RAISE EXCEPTION 'tbm_missed_attendance_minute cron not registered';
         END IF;
       END $$;

       -- (e) SECURITY DEFINER 검증
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM pg_proc p JOIN pg_namespace n ON p.pronamespace=n.oid
                        WHERE n.nspname='public' AND p.proname='tbm_missed_attendance_check'
                        AND p.prosecdef=true) THEN
           RAISE EXCEPTION 'tbm_missed_attendance_check should be SECURITY DEFINER';
         END IF;
       END $$;

       -- (f) tbm-signatures bucket 존재 검증
       DO $$ BEGIN
         IF NOT EXISTS (SELECT 1 FROM storage.buckets WHERE id='tbm-signatures' AND public=false) THEN
           RAISE EXCEPTION 'tbm-signatures bucket missing or public';
         END IF;
       END $$;

       -- (g) Realtime publication 4 테이블 등록 검증
       DO $$ BEGIN
         IF (SELECT count(*) FROM pg_publication_tables
             WHERE pubname='supabase_realtime'
               AND tablename IN ('tbm_sessions','tbm_templates','tbm_checklists','tbm_participants')) <> 4 THEN
           RAISE EXCEPTION 'Realtime publication 4 tables not registered';
         END IF;
       END $$;

       SELECT 'test_013_tbm_isolation: 7/7 assertions PASS' AS result;
       ```
       Dashboard SQL Editor 또는 supabase db remote commit 으로 1회 실행.

    8. **수동 호출 (Open Question — function sanity)**: Dashboard SQL Editor 또는
       PostgREST RPC 로 `SELECT public.tbm_missed_attendance_check();` 1회 → 결과
       NULL (void) + Vault sr_key 미시드 시 NOTICE 'vault secret missing — skip'
       (graceful skip, 정상 동작 — Phase 8 04 검증 패턴 일관).
  </action>
  <verify>
    <automated>test -f scripts/seed_tbm_demo.py && test -f tests/sql/test_013_tbm_isolation.sql && grep -c "tbm-signatures" tests/sql/test_013_tbm_isolation.sql && grep -c "tbm_missed_attendance_minute" tests/sql/test_013_tbm_isolation.sql && grep -c "ROW LEVEL SECURITY\|rowsecurity" tests/sql/test_013_tbm_isolation.sql && grep -c "testuser_w" scripts/seed_tbm_demo.py && echo OK</automated>
  </verify>
  <done>
    - `supabase db push --linked --yes` 성공 (커밋 또는 SUMMARY 에 운영 DB 적용 시각 + NOTICE 출력 기록)
    - `supabase migration list` 에 013 등장
    - PostgREST anon GET `/rest/v1/tbm_templates?select=work_type,title` 5 rows 반환
    - PostgREST anon GET `/rest/v1/tbm_sessions?select=session_id` `[]` (빈 배열)
    - cron.job 의 'tbm_missed_attendance_minute' active=true 등록 확인
    - storage.buckets 에 'tbm-signatures' (public=false, image/png) 등록 확인
    - `scripts/seed_tbm_demo.py` 작성 + 실행 후 group_id=1 에 testuser_w1·w2·w3 시드됨 (PostgREST GET `/rest/v1/profiles?group_id=eq.1&user_role=eq.worker` 3 rows)
    - `tests/sql/test_013_tbm_isolation.sql` 작성 (7 assertions)
    - SUMMARY 에 prerequisite 명시 — Vault sr_key 미시드 환경에서 cron silent skip (graceful, Phase 8 04 검증 패턴 그대로)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon)→PostgREST | 운영 DB read/write — RLS 가 default deny + USING(true) SELECT |
| pg_cron→Edge Function | net.http_post + Vault sr_key — Postgres 내부, agent 불필요 |
| client (anon)→Storage | tbm-signatures bucket INSERT (Option A) — key prefix 가드 |
| Dashboard (manual)→Vault | sr_key 수동 시드 (Phase 8 02 prerequisite 재사용) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | I (Information Disclosure) | tbm-signatures Storage (PII 서명 PNG) | mitigate | (a) bucket public=false (직접 GET 차단), (b) anon INSERT WITH CHECK 가 `(storage.foldername(name))[1] ~ '^[0-9]+$'` 강제 + `lower(right(name,4))='.png'` 강제, (c) SELECT 정책 미등록 = anon default deny — read 는 service_role signed URL 60s 전용. v1.1 = Option B (Edge Function 경유 multipart) 마이그 path 명시. |
| T-9-02 | T (Tampering) | tbm_sessions UNIQUE (group_id, session_date) bypass | mitigate | UNIQUE 제약이 DB-level enforcement. Edge Function tbm-start (plan 09-02) 가 23505 catch 후 409 응답. Vault sr_key 노출 없이 cron 의 PERFORM net.http_post 만 sr_key 사용 (vault.decrypted_secrets SELECT). |
| T-9-05 | D (Denial of Service) | Vault sr_key 미시드 시 cron 무한 RAISE WARNING | accept (graceful skip) | 함수 본문 `IF sr_key IS NULL OR base_url IS NULL THEN RAISE WARNING ... RETURN` — silent skip 으로 cron 자체는 1분마다 정상 종료. Phase 8 04 검증 패턴 그대로. 운영 환경에서 sr_key 시드 후 자연 동작. |
| T-9-06 | I | signature URL signed link 60s 만료 bypass | mitigate | createSignedUrl(path, 60) 마다 서버 측 재발급. 60s 이상 표시 시 client cache invalidate (plan 09-03 의 AsyncImage key 패턴, RESEARCH Pitfall 4 참조). 본 plan 은 server-side signed URL TTL=60s 강제만 보장 (Storage RLS SELECT 미등록). |
| T-9-07 | T | Vault secret git 노출 (T-8-01 재현 회피) | mitigate | 마이그 SQL 본문에 `eyJ` (JWT prefix) 0건 — 코멘트 제외 후 grep 검증. sr_key 는 Phase 8 02 에서 dashboard 시드 완료 + 본 plan 은 vault.decrypted_secrets SELECT 만 사용. |
| T-9-08 | E (Elevation of Privilege) | anon 이 tbm_participants 임의 INSERT 시도 | mitigate | RLS write 정책 미등록 = default deny. tbm-checkin Edge Function (plan 09-02) 만 service_role 로 INSERT. Storage bucket Option A 의 anon INSERT 는 path prefix 가드로 임의 path 차단 (T-9-01 와 보완). |
</threat_model>

<verification>
- 신규 파일 `supabase/migrations/013_tbm_schema.sql` 존재 + 모든 `<done>` 조건 충족
- 신규 파일 `scripts/seed_tbm_demo.py` + `tests/sql/test_013_tbm_isolation.sql` 존재
- `supabase db push --linked --yes` 운영 DB 적용 성공
- `supabase migration list` 에 013 등장
- PostgREST 5 templates 시드 + tbm_sessions 빈 응답 + cron.job 등록 + storage.buckets 'tbm-signatures' 모두 확인
- `tests/sql/test_013_tbm_isolation.sql` 의 7 assertions Dashboard SQL Editor 에서 모두 PASS (`'test_013_tbm_isolation: 7/7 assertions PASS'`)
- service_role JWT hardcode 0건 (`grep -v '^--' supabase/migrations/013_tbm_schema.sql | grep -c 'eyJ' == 0`)
- ai_agent/tests/ 무회귀 (28/28 PASS — capture/scheduler 코드 0 변경, Phase 8 SC #4 보존)
</verification>

<success_criteria>
- TBM-01 충족 — 4 신규 테이블 + RLS + Realtime publication + pg_cron + Storage 버킷 운영 DB 적용
- Plan 09-02 (Edge Function) + Plan 09-03 (Android UI) 가 013 schema 적용을 가정으로 진행 가능 (스키마 + cron + storage 모두 준비됨)
- group_id=1 에 worker 시드 prerequisite 충족 (PoC 시연 가능 — testuser1 manager + testuser_w1·w2·w3 worker)
- Vault sr_key 미시드 환경에서도 cron silent skip 동작 (graceful, 운영 환경에서 시드 후 자연 동작)
- T-9-01·02·05·06·07·08 mitigation 충족
</success_criteria>

<output>
After completion, create `.planning/phases/09-tbm-worker-guide/09-01-SUMMARY.md` documenting:
- supabase db push --linked --yes 실행 결과 (NOTICE 출력 발췌)
- supabase migration list 에 013 등장 확인
- PostgREST 5 templates 시드 응답
- cron.job 등록 응답
- storage.buckets 'tbm-signatures' 응답
- seed_tbm_demo.py 실행 결과 (worker 3명 시드)
- test_013_tbm_isolation.sql 7/7 PASS 응답
- Vault sr_key 시드 prerequisite (Phase 8 02 SUMMARY User Setup Required 재인용)
- Deviations (Rule 1/2/3 별 분류)
</output>
