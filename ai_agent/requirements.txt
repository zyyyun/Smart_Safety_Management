supabase>=2.5.0
APScheduler>=3.10.0
python-dotenv>=1.0.0
httpx>=0.27.0

# ── 쓰러짐 감지 (Next-3 / LP-2)
# Python 3.10 또는 3.11 사용. 3.13 은 torch 공식 wheel 없음 (2026-04 기준).
# CPU 전용 설치:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# CUDA 사용 시엔 그냥 pip install 로 기본 index 에서 GPU wheel 다운로드.
torch>=2.0
torchvision>=0.15
opencv-python>=4.7
numpy>=1.23
pyyaml>=6.0
pandas>=1.5
tqdm>=4.64
requests>=2.28
matplotlib>=3.6
seaborn>=0.12
scipy>=1.9
