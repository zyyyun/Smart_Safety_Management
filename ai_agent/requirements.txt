supabase>=2.5.0
APScheduler>=3.10.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Fall detection / YOLO runtime.
# Python 3.10 or 3.11 is recommended. Python 3.13 may not have stable
# torch wheels in this environment.
# CPU install example:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# CUDA install example:
#   pip install -r requirements.txt
torch>=2.0
torchvision>=0.15
opencv-python>=4.7
numpy>=1.23
pyyaml>=6.0
pandas>=1.5
tqdm>=4.64
requests>=2.28
matplotlib>=3.6
seaborn>=0.12
scipy>=1.9

# General detectors: fire, helmet, forklift, person.
# First run may download/cache YOLO models.
ultralytics>=8.2
